# Sunny 当前开发状态

> 只保留当前 47 指纹、活跃风险和最近高信号事实；完整历史见 `docs/archive/codex-state/`。本文件不是并发任务锁。

## 当前 47 基线

- 远端 release ID：`git-91f179521348_web-d69be09692d9_api-665d6723ba57`
- Web 指纹：`d69be09692d9ec7dfddf23f30b6e385c89071334aeb678b05cf3f9d80439c0cc`
- API 指纹：`665d6723ba5728e5d768676f5dfdcce6bd774874939a4c5db3d26575c6cee0d3`
- migration 指纹：`8a595271f47ae776aefb6d637fddf7eeb1c4b6a27092d0201c0dd8abbffb5f05`
- 当前重构分支：`codex/release/shipment-overview-adapter-20260821`；运行 commit 为 `91f1795213481ec2896ed0348f0aa2ed6defeb22`，运单总览兼容适配器、Shared 运单子路径和防回退门禁已发布。
- 登录/改密自动刷新仍由 session effect 唯一触发并对等价并发去重；路由数据所有权策略覆盖 `pricing` 与 `settings`，两者不触发 App 全局工作区请求；返回 legacy 页面仍立即刷新，并继续共享 5 分钟全局新鲜度时钟。
- 47 当前公网 health/Web 可用、锁 free、recovery clear；provenance 为 `GIT_SOURCE_BUILD/ORIGIN_BRANCH/GHCR_DIGESTS`，Web/API image 与 API release ID 均匹配。

## 活跃工作

- 运单总览上层已不再直接依赖巨型 Repository，兼容适配器切片已完成并待归档；全局优化目标继续，下一切片必须重新比较输入安全、前端 route-owned 数据流和真实 Prisma 查询拆分。

## 活跃风险

- ShipmentOverview 兼容适配器内部仍委托巨型 PrismaRepository；下一步真实 Prisma 查询拆分必须先隔离查询所依赖的数据范围、字段裁剪和角标聚合。裸 `@Body()` 债务仍冻结在 230，逐接口接入 Schema 前必须保护既有非法输入语义。
- 根工作树 `/Users/j1ng/Tools/sunny` 仍包含其他会话和历史未提交改动；禁止清理、覆盖或作为标准发布候选。
- 47 源码仍含 10 个历史 `._*` AppleDouble 文件；Phase43 仅把它们从指纹/同步输入排除并保留 source-drift 告警，没有删除。Git 同源已经恢复，但这些历史元数据仍作为非运行时 source-drift 告警保留。
- 理货开始路径的 Prisma/InMemory 适配器仍保留既有审计动作名差异（`warehouse.tally_task.start` / `warehouse.tally.start`）；该差异已明确冻结，不得在后续结构重构中猜测统一口径。
- 历史 `problemTickets.test.tsx` 仍引用当前界面已不存在的“问题件”按钮，属于待拆分旧测试债务，不得据此恢复旧 UI。
- 47 为全局串行发布资源；标准发布必须从干净发布协调 worktree 取得 baseline receipt，远端基线变化时重新合并。

## 最近高信号事实

- 2026-08-21：运单总览 Port 改用显式 `@siyuan/shared/shipment` 契约，Module 通过唯一 `LegacyShipmentOverviewQueryRepository` 兼容适配器连接总仓储；Controller/Service/Policy/Port 均由架构门禁止回退直连。迁移前 10/10、迁移后 11/11、Shared build、API typecheck、448 路由、17 类门禁自检、治理和主干 CI 通过。Web+API 发布 `git-91f179521348_web-d69be09692d9_api-665d6723ba57`，线上管理员/市场允许路径与业务/仓库拒绝路径通过，市场 46 条站点越界和敏感字段违规均为 0；源码、provenance、镜像/state、health、日志、锁与 recovery 正常，未改 UI、业务契约、schema 或生产业务数据。

- 2026-08-20：用户表格偏好从根 `AppModule` 迁入独立 Nest Module，原 Prisma/InMemory 选择、三条路由、请求/响应、鉴权和用户隔离保持不变；根装配从 41/67/418 降为 40 Controller / 66 Provider / 414 行，并由 17 类失败自测冻结。固定样本 9/9、API typecheck、448 路由、完整治理与主干 CI 通过。API-only 发布 `git-cb9f3c2e440d_web-f7984a62526a_api-0e4acb7911bf`；三条未登录路径均 401，源码 hash、provenance、镜像/state、API release ID、公网 health、日志、锁与 recovery 正常；未改 UI、schema、生产数据或业务规则。

- 2026-08-20：架构控制面首批切片完成。Shared 成为 `PermissionKey` 唯一类型来源，API/Web 保留兼容 re-export；FinanceCatalog、ShipmentOverview、SystemDirectory 从根模块装配迁入独立 Nest Module；架构门阻止重复权限类型并冻结裸 `@Body()` 不再高于 230。三领域 19 条定向断言、Shared/API/Web typecheck、448 路由、governance/security、GitHub CI 全部通过。发布 `git-f9630b68fc74_web-f7984a62526a_api-4e07c0b4cb80`，provenance、镜像/state、API release ID、公网 health、三条未登录 401、锁和 recovery 正常；未改 UI、业务契约、schema、生产数据，未执行迁移或 seed。

- 2026-08-14：深度重构第六十八阶段依据生产 bundle `VN -> summarizeMasterDataSnapshot` 和业务经理真实 `/api/master-data` 响应，定位全局 `agent-data` 屏蔽删除必填 `agents`/`agentChannels` 数组导致整页崩溃。仅在精确 master-data 顶层保留已经为空的两个集合；非空敏感集合仍删除。定向 4/4、三端类型、434 路由、完整治理与安全 Review 通过；发布 `git-45d0cbc8bbc1_web-d83509e7b062_api-5db26f1e8860` 后真实角色两个数组为空、敏感键 0、同错 0。随后并发白名单发布吸收 8 文件但破坏镜像/state provenance，修复本身仍在线有效。

- 2026-08-14：深度重构第六十七阶段把 47 的 537 文件白名单组合运行树逐字节吸收并提交，源码审计 518/518 一致；三端类型、API 17/17、Web 2/2、434 路由和完整治理通过。current-baseline cutover 发布 `git-d8760b1a827b_web-d83509e7b062_api-71777427f952`，provenance 恢复为 `GIT_SOURCE_BUILD/GIT_BUNDLE`，镜像/state、API release ID、源码、仓库分页只读样本、权限码/迁移计数、公网 health、锁与 recovery 正常。发布后遥测暴露业务经理基础资料页面 `undefined.filter` 崩溃，已升为下一轮 P0。

- 2026-08-14：深度重构第六十六阶段参考 Vendure/Medusa 的模块服务边界，把墨家设备 POST 接入口迁入 `warehouse/integration` Controller、Service 与五方法 Repository port；设备 token 首语句、201/响应、解析、幂等、理货覆盖、采样队列/脱敏、失败审计及 Prisma/InMemory 选择未改，`DataController` 从 2,256 降至 1,945 行。墨家固定样本 1/1、token 3/3、Web 组件 4/4、三端类型/434 路由/完整治理通过，独立审查无 P0/P1。发布 `git-08ffa122a7aa_web-3331a4a72688_api-c12712d49090`，线上错误 token 401、源码/容器路由、镜像/state、内外 health、日志、锁与 recovery 正常。P2：采样高压/降级缺专用 Service characterization；Phase65 的 shared-root/Harness 基线漂移已记录但未偿还。

- 2026-08-14：深度重构第五十一阶段参考 Keycloak 服务端会话决策点与 Cerbos 认证/授权分层，把 RBAC Guard 对数据库当前账号、角色、权限和拒绝审计的依赖迁入 `AuthSessionService`；JWT 8 小时、验签、401/403、强制改密 bootstrap、动态权限及数据范围逻辑未改。定向测试 7/7、API typecheck、434 路由治理、安全契约 3/3 通过。发布 `git-7e1e38a21980_web-fa6197b29198_api-488505214fcf`，仅 API 重建，Git provenance、镜像/state、API release ID、公网 200、日志、锁和 recovery 正常。重审确认账号/角色停用及权限变化原本已逐请求即时生效；真实剩余仅改密/主动登出的 token 撤销，不再误列最高优先级。

- 2026-08-14：深度重构第五十阶段参考 Moby immutable digest、Kubernetes client-go fencing 说明和 Docker Compose 并发案例，为每次 47 发布生成 API/Web/db-migrate 唯一镜像 tag，并在 build 后、迁移前、启动前校验镜像 identity；远端队列锁继续负责协调，但不再被当作 fencing。离线篡改拒绝、shell 语法、434 路由治理、安全契约 3/3 通过。发布 `git-62ed598b7dbe_web-fa6197b29198_api-d3d4313a1e04`，Git provenance、release-specific 容器镜像、state、线上 contract、公网 200、日志、锁和 recovery 全部正常，业务代码和数据未改。

- 2026-08-14：深度重构第四十九阶段参考 Vendure RequestContext 与 Medusa query config，把 `GET /api/master-data` 已有权限和销售归属显式下推到 Prisma read repository；Controller 的权限清空与归属终检保留，默认 snapshot 契约不变。定向测试 5/5、API typecheck、434 路由治理与安全契约 3/3 通过。发布前后管理员响应 SHA 均为 `ba1c6c…eb7b`、仓库出库角色均为 `e3b37a…2c79f`，12 个字段与集合数量逐项不变。实施期间检测并吸收另一仓库权限发布；Docker 构建在全局锁外重叠导致失败关闭，已重新构建/重启 API、修复 state、验证公网 200 后清除 recovery。当前发布 `whitelist-c0af1dfb63f19bfc11b7bd2f`，锁 free、recovery clear。

- 2026-08-13：深度重构第四十八阶段在实施 `master-data` 下推前发现 47 有 26 个内容差异和 6 个远端独有文件，立即停止避免覆盖新权限语义。捕获时 496/496 运行时文件逐字节一致；修复 pricing/RBAC/理货定向保护 17/17 和 `RequireAllPermissions` 架构扫描，436 路由治理及三端类型通过。cutover dry-run 又拦截根 `node_modules` 符号链接泄露并补双形态排除。最终发布 `git-3fe515dc49d0_web-0e8727478b26_api-0f0109745a3f`，Git bundle provenance traceable、镜像与 API release ID 匹配、公网 200、锁 free、recovery clear。

- 2026-08-12：深度重构第四十三阶段修复发布证据失败开放：发布指纹与标准 deploy 统一忽略 `._*`/`.DS_Store`，rsync 禁止传输或合成 AppleDouble；provenance 优先报告运行镜像与 state 漂移，白名单入口在任何源码替换前校验镜像身份，仅允许 `scope none + scripts/*` 显式吸收已复核运行时。固定样本、治理/架构/安全契约与线上失败关闭探针通过；最终零服务重启发布 `whitelist-f79893be417ad4571504b903`，Web/API image match true、公网 200、锁 free、recovery clear。历史 10 个元数据文件未删除，业务代码/镜像/数据未改。

- 2026-08-12：深度重构第四十二阶段把 HTTP 审计从拦截器的 `any`/fire-and-forget 巨型 Repository 直连迁入显式 writer port 与有界后台 dispatcher；保留原审计范围、字段和非阻塞响应，增加 100/500ms 两次重试、1,000 条待处理上限、脱敏失败告警，并以稳定 UUID + Prisma upsert/InMemory 去重避免未知提交结果造成重复。定向测试 6/6、API typecheck、432 路由治理与安全契约 3/3 通过；发布 `whitelist-9fda300b81a046c50fa26700`，线上 checksum、API 容器/health/日志、镜像匹配、锁/recovery 通过，生产可回滚探针同 ID 连续 upsert 仅 1 条且已清理。

- 2026-08-12：深度重构第四十一阶段修复 47 运行镜像证据漂移：发布状态、provenance audit 与默认 runtime manifest 统一使用容器 `ImageManifestDescriptor.digest`，老 Docker 回退 `.Image`；v3 清单不再依赖可被 BuildKit 清理的 image config 对象，同时 legacy bootstrap 显式保留 v2 逐字节校验。脚本定向测试、shell 语法、治理/上下文/432 路由与安全契约 3/3 通过；零服务重启发布 `whitelist-5ff54e642bb396194f8e3bbd`，线上 Web/API image match 均为 true，v3 清单采集 4/4，公网 health 200、锁 free、recovery clear。

- 2026-08-12：深度重构第四十阶段新增路由数据所有权策略，仅将已证实自持数据的 `pricing` 从 App 全局导航刷新中隔离；进入报价不再新增运单/业务成本/基础资料请求，且不推进全局时钟，紧接进入业务仍各新增 1 轮。其他路由保留 legacy 全量回退，权限、路由、页面自有请求、5 分钟刷新和 UI 未改。定向测试 9/9、Web typecheck、432 路由治理/安全契约 3/3 通过；发布 `whitelist-b451c554dbd946c6d76e8bd9`，线上三文件 checksum、Web 生产构建/容器、公网 health 200、错误日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第三十八阶段为 `GET /api/finance/business-cost-audits` 新增独立数据库候选查询构建器，把业务审核、默认非作废、销售数据范围、状态、单号、客户编号、转单号、业务员、费用字段和北京时间范围提前下推到 Prisma/PostgreSQL；原 `buildBusinessCostAuditListResponse` 继续执行最终筛选、金额换算、合计、计算字段、排序和分页，不改业务逻辑。定向测试 6/6、API typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-b1fa31f8526c608da58bb0cd`，线上源码 checksum、API 镜像/容器、管理员只读接口 200（202ms）、公网 health、日志、锁和 recovery 均通过。47 当前默认查询由原先加载 69 条降为 49 条，减少 28.99%。

- 2026-08-12：深度重构第三十二阶段把 `App.tsx` 的全局工作区请求编排迁入独立 `workspaceRefresh` app-shell orchestrator；原权限条件、并发请求组、组间顺序、请求参数、逐项失败回退、setter 时机及客服数据确认页 skip 逻辑未改，`App.tsx` 净减少 56 行。迁移前/后页面 characterization 2/2、orchestrator 单测 3/3、Web typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-85f6006e0917073185ee5f0c`，线上两文件 checksum、Web 镜像、容器、health、关键错误、锁和 recovery 均通过。

- 2026-08-12：深度重构第三十一阶段把客服、业务、运营三类问题件创建及回复、关闭、协助 6 条路由迁入独立 `ProblemTicketCommandController/Service/Repository port`；原 Prisma/InMemory 实现、动态权限、阶段校验、字段覆盖、默认值、状态、审计和 lineage 未改，`DataController` 减少 39 行/6 路由。迁移前 E2E 3/3，迁移后定向测试 9/9、路由 AST 6/6 等价、API typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-a78817a573cb4f64682c2883`，线上五文件 checksum、6 条映射、真实角色 401/403/404、容器、health、日志、锁和 recovery 均通过。历史宽 E2E 的无 body 关闭请求在迁移前后均为既有 500，本轮不改变该行为。

- 2026-08-12：深度重构第三十阶段把客服业务/代理数据审核、更新、费用预览/修改、单侧/双侧反审核及待确认列表 11 条路由迁入独立 `CustomerServiceDataConfirmController/Service/Repository port`；原 Prisma/InMemory 实现和所有业务契约未改，`DataController` 减少 116 行/11 路由。迁移前后 characterization 3/3，focused tests 18/18、API typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-160c821a6f076812cec9f864`，线上五文件 checksum、11 条映射、真实角色 401/403/200、10 条缺失样本 404、容器、health、日志、锁和 recovery 均通过。生产无可用 block-mask 角色样本，该分支由本地 service 契约覆盖。

- 2026-08-11：用户明确将“整个系统业务逻辑不得因重构改变”设为深度重构硬门禁。已写入 `AGENTS.md`、ADR 0005 和模块切片模板：外部项目只借鉴架构/UI，不替换 Sunny 业务语义；路由、字段、权限、数据范围、状态、金额、事务、审计和前端操作链路必须以 47 当前行为为基线证明等价；任何真实行为修正必须退出重构切片并单独确认。


## 归档索引

- `docs/archive/codex-state/2026-07-through-2026-07-28.md`：原始 231 条状态记录，完整保留。
- `docs/archive/codex-state/2026-08-12-late-phases.md`：从活动状态移出的 2026-08-12 高信号完成记录。
- `docs/archive/dev-now/`：已完成会话状态。
- `docs/backlog/dev-now/`：本地完成但未发布或仍待处理的历史会话状态。
