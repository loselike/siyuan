# Sunny 当前开发状态

> 只保留当前 47 指纹、活跃风险和最近高信号事实；完整历史见 `docs/archive/codex-state/`。本文件不是并发任务锁。

## 当前 47 基线

- 远端 release ID：`whitelist-b1fa31f8526c608da58bb0cd`
- Web 指纹：`ac046cee89617a8d19251f90792af59fe7fd90f6ded76eb60ff5c3591b873900`
- API 指纹：`dd1b2267e9fb343cd572f2f7a1f0a188e65098d5513c72c84257557df4419e6f`
- migration 指纹：`9dbe533cdab331d1df29639e83680c9bfe495ae20f276cfdf8641e227b0d4081`
- 当前重构分支：`codex/sunny-refactor-phase38`；运行候选提交 `dfa105b` 已发布业务成本审核数据库候选查询。
- 业务成本审核默认列表已把业务审核状态、作废状态、销售数据范围和安全可下推的检索条件提前交给 Prisma/PostgreSQL；原内存响应构建器仍是筛选、金额、排序、分页和导出的最终权威。47 当前数据默认候选从 69 条降至 49 条，少加载 20 条（28.99%），接口契约与权限未改。
- 47 仍处于 `SOURCE_MODE=WHITELIST_CAS`，API health 的 `releaseId` 仍为 `unknown`；Git 同源基线恢复仍是后续治理事项，不能将本次白名单发布当作标准 Git provenance 已恢复。

## 活跃工作

- 深度重构第三十八阶段已完成业务成本审核查询第一步数据库收窄并发布闭环；下一切片继续把该列表的合计与默认排序/分页改成数据库聚合和轻量分页，但必须继续保留现有金额、权限、计算字段和导出契约。

## 活跃风险

- 根工作树 `/Users/j1ng/Tools/sunny` 仍包含其他会话和历史未提交改动；禁止清理、覆盖或作为标准发布候选。
- phase6 已捕获当前 47 巨型 Repository 基线，phase7 已恢复治理保护网，phase8-38 已建立理货、仓租、包裹生命周期、机器导入、仓库 dispatch、面单、业务发票、承运商任务、轨迹、客服、前端 app-shell 和首个财务查询边界，并开始消除双 Repository、前端编排重复和数据库全量加载；后续切片必须从 `codex/sunny-refactor-phase38` 继续，若 47 再变化则先重新做 checksum 基线吸收。
- phase18 的 SSH 结束悬停在 phase23 多文件 CAS 第 4 个文件再次出现；本次中止后脚本完整回滚三份已替换文件、锁 free 且 recovery clear，随后干净重试成功。应单独为 CAS/SSH 增加有界超时与可诊断恢复，不得绕过锁。
- 交接预览存在重构前已冻结的适配器差异：Prisma 按 `createdAt desc` 返回最近一次打印，InMemory 因审计日志 `unshift` 后再 `reverse().find()` 返回第一次打印。第十九阶段只迁移依赖边界，不猜测统一口径；若修正须单独确认业务目标。
- 同箱规补录的 Prisma 生产适配器按 `requestId` 幂等返回既有补录结果，InMemory 适配器忽略 `requestId` 并在重复补录时返回 400；这是本轮前已存在且已冻结的开发/生产差异，不得在结构重构中猜测统一。
- 历史聚合理货纠正只存在于 Prisma 生产适配器，InMemory 适配器没有对应方法；这是本轮前已存在且刻意不在结构重构中补齐的开发环境差异，允许路径测试继续使用生产适配器 characterization。
- 理货开始路径的 Prisma/InMemory 适配器仍保留既有审计动作名差异（`warehouse.tally_task.start` / `warehouse.tally.start`）；该差异已明确冻结，不得在后续结构重构中猜测统一口径。
- 历史 `problemTickets.test.tsx` 仍引用当前界面已不存在的“问题件”按钮，属于待拆分旧测试债务，不得据此恢复旧 UI。
- 历史 `workspace.test.tsx` 的稳定链接/无权限 URL 两条宽页面测试在 phase35 迁移前已因旧 heading 与客服 lazy fixture 的 `shipments is not iterable` 失败；当前侧栏展示/导航契约由独立组件、characterization 和 hook 11 条测试保护，不得借结构重构恢复旧页面文案或顺带修客服数据夹具。
- 47 为全局串行发布资源；标准发布必须从干净发布协调 worktree 取得 baseline receipt，远端基线变化时重新合并。

## 最近高信号事实

- 2026-08-12：深度重构第三十八阶段为 `GET /api/finance/business-cost-audits` 新增独立数据库候选查询构建器，把业务审核、默认非作废、销售数据范围、状态、单号、客户编号、转单号、业务员、费用字段和北京时间范围提前下推到 Prisma/PostgreSQL；原 `buildBusinessCostAuditListResponse` 继续执行最终筛选、金额换算、合计、计算字段、排序和分页，不改业务逻辑。定向测试 6/6、API typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-b1fa31f8526c608da58bb0cd`，线上源码 checksum、API 镜像/容器、管理员只读接口 200（202ms）、公网 health、日志、锁和 recovery 均通过。47 当前默认查询由原先加载 69 条降为 49 条，减少 28.99%。

- 2026-08-12：深度重构第三十七阶段把 `App.tsx` 的员工侧栏 DOM、品牌、主/次菜单、稳定 href、活动态、展开箭头、模块/二级未读格式化和 AI 助手卡迁入独立 `StaffSidebar`；菜单权限、文案、CSS、URL、修饰键、未读接口与数据行为未改，`App.tsx` 净减少 81 行。迁移前当前行为 characterization 4/4，迁移后组件/导航/页面测试 8/8，Web typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-a94cd9e2411ad11bdce6d36c`，线上两文件 checksum、Web 镜像、容器、内外 health、关键错误、锁和 recovery 均通过。

- 2026-08-12：深度重构第三十六阶段把 `App.tsx` 的主/次菜单点击、活动目录展开/收起、sidebar sub-nav 注册/清理/别名解析、context 和品牌返回迁入独立 `staffSidebarNavigation`；普通点击、Ctrl/Meta/Shift/Alt 修饰键、稳定 href、URL、active 状态和数据刷新链路未改，`App.tsx` 净减少 47 行。迁移前/后 App characterization 3/3，导航 hook 单测 6/6，Web typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-b05d7bb207d69df556ef4f44`，线上两文件 checksum、Web 镜像、容器、内外 health、关键错误、锁和 recovery 均通过。

- 2026-08-12：深度重构第三十五阶段把 `App.tsx` 的 requested route 初始化、统一 navigate/history、15 秒数据刷新触发、session refresh、无权限 replace fallback 和 popstate 迁入独立 `staffRouteNavigation`；各 hook 保持原副作用位置，canonical/reload href、version check、提示和菜单状态未改，`App.tsx` 净减少 22 行。当前 App characterization 2/2、hook 单测 3/3、Web typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-5e3d9e8e8318cec867d42f2c`，线上两文件 checksum、Web 镜像、容器、health、关键错误、锁和 recovery 均通过。两条旧宽页面路由测试在迁移前已失败并记录为测试债务。

- 2026-08-12：深度重构第三十四阶段把 `App.tsx` 的通知目标初始化、同源/路由/可见菜单校验、跳转和匹配消费清理迁入独立 `useNotificationNavigation`；原 warning、history 顺序、目标 truthy 规则、运单详情打开和其他查询参数保留未改，`App.tsx` 净减少 27 行。迁移前/后 App characterization 1/1、hook 单测 3/3、Web typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-9143e7267a5a474b451ad79e`，线上两文件 checksum、Web 镜像、容器、health、关键错误、锁和 recovery 均通过。

- 2026-08-12：深度重构第三十三阶段把 `App.tsx` 的 `siyuan-session` 持久化和 current-session 刷新迁入独立 `sessionStore` / `useCurrentSessionRefresh`；原 key、JSON、异常、刷新时机、限流、并发复用、token 竞争保护与退出清理未改，`App.tsx` 净减少 42 行。迁移前/后 App characterization 2/2、store/hook 单测 4/4、Web typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-795b1ca9b284c0ada90edab1`，线上三文件 checksum、Web 镜像、容器、health、关键错误、锁和 recovery 均通过。token 仍在 localStorage，本阶段只收口边界，不冒充安全问题已修复。

- 2026-08-12：深度重构第三十二阶段把 `App.tsx` 的全局工作区请求编排迁入独立 `workspaceRefresh` app-shell orchestrator；原权限条件、并发请求组、组间顺序、请求参数、逐项失败回退、setter 时机及客服数据确认页 skip 逻辑未改，`App.tsx` 净减少 56 行。迁移前/后页面 characterization 2/2、orchestrator 单测 3/3、Web typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-85f6006e0917073185ee5f0c`，线上两文件 checksum、Web 镜像、容器、health、关键错误、锁和 recovery 均通过。

- 2026-08-12：深度重构第三十一阶段把客服、业务、运营三类问题件创建及回复、关闭、协助 6 条路由迁入独立 `ProblemTicketCommandController/Service/Repository port`；原 Prisma/InMemory 实现、动态权限、阶段校验、字段覆盖、默认值、状态、审计和 lineage 未改，`DataController` 减少 39 行/6 路由。迁移前 E2E 3/3，迁移后定向测试 9/9、路由 AST 6/6 等价、API typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-a78817a573cb4f64682c2883`，线上五文件 checksum、6 条映射、真实角色 401/403/404、容器、health、日志、锁和 recovery 均通过。历史宽 E2E 的无 body 关闭请求在迁移前后均为既有 500，本轮不改变该行为。

- 2026-08-12：深度重构第三十阶段把客服业务/代理数据审核、更新、费用预览/修改、单侧/双侧反审核及待确认列表 11 条路由迁入独立 `CustomerServiceDataConfirmController/Service/Repository port`；原 Prisma/InMemory 实现和所有业务契约未改，`DataController` 减少 116 行/11 路由。迁移前后 characterization 3/3，focused tests 18/18、API typecheck、432 路由治理和安全契约 3/3 通过；发布 `whitelist-160c821a6f076812cec9f864`，线上五文件 checksum、11 条映射、真实角色 401/403/200、10 条缺失样本 404、容器、health、日志、锁和 recovery 均通过。生产无可用 block-mask 角色样本，该分支由本地 service 契约覆盖。

- 2026-08-12：深度重构第二十九阶段把 Prisma/InMemory 完全重复的客服数据确认审核周期、快照、主代理计费附加和业务/代理敏感字段裁剪 7 个纯函数原样迁入单一策略；两套函数体各 7/7 AST 等价，两个巨型 Repository 各减少 90 行，净减少 78 行运行时重复实现。迁移前 characterization 2/2、迁移后策略/characterization 9/9、API typecheck、432 路由治理与安全契约 3/3 通过；发布 `whitelist-a249ced4ea0553af9d05f4fd`，线上三文件 checksum、真实角色 401/403/200、字段与当前周期 0 mismatch、容器、health、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十六阶段把客服问题标签查询/创建/更新/删除迁入 `ProblemTicketTagController/Service/port`，原 GET 11 项动态权限与拒绝审计、两套 Repository 的规范化、上限、重名、排序、返回、事务和审计实现未改。`DataController` 减少 4 路由/27 行；迁移前/后 E2E 及 service 5/5、API typecheck、432 路由门和完整治理通过；发布 `whitelist-59208403aa9168c5acce03a4`，线上五文件 checksum、4 路由映射、401/403/404/200 无业务写入探针、镜像/容器、内外 health、错误日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十五阶段把运单/运营单的两条人工轨迹新增迁入 `TrackingManualEventCommandController/Service/port`，原两套 Repository 的可见范围、轨迹事件、最新轨迹、返回和 lineage 实现未改。`DataController` 减少 2 路由/13 行；迁移前后固定 E2E/lineage、service 3/3、API typecheck、432 路由门和完整治理通过；发布 `whitelist-b2bf6880f1e548adbb9b5724`，线上五文件 checksum、两路由 401/403/404 无业务写入探针、镜像/容器、内外 health、错误日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十四阶段把外部轨迹批量导入迁入 `TrackingImportCommandController/Service/port`，原两套 Repository 的动态权限顺序、拒绝审计、日期、同票最新轨迹、统计、运单更新、审计和 lineage 实现未改。`DataController` 减少 1 路由/12 行；迁移前后固定 E2E/lineage、service 4/4、API typecheck、432 路由门和完整治理通过；发布 `whitelist-314b33fff32a6b100acd7db0`，线上五文件 checksum、路由 401/403/400 无业务写探针、镜像、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十三阶段把承运商任务执行与失败重试两条路由迁入 `CarrierTaskCommandController/Service/port`，原两套 Repository 的状态、次数、错误、运单轨迹、事务、响应和 lineage 实现未改。`DataController` 减少 2 路由/19 行；迁移前后固定 E2E/lineage、service 3/3、API typecheck、432 路由门和完整治理通过；发布 `whitelist-3a18fb14982711c70a4b0ece`，线上五文件 checksum、两路由 401/403/404 无业务写探针、镜像、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十二阶段把业务发票上传、代理发票模板下载和业务发票下载三条路由迁入 `ShipmentBusinessInvoiceController/Service/port` 与独立 Excel 文件存储，原 Prisma/InMemory 的范围、模板选择、状态、持久化和审计实现未改。`DataController` 减少 3 路由/71 行；迁移前后 E2E 1/1、service 4/4、存储 2/2、API typecheck、432 路由门和完整治理通过；发布 `whitelist-5f3689771c14d35b5ac79bcc`，线上六文件 checksum、三路由 401、真实拒绝角色 403、文件校验 400、缺失运单 404、镜像/状态、容器、日志、锁和 recovery 均通过。

- 2026-08-11：用户明确将“整个系统业务逻辑不得因重构改变”设为深度重构硬门禁。已写入 `AGENTS.md`、ADR 0005 和模块切片模板：外部项目只借鉴架构/UI，不替换 Sunny 业务语义；路由、字段、权限、数据范围、状态、金额、事务、审计和前端操作链路必须以 47 当前行为为基线证明等价；任何真实行为修正必须退出重构切片并单独确认。

- 2026-08-11：业务管理 → 运单管理新增“站点”“业务员”组合筛选。以 47 当前源码为基线构建干净恢复候选，Shared/API/Web 类型检查和两个 Web 定向测试通过；仅同步 `apps/web/src/modules/orders/OrdersPage.tsx` 与 `apps/web/src/styles.css`，发布批次 `whitelist-6ccdfdace5b37aa21d59a0cb`。线上源码校验和与候选一致，静态产物包含筛选样式标记；未改 API、Prisma 或线上业务数据。

- 2026-08-06：Shared 巨型入口开始实质拆分，599 行仓库契约迁入独立模块；原块与新文件 checksum 完全一致，三端类型检查和线上 Web/API 验证通过。
- 2026-08-06：仓库巨型页面继续拆分，“处理理货”弹窗成为独立受控组件；合并全选、拆分单选、草稿和提交锁定向测试 3/3 通过，白名单发布 47。

## 归档索引

- `docs/archive/codex-state/2026-07-through-2026-07-28.md`：原始 231 条状态记录，完整保留。
- `docs/archive/dev-now/`：已完成会话状态。
- `docs/backlog/dev-now/`：本地完成但未发布或仍待处理的历史会话状态。
