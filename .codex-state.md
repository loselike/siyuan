# Sunny 当前开发状态

> 只保留当前 47 指纹、活跃风险和最近高信号事实；完整历史见 `docs/archive/codex-state/`。本文件不是并发任务锁。

## 当前 47 基线

- 远端 release ID：`whitelist-b2bf6880f1e548adbb9b5724`
- Web 指纹：`d7ea19f297e7f3cec11edf20350760d6e1c0a5207320f88d1f7389d3b91ae434`
- API 指纹：`fdbe8f84c2abb4664c6563d165aa40605010a28d9a6f9515dc36923cb371a93e`
- migration 指纹：`9dbe533cdab331d1df29639e83680c9bfe495ae20f276cfdf8641e227b0d4081`
- 当前重构分支：`codex/sunny-refactor-phase25`；运行候选提交 `dceea39` 已发布人工轨迹新增命令边界。
- 运单/运营单的两条人工轨迹新增已接入 `TrackingManualEventCommandController/Service/Repository port`；原 Prisma/InMemory 的范围、轨迹持久化、最新轨迹和 lineage 实现未改。`DataController` 减少 2 路由/13 行，治理预算收紧为 225/2,554；五份运行源码与 47 校验和一致，两路由线上 401/403/404 无业务写入探针、容器、公网/容器内 health、错误日志、发布锁和 recovery 均通过。
- 47 仍处于 `SOURCE_MODE=WHITELIST_CAS`，API health 的 `releaseId` 仍为 `unknown`；Git 同源基线恢复仍是后续治理事项，不能将本次白名单发布当作标准 Git provenance 已恢复。

## 活跃工作

- 深度重构第二十五阶段已完成人工轨迹新增的 Controller/Service/port 迁移、治理预算收紧和 47 发布闭环；下一阶段拆分客服问题标签查询与维护边界，先冻结多权限查询及增删改权限语义。

## 活跃风险

- 根工作树 `/Users/j1ng/Tools/sunny` 仍包含其他会话和历史未提交改动；禁止清理、覆盖或作为标准发布候选。
- phase6 已捕获当前 47 巨型 Repository 基线，phase7 已恢复治理保护网，phase8-25 已建立理货、仓租、包裹生命周期、机器导入、仓库 dispatch、面单、业务发票、承运商任务和轨迹命令模块边界；后续切片必须从 `codex/sunny-refactor-phase25` 继续，若 47 再变化则先重新做 checksum 基线吸收。
- phase18 的 SSH 结束悬停在 phase23 多文件 CAS 第 4 个文件再次出现；本次中止后脚本完整回滚三份已替换文件、锁 free 且 recovery clear，随后干净重试成功。应单独为 CAS/SSH 增加有界超时与可诊断恢复，不得绕过锁。
- 交接预览存在重构前已冻结的适配器差异：Prisma 按 `createdAt desc` 返回最近一次打印，InMemory 因审计日志 `unshift` 后再 `reverse().find()` 返回第一次打印。第十九阶段只迁移依赖边界，不猜测统一口径；若修正须单独确认业务目标。
- 同箱规补录的 Prisma 生产适配器按 `requestId` 幂等返回既有补录结果，InMemory 适配器忽略 `requestId` 并在重复补录时返回 400；这是本轮前已存在且已冻结的开发/生产差异，不得在结构重构中猜测统一。
- 历史聚合理货纠正只存在于 Prisma 生产适配器，InMemory 适配器没有对应方法；这是本轮前已存在且刻意不在结构重构中补齐的开发环境差异，允许路径测试继续使用生产适配器 characterization。
- 理货开始路径的 Prisma/InMemory 适配器仍保留既有审计动作名差异（`warehouse.tally_task.start` / `warehouse.tally.start`）；该差异已明确冻结，不得在后续结构重构中猜测统一口径。
- 历史 `problemTickets.test.tsx` 仍引用当前界面已不存在的“问题件”按钮，属于待拆分旧测试债务，不得据此恢复旧 UI。
- 47 为全局串行发布资源；标准发布必须从干净发布协调 worktree 取得 baseline receipt，远端基线变化时重新合并。

## 最近高信号事实

- 2026-08-12：深度重构第二十五阶段把运单/运营单的两条人工轨迹新增迁入 `TrackingManualEventCommandController/Service/port`，原两套 Repository 的可见范围、轨迹事件、最新轨迹、返回和 lineage 实现未改。`DataController` 减少 2 路由/13 行；迁移前后固定 E2E/lineage、service 3/3、API typecheck、432 路由门和完整治理通过；发布 `whitelist-b2bf6880f1e548adbb9b5724`，线上五文件 checksum、两路由 401/403/404 无业务写入探针、镜像/容器、内外 health、错误日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十四阶段把外部轨迹批量导入迁入 `TrackingImportCommandController/Service/port`，原两套 Repository 的动态权限顺序、拒绝审计、日期、同票最新轨迹、统计、运单更新、审计和 lineage 实现未改。`DataController` 减少 1 路由/12 行；迁移前后固定 E2E/lineage、service 4/4、API typecheck、432 路由门和完整治理通过；发布 `whitelist-314b33fff32a6b100acd7db0`，线上五文件 checksum、路由 401/403/400 无业务写探针、镜像、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十三阶段把承运商任务执行与失败重试两条路由迁入 `CarrierTaskCommandController/Service/port`，原两套 Repository 的状态、次数、错误、运单轨迹、事务、响应和 lineage 实现未改。`DataController` 减少 2 路由/19 行；迁移前后固定 E2E/lineage、service 3/3、API typecheck、432 路由门和完整治理通过；发布 `whitelist-3a18fb14982711c70a4b0ece`，线上五文件 checksum、两路由 401/403/404 无业务写探针、镜像、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十二阶段把业务发票上传、代理发票模板下载和业务发票下载三条路由迁入 `ShipmentBusinessInvoiceController/Service/port` 与独立 Excel 文件存储，原 Prisma/InMemory 的范围、模板选择、状态、持久化和审计实现未改。`DataController` 减少 3 路由/71 行；迁移前后 E2E 1/1、service 4/4、存储 2/2、API typecheck、432 路由门和完整治理通过；发布 `whitelist-5f3689771c14d35b5ac79bcc`，线上六文件 checksum、三路由 401、真实拒绝角色 403、文件校验 400、缺失运单 404、镜像/状态、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十一阶段把运单面单列表、生成、上传、下载、作废五条路由迁入 `ShipmentLabelLifecycleController/Service/port` 与独立文件存储，原 Prisma/InMemory 的权限、生成复用、状态、转单号和审计实现未改。`DataController` 减少 4 路由/78 行；迁移前后 E2E 1/1、service 8/8、存储 2/2、API typecheck、432 路由门和完整治理通过；发布 `whitelist-6b366bf24b30b19afb54d457`，线上七文件 checksum、五路由 401、真实拒绝角色 403、文件校验 400、缺失运单 404、镜像/状态、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第二十阶段把出库确认及基础、批量、贴唛头三层动态权限迁入 `WarehouseDispatchController/Service/Repository/Authorizer`，原 Prisma/InMemory 的状态、费用、交接单、包裹事务、轨迹任务和审计实现未改。`DataController` 减少 1 路由/14 行；迁移前后 dispatch E2E 2/2、service 5/5、API typecheck、432 路由门和完整治理通过；发布 `whitelist-f13e61b7c0b453cfba526077`，线上五文件 checksum、401/403/404 无写探针、镜像/状态、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第十九阶段把待出库列表、报关标记、交接预览和交接打印四路由迁入独立 `WarehouseDispatchController/Service/port`，原 Prisma/InMemory 的范围、状态、去重、打印版本、时间和审计实现未改。`DataController` 减少 1 路由/7 行；迁移前后 E2E 2/2、service 2/2、API typecheck、432 路由门和完整治理通过；发布 `whitelist-6730149631d190da8b9651de`，线上六文件 checksum、401/403/200/400/404 无写探针、镜像/状态、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第十八阶段把机器过机 Excel 导入从 `DataController` 迁入独立 Controller/Service/port，完整保留 multipart、20MB、文件名/MIME/扩展名/内容校验、大小写不敏感 commit、SHA-256 批次幂等以及两套 Repository 的权限、屏蔽、事务、包裹和审计语义。`DataController` 减少 1 路由/21 行；迁移前后机器导入 E2E 2/2、service 5/5、API typecheck、432 路由门和完整治理通过；发布 `whitelist-06a15854f09caf852dc2f045`，线上五文件 checksum、401/403/400、有效 XLSX 只预览 201、镜像/状态、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第十七阶段把包裹创建、手工收货、同箱规补录、拆票、修改、备注和异常七条路由从 `DataController` 迁入独立 Controller/Service/port，原两套 Repository 的校验、客户范围、状态、事务、审计与既有幂等行为未改。`DataController` 减少 7 路由/48 行；迁移前后生命周期 E2E 2/2、service 3/3、API typecheck、432 路由门和完整治理通过；发布 `whitelist-e9e2c208ddd61523faedc7e6`，线上五文件 checksum、七路由 401、拒绝 403、管理员 400/404 探针、容器、日志、锁和 recovery 均通过。

- 2026-08-12：深度重构第六阶段先把 47 当前 CAS 运行源码精确捕获为 `0323ac6`，再以 `5540cda` 抽取水单展示纯策略，统一 Prisma/InMemory 的付款编号清洗、凭证裁剪、凭证审计快照和列表汇总；两份巨型 Repository 各减少 69 行。策略单测 4/4、财务 E2E 2/2、API typecheck 通过，发布批次 `whitelist-ce14f12fbe842965a0662c7c`，线上 423/423 源码同哈希，未登录水单列表/凭证均 401。

- 2026-08-12：深度重构第五阶段继续 Medusa 运行时契约切片，为水单创建、修改、到账、取消匹配和作废接入 parser；合法数字字符串和空可选 body 保持兼容，畸形类型在 application service/Repository 前返回 400。Parser 单测 21/21、财务 E2E 3/3、API typecheck 通过，发布批次 `whitelist-af4e5b917580d563818c3c60`，三个目标文件线上 checksum 一致。

- 2026-08-11：深度重构第四阶段参考 Vendure 的领域模块与稳定 port，新增 WaterReceiptLifecycleRepository/Service，把水单查询、创建、修改、到账、归档、作废、凭证删除和导出从综合 FinanceReceivableService 移入独立边界；两套旧 Repository 继续作为适配器。服务单测 4/4、财务 E2E 3/3、API typecheck 通过，发布批次 `whitelist-62fb932c54798be6f35a933f`，五个目标文件线上 checksum 一致。

- 2026-08-11：深度重构第三阶段参考 Medusa 的运行时契约做最小切片：新增通用 Runtime Input Pipe 和水单匹配 parser，保留 `receivableId`/旧 `receivableFinanceItemId`、数字字符串与原默认值；非法币种、来源、金额在 Repository 前返回 400。单测 7/7、财务 E2E 2/2、API typecheck 通过，发布批次 `whitelist-706ae8c36f39b20cd997d314`，三个目标文件线上 checksum 一致。

- 2026-08-11：用户明确将“整个系统业务逻辑不得因重构改变”设为深度重构硬门禁。已写入 `AGENTS.md`、ADR 0005 和模块切片模板：外部项目只借鉴架构/UI，不替换 Sunny 业务语义；路由、字段、权限、数据范围、状态、金额、事务、审计和前端操作链路必须以 47 当前行为为基线证明等价；任何真实行为修正必须退出重构切片并单独确认。

- 2026-08-11：建立 `codex/sunny-refactor-phase1` 隔离 worktree，以 47 运行源码为基线；记录模块化单体 ADR 和 GitHub 参考项目。首个财务水单切片把币种、输入金额模式、汇率冲突与换算策略从 Prisma/InMemory 巨型 Repository 抽为单一策略，定向测试 8/8 与 API typecheck 通过，并发布 47。

- 2026-08-11：第二阶段恢复当前水单待审核匹配语义的 E2E 保护，建立 `WaterReceiptAllocationRepository` port 与 application service；Controller 的五个分配入口不再经过综合 Finance service，现有 Prisma/InMemory 事务、权限、金额与审计实现保持不变。定向单测 2/2、财务 E2E 2/2、API typecheck 通过并发布 47。

- 2026-08-11：业务管理 → 运单管理新增“站点”“业务员”组合筛选。以 47 当前源码为基线构建干净恢复候选，Shared/API/Web 类型检查和两个 Web 定向测试通过；仅同步 `apps/web/src/modules/orders/OrdersPage.tsx` 与 `apps/web/src/styles.css`，发布批次 `whitelist-6ccdfdace5b37aa21d59a0cb`。线上源码校验和与候选一致，静态产物包含筛选样式标记；未改 API、Prisma 或线上业务数据。

- 2026-08-06：Shared 巨型入口开始实质拆分，599 行仓库契约迁入独立模块；原块与新文件 checksum 完全一致，三端类型检查和线上 Web/API 验证通过。
- 2026-08-06：仓库巨型页面继续拆分，“处理理货”弹窗成为独立受控组件；合并全选、拆分单选、草稿和提交锁定向测试 3/3 通过，白名单发布 47。
- 2026-08-03：完成仓库效率治理并发布 47；Finance Catalog 参考切片、问题件只读切片、最小验证选择器和上下文归档规则已落地。
- 2026-08-03：发布门禁修复 worktree `.git` 指针同步和标准/远端指纹排除不一致；recovery 已按校验和流程清除，锁和恢复状态均为 clear。
- 2026-08-03：47 Finance Catalog 允许/拒绝为 200/403，问题件允许/拒绝/未登录为 200/403/401；容器健康，线上无源码漂移。
- 2026-08-02：原 231 条状态历史及 151 个终态会话完整归档，64 个本地未发布会话移入 backlog，没有删除内容。

## 归档索引

- `docs/archive/codex-state/2026-07-through-2026-07-28.md`：原始 231 条状态记录，完整保留。
- `docs/archive/dev-now/`：已完成会话状态。
- `docs/backlog/dev-now/`：本地完成但未发布或仍待处理的历史会话状态。
