# Sunny 当前开发状态

## 当前 2026-07-26 代码瘦身第四十九阶段已发布 47

2026-07-26 已将两套 Repository 中与 47 四份源码逐字一致的代理来源类型、来源归一化、范围分组和范围键收敛到现有 `pricing/agent-markup-query.shared.ts`。范围键、模块允许值、空代理排除、相同价格表/文件重复行数合并和中文文件名排序保持不变；参与批量规则和导入目标的更广模块识别函数保留原位，没有扩展到写入路径。两套 Repository 各增加 1 行、删除 41 行，共享运行时增加 47 行、删除 1 行后生产源码净减少 34 行；计入测试后生产源码与测试合计净减少 14 行。共享 helper 与完整混合加价 API 固定样本 5/5、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三文件白名单补丁，只构建/重启 API，无迁移；production build、源码校验和、旧声明清零、编译产物来源清洗/合并/排序探针、41 条加价规则及 750 行价格表只读响应发布前后哈希一致、客户 403、未登录 401、容器/宿主/公网 health 均通过，API 启动成功且实际错误日志为 0。

## 当前 2026-07-26 代码瘦身第四十八阶段已发布 47

2026-07-26 已审计两套 Repository 的代理加价只读响应簇；确认 47 的列表、分组和展示构造器包含本地尚无的去重线路计数、规则比较、禁用规则处理、规则拆分说明和迪拜海运按 CBM 展示，因此保留两边响应构造器不动。仅将本地与 47 四份源码逐字一致的命中开关、金额格式、价格行过滤/排序、范围等级和规则匹配等八个只读原语收敛到 `pricing/agent-markup-query.shared.ts`。两套 Repository 合计增加 2 行、删除 131 行，新增 74 行共享运行时后生产源码净减少 55 行；计入 70 行测试后生产源码与测试合计净增加 15 行。共享 helper 与完整混合加价 API 固定样本 4/4、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三文件白名单补丁，只构建/重启 API，无迁移；production build、源码校验和、旧定义清零、编译产物过滤/排序/格式探针、三组真实只读响应发布前后哈希一致、客户 403、未登录 401、容器/宿主/公网 health 均通过，API 启动成功且实际错误日志为 0。

## 当前 2026-07-26 代码瘦身第四十七阶段已发布 47

2026-07-26 已将 `PrismaRepository` 与 `InMemoryRepository` 中重复的迪拜价格表响应构造、空海运识别、价格档位和备注去重收敛到 `pricing/dubai-pricing.shared.ts`。本地与 47 的响应构造存在一处既有业务差异：本地空海运都应用加价，47 仅海运应用加价；共享构造器通过显式回调保留两边现状，没有用本地实现覆盖远端策略。两套 Repository 各增加 4 行、删除 75 行，新增 90 行共享运行时后生产源码净减少 52 行；计入 49 行测试后生产源码与测试合计净减少 3 行。共享 helper 与完整 API 固定样本 3/3、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三文件白名单补丁，只构建/重启 API，无迁移；production build、源码校验和、旧定义清零、编译产物 AIR 18/SEA 1802/按方/备注去重探针、发布前后只读响应哈希一致、管理员 200、业务组 403、未登录 401、容器/宿主/公网 health 均通过，API 启动成功且实际错误日志为 0。

## 当前 2026-07-26 代码瘦身第四十六阶段已发布 47

2026-07-26 已将 `PrismaRepository` 与 `InMemoryRepository` 中逐字一致的亚马逊 ONT8 仓库 profile，以及仓库编码归一化、计费重计算、仓库匹配 profile、匹配优先级和价格行选择五个纯实现收敛到 `pricing/amazon-pricing.shared.ts`。本地和 47 的六个声明逐项 SHA-256 一致；共享实现只把原 `roundMoney` 调用改为同公式的模块内报价取整函数。两套 Repository 分别净减少 138/139 行，新增共享运行时后生产源码净减少 135 行；增加 70 行边界测试后生产源码与测试合计净减少 65 行。共享 helper 与 ONT8 映射/精确仓高重量兜底完整报价样本共 9/9、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三文件白名单补丁，只构建/重启 API，无迁移；production build、源码校验和、编译产物计费重/映射/兜底探针、业务组 quote-meta 200、未登录 401、容器/宿主/公网 health 均通过，API 启动成功且本次窗口实际错误日志为 0。

## 当前 2026-07-26 代码瘦身第四十五阶段已发布 47

2026-07-26 已将 `PrismaRepository` 与 `InMemoryRepository` 中八个逐项哈希一致的亚马逊重量档、CBM 档位、价格行类型匹配和最高开放重量档纯函数收敛到扩展后的 `pricing/amazon-pricing.shared.ts`，原 `amazon-origin.shared.ts` 同步更名并保留此前出货仓归一化能力；`pricing-excel.ts` 只切换模块导入路径。两套 Repository 分别净减少 73/71 行，新增共享运行时后生产源码净减少 69 行；测试扩展后生产源码与测试合计净减少 49 行。共享 helper 与亚马逊 21/51/100KG 来源档位固定样本共 6/6、治理检查和 `git diff --check` 通过。已基于 47 当前源码生成四文件白名单候选，只构建/重启 API，无 Prisma 变更或迁移；production build、四文件 SHA-256、编译产物重量/CBM 边界与最高档开放探针、业务组 quote-meta 200、未登录 401、容器/宿主/公网 health 均通过，API 启动成功且本次窗口实际错误日志为 0。旧模块已删除，完整备份位于 `/opt/siyuan/backups/codex-20260726-codebase-slimming-phase-45`。

## 当前 2026-07-26 代码瘦身第四十四阶段已发布 47

2026-07-26 已用 TypeScript AST 复扫两套巨型 Repository 的显式私有方法、私有属性和全 API 运行时引用；仅发现财务付款、业务员数据范围、仓库理货编号和内存财务旧入口等禁区候选，全部保留不动。随后将 Prisma、InMemory 与 Excel 解析器中三份等价的亚马逊出货仓归一化规则，以及两套 Repository 的来源去重排序，收敛到 `pricing/amazon-origin.shared.ts`。本地与 47 两套 Repository 原实现逐项哈希一致，Excel 入口的字符串转换语义等价。两套 Repository 各减少 51 行、解析器减少 39 行，新增 51 行共享运行时后生产源码净减少 90 行；计入 21 行测试后全仓功能代码净减少 69 行。共享 helper 与业务员出货仓过滤固定样本 3/3、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用四文件白名单补丁，只构建/重启 API，无迁移；production build、源码校验和、构建产物归一化/拒绝/排序样本、业务员 quote-meta 200、未登录 401、容器/宿主/公网 health 均通过，API 本次窗口实际错误日志为 0。

## 当前 2026-07-26 代码瘦身第四十三阶段已发布 47

2026-07-26 已复扫上一阶段删除后产生的级联孤立实现，并扩展检查 API 非测试运行时源码的顶层私有声明；从两套 Repository 各删除一份零调用备注合并函数，从 `pricing-excel.ts` 删除三份已被现行共享规则替代的时效裁剪、渠道要求脱敏和仓库编码判断函数。五个定义在本地和 47 都只有定义本身，远端函数块与本地变更前哈希一致；现行共享裁剪规则和仓库编码生产路径保留不动。三文件生产源码净减少 147 行，没有新增包装或测试代码；删除后顶层私有声明复扫无新候选。三个现行解析固定样本 3/3、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三文件白名单删除补丁，只构建/重启 API，无迁移；production build、源码校验和、零指纹复扫、管理员加价规则 200/20、南非规则 200/19、价格行 200/20 及分页、未登录 401、容器/宿主/公网 health 均通过，API 本次窗口实际错误日志为 0。

## 当前 2026-07-26 代码瘦身第四十二阶段已发布 47

2026-07-26 已经本地与 47 生产源码词边界复扫，从 `PrismaRepository` 与 `InMemoryRepository` 删除九个只有定义、没有调用/导出的历史私有函数，涉及旧价格表模块过滤、旧亚马逊档位推断、旧渠道备注装饰和内存路径副本。现行 Prisma 价格行裁剪实现保留 1 定义/1 调用；API、RBAC、字段裁剪、报价匹配、数据库、状态和审计均未改变。两套 Repository 分别减少 69/67 行，本阶段无新增包装或测试代码，生产源码净减少 136 行。三个现行查价/字段裁剪固定样本 3/3、治理检查和 `git diff --check` 通过；三个扩大 E2E 失败已用变更前提交 `8456dd5` 原样复现，确认为既有基线。已从 47 当前源码应用两文件白名单删除补丁，只重建/重启 API，无迁移；production build、零指纹复扫、管理员加价规则 200/20、价格行 200/100 及分页、旧报价元数据 200、未登录 401、容器及公网 health 均通过，API 实际错误日志为 0。

## 当前 2026-07-26 代码瘦身第四十一阶段已发布 47

2026-07-26 已把 `PrismaRepository` 与 `InMemoryRepository` 中手工收货箱规展开、合并单号解析、北京时间查询边界、入库标签、理货二维码、录单包裹 ID 归一化、包裹重量/拆票汇总和拆票序号九组逐字等价纯函数收敛到 `warehouse-domain.shared.ts`；内存理货最近一个月截止时间复用既有查询 helper，并删除两份零调用测量取整函数。两套实现语义不同的包裹分组汇总明确保留原位。API、RBAC、数据库、状态、审计和页面均未改变。本地两套巨型 Repository 共减少 249 行，新增 163 行共享运行时与 84 行测试后，生产运行时代码净减少 86 行，生产源码与回归测试合计净减少 2 行（不计状态文档）；47 生产运行时代码净减少 81 行。共享 helper 单测 3/3、仓库固定业务样本 4/4、治理检查和 `git diff --check` 通过；全量 API typecheck 仍仅有当前基线既有错误，新模块及测试错误为 0。已从 47 当前源码应用三文件白名单补丁，只重建/重启 API，无迁移；production build、九组 helper 单一定义、北京时间边界、管理员今日收货/在库/理货只读探针、未登录 401、容器及公网 health 均通过，API 最近错误日志为 0。

## 当前 2026-07-25 代码瘦身第四十阶段已发布 47

2026-07-25 已把 `PrismaRepository` 中仓库包裹全集、包裹分组和手工收货客户三个无副作用查询迁入独立 `PrismaWarehouseInventoryQueryRepository`，并以 `LegacyWarehouseInventoryQueryRepository` 保持内存测试路径。`WarehouseInventoryQueryController` 的五条 GET 路径、HTTP 方法、参数、权限装饰器、状态码、错误文案和返回字段不变；会写 `warehouse.today_receipts.view` / `warehouse.in_stock.view` 查看审计的今日收货与在库查询继续调用原 Repository，未迁移审计实现。墨家设备重复检测改为调用同一库存查询边界，重复判断字段、写入、失败审计和返回文案不变。包裹理货状态映射与分组汇总迁入 `warehouse-query.shared.ts`，供在库查询和录单包裹选择继续复用。本地 `PrismaRepository` 减少 94 行，47 当前 Repository 减少 107 行；新增领域边界后运行时代码净增加约 78 行，因此改善职责边界，不宣称查询性能或全仓总代码量下降。领域契约与只读 E2E 5/5、墨家固定样本 1/1、分组及合票固定样本 1/1、治理检查和 `git diff --check` 通过；全量 API typecheck 仍只被当前基线既有错误阻断，目标文件错误为 0。已基于 47 当前源码发布七文件白名单，仅重建/重启 API，无迁移；完整保留远端理货生命周期、创建时间排序、仓租、重复理货、客服数据确认和付款银行功能。47 production build 通过，旧 Repository 三个查询和私有映射为 0、领域 Repository 为 3、共用 helper 调用为 3、两个查看审计查询及动作指纹为 4，五条库存 GET 路由各映射一次；管理员包裹 200/1441、分组 200/412、客户 200/10 且字段/排序完整，三类理货状态均存在，客户保留 403“没有访问权限”，未登录保留 401“缺少登录凭证”，容器内、宿主和公网 health 均为 200，API 最近错误日志为 0。

## 当前 2026-07-25 代码瘦身第三十九阶段已发布 47

2026-07-25 已把 `PrismaRepository` 中仓库合票明细、理货任务列表、理货历史链和理货结果包裹四个只读查询实现迁入独立 `PrismaWarehouseTallyQueryRepository`，并以 `LegacyWarehouseTallyQueryRepository` 保持内存测试路径。Controller 的四条 GET 路径、HTTP 方法、参数、权限装饰器、角色与业务员数据范围、查询过滤/排序、返回字段、状态码和错误文案不变；理货创建、修改、完成、标签生成/打印/下载/扫描、重复理货统计、数据库写入、状态流转、审计和 lineage 均保持原位。共用包裹/任务映射和结果包裹查询 helper 迁入 `warehouse-query.shared.ts`；标签生成继续执行原“已完成理货查看权限”检查后调用同一查询 helper。本地 `PrismaRepository` 减少 242 行，47 当前 Repository 减少 248 行；新增领域边界使运行时代码净增加约 130 行，因此改善职责边界和可测试性，不宣称性能或全仓总代码量下降。领域契约与只读 E2E 5/5、仓库理货完整固定样本 1/1、治理检查和 `git diff --check` 通过；全量 API typecheck 仍被当前基线既有错误阻断，目标文件错误为 0。已基于 47 当前源码发布六文件白名单，仅重建/重启 API，无迁移；完整保留远端理货生命周期、根任务链、重复理货统计和付款银行功能。47 production build 通过，旧 Repository 四个查询实现为 0、领域 Repository 为 4，四条 GET 路由各映射一次；管理员任务列表 200/5 且关键字段完整，结果包裹 200/1，历史链和空合票明细均为 200 数组，缺失任务保留 404“理货任务不存在”，客户保留 403“没有访问权限”，未登录保留 401“缺少登录凭证”，容器内、宿主和公网 health 均为 200，API 最近错误日志为 0。

## 当前 2026-07-25 代码瘦身第三十八阶段已发布 47

2026-07-25 已把 `App.tsx` 中页面渲染异常隔离、错误上报、重试/刷新恢复卡片和懒加载回退整合迁入独立 `AppPageBoundary.tsx`。App 继续以相同 `menuKey/sectionKey/resetKey` 包裹原页面树，错误报告字段、发布编号、路由、错误文案、按钮、Suspense 回退文案和页面组件参数均保持不变；本地与 47 的 App 均减少 89 行，新组件 109 行，运行时代码净增加 20 行，因此改善根应用职责边界，不宣称性能或总代码量下降。新增组件契约测试 2/2、`git diff --check` 通过；Web 全量 typecheck 仍只被财务审核页与仓库测试桩既有错误阻断，App 与新组件错误为 0。已基于 47 当前 App 源码应用白名单补丁，完整保留远端模块初始路由、侧栏展开、问题件弹窗、代理字段、排货费用目录和审核就绪判断；仅重建/重启 Web，无迁移。47 production build 通过，旧边界声明为 0、新边界引用为 3、远端功能指纹保持 18，静态产物含“页面加载失败”；实际监听端口 18899、容器内及公网首页/API health 均为 200，Web 最近错误日志为 0。

## 当前 2026-07-25 代码瘦身第三十七阶段已发布 47

2026-07-25 已把仓库巨型页顶部的 10 类页面模型、列配置、时间与箱规计算、理货状态判断、交接单号、测试扫描包裹构造及 API 包裹映射等纯数据逻辑等量迁入独立 `warehousePageModel.ts`。`WarehousePage` 的 React 状态、事件、API 调用、权限判断、按钮、筛选、表格字段、提交载荷和 JSX 保持原位；本地页面减少 487 行、模型为 537 行，运行时代码净增加约 50 行，因此改善职责边界和可测试性，不宣称性能提升或全仓总代码量下降。模型与既有页面定向测试 3/3、`git diff --check` 通过；Web 全量 typecheck 仍只被财务审核页与仓库测试桩既有错误阻断，目标文件错误为 0，一个既有整页仓库测试在 30 秒无有效结果后按安全规则终止。已基于 47 当前源码应用 Warehouse 两个运行时文件白名单补丁，完整保留远端缓存、租金详情、重复理货统计、设备备注/测量、分页等功能；仅重建/重启 Web，无迁移。47 production build 通过，页面内迁移声明为 0、模型导出为 29、远端功能指纹为 20，静态产物保留“部分到仓”；容器内与公网 8899 首页/API health 均为 200，Web 最近错误日志为 0。

## 当前 2026-07-25 代码瘦身第三十六阶段已发布 47

2026-07-25 已把报价巨型页中的价格/加价格式化、来源与状态展示、渠道要求及自定义备注展示、两类复制报价文案等 11 个纯展示辅助导出迁入独立 `pricingPageDisplay.tsx`，并删除零调用的 `getMarkupTypeLabel`。`PricingPage` 的 React 状态、事件、API 调用、权限判断、表格字段和 JSX 调用位置保持不变；本地和 47 页面均减少 175 行，新展示模块 188 行，运行时代码净增加 13 行，因此改善展示职责边界，不宣称性能提升或全仓总代码量下降。新增展示契约测试 2/2、`git diff --check` 通过；Web 全量 typecheck 仍只被财务审核页及仓库测试桩的既有错误阻断，两个目标文件错误为 0。已基于 47 当前源码应用两个 Web 文件白名单补丁，保留远端迪拜图片加价单位、南非、加拿大和加价工作台功能；仅重建/重启 Web，无迁移。47 production build 通过，页面目标声明为 0、展示模块导出为 11、静态产物保留“时效待确认”文案；容器内与公网 8899 首页/API health 均为 200，Web 最近错误日志为 0。

## 当前 2026-07-25 代码瘦身第三十五阶段已发布 47

2026-07-25 已把报价巨型页顶部 33 个纯类型、配置和计算辅助导出等量迁入独立 `pricingPageModel.ts`，`PricingPage` 保留原组件状态、JSX、API 调用和六个既有测试导出入口。页面文件在本地减少 310 行、47 当前源码减少 312 行；新增模型 371 行，运行时代码净增加约 60 行，因此本阶段改善模块边界和可测试性，不宣称全仓代码量下降或性能提升。三个纯函数固定样本 3/3、`git diff --check` 通过；Web 全量 typecheck 仍被财务审核页及仓库测试桩的既有错误阻断，报价两个目标文件已无类型错误。已基于 47 当前源码应用两个 Web 文件白名单补丁，完整保留远端迪拜、南非、加拿大和加价工作台新增功能；仅重建/重启 Web，无迁移。47 production build 通过，页面内目标声明为 0、模型声明存在，静态产物包含原查价超时文案；容器内首页/API health 与公网 8899 首页/API health 均为 200，Web 最近错误日志为 0。

## 当前 2026-07-25 代码瘦身第三十四阶段已发布 47

2026-07-25 已把 `DataController` 中内部面单列表和承运商任务列表两个无写副作用 GET 迁入独立 `ShipmentFulfillmentQueryController`。API 路径、GET 方法、路径参数、权限装饰器、客户显式拒绝、面单双权限判断及拒绝审计元数据、Repository 查询、运单/任务数据范围、失败原因字段裁剪和错误文案不变；面单生成/上传/作废、任务执行/重试及其状态流转、审计和 lineage 保持原位。新 E2E 2/2、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三个 API 文件白名单补丁，保留远端新增功能，只重建/重启 API，无迁移。线上旧 Controller 目标路由为 0、新 Controller 为 2；当前无面单记录，管理员和仓库读取现有运单均为 200/0，非空字段样本由本地 E2E 覆盖；管理员承运商任务 200/2 且字段完整，客户分别保留显式面单拒绝和默认任务权限拒绝，未登录保留 401，公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 19 行；运行时代码净增加 21 行，契约测试增加 97 行，不改变查询性能或业务行为。

## 当前 2026-07-25 代码瘦身第三十三阶段已发布 47

2026-07-25 已把 `DataController` 中仓库待出库/已出库列表和已打印交接单预览两个无写副作用 GET 迁入独立 `WarehouseDispatchQueryController`。API 路径、GET 方法、路径参数、权限装饰器、Repository 查询、状态筛选、数据范围、敏感字段裁剪、返回字段和错误文案不变；出库确认和交接单打印写接口及其状态流转、审计和 lineage 保持原位。新 E2E 2/2、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三个 API 文件白名单补丁，保留远端新增功能，只重建/重启 API，无迁移。线上旧 Controller 目标路由为 0、新 Controller 为 2；管理员和仓库角色列表均为 200/1 条且状态集合正确，仓库继续不返回付款或费用字段；既有已打印交接单 200 且字段完整，未打印运单保留 404“尚未打印代理交接单”，客户保留 403，未登录保留 401，公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 12 行；运行时代码净增加 10 行，契约测试增加 100 行，不改变查询性能或业务行为。

## 当前 2026-07-25 代码瘦身第三十二阶段已发布 47

2026-07-25 已把 `DataController` 中录单可选仓库包裹和单票录单详情两个无写副作用 GET 迁入独立 `OrderEntryQueryController`，并清理一个无用类型导入。API 路径、GET 方法、查询参数、权限装饰器、客户/仓库显式拒绝、Repository 查询、包裹占用过滤、业务数据范围、费用字段裁剪、返回字段和错误文案不变；会触发超时清理的草稿列表及录单创建、草稿保存/删除、审核状态流转保持原位。新 E2E 2/2、既有非空完整链路 1/1、治理检查和 `git diff --check` 通过；API 全量 typecheck 仍被当前基线无关既有错误阻断，目标文件不在错误清单，47 production build 通过。已基于 47 当前源码应用三个 API 文件白名单补丁，保留远端新增功能，只重建/重启 API，无迁移。线上旧 Controller 目标路由为 0、新 Controller 为 2；业务组本人和范围外包裹查询均为 200/0 且符合数据范围，管理员详情 200 且字段结构完整，范围外业务组保留 404“录单不存在”，财务/客户保留 403，未登录保留 401，公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 19 行；运行时代码净增加 10 行，契约测试增加 106 行，不改变查询性能或业务行为。

## 当前 2026-07-25 代码瘦身第三十一阶段已发布 47

2026-07-25 已把 `DataController` 中专线运单池和单票内部流通日志两个无写副作用 GET 迁入独立 `OperationsLineShipmentQueryController`，并清理两个无用类型导入。API 路径、GET 方法、查询参数、权限装饰器、客户显式拒绝、Repository 查询、数据范围、敏感字段裁剪、分页和错误文案不变；专线状态更新、轨迹、问题件和导入等写接口保持原位。新 E2E 2/2、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三个 API 文件白名单补丁，只重建/重启 API，无迁移。线上旧 Controller 目标路由为 0、新 Controller 为 2；管理员专线池 200/5 条并与数据库全量专线集合一致，业务员 200/0 条并与录单人范围集合一致，仓库 200 且继续裁掉预估应收、行级应收、备注和代理字段；管理员内部日志 200，范围外业务员保留 404“运单不存在”，仓库和客户默认权限保留 403，未登录保留 401，公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 15 行；运行时代码净增加 9 行，契约测试增加 112 行，不改变查询性能或业务行为。

## 当前 2026-07-25 代码瘦身第三十阶段已发布 47

2026-07-25 已把 `DataController` 中客服转单号候选列表和问题件列表两个无写副作用 GET 迁入独立 `CustomerServiceQueryController`。API 路径、GET 方法、权限装饰器、Repository 查询、审批周期判断、数据范围、字段裁剪和错误文案不变；转单号填写及问题件新增/回复/关闭/协助等写接口保持原位。新 E2E 2/2、治理检查和 `git diff --check` 通过。已基于 47 当前源码应用三个 API 文件白名单补丁，完整保留远端新增的数据确认和常用标签功能，只重建/重启 API，无迁移。线上旧 Controller 目标路由为 0、新 Controller 为 2；管理员和客服转单号候选均为 200/0 条且与远端最新审批周期数据库条件精确一致，客户保留 403、未登录保留 401；管理员与客服问题件均为 200/3 条且集合一致，客户 200/0 条并与本人客户可见问题件数据库集合一致，仓库保留 403、未登录保留 401；远端既有数据确认和常用标签 GET 均继续 200，公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 12 行；运行时代码净增加 10 行，契约测试增加 99 行，不改变查询性能或业务行为。

## 当前 2026-07-25 代码瘦身第二十九阶段已发布 47

2026-07-25 已把 `DataController` 中运单列表、运单状态统计和员工端未读角标三个无写副作用 GET 迁入独立 `ShipmentOverviewQueryController`。API 路径、GET 方法、权限装饰器、Repository 查询、数据范围、字段裁剪和客户角标 403 原文案不变；角标读状态写入和全部运单写接口保持原位。审核队列候选因会触发超时删除和审计写入已明确排除。新 E2E 2/2、治理检查和 `git diff --check` 通过；本地 API 全量 typecheck 仍被当前基线 15 个无关既有错误阻断，目标文件不在错误清单。已基于 47 当前源码应用三个 API 文件白名单补丁，只重建/重启 API，无迁移。线上旧 Controller 目标路由为 0、新 Controller 为 3，镜像编译产物与 Nest 三条路由映射存在；管理员运单列表 200/5 条且状态统计逐状态一致，业务员和客户 API 数量与数据库原数据范围条件精确一致，财务 200/5 条且付款及市场成本字段继续裁掉，客户角标保留 403“客户不使用员工端导航角标”，仓库保留 403“没有访问权限”，未登录保留 401“缺少登录凭证”，公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 19 行；运行时代码净增加 10 行，契约测试增加 86 行，不改变查询性能或业务行为。

## 当前 2026-07-25 代码瘦身第二十八阶段已发布 47

2026-07-25 已把 `DataController` 中代理资料和代理渠道两个纯 GET 迁入现有 `MasterDataChannelQueryController`，并把两个 Controller 共用的业务角色真实代理限制判断等量提取到 `rbac.ts`。API 路径、GET 方法、权限装饰器、原两条显式拒绝文案、完整主数据快照字段和数组响应不变；代理及代理渠道所有写接口、Repository、数据库和审计保持原位。主数据查询 E2E 3/3 和 `git diff --check` 通过。已基于 47 当前源码应用三个 API 文件白名单补丁，只重建/重启 API，无迁移。线上两个目标 GET 在旧 Controller 中均为 0、在领域 Controller 中各为 1；管理员响应与快照对应数组逐项相等，市场两个接口均为 200，业务角色完整快照继续裁掉真实代理及渠道且直接接口为 403，客户均为 403，未登录为 401，公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 35 行；运行时代码净增加 1 行，新增敏感角色与字段契约测试净增加 67 行，不改变查询性能或业务行为。

## 当前 2026-07-25 代码瘦身第二十七阶段已发布 47

2026-07-25 已把 `DataController` 中旧报价元数据、迪拜当前展示、迪拜展示版本和南非价格表图片四个纯 GET 迁入现有 `PriceBookQueryController`。API 路径、GET 方法、权限装饰器、Repository 查询、响应字段和错误文案不变；业务员旧报价元数据的内部代理字段继续裁剪为 `agents=[]`，迪拜图片文件响应、版本发布/重试、南非图片上传和所有写接口保持原位。价格表查询 E2E 2/2 和 `git diff --check` 通过。已基于 47 当前源码应用两个 API 文件白名单补丁，只重建/重启 API，无迁移。线上四个目标 GET 在旧 Controller 中均为 0、在领域 Controller 中各为 1；业务员元数据/迪拜展示、市场迪拜版本/南非图片均为 200，客户四路径均为 403“没有访问权限”，未登录为 401“缺少登录凭证”，公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 27 行；运行时代码总量不变，增加的角色与字段裁剪契约测试使全仓净增加 46 行，不改变查询性能。

## 当前 2026-07-25 代码瘦身第二十六阶段已发布 47

2026-07-25 已把 `DataController` 中历史价格源列表和健康报告两个无副作用 JSON GET 迁入现有 `PriceBookQueryController`。API 路径、GET 方法、`module` 参数、权限装饰器、Repository 查询、响应字段和错误文案不变；历史源导入、删除、重建及其他价格表写接口保持原位。价格表查询 E2E 2/2 和 `git diff --check` 通过。已基于 47 当前源码应用两个 API 文件白名单补丁，只重建/重启 API，无迁移。线上两个目标 GET 在旧 Controller 中均为 0、在领域 Controller 中各为 1，管理员均为 200 且结构正确，客户均为 403“没有访问权限”，未登录为 401“缺少登录凭证”，容器内及公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 再减少 12 行；运行时代码总量不变，增加的契约测试使全仓净增加 24 行，不改变查询性能。

## 当前 2026-07-25 代码瘦身第二十五阶段已发布 47

2026-07-25 已把 `DataController` 中价格表列表、同步体检和导入任务三个无副作用 JSON GET 迁入独立 `PriceBookQueryController`。API 路径、GET 方法、查询参数、权限装饰器、Repository 查询、分页、字段裁剪、响应结构和原错误文案不变；会触发后台规则刷新的进度 GET、会写 lineage 的线路 GET、会写审计的下载 GET、远端既有加价线路 GET 及所有写接口保持原位。新只读 E2E 2/2、治理检查和 `git diff --check` 通过。已基于 47 当前源码对三个 API 运行时文件应用白名单补丁，只重建/重启 API，无迁移。线上三个目标 GET 在旧 Controller 中均为 0、在新 Controller 中各为 1，Nest 映射各为 1；管理员列表/同步体检为 200 且结构正确，缺失任务和禁止完整明细保留原 404/400 文案，市场角色允许，客户角色三个路径均为 403“没有访问权限”，未登录为 401“缺少登录凭证”，容器内及公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 净缩小 21 行；新增领域边界和测试使全仓总行数没有减少，也不改变查询性能。

## 当前 2026-07-25 代码瘦身第二十四阶段已发布 47

2026-07-25 已把 `DataController` 中承运商、公司渠道和渠道类别三个纯 GET 路由迁入独立 `MasterDataChannelQueryController`。API 路径、GET 方法、权限装饰器、`getMasterData()` 快照来源、响应数组和字段不变；10 个对应写路由、数据库、状态流转和审计保持原位。新只读 E2E 2/2、治理检查和 `git diff --check` 通过。已基于 47 当前源码对三个 API 运行时文件应用白名单补丁，只重建/重启 API，无迁移。线上三个目标 GET 在旧 Controller 中均为 0、在新 Controller 中各为 1，Nest 映射各为 1；管理员响应与完整基础资料快照对应数组逐项一致，客户角色三个路径均为 403“没有访问权限”，未登录为 401“缺少登录凭证”，容器内及公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 净缩小 18 行；新增领域边界和测试使全仓总行数没有减少，也不改变查询性能。

## 当前 2026-07-25 代码瘦身第二十三阶段已发布 47

2026-07-25 已把 `DataController` 中四个仓库理货纯 GET 路由迁入独立 `WarehouseTallyQueryController`：合票明细、理货任务列表、理货历史链、理货结果包裹。API 路径、GET 方法、参数、权限装饰器、Repository 调用、响应结构和原错误文案不变；仓库写接口、状态流转、审计和远端既有重复理货统计查询保持原位。新只读 E2E 与两个既有仓库理货固定样本共 4/4 通过，治理检查和 `git diff --check` 通过。已基于 47 当前源码对三个 API 运行时文件应用白名单补丁，只重建/重启 API，无迁移。线上四个目标 GET 在旧 Controller 中均为 0、在新 Controller 中各为 1，Nest 映射各为 1；管理员三类正常查询均为 200 数组，缺失任务结果包裹为 404“理货任务不存在”，客户角色四条路径均为 403“没有访问权限”，未登录为 401“缺少登录凭证”，容器内及公网 health 为 200，API 实际错误日志为 0。该阶段使 `DataController` 净缩小 25 行；新增模块和测试使全仓总行数没有减少，也不改变查询性能。

## 当前 2026-07-25 代码瘦身第二十二阶段已发布 47

2026-07-25 已把 `DataController` 中五个仓库库存纯 GET 路由迁入独立 `WarehouseInventoryQueryController`：包裹、今日收货、在库、包裹组、手工收货客户候选。路由路径、HTTP GET、查询参数、权限装饰器、Repository 调用和响应完全沿用原实现；仓库写接口保持原位。新只读 E2E 与两个既有仓库固定样本共 4/4 通过，治理检查和 `git diff --check` 通过。已基于 47 当前源码对三个 API 运行时文件应用白名单补丁，只重建/重启 API，无迁移。线上五个目标路由在旧 Controller 中均为 0、在新 Controller 中各为 1，Nest 启动映射各为 1；管理员五个查询均为 200 且结构正确，客户角色为 403“没有访问权限”，未登录为 401“缺少登录凭证”，容器内及公网 health 为 200，API 实际错误日志为 0。该阶段缩小巨型 Controller 32 行，但因新模块边界和测试总行数不减少；实际查询实现尚在巨型 Repository。

## 当前 2026-07-25 代码瘦身第二十一阶段已发布 47

2026-07-25 已删除第二阶段遗留的 `PrismaRepository.getDepartments/getSites` 两个零调用兼容转发、仅由它们使用的领域 Repository 导入及对应兼容测试；生产继续通过 `SystemDirectoryController -> SystemDirectoryService -> PrismaSystemDirectoryRepository` 查询，内存 E2E 继续由 Legacy 适配器注入原 `PrismaRepository` 令牌并调用 `InMemoryRepository` 的原实现。领域 Repository 与接口 E2E 共 4/4、治理检查和 `git diff --check` 均通过。已基于 47 当前源码对两个 API 运行时文件应用白名单补丁，只重建/重启 API，无迁移。线上管理员部门/站点均为 200 且返回数组，运营角色均为 403“没有访问权限”，未登录为 401“缺少登录凭证”；容器内及公网 health 为 200，API 实际错误日志为 0。复扫未发现 `PrismaRepository` 中其他同类领域 Repository 兼容转发。

## 当前 2026-07-25 代码瘦身第二十阶段已发布 47

2026-07-25 经本地与 47 生产源码复扫确认零调用后，已结束仓库理货历史链查询兼容窗口，从 `ApiClient` 删除 `warehouseTallyTaskHistoryChain` 转发及对应兼容测试；`WarehouseQueryClient` 领域方法与仓库页、财务录单页生产调用保持不动。定向测试 2 个文件 9/9 通过，`git diff --check` 通过；已基于 47 当前源码应用单文件白名单补丁，只重建/重启 Web，无迁移。47 旧方法和旧生产调用均为 0，领域调用为 3；管理员查询为 200 且返回数组，客户角色仍为 403“没有访问权限”，未登录仍为 401“缺少登录凭证”，容器内及公网首页/API health 均为 200，Web 错误日志为 0。继续扫描后暂未发现适合扩展现有领域客户端且有生产调用、同时避开财务/账号/状态流转/审计实现的新纯 GET；未迁移零调用或领域不匹配的方法。

## 当前 2026-07-25 代码瘦身第十九阶段已发布 47

2026-07-25 已将仓库理货历史链只读查询 `warehouseTallyTaskHistoryChain(packageId)` 迁入现有 `WarehouseQueryClient`，仓库页和财务录单页生产调用改用领域客户端；旧 `ApiClient` 同签名入口保留兼容转发。API 路径、GET 方法、参数编码、返回数组、RBAC、数据库、状态和页面行为均未改变。定向测试 2 个文件 10/10 通过，`git diff --check` 通过；本地旧入口直接调用为 0。已基于 47 当前源码对四个运行时文件应用白名单补丁，仅重建/重启 Web，无迁移；47 旧入口直接调用为 0、领域调用为 3。线上管理员查询为 200 且返回数组，客户角色仍为 403“没有访问权限”，未登录仍为 401“缺少登录凭证”；容器内及公网首页/API health 均为 200，Web 错误日志为 0。旧兼容入口待兼容窗口结束并再次确认零调用后删除。

## 当前 2026-07-25 代码瘦身第十八阶段已发布 47

2026-07-25 经本地与 47 生产源码复扫确认零调用后，已结束南非费率规则查询兼容窗口，从 `ApiClient` 删除 `southAfricaRateRules` 转发、对应兼容测试和无用类型导入；`PriceBookQueryClient` 领域方法与报价页两处生产调用保持不动。定向测试 2 个文件 9/9 通过；已基于 47 当前源码应用单文件白名单补丁，只重建/重启 Web，无迁移。线上管理员查询为 200 且 `rules` 结构正确，客户角色仍为 403“没有访问权限”，未登录仍为 401“缺少登录凭证”，容器内与公网首页/API health 均为 200，Web 错误日志为 0。下一候选为扩展现有 `WarehouseQueryClient` 承接仓库理货历史链 GET；实施前当前分支有两处、47 有三处生产调用，其中财务录单页面仅消费仓库历史数组，不涉及财务接口或金额逻辑。

## 当前 2026-07-25 代码瘦身第十七阶段已发布 47

2026-07-25 按用户继续指令放宽“多 GET”约束，已把南非费率规则 `GET /pricing/south-africa/rules` 迁入现有 `PriceBookQueryClient`，并把报价页初始化和规则刷新两处生产调用切到领域客户端；旧 `ApiClient.southAfricaRateRules` 保留原签名兼容转发。定向测试 2 个文件 10/10 通过；本地与 47 生产源码旧入口直接调用均为 0。已基于 47 当前源码应用三个运行时文件白名单补丁，只重建/重启 Web，无迁移。线上管理员查询为 200 且 `rules` 结构正确，客户角色仍为 403“没有访问权限”，未登录仍为 401“缺少登录凭证”；容器内与公网首页/API health 均为 200，Web 错误日志为 0。

## 当前 2026-07-25 代码瘦身第十六阶段已发布 47

2026-07-25 经本地与 47 生产源码复扫确认零调用后，已结束旧报价和迪拜查询兼容窗口，从 `ApiClient` 一次性删除 `legacyPricingMeta`、`legacyPricingSources`、`legacyPricingHealth`、`dubaiPriceTable`、`dubaiPriceDisplay`、`dubaiPriceDisplayVersions` 六个转发方法、两组兼容测试和三个无用类型导入。`PriceBookQueryClient` 六个领域方法与报价页三个生产调用保持不动。定向测试 2 个文件 8/8 通过；已基于 47 当前源码应用单文件白名单补丁，只重建/重启 Web，无迁移。线上六个只读接口均为 200 且结构正确，未登录请求仍为 401“缺少登录凭证”，容器内与公网首页/API health 均为 200，Web 错误日志为 0。下一窄切片扫描未发现同时满足多生产 GET、可扩展现有客户端且避开财务/账号/状态/审计实现的候选，未为凑数量迁移零调用方法。

## 当前 2026-07-25 代码瘦身第十五阶段已发布 47

2026-07-25 已把旧报价元数据、历史价格源和健康报告三个纯 GET 封装迁入现有 `PriceBookQueryClient`，并把报价页元数据调用切到领域客户端；旧 `ApiClient` 方法继续保留同签名转发，API 路径、参数、响应、权限和页面业务行为均未改变。定向测试 2 个文件 10/10 通过；本地与 47 生产源码旧入口直接调用均为 0。已基于 47 当前源码应用三个运行时文件白名单补丁，只重建/重启 Web，无迁移；管理员三个接口均返回 200 且结构正确，未登录请求仍返回 401“缺少登录凭证”，容器内与公网首页/API health 均为 200，Web 错误日志为 0。三个旧报价兼容入口和三个迪拜兼容入口待兼容窗口结束后统一删除。

## 当前 2026-07-16 加拿大海派通用时效清洗已发布 47

2026-07-16 已修复并发布通用时效清洗对无标点优惠阶梯的遗漏：如“有ERS加急服务 开船后32-38天 1:200优惠…”现在只显示“开船后32-38天”。规则位于 API 统一展示归一化层，已导入的加拿大价格表无需重新导入；查价响应会即时按新规则输出。由于本机工作区存在其他未发布改动，本轮只在 47 `apps/api/src/modules/pricing-excel.ts` 应用精确补丁，重建并重启 `api`，未执行迁移、未重启 Web。发布后 API 新镜像创建于 22:05:27；容器内以当前加拿大原文实测输出“开船后32-38天”，容器内及公网 `http://47.120.33.111:8899/api/health` 均正常。

## 当前 2026-07-16 查价成功提示去代理名称已发布 47

2026-07-16 已发布报价查价成功提示精简：美国空海运等统一 legacy 查价及南非普通查价成功均只提示“查价成功”，不再拼接代理、国家、重量或报价金额；未改价格表管理和代理加价规则中的内部代理信息。由于本机工作树有大量其他未发布改动，本次未整仓同步，只在 47 `/opt/siyuan/apps/web/src/modules/pricing/PricingPage.tsx` 应用精确补丁并仅重建/重启 `web`，未运行 Prisma 迁移、未重启 API。发布后 Web 容器新镜像创建于 20:47:53，容器内首页 hash 与宿主服务一致，API 健康检查正常，公网 `http://47.120.33.111:8899/` 和 `/api/health` 均返回 200。

## 当前 2026-07-14 细粒度权限旧业务约束继承已发布 47

2026-07-14 发布前全面复核细粒度权限迁移对旧角色边界的影响并完成修正：业务员及业务组不继承真实代理/代理渠道、报价内部来源/成本/毛利权限，录单和运单响应继续裁掉代理及付款字段；业务自审、本人客户资料维护、本人客户水单凭证上传/删除继续保留；客户门户恢复本人运单、问题件、应收、账户和流水只读；市场保留排货所需真实代理与价格管理权限，财务恢复运单读取。新增迁移已在 47 事务回滚预演通过后正式执行，旧权限键和业务角色代理授权均为 0。发布后 API/Web/Postgres/Redis 全部正常，公网健康检查通过；容器内短期 JWT 实测业务、市场、客户、财务四类权限与字段裁剪全部通过。发布范围为 Web + API + migration，当前未做 git stage/commit。剩余：完整订单测试仍有 2 个既有夹具断言不一致（无渠道费率时应保存草稿、内存样本仓库角色名），报价测试仍有 1 个既有虚拟加价批量删除断言失败；均非本轮权限修复引入。Web 构建仍有既有 AntD vendor 大 chunk warning。

## 当前 2026-07-10 迪拜空海运按渠道代码分类已发布 47

2026-07-10 已按用户要求将迪拜价格表空/海运分类补丁发布到 47。本轮修复只涉及 API 运行时：迪拜双表组装优先按 `cbmPrice/CBM/方` 判海运，同时兜底按 `AH海运/海运/海派` 判海运；旧导入 legacy 行转表格行时补透 `cbmPrice`、`priceTierLabel`、`sourceSheetName`、`realChannelName`，避免已识别 `AH海运-*` 的行继续落在空运表。发布前验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "dubai|迪拜|price table|海运"`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "迪拜|空海运|价格表"`、`git diff --check`。发布时发现本机与 47 仍有其他未发布 Web/财务/主数据差异，为避免扩大范围，本轮只 rsync `apps/api/src/modules/in-memory.repository.ts`、`apps/api/src/modules/prisma.repository.ts` 到 47，未同步无关文件，未运行迁移；47 `COMPOSE_BAKE=false docker compose build api`、`docker compose up -d --remove-orphans api` 成功，`api/postgres/redis/web` 均 Up，容器本地和公网 `/api/health` 正常，API 日志包含 `Nest application successfully started`。

## 当前 2026-07-10 美国加拿大迪拜查价表单排版修复已发布 47

2026-07-10 已修复美国、加拿大、迪拜三个新增查价模块的表单重叠并发布到 47。根因是三个表单区块共用同一个 CSS grid-area，现改为查询条件与操作区同排、尺寸信息和辅助信息依次占满下一行；宽屏模块入口第一排 4 项、第二排 3 项均铺满可用宽度，1200px 以下切为单列业务分区，720px 以下输入项改为单列。发布前通过报价 Web 定向测试 7 项、Web production build、`git diff --check`，本地浏览器 1366x900 和 820x900 均无区块覆盖或横向溢出。47 checksum 判定范围为 `web`，未发现 API、Shared、依赖或 Prisma 差异，未运行迁移；只重建并重启 `web`，`api/web/postgres/redis` 均正常，容器内 Web 和 `/api/health` 返回 200，公网 `8899` 首页和 API 健康检查正常。线上新 CSS/JS 资源均包含 `pricing-calculator-grid-air-sea` 和三个模块名称；Chrome 自动截图控制超时，未取得发布后截图证据。Web 构建仍有既有 `antd-vendor` 大 chunk warning。

## 当前 2026-07-10 报价查价三模块与客服链路增量已发布 47

2026-07-10 已按发布会话将当前 checksum 差异发布到 47，范围判定为 `web+api`，未发现 Prisma schema/migrations 待同步差异，未运行线上迁移。发布内容包含：报价查价新增 `美国空海运查询`、`加拿大空海查询`、`迪拜空海运查询` 三个独立模块及导入/查价/代理加价支持；迪拜空海运样表最小解析；客服数据确认、转单号、离港/到港/派送/签收、问题件等链路事件补齐。发布前通过报价 Web 7 项定向测试、报价 API 2 项定向测试、链路覆盖/Watcher 18 项测试、链路 E2E 9 项测试、API/Web 类型检查、Shared/API/Web 构建和 `git diff --check`。47 使用 checksum 同步，只重建并重启 `api web`；`api/web/postgres/redis` 均正常，容器内 `/api/health` 返回正常、Web 返回 200，API 日志确认 Nest 启动完成。剩余：美国、加拿大真实样表尚未提供，当前仅按同类空海运结构验证；未做三模块真实浏览器导入/查价截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前代码未做 git stage/commit。

## 当前 2026-07-10 报价查价美国加拿大迪拜空海运独立模块本地落地

2026-07-10 已按任务卡 `2026-07-10-报价查价-新增美国加拿大迪拜空海运独立查价模块` 本地落地，未发布 47。实现范围：报价查价中心新增 `美国空海运查询`、`加拿大空海查询`、`迪拜空海运查询` 三个独立模块入口；价格表导入模块下拉同步新增三项；Shared/API/Web/内存仓储/Prisma 仓储的 `LegacyPricingModule`、quote 路由、模块 meta、导入 targetModule 校验和代理加价 legacyModule 校验均补齐三模块。新模块查价只读取当前模块数据池，支持目的地/渠道关键词/计费重量 KG/方数 CBM，未命中提示当前模块未匹配，超时提示当前模块查价超时；业务员响应继续裁剪成本、毛利、价格表名称、源 sheet、源行号和内部来源。迪拜/阿联酋空派、海派样表补最小解析口径，能解析 KG 档、CBM 档、时效和按行渠道要求；实际样表 `/Users/j1ng/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_t9bbwj58u3yk22_45b9/temp/RWTemp/2026-07/f3b71ab8ca30b03dbff8d8a0b03e549b/迪拜专线 6.29（生效）大客户(3).xlsx` 本地试跑解析 64 行。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "美国|加拿大|迪拜|空海运|价格表|查价模块"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "usa|canada|dubai|pricing module|isolation"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。补充：此前 `.codex-state.md` 中记录的 `LegacyPricingModule` 扩展导致 API typecheck 阻塞已在本轮收口。剩余：未做 47 发布；未用真实浏览器完成三模块导入/查价截图验收；美国、加拿大真实样表未提供，本轮只按同类空海运通用结构预留解析与匹配。

## 当前 2026-07-10 全系统链路库客服管理第一批本地落地

2026-07-10 继续按“全系统可解释性底座”推进链路库，客服管理第一批已本地落地，未发布 47，未新增 Prisma migration，未修改 47 发布规则。实现范围：`customer_service.data_confirm.approve`、`customer_service.transfer.update`、`customer_service.departure.confirm`、`customer_service.departed.update`、`customer_service.arrived_port.confirm`、`customer_service.delivering.confirm`、`customer_service.signed.confirm`、`customer_service.problems.change` 均接入运行时目录 key；客服数据确认、代理数据确认、转单号维护、ETA/ETD 维护、离港/到港/派送/签收状态推进，以及问题件创建/回复/关闭都写入 sidecar 链路事件。客服事件统一通过运单 sourceRef 纳入整票 trace，`traceShipment` 已能从一票运单查到 `customer_service_data_confirm`、`shipment_transfer_update`、`shipment_departure_confirm`、`shipment_departed_update`、`shipment_arrived_port_confirm`、`shipment_delivering_confirm`、`shipment_signed_confirm` 和 `problem_ticket`。客服覆盖清单更新为 11 个目录中 8 个 wired、3 个 pending（客服看板聚合、客服待排货快照、售后独立动作）。验证通过：`npm test -w @siyuan/api -- --run src/modules/lineage-event-coverage.test.ts`、`npm test -w @siyuan/api -- --run src/modules/lineage-watcher.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.lineage.e2e.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "customer service transfer operational|problem tickets"`、`npm run build -w @siyuan/shared`、`git diff --check`。补充：`npm run typecheck -w @siyuan/api` 当前被既有 `LegacyPricingModule` 扩展不一致阻塞，错误集中在 `usaAirSea/canadaAirSea/dubaiAirSea`，非本轮客服链路 hook 引入。剩余：客服看板/待排货聚合快照和售后独立运行时入口未接入；全系统物流轨迹、基础资料、系统管理和运营工作台仍需逐批补 hook。

## 当前 2026-07-10 全系统链路库市场管理第一批本地落地

2026-07-10 继续按“全系统可解释性底座”推进链路库，市场管理第一批已本地落地，未发布 47，未新增 Prisma migration，未修改 47 发布规则。实现范围：`market.pending_routing.route`、`market.pending_routing.delete`、`market.routed.reroute` 均接入运行时目录 key；市场排货确认、删除待排货、已出库/待离港退回重排都写入 sidecar 链路事件。排货事件记录运单、公司渠道、代理、代理渠道、应付成本、计费重、单价、其他费和贴麦头要求；删除待排货记录删除原因、操作人和状态；退回重排记录原渠道/代理、退回原因、操作人和状态回退。`traceShipment` 已能通过运单查到 `shipment_route`、`shipment_pending_routing_delete`、`shipment_reroute`。市场覆盖清单更新为 6 个目录中 3 个 wired、3 个 pending（市场看板聚合、排货审核通过、本周排货快照）。验证通过：`npm test -w @siyuan/api -- --run src/modules/lineage-event-coverage.test.ts`、`npm test -w @siyuan/api -- --run src/modules/lineage-watcher.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.lineage.e2e.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "出库|面单|贴麦头|dispatch|shipping|label"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`git diff --check`。补充：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "route|pending-routing|reroute|排货"` 无命中用例，仅作为无效尝试记录；本轮新增的市场 trace 已由 `app.lineage.e2e.test.ts` 覆盖。剩余：市场排货审核通过运行时入口未明确接线；市场看板/本周排货聚合快照未接入；全系统客服、物流轨迹、基础资料、系统管理和运营工作台仍需逐批补 hook。

## 当前 2026-07-09 亚马逊重量段KG档位与渠道要求过滤已发布 47

2026-07-09 已按发布会话把当前与 47 的 checksum 差异发布到 47，发布范围判定为 `web+api`，未发现 Prisma schema/migrations 待同步差异，未运行线上迁移。同步内容包含：亚马逊重量段仅保留 `12KG+ / 51KG+ / 100KG+`、报价查价渠道要求过滤按方与赔偿说明、三大查价模块按亮崽规则补齐大件/超大件匹配、报价查价结果区精简与南非匹配关键词字号优化，以及对应 API/Web 解析与测试文件。发布前验证通过：`git diff --check`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亚马逊|重量段|12KG|51KG|100KG|按方"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "amazon|weightBand|tier|按方"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "渠道要求|按方包税|赔偿|理赔|时效赔偿|备注|过滤"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "渠道要求|按方包税|赔偿|理赔|price book|legacy"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亮崽|大件|超大件|卡派|欧洲海运|欧洲空运|亚马逊|尺寸|包装"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "liangzai|oversize|large cargo|inquiry|europeExpress|amazon|permission"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "报价结果|业务报价|南非|匹配关键词|空态|查价"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`。已执行 checksum rsync 同步、47 `COMPOSE_BAKE=false docker compose build api web`、`docker compose up -d --remove-orphans api web`；47 容器内 `/api/health` 正常、Web 本机 200，公网 `http://47.120.33.111:8899/api/health` 正常、首页 200，API 日志包含 `Nest application successfully started`。剩余：旧价格表不会自动回填渠道要求过滤，需要重新导入才应用；未做真实浏览器截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-09 报价查价亚马逊重量段仅保留KG档位本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-亚马逊重量段仅保留KG档位` 本地落地，未发布 47。实现范围：`报价查价 -> 查价 -> 亚马逊查询` 的 `重量段` 下拉只保留 `12KG+ / 51KG+ / 100KG+`，不再展示 `按方包税 / 按方不包税 / 按方未标注`；前端自动重量段仍按实重/计费重匹配三档 KG，并补 55KG、100KG 回归断言。API 内存仓储和 Prisma 仓储对亚马逊 legacy 查价做防守归一化，直传按方 `tier/weightBand` 时也会按计费重归入 KG 档，不再返回按方 CBM 报价；legacy meta 的亚马逊 tiers 也收敛为三档 KG。未删除历史按方价格数据，未改价格表解析、报价金额、加价规则、权限裁剪或其他查价模块。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亚马逊|重量段|12KG|51KG|100KG|按方"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "amazon|weightBand|tier|按方"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器下拉截图验收；当前工作树仍有大量其他会话未提交改动。

## 当前 2026-07-09 渠道要求过滤和大件规则已发布 47

2026-07-09 已按发布会话把当前与 47 的 checksum 差异发布到 47，发布范围判定为 `web+api`，未发现 Prisma schema/migrations 待同步差异，未运行线上迁移。同步内容包含：报价查价渠道要求过滤按方与赔偿说明、三大查价模块按亮崽规则补齐大件/超大件匹配、报价查价结果区精简与南非匹配关键词字号优化，以及对应 API/Web 解析与测试文件。发布前验证通过：`git diff --check`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "渠道要求|按方包税|赔偿|理赔|时效赔偿|备注|过滤"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "渠道要求|按方包税|赔偿|理赔|price book|legacy"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亮崽|大件|超大件|卡派|欧洲海运|欧洲空运|亚马逊|尺寸|包装"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "liangzai|oversize|large cargo|inquiry|europeExpress|amazon|permission"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "报价结果|业务报价|南非|匹配关键词|空态|查价"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`。已执行 checksum rsync 同步、47 `COMPOSE_BAKE=false docker compose build api web`、`docker compose up -d --remove-orphans api web`；47 容器内 `/api/health` 正常、Web 本机 200，公网 `http://47.120.33.111:8899/api/health` 正常、首页 200，API 日志包含 `Nest application successfully started`。剩余：未做真实浏览器截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-09 报价查价渠道要求过滤按方与赔偿说明本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-渠道要求过滤按方与赔偿说明` 本地落地，未发布 47。实现范围：Web/API 价格表解析的渠道要求合并统一增加段落级过滤，导入时会删除 `按方包税计算方式`、`最低1CBM起运`、`1CBM=363KGS`、体积计费、时效/延误/赔偿/理赔条款等无关说明；同一渠道要求中的品名限制、尺寸限制、包装、查验、清关、关税、偏远、卸货、附加费、单询和资料要求继续保留。解析后的列表短摘要、推荐卡、详情弹窗和复制报价沿用同一 `specialRemark/productSurchargeRemark/remark` 数据口径，未改报价金额、匹配逻辑、加价规则、权限裁剪或数据库结构。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "渠道要求|按方包税|赔偿|理赔|时效赔偿|备注|过滤"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "渠道要求|按方包税|赔偿|理赔|price book|legacy"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；旧价格表不会自动回填，需要重新导入才应用新过滤。

## 当前 2026-07-09 三大查价模块大件/超大件按亮崽规则本地落地

2026-07-09 已按用户计划 `三大查价模块按亮崽规则补齐大件/超大件匹配` 本地落地，未发布 47，未新增数据库字段，明确不处理南非。实现范围：后端 Prisma 仓储和内存仓储统一新增货物画像判定，长度 `>180cm`、宽/高 `>80cm`、单件体积 `>0.15CBM` 或品名/包装含大件关键词时识别为大件/超大件；欧洲空海运铁路快递遇到大件直接返回可读拒绝原因并提示走欧洲海运超大件查询；欧洲海运超大件和亚马逊遇到大件时只保留卡派、海卡、超大件、托盘、木箱、木架等可承接渠道，普通快递/小包/普货不参与推荐。前端欧洲空海运铁路快递表单补 `品名、包装、长宽高、件数、单件实重` 可选输入，并在提交前用同一口径提示切到超大件模块。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亮崽|大件|超大件|卡派|欧洲海运|欧洲空运|亚马逊|尺寸|包装"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "liangzai|oversize|large cargo|inquiry|europeExpress|amazon|permission"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未用真实浏览器重新导入 7.3/7.6/7.8 三份样本验收；单件超重仍按任务假设不设全局阈值，只依赖渠道限制文本和后续更细任务。

## 当前 2026-07-09 报价查价结果区精简与南非关键词字号优化本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-结果区精简与南非关键词字号优化` 本地落地，未发布 47。实现范围：`报价查价 -> 查价` 四个模块移除单独占空间的通用 `报价结果` 中间卡；查价加载、失败、未命中状态改为在当前模块的 `业务报价` 卡内紧凑展示，成功后直接展示最终业务报价。南非专线报价表 `匹配关键词` 标签新增紧凑样式，字号和内边距收小，保留关键词完整可见。未改报价金额、排序、加价规则、价格表解析、权限裁剪或 API。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "报价结果|业务报价|南非|匹配关键词|空态|查价"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；当前工作树仍有大量其他会话未提交改动。

## 当前 2026-07-09 47 发布状态复核无待同步文件

2026-07-09 发布会话复核当前本机与 47 `/opt/siyuan` 的 checksum 差异：`rsync --dry-run --checksum --itemize-changes` 无文件内容差异，本轮无需重新构建/重启。47 健康检查通过：`api/web/postgres/redis` 均 Up，容器内 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，容器内首页 200，公网 `http://47.120.33.111:8899/api/health` 正常，公网首页 200，API 日志包含 `Nest application successfully started`。剩余：当前工作树仍有大量未提交改动；`.codex-state.md` 顶部仍有其他会话写入的“本地落地/未发布”历史条目，但按 checksum 实际代码与 47 一致；Web 构建既有 `antd-vendor` 大 chunk warning 未处理。

## 当前 2026-07-09 南非面膜报价备注按规则原文输出本地落地

2026-07-09 已按用户授权特例任务 `2026-07-09-报价查价-南非面膜报价备注按规则原文输出` 本地落地，未发布 47。实现范围：南非专线面膜命中 `化妆品类 / 化妆品类` 时，报价结果 `remark` 和复制模板 `quoteText` 按规则备注原文输出；默认南非化妆品规则备注修正为 `无牌无侵权；约翰内斯堡自提、低消0.5CBM  报关件需要单询`，不再遗漏 `约翰内斯堡自提`，也不再追加旧句号。内存仓储与 Prisma lookup 的无备注兜底统一使用同一原文备注常量；Web/API 测试同步断言完整模板。未改南非运费公式、权限裁剪、其他查价模块或数据库结构。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "南非|面膜|备注|quoteText|复制"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "southAfrica|面膜|quoteText|备注|CBM|化妆品"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；若线上数据库已有旧备注文本，需要在基础规则里同步维护或另开数据修正/发布卡。

## 当前 2026-07-09 报价查价价格表按查价模块强隔离本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-价格表按查价模块强隔离` 本地落地，未发布 47，未新增数据库字段。实现范围：价格表导入目标模块继续作为价格表/价格行强归属；Prisma legacy 查价 fallback 从普通价格表取行时也按当前 legacy 模块对应的有效价格源文件收窄，避免缺 legacy 行时按行内容二次推断导致跨模块串价。`getLegacyPricingMeta` 的模块行数/来源数改为按价格表目标模块统计。代理加价规则继续按当前查价模块过滤，未归类规则不会默认跨模块命中。同步体检响应和页面新增 `模块异常/模块体检`，可提示 `价格表模块为空`、`价格行模块为空`、`同一价格表混入多个模块`；测试补齐欧洲海运超大件价格表在亚马逊和欧洲空运查询中的后端负例，以及混模块体检异常。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "价格表|查价模块|隔离|亚马逊|欧洲海运|欧洲空运|跨模块"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "price book|legacy module|isolation|amazon|inquiry|europeExpress"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器导入/切换四个查价模块截图验收；同步体检的“当前模块查价命中了其他模块价格表”目前通过跨模块查询返回空的 e2e 闭环证明，未新增独立历史污染修复动作。

## 当前 2026-07-09 代理资料物理删除与南非报价备注已发布 47

2026-07-09 已按发布会话把当前与 47 的 checksum 差异发布到 47，发布范围判定为 `web+api`，未发现 47 待同步 Prisma schema/migrations 差异，未运行线上迁移。同步内容包含：基础资料库代理资料删除改为物理删除、南非专线品名自动识别物料类别、南非面膜报价备注按规则原文输出，以及对应 Web/API/Shared 代码、测试和任务文档。发布前验证通过：`git diff --check`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "南非|品名|物料|类别|自动匹配|面膜|化妆品"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "southAfrica|material|pricing"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "南非|面膜|备注|quoteText|复制"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "southAfrica|面膜|quoteText|备注|CBM|化妆品"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm run build -w @siyuan/api`。已执行 checksum rsync 同步、47 `COMPOSE_BAKE=false docker compose build api web`、`docker compose up -d --remove-orphans api web`；47 容器内 `/api/health` 正常、Web 本机 200，公网 `http://47.120.33.111:8899/api/health` 正常、首页 200，API 日志包含 `Nest application successfully started`。剩余：未做真实浏览器截图验收；公网 `18899` 端口不可直连但 47 对外入口 `8899` 正常；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-09 基础资料库代理资料删除改为物理删除本地落地

2026-07-09 已按任务卡 `2026-07-09-基础资料库-代理资料删除改为物理删除` 本地落地，未发布 47，未新增数据库字段。实现范围：`基础资料库 -> 代理资料` 的 `删除` 改为物理删除确认，弹窗明确展示 `物理删除` 和 `删除后不可恢复`，不再把删除动作包装成停用；前端删除成功后从代理资料列表移除代理、清空已选项，并同步移除本地无引用代理银行账户。API 新增 `POST /api/master-data/agents/batch-delete` 和 `DELETE /api/master-data/agents/:id`，继续要求 `master-data:agents:write`；内存仓储和 Prisma 仓储删除前整体校验业务引用，运单、代理渠道、价格表/价格行、代理加价规则、应付/付款申请/付款水单/代理账单等引用存在时整批拒绝并返回原因，不会自动改成停用；无引用代理会连同未被业务引用的代理银行/收款银行账户物理删除。删除成功写 `master_data.agent.delete` 审计，包含删除前代理快照、删除数量、代理 ID、代理简称、`hardDelete: true` 和删除时间。验证通过：`npm test -w @siyuan/web -- --run src/modules/masterData/masterData.test.tsx -t "代理资料|删除|物理删除|不可恢复|批量|已选"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "master data|agent|delete|hardDelete|reference|audit"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；引用校验按当前已知业务主表覆盖，未来新增代理外键/代理名业务表时需要同步加入删除保护。

## 当前 2026-07-09 报价查价南非专线品名自动识别物料类别本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-南非专线品名自动识别物料类别` 本地落地，未发布 47。实现范围：`报价查价 -> 查价 -> 南非专线查询` 在用户输入 `品名/明细关键词` 后，基于现有南非物料规则的一级分类、物料名和关键词自动识别 `指定物料类别`；支持包含匹配和长关键词优先，例如 `面膜 / 补水面膜套装` 自动匹配 `化妆品类`。无法识别时保持自动匹配，不沿用旧类别误导查价；用户手动选择类别后以手动值为准，清空品名会清空自动识别状态。API 南非查价同步增强显式 `category` 的优先级，确保手动类别覆盖关键词自动命中。未改南非运费金额公式、图片价格表导入、其他三大查价模块或权限裁剪。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "南非|品名|物料|类别|自动匹配|面膜|化妆品"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "southAfrica|material|pricing"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器手动切换类别和再次改品名的截图验收；其他品名是否命中取决于南非物料规则是否已维护关键词。

## 当前 2026-07-09 报价/代理资料/客服查询网站/链路库增量已发布 47

2026-07-09 已按发布会话梳理并发布当前与 47 的 checksum 差异，发布范围为 `api+web+migrate`。同步内容包含：价格表导入代理简称与查价模块前置选择、三大查价模块匹配逻辑对齐亮崽验证、南非专线面膜自动报价与报价表展示、查价表单 Tab 切换与方数自动计算、价格表绑定基础资料代理简称、代理资料创建时间新建置顶与批量选择治理、代理资料默认查询网站、客服面单上传确认预览与查看修改、仓库表格密度与列设置按钮文案，以及业务/仓库/财务链路库第一批 hook。线上迁移已执行 `20260709203000_price_book_agent_binding`、`20260709210000_agent_created_at`、`20260709223000_agent_tracking_website`，当前 71 个迁移全部成功。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、报价 Web 小验证、报价 API 精确小验证、基础资料库 Web、客服 Web、仓库 Web、lineage coverage/watcher/e2e、订单代理/查询网站/批量 API。发布使用 `rsync --checksum` 同步；47 分段 `COMPOSE_BAKE=false docker compose build db-migrate api`、`docker compose build web` 成功；`docker compose --profile tools run --rm db-migrate` 成功应用迁移；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，容器本地 `/api/health` 正常、首页 200，公网 `http://47.120.33.111:8899/api/health` 正常、首页 200，最终 checksum dry-run 无差异。剩余：当前工作树仍未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning；一次宽关键词 API pricing 测试因用例间状态污染失败，精确用例通过。

## 当前 2026-07-09 报价查价价格表导入代理简称与查价模块前置选择本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-价格表导入代理简称与查价模块前置选择` 本地落地，未发布 47。实现范围：`报价查价 -> 价格表管理` 导入工具区改为先选择 `代理简称`、再选择 `查价模块`、最后点击 `增加价格表`；未选代理或未选模块时按钮禁用，绕过按钮直接触发文件输入也会先提示代理简称。代理简称下拉只使用基础资料库启用代理，并支持按简称、详细公司名和代理编码搜索。价格表管理新增列名统一为 `代理简称`，不删除现有列；代理加价规则的筛选、列表、新增/编辑弹窗、体检相关表格统一展示 `代理简称` 口径。Shared/API 放开普通价格表导入目标模块到四大查价模块，后端继续校验代理存在且启用，并用所选代理简称覆盖 Excel 内部代理文本写入价格表、价格行和导入任务。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "价格表|导入|代理简称|查价模块|模糊|代理加价规则"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "price book|import|agent|shortName|targetModule|markup"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器上传 Excel 人工验收；历史未绑定代理价格表不自动回填。

## 当前 2026-07-09 报价查价三大查价模块匹配逻辑对齐亮崽验证完成

2026-07-09 按任务卡 `2026-07-09-报价查价-三大查价模块匹配逻辑对齐亮崽` 复核当前代码，三大查价模块的数据池隔离、价格表导入目标模块绑定、基础资料代理简称绑定、亚马逊出货仓和重量段匹配、欧洲空运/海运时效与渠道要求解析、业务员内部字段裁剪、代理加价规则按模块隔离等关键链路已在当前工作树具备覆盖。本轮未新增业务代码，更新 `docs/dev-now.md` 指向当前任务并执行任务卡小验证。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亮崽|亚马逊|欧洲海运|欧洲空运|铁路|快递|时效|渠道要求|重量段|出货仓|权限"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "legacy pricing|liangzai|amazon|inquiry|europeExpress|weightBand|transit|channel requirement|permission|markup"`。剩余：未做 47 发布；未做三份真实 Excel 样本的人工导入浏览器验收；旧价格表仍需重新导入才会应用新解析和模块池归属。

## 当前 2026-07-09 报价查价南非专线面膜自动报价与报价表展示本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-南非专线面膜自动报价与报价表展示` 本地落地，未发布 47，未新增数据库字段。实现范围：`报价查价 -> 查价 -> 南非专线查询` 表单收敛为 `品名/明细关键词`、`指定物料类别`、`体积 CBM`，移除重量、尺寸、件数和备注/申报输入；用户输入 `面膜` 和 `1.000CBM` 后自动按启用中的南非物料规则命中 `化妆品类 / 化妆品类`，按体积和 `0.5CBM` 低消计算运费，输出任务卡指定 5 行报价模板，复制文案同步该模板且不再展示风险费、单证费用、预估合计。查询页补充只读完整南非报价表，并高亮当前命中规则；内存仓储和 Prisma 仓储的南非查价匹配改为忽略重量/申报备注，停用规则不参与命中，默认面膜规则归入化妆品类并使用 `¥3500.00/CBM`。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "南非|面膜|重量|申报|报价表|复制"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "southAfrica|面膜|quoteText|CBM|化妆品"`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图/复制粘贴人工验收；普通角色敏感字段裁剪沿用既有报价查价权限和测试覆盖，本轮未扩展全量角色矩阵。

## 当前 2026-07-09 报价查价查价表单 Tab 切换与方数自动计算本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-查价表单Tab切换与方数自动计算` 本地落地，未发布 47，未改 API/Shared/Prisma。实现范围：`报价查价 -> 查价` 表单的四个查价模块补齐业务录入顺序 `tabIndex`，`Tab`/`Shift+Tab` 可在当前模块可编辑字段间顺序切换；欧洲海运超大件和南非专线等含尺寸/方数字段的表单支持按 `长 × 宽 × 高 × 件数 / 1000000` 自动回填 `方数 CBM`，保留 3 位小数；用户手动修改 `方数 CBM` 后按手动值重新计算计费重并参与查价，后续再明确修改尺寸/件数时重新按尺寸生成方数。南非专线表单补充重量、尺寸、件数和备注/申报信息字段，并继续复用现有南非查价接口。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "Tab|方数|CBM|尺寸|计费重|查价"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器键盘巡航截图验收；本卡不改变报价公式、加价规则、结果排序或字段裁剪。

## 当前 2026-07-09 全系统链路库财务管理第一批本地落地

2026-07-09 继续按“全系统可解释性底座”推进链路库，财务管理第一批已本地落地，未发布 47，未新增 Prisma migration，未修改 47 发布规则。实现范围：`finance.receivables.audit`、`finance.business_costs.audit`、`finance.payables.audit`、`finance.payment_applications.create`、`finance.paid_verification.confirm`、`finance.water_receipt_arrivals.arrive`、`finance.water_receipts.create`、`finance.water_receipts.match` 均接入运行时目录 key；应收审核、业务成本审核、应付审核、付款申请、付款核销、水单新增、到账和匹配都写入 sidecar 链路事件。`traceShipment` 现在除按运单 businessId 查询外，也会纳入以该运单作为 sourceRef 的结果记录，因此一票运单可追到水单匹配、应收应付、付款申请和付款核销。财务覆盖清单更新为 10 个目录中 8 个 wired、2 个 pending（财务看板聚合快照、代理账单 AI 解析建议）。验证通过：`npm test -w @siyuan/api -- --run src/modules/lineage-event-coverage.test.ts`、`npm test -w @siyuan/api -- --run src/modules/lineage-watcher.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.lineage.e2e.test.ts`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`git diff --check`。补充：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "water receipt|receivable|payable|payment|paid"` 被既有业务成本 e2e 阻塞，该用例单独运行也在录单提交处返回 400，本轮新增 lineage 财务链路 e2e 已覆盖财务 hook。剩余：财务看板聚合、代理账单 AI 过程事件未接入；全系统市场、客服、物流轨迹、基础资料、系统管理和运营工作台仍需逐批补 hook。

## 当前 2026-07-09 基础资料库代理资料创建时间与批量选择本地落地

2026-07-09 已按任务卡 `2026-07-09-基础资料库-代理资料创建时间新建置顶与批量选择治理` 本地落地，未发布 47。实现范围：`AgentSummary/Agent` 补 `createdAt`，新增迁移 `20260709210000_agent_created_at`，历史代理以迁移执行时间作为数据库默认兜底；内存仓储和 Prisma 仓储的代理资料默认按 `createdAt desc` 返回，新建代理带服务端创建时间并显示在第一页第一行。`基础资料库 -> 代理资料` 新增 `创建时间` 列，按北京时间格式展示；代理资料从单选改为 `ManagedTable` 当前页批量选择，工具区显示 `已选 X 条`，单选可修改，多选禁用修改，删除/停用支持多选并在二次确认中展示影响数量，成功后清空已选。新增 `POST /api/master-data/agents/batch-enabled`，逐条复用现有停用逻辑、权限和审计。验证通过：`npm test -w @siyuan/web -- --run src/modules/masterData/masterData.test.tsx -t "代理资料|创建时间|新建|第一行|批量选择|全选|已选"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "基础资料库|代理资料|代理渠道|公司渠道|渠道类别|客户资料|批量选择"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "createdAt|batch"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。补充：任务卡原 API 小验证因关键词 `audit` 额外命中既有运单全流程用例，该用例在付款申请处返回 400；`npm run typecheck -w @siyuan/api` 被当前工作树既有 `prisma.repository.ts` 包裹/面单变量类型问题阻塞，非本卡代理资料改动引入。剩余：未做 47 发布；未做真实浏览器截图验收；代理渠道、公司渠道、渠道类别、客户资料本轮通过既有设置用例检查，未发现必须随本卡新增 createdAt 排序的改造点。

## 当前 2026-07-09 全系统链路库仓库管理第一批本地落地

2026-07-09 继续按“全系统可解释性底座”推进链路库，仓库管理第一批已本地落地，未发布 47，未新增 Prisma migration，未修改 47 发布规则。实现范围：`warehouse.today.receive`、`warehouse.packages.update`、`warehouse.packages.split`、`warehouse.tally.create`、`warehouse.tally.complete`、`warehouse.queue.dispatch`、`warehouse.queue.label` 均接入运行时目录 key；收货、包裹修改/备注/异常、拆分、理货创建/完成、理货标签生成/打印/下载/扫描应用、运单面单生成/上传、确认出库都写入 sidecar 链路事件。`traceShipment` 纳入 `warehouse_label`，一票运单可追到仓库面单与出库结果；仓库覆盖清单更新为 10 个目录中 7 个 wired、3 个聚合快照 pending。验证通过：`npm test -w @siyuan/api -- --run src/modules/lineage-event-coverage.test.ts`、`npm test -w @siyuan/api -- --run src/modules/lineage-watcher.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.lineage.e2e.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "warehouse package|tally|label|dispatch|split"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：仓库待排货/已出库/看板聚合快照未接入；全系统其他模块仍需分批补 hook，下一批建议财务管理第一批，把水单新增/到账/匹配、应收应付审核、付款申请和已付款核销改为目录 key 并补 trace 验收。

## 当前 2026-07-09 客服面单上传确认预览与查看修改本地落地

2026-07-09 已按任务卡 `2026-07-09-customer-service-label-upload-confirm-preview` 本地落地，未发布 47。实现范围：`客服管理 -> 上传面单` 弹窗改为选择、拖拽、粘贴图片/PDF 后先进入待确认预览状态，展示文件名、类型、大小和预览入口；只有点击 `确认上传` 才调用现有上传接口。待确认状态支持 `修改` 重新选择或取消；上传成功后行内显示 `已上传面单`，再次点击进入弹窗可查看/下载当前面单，也可 `修改/替换面单`。继续复用现有上传 API、图片/PDF 与 10MB 校验、客服/管理员权限、`shipment.label.upload` 审计和文件存储口径，不改变运单状态。验证通过：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "面单|上传|粘贴|确认|修改|查看"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "label|upload|customer service"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。补充：`npm run typecheck -w @siyuan/api` 被当前工作树既有 `apps/api/src/modules/prisma.repository.ts` 类型错误阻塞，非本卡改动。剩余：未做 47 发布；未做真实浏览器粘贴/拖拽截图验收；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 全系统链路库业务管理第一批本地落地

2026-07-09 已把全系统链路库目标收口为“业务结果可解释性底座”：记录原始数据、过程规则、中间结果和最终结果，支持从报价、运单、仓库包裹、出库、应收应付、水单匹配等结果反查底层行、规则、操作人和时间。本轮在既有通用 Watcher + 报价查价第一批基础上，补齐 `业务管理` 第一批运行时 hook：录单提交、草稿保存/编辑、草稿删除、审核通过、审核驳回、运单修改、删除/恢复/彻底删除均改为目录 key 事件；`traceShipment` 纳入 `shipment_draft_delete`；覆盖清单更新为业务管理 8 个目录中 7 个 wired、`orders.ai.suggestion` pending。验证通过：`npm test -w @siyuan/api -- --run src/modules/lineage-event-coverage.test.ts`、`npm test -w @siyuan/api -- --run src/modules/lineage-watcher.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.lineage.e2e.test.ts`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order entry|review|draft"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未发布 47；未新增 Prisma migration；全模块低频动作还未全部接线，下一批建议进入仓库管理或财务管理，把已 partial 的包裹/出库/水单匹配改为目录 key hook，并补 trace 验收。

## 当前 2026-07-09 代理资料维护默认查询网站本地落地

2026-07-09 已按任务卡 `2026-07-09-agent-default-tracking-website` 本地落地，未发布 47。实现范围：基础资料库代理资料新增 `查询网站` 字段，支持 `https://track.example.com?no={transferNo}` 模板；Shared 类型、Prisma schema 和迁移 `20260709223000_agent_tracking_website`、内存仓储、Prisma 仓储、代理创建/编辑/查询均补齐该字段，代理资料创建/更新继续写 `master_data.agent.create/update` 审计。`客服管理` 统一修改弹窗在没有单票已保存查询网站时，按运单 `agentId` 优先、`agentName/shortName/code` 兼容匹配代理默认查询网站，并将 `{transferNo}` 替换为当前转单号；客服仍可按单覆盖且不会反写代理资料。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx src/modules/customerService/customerService.test.tsx -t "代理|查询网站|转单号|客服"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "agent|代理|tracking website|查询网站|master data"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；本轮新增 Prisma 迁移，发布时需要执行 migrate deploy；历史代理不回填查询网站。

## 当前 2026-07-09 报价查价价格表绑定基础资料代理简称本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-价格表绑定基础资料代理简称` 本地落地，未发布 47。实现范围：价格表上传入口在查价模块之外新增 `所属代理` 必选项，选项来自基础资料库启用代理简称；导入任务和直接导入接口接收并校验 `agentId/agentShortName`，新导入价格表、导入任务和价格行统一绑定所选代理简称，Excel 原始代理文本和文件名不再覆盖系统代理身份。价格表管理新增 `所属代理` 列，历史未绑定价格表显示 `未绑定代理`；代理加价规则筛选、新增和编辑代理下拉复用基础资料代理简称，查价和规则命中优先按价格表绑定代理口径计算。Shared/Prisma 补齐价格表和导入任务代理绑定字段，并新增迁移 `20260709203000_price_book_agent_binding`。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "价格表|所属代理|代理简称|代理加价|查价|来源"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "price book|agent|shortName|markup|lookup"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器上传 `7.3振韵.xls` 人工验收；历史价格表不会自动补绑定代理，需按任务卡提示重新处理。

## 当前 2026-07-09 仓库表格密度与列设置按钮文案本地落地

2026-07-09 已按任务卡 `2026-07-09-warehouse-table-density-column-button-label` 本地落地，未发布 47。实现范围：仓库模块 `ManagedTable` 的列设置入口统一迁到表格工具条，按钮文案统一显示 `列设置`，弹窗标题仍保留 `待出库列设置`、`在仓数据列设置` 等上下文；`仓库管理 -> 待出库` 删除按钮文案 `待出库列设置`，收紧待出库表格列宽和最小横向宽度，列设置入口不再占用固定右侧表头假列。同步覆盖今日收货、在仓数据、待排货、未完成/已完成理货、已出库和理货明细表格的列设置入口布局。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "列设置|待出库|在仓数据|今日收货"`、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx -t "列设置|column"`。剩余：未做 47 发布；未做真实 1366px 浏览器截图验收；未运行全量 typecheck，当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 报价/基础资料/个人中心增量已发布 47

2026-07-09 已按发布会话梳理并发布当前与 47 的 checksum 差异，发布范围为 `api+web+migrate`。同步内容包含：报价查价亚马逊出货仓按地名筛选、代理加价规则按查价大类拆分管理与命中、价格表按查价模块导入归属、业务员隐藏价格表来源名称、亚马逊按计费重自动匹配重量段、基础资料库代理资料三组收款银行账户、业务录单费用名称下拉、个人中心去掉登录日志并改账号事件、财务资料表格紧凑排版，以及相关测试/任务卡。线上迁移已执行 `20260709183000_agent_markup_legacy_module`，68 个迁移全部成功。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、报价 Web 小验证、报价 API 关键用例、基础资料库 Web、设置个人中心 Web、财务 Web/API、订单账号事件/费用名称 API。发布：先用 `rsync --checksum` 同步；47 初次组合 build 会话静默卡住，确认无活跃构建进程后中断，改为分段 `COMPOSE_BAKE=false docker compose build db-migrate api`、`docker compose build web` 成功；`docker compose --profile tools run --rm db-migrate` 成功应用迁移；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，容器本地 `/api/health` 正常、首页 200，公网 `http://47.120.33.111:8899/api/health` 正常、首页 200，API 日志包含 `Nest application successfully started`。剩余：当前工作树仍未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning；一次宽关键词 API pricing 测试因用例间状态污染出现 1 个失败，单独运行该业务员渠道用例通过。

## 当前 2026-07-09 报价查价按重量自动匹配重量段和价格行本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-按重量自动匹配重量段和价格行` 本地落地，未发布 47。实现范围：亚马逊查询根据实重、方数、尺寸或计费重自动推导 `12KG+ / 50KG+ / 100KG+`，用户修改实重后默认随重量段切换，保留手动选择重量段作为覆盖入口；查价提交会携带最终计费重与自动匹配后的重量段，`FTW5 + 500KG` 不再沿用默认 `12KG+`。API 内存仓储与 Prisma 仓储在 legacy 亚马逊报价中按最终计费重归一化重量段，避免旧请求里的 stale `weightBand/tier` 导致漏价；宽泛重量区间仍按区间覆盖能力命中，不影响现有地名出货仓和业务员字段裁剪。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "重量段|自动匹配|亚马逊|欧洲海运|欧洲空运|南非"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "weightBand|重量段|chargeable|amazon|inquiry|europe|southAfrica"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；南非和欧洲海运/欧洲空运本轮只保持既有自动计费/无重量预览口径并用现有小验证覆盖，未新增价格公式。

## 当前 2026-07-09 报价查价亚马逊出货仓按地名筛选本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-亚马逊出货仓按地名筛选` 本地落地，未发布 47。实现范围：`报价查价 -> 查价 -> 亚马逊查询` 的 `出货仓` 下拉在前端再次过滤为地名/区域口径，只保留 `义乌仓 / 华东 / 华南 / 汕头 / 深圳/广州仓` 等亮崽地名集合，避免接口或测试桩异常返回渠道/线路名时展示到下拉；Web 回归断言补齐 `义乌仓`、`汕头` 和 `欧洲铁路超大件专线` 等渠道名排除。API 侧保留现有按 `sourceSheetName/origin` 归一化后的地名过滤逻辑，并修复无大类删除标记对虚拟默认加价行的抑制，避免本卡 API 验证命令中的既有用例回退。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亚马逊|出货仓|义乌|华东|华南|渠道名|FTW"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "amazon|origin|warehouse|FTW|出货仓|lookup"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；旧价格表不回填，需要已有行能解析出地名或重新导入。

## 当前 2026-07-09 报价查价价格规则按查价大类拆分管理和命中本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-价格规则按查价大类拆分管理和命中` 本地落地，未发布 47。实现范围：`报价查价 -> 代理加价规则` 增加亚马逊、欧洲海运超大件、欧洲空海运铁路快递、南非专线四个查价大类切换；规则列表、KPI、筛选、来源价格表、新增/编辑弹窗、批量启停/删除、同步体检和查看线路均带当前大类上下文，新增/编辑规则默认写入当前 `legacyModule`，来源价格表只展示当前大类价格表。Shared/API/Prisma 增加代理加价规则 `legacyModule` 字段与迁移；内存仓储和 Prisma 仓储在规则列表、导出、体检、批量操作、创建/更新、线路加价展示和 legacy 查价命中时按大类隔离；有来源价格表的历史规则可按价格表大类归属，无法判断且无价格表的规则保持未归类，不默认跨大类生效。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "代理加价规则|查价模块|价格规则|来源价格表|亚马逊|欧洲海运|欧洲空运|南非"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|agent markup|legacy module|price book|amazon|inquiry|europeExpress|southAfrica"`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；`npm run typecheck -w @siyuan/web` 被当前工作树既有 `apps/web/src/modules/masterData/MasterDataPage.tsx` 银行账户表单类型问题阻塞，非本卡报价改动引入。

## 当前 2026-07-09 基础资料库代理资料收款银行账户一二三本地落地

2026-07-09 已按任务卡 `2026-07-09-基础资料库-代理资料收款银行账户一二三` 特例本地落地，未发布 47。实现范围：`基础资料库 -> 代理资料` 表格在仓库地址后展示 `收款银行账户一 / 二 / 三`，管理员和具备 `finance:payable:bank` 的角色可查看完整银行信息；代理新增/修改弹窗支持三组收款银行账户的收款方、开户银行、银行账号、币种、备注和启停维护，任一组填写不完整时阻止保存并提示第几组缺字段，清空已有账户会同步停用。Web/API 统一 `agent-bank-accounts` 支持按 id 更新、`includeDisabled` 查询、启停状态保存，并同步启用账户到 `PayeeBankAccount`，供待付款/付款申请选择；审计中银行账号脱敏。验证通过：`npm test -w @siyuan/web -- --run src/modules/masterData/masterData.test.tsx -t "代理资料|收款银行|账户一|账户二|账户三"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "基础资料库|代理资料|银行账号"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "待付款|付款申请|收款银行|代理"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "agent bank|payee bank|payment applications|收款银行"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。补充：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "agent bank|payee bank|payment applications|收款银行"` 无命中用例。剩余：未做 47 发布；未做真实浏览器截图验收；历史付款申请和已付款单据不回写。

## 当前 2026-07-09 报价查价价格表按查价模块导入归属本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-价格表按查价模块导入归属改造` 本地落地，未发布 47。实现范围：`报价查价 -> 价格表管理` 的普通价格表导入新增目标查价模块选择，未选择时不能提交；导入任务与直接导入接口都要求 `targetModule`，并把整张价格表有效价格行写入所选 legacy 模块，南非专线仍不进入普通导入模块选择。价格表管理列名改为 `查价模块`，新导入价格表展示单一模块与导入行数；内存仓储与 Prisma 仓储的新导入报价查询按价格表目标模块隔离，旧价格表无目标模块时保留既有行级推断兼容。导入审计、导入任务 raw lineage 和完成审计记录目标模块；Web/API 测试桩与 e2e 覆盖必选模块、单模块归属、跨模块不串查。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "价格表|查价模块|导入|渠道分类|亚马逊|欧洲海运|欧洲空运"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "price book|legacy|module|import|lookup"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`。剩余：未做 47 发布；旧价格表不自动迁移，需删除后按正确模块重新导入；未做真实浏览器上传 7.3 振韵文件人工验收；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 报价查价业务员隐藏价格表来源名称本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-业务员查价隐藏价格表来源名称` 本地落地，未发布 47。实现范围：`报价查价 -> 查价` 普通业务员/查价角色的亚马逊、欧洲海运超大件、欧洲空海运铁路快递查价成功提示统一为 `查价成功，已输出报价`，不再把内部代理、价格表来源或源文件名拼进 toast；报价结果、详情和复制报价继续只展示销售价、时效、重量段和渠道要求。API 内存仓储与 Prisma 仓储对普通查价响应裁剪内部来源和成本字段：legacy 推荐不返回 `sourceId/sourceFile/origin/costUnitPrice/costTotal/grossProfit/markup/raw`，通用价格查价的 `price` 不返回成本、来源小表、行加价来源，并将价格表 ID 置空；普通角色的 legacy quote meta 不返回内部代理和来源选项。管理员/市场仍保留价格表管理、加价规则和内部详情查看能力。同步放宽 shared legacy 推荐成本字段为可选，并补 Web/API 测试桩与 e2e 断言。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "业务员|查价成功|价格表|来源|亚马逊|欧洲海运|欧洲空运"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "business|operator|source|price book|lookup|legacy"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；当前工作树仍有大量既有未提交改动，本轮只按任务卡触碰报价查价来源裁剪相关文件。

## 当前 2026-07-09 业务录单费用名称下拉选择本地落地

2026-07-09 已按任务卡 `2026-07-09-business-order-entry-fee-name-select` 本地落地，未发布 47。实现范围：`业务管理 -> 录单` 的应收费用、业务成本，以及有权限可见的应付费用名称从自由输入改为启用费用名称下拉；应收默认优先填启用的 `运费`，业务成本默认优先填启用的 `业务员成本`，资料库缺失时为空待选。默认财务资料补充常用费用名以兼容现有录单测试和初始环境；Web 测试桩同步启用/停用费用样本。API 在创建录单和更新录单草稿前校验有效费用行的费用名称必须存在于启用 `FEE_NAME`，并保持客户归属、角色应付权限等越权校验先于费用名称校验，避免未授权请求泄露资料库校验结果。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "录单|费用名称|finance catalog"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "finance catalog"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order entries|fee name"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；历史费用名称不回填；已有真实数据库如果已存在财务资料，新增默认费用名不会自动补入，需要在基础资料库维护后才会出现在录单下拉。

## 当前 2026-07-09 个人中心去掉登录日志并优化账号事件布局本地落地

2026-07-09 已按任务卡 `2026-07-09-system-personal-center-account-events` 本地落地，未发布 47。实现范围：`个人中心` 弹窗不再请求 `/api/auth/login-logs`，不再展示 `登录日志` 表格；弹窗重排为 `账号资料 / 修改密码 / 账号事件` 三块，员工账号和当前角色只读展示，姓名、手机号、性别、昵称仍通过 `/api/auth/profile` 保存并提示 `个人资料已更新`，修改密码继续调用 `/api/auth/change-password` 并退出重新登录。新增只读 `/api/auth/account-events`，内存仓储和 Prisma 仓储均只返回当前登录用户本人的 `auth.profile.update`、`auth.password.change` 及目标为当前用户的 `system.staff.*` 事件，明确排除 `auth.login.*` 登录成功/失败、IP、地区、设备等登录日志；测试桩同步补本人事件裁剪。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "个人中心|个人资料|修改密码|登录日志|账号事件"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "account events|账号事件|login logs|auth"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实 1366px 浏览器截图验收；系统管理操作日志保留原有登录审计能力，本轮未重构。

## 当前 2026-07-09 基础资料库财务资料表格紧凑排版本地落地

2026-07-09 已按任务卡 `2026-07-09-基础资料库-财务资料表格紧凑排版修复` 本地落地，未发布 47。实现范围：`基础资料库 -> 财务资料` 费用名称、结算方式、货物类型、品名页签的表格列宽重新收敛，名称列作为主列，默认币种/状态/排序/操作列改为紧凑宽度；表格使用固定布局并按业务列 + `列设置` 系统列计算横向滚动宽度，避免右侧大空白和整页横向溢出。筛选区搜索框、只看启用、刷新保持紧凑同排，窄屏可换行；备注文本改为省略展示，操作列继续固定右侧；无写权限时不再渲染编辑/停用/删除按钮，只显示 `只读`。停用、删除二次确认、排序、新增/编辑等业务逻辑未改。验证通过：`npm test -w @siyuan/web -- --run src/modules/masterData/masterData.test.tsx -t "财务资料|费用名称|结算方式|货物类型|品名|停用|删除|排序"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "finance catalog|财务资料|品名|货物类型|结算方式"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "基础资料库|财务资料|费用名称|结算方式|货物类型|品名"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实 1440/1280 浏览器截图验收；汇率页本轮排查未发现复用 `FinanceCatalogPage`，未改汇率表格业务。

## 当前 2026-07-09 47 发布状态复核无待同步文件

2026-07-09 发布会话复核当前本机与 47 `/opt/siyuan` 的 checksum 差异：`rsync --dry-run --checksum --itemize-changes` 无文件内容差异，本轮无需重新构建/重启。47 健康检查通过：`api/web/postgres/redis` 均 Up，容器本地 `http://127.0.0.1:18899/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，容器本地首页 200，公网 `http://47.120.33.111:8899/api/health` 正常，公网首页 200，API 日志包含 `Nest application successfully started`。剩余：当前工作树仍有大量未提交改动；公网 18899 仍不是对外入口，以 8899 为准；Web 构建既有 `antd-vendor` 大 chunk warning 未处理。

## 当前 2026-07-09 仓库包裹入仓数据修改本地落地

2026-07-09 已按任务卡 `2026-07-09-warehouse-package-edit-inbound-data` 本地落地，未发布 47。实现范围：`仓库管理 -> 今日收货` 和 `在仓数据` 对可修改的入仓包裹增加 `修改` 操作，编辑弹窗支持客户编号、快递单号、组合号、总件数/件序、件数、单件实重、长宽高、扫描时间、备注和人工异常，保存后刷新今日收货/在仓数据并同步重算体积、5000/6000 材积重、计费重和统计。API `PATCH /api/warehouse/packages/:id` 补齐受控字段更新、客户/单号/组合号同步、重复校验、状态/正式运单绑定拦截和 `warehouse.package.update` 审计；内存仓储、Prisma 仓储和 Web 测试桩口径保持一致。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "今日收货|在仓数据|修改|客户编号|件数|异常"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "warehouse package|update|remark|exception"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 仓库/基础资料库/财务水单凭证与 47 Nginx 上传代理修复已发布 47

2026-07-09 用户授权直接验证 47 并发布后，已按当前与 47 的 checksum 差异发布，发布范围为 `api+web`，未发现 Prisma schema/migration 待同步差异，未运行线上迁移。同步内容包含当前仓库待出库批量勾选出货、基础资料库财务资料横向切换与硬删除、财务水单凭证上传显示修复，以及发布验收发现的 47 Nginx `/api/uploads/...` 代理修复：`deploy/nginx.conf` 将 `location /api/` 改为 `location ^~ /api/`，避免 `.png/.jpg` 等上传凭证路径被静态资源正则抢先匹配导致 404。47 当前 Compose 验证：`UPLOAD_DIR` 默认 `/app/uploads/vouchers`，named volume `api-uploads:/app/uploads`，与后端统一上传根目录兼容。验证通过：`git diff --check`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、47 `docker compose build api web`、追加 `docker compose build web`、`docker compose up -d --remove-orphans api web`、本机 `http://127.0.0.1:18899/api/health` 正常、首页 200、临时 PNG 通过 `http://127.0.0.1:18899/api/uploads/vouchers/...` 返回 200 `image/png`，重启 `api/web` 后同一路径仍返回 200，公网 `http://47.120.33.111:8899/api/health` 与首页 200。剩余：公网 `18899` 仍超时但 `8899` 正常，这是 47 现有入口状态；Web 构建仍有既有 `antd-vendor` 大 chunk warning；未做真实浏览器人工上传截图验收；历史丢失文件或旧错误 URL 不回填。

## 当前 2026-07-09 仓库待出库批量勾选出货本地落地

2026-07-09 已按任务卡 `2026-07-09-warehouse-dispatch-batch-select-ship` 本地落地，未发布 47。实现范围：`仓库管理 -> 待出库` 保持选择列固定在第一列，并让表头选择同时保留全选框和 `选择` 标题；顶部 `已选 X 票 / Y 件` 改为按当前实际选中行计算，筛选/翻页/队列数据变化时自动清理不可见或已出库的待出库选择，未勾选点击顶部 `出货` 仍提示 `请先勾选待出库订单`。批量出货继续复用现有代理交接单和逐票出货链路，出货成功后清空选择并移出待出库队列；无 `warehouse:write` 时仍不展示批量勾选和出货入口。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待出库|批量出货|全选|已选|出货"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "dispatch|handover|outbounded"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 基础资料库财务资料横向切换与硬删除本地落地

2026-07-09 已按任务卡 `2026-07-09-基础资料库-财务资料横向切换与硬删除` 本地落地，未发布 47。实现范围：`基础资料库 -> 财务资料` 改为 `费用名称 / 结算方式 / 货物类型 / 品名` 横向切换，默认只展示费用名称，切换后只显示当前模块的搜索、只看启用、新增按钮、表格和操作列；财务资料行保留编辑、启用/停用、排序，新增二次确认硬删除。前端读写入口按 `finance:read/master-data:finance:read` 与 `finance:settle/master-data:finance:write` 收紧；后端 `DELETE /api/finance/catalog/:id` 改为硬删除，删除后不再返回该资料并写 `finance.catalog.delete` 审计；停用改走 `PUT enabled=false`，不再占用 DELETE 语义。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "基础资料库|财务资料|费用名称|结算方式|货物类型|品名|删除"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "finance catalog|财务资料|品名|货物类型|结算方式"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "finance catalog|delete|audit|permission"`、`git diff --check`。补充：`npm run typecheck -w @siyuan/web` 被既有 `WarehousePage.tsx` 的 `WarehousePackageUpdateInput.customerCode` 类型问题阻塞；`npm run typecheck -w @siyuan/api` 被既有仓库包裹更新字段类型问题阻塞，非本卡改动。剩余：未做 47 发布；未做真实浏览器截图验收；旧会话/旧前端若仍调用 DELETE 做停用需切到 PUT，本仓前端已切换。

## 当前 2026-07-09 财务水单到账查询凭证上传后无法显示修复本地落地

2026-07-09 已按任务卡 `2026-07-09-财务管理-水单到账查询凭证上传后无法显示修复` 本地落地，未发布 47。实现范围：`财务管理 -> 水单到账查询/水单匹配` 的水单凭证上传后，前端会把后端返回的 `/api/uploads/...` 相对地址解析到 API 地址用于弹窗即时预览和查看大图；图片加载失败时提示 `图片已上传但预览地址不可访问，请联系管理员检查上传目录配置` 并保留本地预览兜底。API 上传目录改为统一上传根目录 + `vouchers` 子目录，返回 URL 固定为 `/api/uploads/vouchers/{fileName}`，兼容 `UPLOAD_DIR=/app/uploads` 与 `/app/uploads/vouchers`；静态服务统一挂载上传根目录，避免 `/api/uploads/uploads/...` 或 404。水单列表对无 `finance:water-receipt:voucher` 权限的角色裁掉凭证 URL；上传/替换水单凭证审计补齐水单 ID/编号、文件名、大小、MIME、上传人、上传时间和替换前凭证信息。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "水单|凭证|upload|voucher|预览"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "water receipt|voucher|upload"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "water receipt|voucher|upload"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未在真实 47/Nginx 环境执行上传后重启容器验证；历史已丢失或旧错误 URL 的图片文件不回填。

## 当前 2026-07-09 全局重要操作二次确认第一阶段本地落地

2026-07-09 已按任务卡 `2026-07-09-全局重要操作二次确认统一治理` 完成第一阶段本地落地，未发布 47。实现范围：共享 UI 新增 `ConfirmActionButton`、`confirmDangerAction`、`buildConfirmActionTitle`、`buildConfirmActionSummary`，统一确认标题、操作对象、状态变化、影响数量、财务金额/币种、风险提示和高风险原因输入；代表页面 `财务管理 -> 水单到账查询/水单匹配` 的 `到账`、`作废` 已接入共享确认，其中 `作废` 必须填写至少 2 个字符原因并传给 `voidWaterReceipt` 审计链路。已完成全局重要操作扫描，待接入主要集中在财务应收/应付/待付款/已付款/代理账单、报价规则和价格表、仓库收货/理货/出库、客服状态推进、业务/市场审核驳回、基础资料和系统管理停用删除/权限变更。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "二次确认|到账|作废|审核|反审核|确认付款|反核销"`、`npm test -w @siyuan/web -- --run src/modules/shared/ui.test.ts -t "confirm|确认|danger"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：本轮只做第一阶段和代表页，其他高风险操作仍需按模块逐步接入；未做真实浏览器截图验收；未做 47 发布。

## 当前 2026-07-09 全局日期选择器第一阶段本地落地

2026-07-09 已按任务卡 `2026-07-09-全局日期选择器美化与统一格式治理` 完成第一阶段本地落地，未发布 47。实现范围：共享 UI 新增 `AppDatePicker`、`AppDateRangePicker`、`APP_DATE_FORMAT`、`isAppDateValue`、`isAppDateRangeInvalid`，统一使用 AntD 日期面板、`YYYY-MM-DD` 字符串值、右侧日历图标、圆润边框、轻阴影弹层和系统主色选中态；代表页面 `财务管理 -> 待付款` 的 `申请付款日期起/止` 已从裸 `input type="date"` 替换为 `AppDatePicker`，查询仍提交 `applicationDateFrom/applicationDateTo=YYYY-MM-DD`，重置清空日期，结束日期早于开始日期时前端提示 `结束日期不能早于开始日期` 并阻止查询。已完成全局日期控件扫描，剩余裸日期/日期时间入口主要集中在：财务已付款、水单、代理账单、录单/财务录单；仓库今日收货和手动扫描时间；客服生命周期 ETD/ETA；基础资料有效期；系统管理现有 AntD DatePicker 待统一到共享组件。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "日期|待付款|查询|重置"`、`npm test -w @siyuan/web -- --run src/modules/shared/ui.test.ts -t "date|日期"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器/移动端截图验收；本轮按任务卡第一阶段只替换待付款代表页，其他模块仍需第二/三阶段继续替换。

## 当前 2026-07-09 物流轨迹最新轨迹列表与导入覆盖本地落地

2026-07-09 已按任务卡 `2026-07-09-物流轨迹管理-最新轨迹列表化与导入覆盖当前轨迹` 本地落地，未发布 47。实现范围：`物流轨迹管理 -> 最新轨迹` 从卡片堆叠改为 `ManagedTable` 列表，字段包含更新时间、运单号、客户、转单号、当前轨迹、地点、状态、未更新天数和查看详情；`导入轨迹` 入口合并到最新轨迹工具区，独立 `手动添加轨迹` 二级入口隐藏。导入预览展示原始行数、可覆盖运单数、未匹配、错误行、每票覆盖后的当前轨迹和覆盖更新时间；确认后按同一运单最新 `轨迹日期时间` 覆盖 `latestTracking/latestTrackingUpdatedAt`，清零 `trackingStaleDays`，保留轨迹事件和 `tracking.manual_import` 审计，不自动改变签收等运单状态。验证通过：`npm test -w @siyuan/web -- --run src/modules/tracking/tracking.test.tsx -t "最新轨迹|列表|导入轨迹|覆盖|更新时间"`、`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx -t "物流轨迹管理|最新轨迹|手动添加轨迹|权限"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "tracking|import|overwrite|updatedAt|permission"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图/Excel 人工验收；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 财务/报价增量与 API 构建修复已发布 47

2026-07-09 已按发布会话梳理当前与 47 的 checksum 差异并发布，发布范围为 `api+web`，未运行数据库迁移。同步内容包括：欧洲空运横向小表时效解析修复、财务应付审核与待付款付款申请单相关补齐、共享表格类型修复，以及发布构建中发现的 API 类型问题修复：价格查价血缘记录不再读取不存在的 `PriceLookupResponse.selected`，仓库包裹体积改用现有 `cbm` 字段，血缘 sidecar 采样写入异常改为吞掉并禁用本轮 watcher，避免影响主业务构建/测试。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、报价 Web/API 小验证、财务 Web/API 小验证、仓库 API 小验证。发布使用 `rsync --checksum` 同步；47 `docker compose build api web` 后因并发本地增量又追加重建 `api`，最终 `api/web/postgres/redis` 均 Up，公网 `http://47.120.33.111:8899/api/health` 返回正常，公网首页 200，最终 checksum dry-run 无差异。剩余：当前工作树仍未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning；47 的 18899 端口公网访问超时，但 8899 公网与容器本地 18899 均正常。

## 当前 2026-07-09 欧洲空运横向小表时效解析修复本地落地

2026-07-09 已按任务卡 `2026-07-09-报价查价-欧洲空运横向小表时效解析修复` 本地落地，未发布 47。实现范围：Web 上传预解析和 API 导入解析的横向小表时效识别优先保留标准时效列；补充扫描价格列右侧非备注说明区，只有包含 `参考时效/全程时效/派送时效/运输时效/航程时效/船期/开船/发车/提取/起飞/飞/派送` 等业务运输信号且带 `天/日` 时才写入 `transitLabel/transitDays`；右侧说明格里的限制、拒收、不包税、保留天数等内容仍进入 `渠道要求`。同时收紧备注兜底解析，`保留7日`、`7日后退回`、`时效赔付/延误/不保证`、`超时不赔`、`无法送货上门` 不再误入时效，`parseTransitDays` 不再从无效整段文本里兜底抓数字。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "欧洲空运|横向小表|时效|渠道要求|备注|保留7日"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "欧洲空运|horizontal|transit|渠道要求|remark|price book"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：旧价格表需要重新导入才会获得新解析结果；未做真实浏览器截图验收；未做 47 发布；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 财务应付审核字段与付款前置流程差距补齐本地落地

2026-07-09 已按用户授权的特例扩大范围落地 `财务管理-应付审核字段与付款前置流程差距补齐`，未发布 47。实现范围：`财务管理 -> 市场应付审核` 删除/批量删除文案统一为 `删除`，批量审核/反审核/删除确认展示已选数量，金额类字段改为纯数字展示且币种独立展示，操作列锁定可见；审核成功后提示进入 `待付款` 并跳转补充付款前置资料。`待付款` 保留并落库供应商账单截图，银行选择支持筛选，金额汇总改为纯数字。API 内存仓储和 Prisma 仓储补齐付款申请反审/删除前置阻断：已进入付款、已支付或已有付款凭证的应付不能随意反审核；银行资料和付款审计中的账号脱敏；新建运单保留 `agentId/agentName`，付款银行必须与待付款代理和币种匹配。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "payable|应付|待付款|代理账单|凭证|银行|全选|已选"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "payable audits|payment applications|bank and bill metadata"`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图/粘贴图片人工验收；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 财务待付款付款申请单差距补齐本地落地

2026-07-09 已按任务卡 `2026-07-09-财务管理-待付款付款申请单差距补齐` 本地落地，未发布 47。实现范围：`财务管理 -> 待付款` 标题、筛选、表格、金额/币种展示和当前页勾选交互沿用并补齐任务卡口径；`生成付款申请` 改为可在无勾选、跨收款方/银行/币种、缺银行、缺供应商账单截图时给出明确提示；付款资料弹窗继续支持选择已保存银行、手动填写银行、选择是否保存银行、上传/粘贴/预览供应商账单截图。后端内存仓储和 Prisma 仓储补齐付款申请提交前银行与账单截图校验、跨组拒绝、选择银行审计、上传账单后状态更新，以及无代理名待付款行使用已选收款银行代理作为付款申请收款方兜底；审计中的银行账号保持脱敏。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "待付款|付款申请|供应商账单|收款银行|已选|全选|凭证"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "pending payment|payment applications|payable audits|bank|bill"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图/粘贴图片人工验收；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 财务应收审核字段与逻辑差距补齐本地落地

2026-07-09 已按任务卡 `2026-07-09-财务管理-应收审核字段与逻辑差距补齐` 本地落地，未发布 47。实现范围：`财务管理 -> 应收审核` 默认列补齐业务员、费用名称、客户编号、运单号、转单号、币种、金额、结算方式、匹配水单编号、合计、制单/审单信息、备注、对账状态和操作；金额与匹配金额改为纯数字展示，币种独立展示，合计按 RMB 汇总展示。应收审核水单匹配入口不再限制人工应收，已审核且未收齐的系统自动应收也可打开匹配水单弹窗；弹窗只列出同客户、同币种、已到账/部分匹配且有余额的水单。内存仓储补齐系统自动应收匹配、余额扣减、匹配状态、撤销匹配恢复和已匹配反审核拦截；Web 测试桩补系统自动应收样本、可用水单筛选和测试间状态重置。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "应收|receivable|水单|匹配|全选|已选"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "receivable|water receipt|match|reverse"`、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx -t "selection|全选|当前页|已选"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；Prisma 持久化侧系统自动应收匹配仍受现有 `WaterReceiptMatch` 只关联 `ShipmentFinanceItem` 的模型限制，本轮未新增迁移。

## 当前 2026-07-09 市场待排货操作列补齐本地落地

2026-07-09 已按任务卡 `市场待排货操作列补齐` 本地落地，未发布 47。实现范围：`市场管理 -> 待排货` 行操作固定为 `审核通过 / 修改 / 操作日志 / 删除`；`审核通过` 与 `修改` 复用现有排货审核和市场排货弹窗；`操作日志` 打开当前运单排货生命周期记录并补足进入待排货兜底日志；新增受控 `DELETE /api/shipments/:id/pending-routing`，要求 `routing:write`，仅允许 `WAITING_SORT`，必须填写删除原因，内存仓储和 Prisma 仓储均做软删除并写入 `shipment.route.delete` 审计，包含删除原因、操作人、删除时间和删除前状态。前端删除确认弹窗要求填写原因，成功后从本地待排货列表移除并提示。验证通过：`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx -t "待排货|审核通过|修改|操作日志|删除"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing|delete|audit"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实 1366px 浏览器截图验收；当前仓库仍有大量既有未提交改动。

## 当前 2026-07-09 仓库/财务/客服/报价/市场增量已发布 47

2026-07-09 已按发布会话梳理并发布当前增量到 47，发布范围为 `api+web`，本轮未发现 Prisma schema/migration 待同步差异，未运行线上迁移。发布内容包含：仓库理货完成后回写在仓包裹 `tallyTaskId/tallyTaskNo` 并显示 `理` 标识、仓库当前页全选行为、欧洲海运超大件总备注和时效绑定、客服状态池统一 `修改` 入口、财务水单凭证文件名编码修复、财务已付款核销单/应收审核水单匹配状态与批量操作提示、市场待排货删除入口/市场看板与共享待排货列补充、共享 `ManagedTable` 当前页选择治理第一阶段。发布前验证通过：`git diff --check`、仓库 Web/API 定向测试、共享表格分页选择测试、财务水单/应收/已付款相关 Web/API 测试、客服状态池 Web/API 定向测试、报价 Web/API 定向测试、市场排货 Web 定向测试、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`。发布过程先按脚本同步并构建 `api/web`，后发现少数文件因时间/大小未触发常规 rsync，已改用 `rsync --checksum` 精确补同步并重建 `api/web`；其中 `apps/web/src/modules/shared/ui.tsx` 补了 AntD `Table` `onChange` 回调类型，修复 47 Docker Web build 的隐式 any 阻断。47 健康检查通过：`api/web/postgres/redis` 均 Up；容器内 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，本机 Web 200；公网 `/api/health` 正常，公网首页 200；API 日志包含 `Nest application successfully started`。剩余：未做真实浏览器截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；旧价格表/旧理货历史/历史乱码凭证名不自动回填；当前工作树仍未 git stage/commit。

## 当前 2026-07-09 全局表格统一格式与当前页选择治理第一阶段

2026-07-09 已按任务卡 `2026-07-09-全局表格-统一格式与当前页选择治理` 完成第一阶段本地落地，未发布 47。本轮阶段范围限定为共享 `ManagedTable` 能力与一个代表页面验证：共享表格统一显示 `已选 X 条`，受控 `rowSelection` 在翻页、刷新或数据源变化时只保留当前可见行选择，避免跨页残留；代表页面选择 `财务管理 -> 应收审核`，批量审核/反审核/作废确认文案带已选数量，并避免共享表格自动追加额外列设置占位。接入清单：`ManagedTable` 已覆盖共享能力；`财务管理 -> 应收审核` 已接入代表页面；仓库、报价、业务、客服、市场、基础资料库、系统管理、物流轨迹、问题件/运营列表需后续按模块复核自定义表格、手写列设置和手写复选框。验证通过：`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx -t "selection|全选|当前页|pagination|已选"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "全选|已选|表格|列设置"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "全选|当前页|已选|表格"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。

## 当前 2026-07-09 财务已付款付款核销单综合落地

2026-07-09 已按任务卡 `2026-07-09-财务管理-已付款付款核销单综合落地` 本地落地，未发布 47。实现范围：`财务管理` 二级入口展示为 `已付款`，页面标题为 `深圳思远国际货运代理有限公司付款核销单`；已付款页筛选覆盖代理、业务员、客户编号、运单号、应付费用、币种、合计金额、状态、收款方、付款方、备注、申请付款日期和付款日期；表格展示付款核销单字段，金额列只显示数字、币种单独展示，对账单凭证和水单用缩略图展示并支持双击预览。勾选使用 `ManagedTable` 当前页选择能力，不可操作记录禁选；已选记录按收款方名称、币种、收款方银行账号合并展示付款组、银行信息和合计金额。确认支付、反核销、补充继续沿用现有接口和状态流转；内存仓储补齐已付款列表申请/付款日期筛选与排序，和 Prisma 查询口径保持一致。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "paid|payment|已付款|付款核销"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "paid payment|payment applications"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；银行账号脱敏仍依赖现有后端权限裁剪口径，本轮未新增数据库字段或导出口径。

## 当前 2026-07-09 仓库理货完成同步与在仓理标识本地落地

2026-07-09 已按任务卡 `2026-07-09-warehouse-tally-complete-sync-mark` 本地落地，未发布 47。实现范围：仓库在 `在仓数据` 发起理货并完成后，API 内存仓储和 Prisma 仓储都会把来源在仓包裹回写 `tallyTaskId/tallyTaskNo`，前端刷新/兜底同步后在 `客户编号-快递单号` 旁显示蓝色 `理` 标识；点击 `理` 可打开理货历史详情或回到已完成理货追溯。重复对同一包裹发起理货会被拦截，提示 `包裹已完成理货，可查看理货记录` 或 `包裹已有未完成理货任务`，不生成重复已完成任务。Web 测试桩同步补齐完成理货后的包裹标记和重复理货拒绝。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "理货|已完成理货|在仓数据|理"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "tally|label|in-stock"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`。剩余：未做 47 发布；未做真实浏览器点击截图；不回填历史已完成但缺少包裹关联字段的旧理货数据。

## 当前 2026-07-09 仓库表头全选当前页本地落地

2026-07-09 已按任务卡 `2026-07-09-warehouse-current-page-selection` 本地落地，未发布 47。实现范围：仓库 `今日收货 / 在仓数据 / 未完成理货` 的手写表头全选改为只作用于当前分页页内可见行；为相关表格补充受控分页状态，翻页不自动选中下一页；在数据刷新、查询/重置筛选或可见数据变化后清理不可见已选项，避免批量理货、批量录单等操作误处理未实际勾选行。共享 `ManagedTable` selection 分页行为补回归测试。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "全选|当前页|已选|批量理货|批量录单"`、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx -t "selection|全选|pagination"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；只覆盖任务卡指定仓库表格和共享表格分页选择回归。

## 当前 2026-07-09 欧洲海运超大件总备注绑定本地落地

2026-07-09 已按用户补充口径本地修复欧洲海运超大件横向小表解析，未发布 47。实现范围：Web 上传预解析和 API 导入解析均把 `操作明细收费 / 渠道货物限制 / 卸货能力要求 / 特别提示 / 特别声明 / 注意事项 / 渠道说明` 等连续区域识别为 sheet 总备注，并绑定到同 sheet 全部价格行；小表右侧或局部 `备注/渠道要求/说明/附加费/限制` 仍只绑定当前小表，形成 `渠道要求 = 小表备注 + 总备注` 或 `总备注`。横向小表备注列识别同时查看当前表头和上一行合并表头，兼容振韵真实表里 `备注` 表头在上一行、内容在右侧列的格式；总备注不再过滤时效行，时效可从小表备注或总备注回填为完整原文，例如 `开船30-40天提取 （不包含塞港，海关查验）`，`transitDays` 取最小天数。真实样本 `/Users/j1ng/Downloads/7.3振韵.xls` 抽样通过：`欧洲海运普货超大件专线` 解析 260 条命中行，均带 `transitLabel=开船30-40天提取 （不包含塞港，海关查验）`、`transitDays=30`，渠道要求均包含 `操作明细收费 / 渠道货物限制 / 卸货能力要求 / 特别提示`。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "振韵|欧洲海运|超大件|时效|渠道要求|备注"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "振韵|欧洲海运|超大件|时效|渠道要求|price book|legacy"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：旧价格表需要重新导入才会获得新解析结果；未做 47 发布；未做真实浏览器截图验收。

## 当前 2026-07-09 客服状态池合并修改入口本地落地

2026-07-09 已按任务卡 `客服状态池合并修改入口` 本地落地，未发布 47。实现范围：`客服管理 -> 待离港 / 已离港 / 已到港 / 已派送 / 已签收` 生命周期状态池行内统一为一个 `修改` 入口；统一弹窗按基础信息、转单信息、轨迹信息、同步选项、批注分区展示，保留新转单号、分单号、ETD/ATD、ETA/ATA、查询网站、查询网站对业务显示、网站是否同步业务、是否推送业务和本次修改说明。保存默认不改变状态，状态推进仍由 `确认离港/增加批注`、`确认到港/增加批注`、`确认派送/增加批注`、`确认签收/增加批注` 触发；新转单号留空时保留原转单号；非空批注进入现有运营审计 after。验证通过：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "修改|修改转单号|待离港|ETD|ETA|查询网站|分单号"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "customer service|transfer|operational|ETA|ETD"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；API 本轮只调整既有 e2e 用例标题以命中任务卡验证命令，未改接口逻辑。

## 当前 2026-07-09 财务水单凭证文件名编码修复本地落地

2026-07-09 已按任务卡 `2026-07-09-finance-water-receipt-voucher-filename-encoding` 本地落地，未发布 47。实现范围：`财务管理 -> 水单匹配 -> 记录水单凭证` 上传图片时保留可读中文文件名；粘贴截图若只有空名或 `image.png/jpg/webp/gif` 这类剪贴板占位名，前端生成 `水单凭证-YYYYMMDDHHmmss.ext`；`/api/finance/voucher-images` 保存展示名前执行 UTF-8 文件名修正，避免新上传出现 `å/æ` 类乱码；真实存储路径仍使用日期 + UUID 文件名，不拼接用户原始文件名。水单列表和预览弹窗现在同时展示可读文件名。非法类型仍按图片白名单拒绝；历史乱码文件名不回填。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "水单|凭证|voucher|upload"` 3/3、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "water receipt|voucher|upload"` 1/1、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未批量修复历史乱码凭证名；未做真实浏览器粘贴截图验收。

## 当前 2026-07-09 价格表时效与完整渠道要求兼容修复已发布 47

2026-07-09 已按发布会话梳理并发布价格表时效与完整渠道要求兼容修复到 47，发布范围为 `api+web`，本轮未发现 Prisma schema/migration 待同步差异，未运行线上迁移。同步差异包含 `.codex-state.md`、`docs/dev-now.md`、`apps/api/src/modules/pricing-excel.ts`、`apps/api/src/modules/app.pricing.e2e.test.ts`、`apps/web/src/modules/pricing/excel.ts`、`apps/web/src/modules/pricing/PricingPage.tsx`、`apps/web/src/modules/pricing/pricing.test.tsx`。发布前验证通过：`git diff --check`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "振韵|拓普达|渠道说明|渠道要求|备注|价格表|亚马逊|欧洲海运|欧洲空运|查价"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "通用价格表|亚马逊仓库汇总|渠道要求|备注|亚马逊|欧洲海运|欧洲空运|查价"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing transit|振韵|拓普达|generic price book|渠道要求|price book|legacy"`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`。已执行 `npm run sync:47 -- --apply`、47 `docker compose build api web`、`docker compose up -d --remove-orphans api web`；最终 rsync dry-run 为空。47 健康检查通过：`api/web/postgres/redis` 均 Up；容器内 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，本机 Web 200；公网 `/api/health` 正常，公网首页 200；API 日志包含 `Nest application successfully started`。剩余：旧价格表需要重新导入才会获得新解析结果；未做真实浏览器截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-09 价格表时效与完整渠道要求兼容修复

2026-07-09 已按用户提供的振韵、拓普达、驰汉真实价格表样例继续本地修复价格表解析，未发布 47。实现范围：Web 上传预解析和 API 导入解析均保留 `参考时效 / 全程时效 / 派送时效 / 船期 / 时效` 原始业务文案，例如 `参考时效起飞6-8天提取...非亚马逊地址+1元/kg`、`开船后35-40自然日派送`、`正常开船后40-45天左右`；内部排序用 `transitDays` 只从带 `天/日/days` 的时效表达取最小天数，避免 `+1元/kg`、`100RMB` 等费用数字污染。渠道要求继续合并小表备注、sheet 总备注、产品附加、特别说明等完整备注区，并过滤 WPS/Excel 图片公式 `=DISPIMG(...)`，避免图片占位符进入用户可见文本。详情弹窗仍只展示一个 `渠道要求` 块，表格列只显示短摘要，点击短摘要或结果行查看全文。真实样本抽样通过：`/Users/j1ng/Downloads/7.3振韵.xls` 解析 2805 行、`/Users/j1ng/Downloads/7.6-拓普达.xlsx` 解析 1209 行、`/Users/j1ng/Downloads/7.8-驰汉.xlsx` 解析 84 行；三份样本均未发现 `DISPIMG` 泄漏，振韵 `+1元/kg` 样本 `transitDays=6`。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "振韵|拓普达|渠道说明|渠道要求|备注|价格表|亚马逊|欧洲海运|欧洲空运|查价"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing transit|振韵|拓普达|generic price book|渠道要求|price book|legacy"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`。剩余：旧价格表需要重新导入才会获得新解析结果；未做 47 发布；未做真实浏览器截图验收；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-09 全价格表备注统一进渠道要求已发布 47

2026-07-09 已按发布会话梳理并发布全价格表备注统一进入 `渠道要求` 的增量到 47，发布范围为 `api+web`，本轮未发现 Prisma schema/migration 待同步差异，未运行线上迁移。同步差异包含 `apps/api/src/modules/pricing-excel.ts`、`apps/api/src/modules/app.pricing.e2e.test.ts`、`apps/web/src/modules/pricing/excel.ts`、`apps/web/src/modules/pricing/PricingPage.tsx`、`apps/web/src/modules/pricing/pricing.test.tsx` 和状态文档。发布前验证通过：`git diff --check`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "振韵|渠道要求|备注|价格表|查看线路"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "通用价格表|亚马逊仓库汇总|渠道要求|备注|亚马逊|欧洲海运|欧洲空运|查价"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "generic price book|振韵|渠道要求|price book|legacy"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm run build -w @siyuan/api`。已执行 `npm run sync:47 -- --apply`、47 `docker compose build api web`、`docker compose up -d --remove-orphans api web`；最终 rsync dry-run 为空。47 健康检查通过：`api/web/postgres/redis` 均 Up；容器内 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，本机 Web 200；公网 `/api/health` 正常，公网首页 200；API 日志包含 `Nest application successfully started`。剩余：旧价格表需要重新导入才会带新解析出的渠道要求；未做真实浏览器截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-09 全价格表备注统一进渠道要求

2026-07-09 已按用户补充口径本地修复所有已支持价格表的渠道要求解析与展示，未发布 47。实现范围一：标准列价格表、亚马逊仓库汇总表、横向重量段小表都把行级/小表级 `备注/渠道要求/说明/附加费/尺寸/限制` 等内容写入对应价格行渠道要求；一列备注对应多列价格时复制到同一小表生成的全部价格行。实现范围二：sheet 表尾 `渠道说明/特别提示/总备注/通用备注/注意事项/责任说明/燃油附加` 等总备注追加到该 sheet 每个价格行或小表，形成 `渠道要求 = 行级/小表备注 + sheet 总备注 + 产品附加/特别说明`；本轮已补振韵截图里的 `渠道说明` 灰色总备注区，以及查验费、清关、关税递延、品名、等时、准点、条款等关键词。实现范围三：兼容拓普达这类只有一个大备注区的格式，显式进入总备注区后不再要求每一行都有备注关键词，直到遇到新的价格表头才停止，因此自由文本/品类清单也会进渠道要求。实现范围四：`亚马逊查询`、`欧洲海运超大件查询`、`欧洲空海运铁路快递查询` 的结果表格 `渠道要求` 列只展示固定宽度短摘要，点击短摘要或点击结果行进入 `渠道要求详情` 查看完整文本，不再用长备注挤压外部表格空间。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "振韵|渠道说明|渠道要求|备注|价格表|亚马逊|欧洲海运|欧洲空运|查价"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "振韵|generic price book|渠道要求|price book|legacy"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：旧价格表需要重新导入才会带新解析出的渠道要求；未做 47 发布；未做真实浏览器截图验收。

## 当前 2026-07-09 查价结果列表行点击打开渠道要求详情

2026-07-09 已按用户补充口径本地修复报价查价结果交互，未发布 47。实现范围：`报价查价 -> 查价` 的下方报价结果列表行现在可直接点击打开与推荐卡片一致的 `渠道要求详情` 弹窗，查看渠道要求、产品附加、特别说明/尺寸要求、时效、业务报价和内部可见成本/毛利；普通查价结果表行点击也打开详情并保留当前选中高亮，legacy/业务报价结果表行点击打开对应渠道详情。行内 `渠道要求` 按钮保留并阻止冒泡，避免重复触发。未改报价金额、排序、加价、权限裁剪或 API。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "渠道要求|备注|亚马逊|欧洲海运|欧洲空运|查价"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器点击截图验收。

## 当前 2026-07-09 振韵右侧备注列按小表复制到渠道要求

2026-07-09 已按用户补充口径本地修复振韵小表备注解析，未发布 47。实现范围：Web 上传预解析和 API 导入任务解析的横向重量段小表，识别价格列右侧 `备注/渠道要求/要求/说明/附加费/尺寸/限制` 等命名列；这类列中的文本不再只按单行匹配，而是作为当前小表渠道要求，复制到该小表生成的所有价格行。同步扩展渠道要求关键词，覆盖托盘、偏远、收费、退回、不可堆叠、最长边、计费重等振韵备注常见内容；保持小表之间互不串用，不改报价金额、加价、权限或历史数据。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "振韵|渠道要求|备注|价格表|查看线路"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "振韵|渠道要求|price book|legacy"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：旧价格表需要重新导入才会带新解析出的渠道要求；未做 47 发布；当前工作树仍有大量既有未提交改动。

## 当前 2026-07-08 报价查价渠道要求合并已发布 47

2026-07-08 已按发布会话梳理并发布当前报价查价渠道要求合并增量到 47，发布范围为 `api+web`，本轮未发现新的 Prisma schema/migration 待同步差异，未运行线上迁移。同步差异包含 `apps/api/src/modules/prisma.repository.ts`、`apps/web/src/modules/pricing/PricingPage.tsx`、报价相关测试和状态文档；已执行 `npm run sync:47 -- --apply`、47 `docker compose build api web`、`docker compose up -d --remove-orphans api web`。验证通过：`git diff --check`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "渠道要求|备注|亚马逊|欧洲海运|欧洲空运|查价"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "price books with remarks|legacy"`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`。47 健康检查通过：`api/web/postgres/redis` 均 Up；容器内 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，本机 Web 200；公网 `/api/health` 正常，公网首页 200；API 日志包含 `Nest application successfully started`。剩余：Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit；振韵截图红框这类每页大块备注尚未落地，当前只发布既有小表/价格表备注与渠道要求展示链路。

## 当前 2026-07-08 仓库录单草稿箱提交链路复核通过

2026-07-08 按用户提供任务卡 `2026-07-08-warehouse-order-entry-submit-draft-box` 复核当前代码：仓库在仓数据录单预填、保存草稿、草稿箱继续编辑/删除、提交审核、草稿占用包裹排除、删除草稿释放包裹和草稿删除审计均已在当前工作树存在，本轮未新增业务代码。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "录单|草稿|提交审核|warehouse package"` 4/4、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "录单|草稿箱|提交审核|warehouse package"` 3/3、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order entry|draft|warehouse package|review"` 5/5。剩余：本轮不发布 47；未做真实浏览器点击截图；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 报价查价备注合并为渠道要求本地落地

2026-07-08 已按任务卡 `2026-07-08-报价查价-备注合并为渠道要求展示` 本地落地，未发布 47。实现范围：`亚马逊查询`、`欧洲海运超大件查询`、`欧洲空海运铁路快递查询` 的查价结果表格不再单独展示 `备注` 列；推荐报价卡片不再展示 `备注 xxx`，有备注类内容时统一显示 `渠道要求` 标识；渠道要求详情弹窗把原 `完整备注` 文案收敛为 `渠道要求`，并继续展示 remark、产品附加和特别说明/尺寸要求。复制报价中的备注行同步改为 `渠道要求`。补齐 Prisma legacy 查价映射：按价格表 id/文件名把价格表备注兜底作为 `remark` 返回，避免已有价格表备注在查价结果中显示为 `暂无渠道要求`。未改报价金额、排序、加价命中、权限裁剪、数据库字段或南非专线逻辑。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "渠道要求|备注|亚马逊|欧洲海运|欧洲空运|查价"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "price books with remarks|legacy"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收；历史价格若本身没有价格表备注或小表说明，仍需重新导入或维护备注。

## 当前 2026-07-08 报价查价结果时效/备注已发布 47

2026-07-08 已按发布会话梳理并发布 `报价查价` 旧报价结果展示增量到 47，发布范围为 `web`，未重建 API、未运行线上迁移。实现范围：`亚马逊查询`、`欧洲海运超大件查询`、`欧洲空海运铁路快递查询` 共用业务报价结果表新增独立 `时效`、`备注` 列，推荐卡同步展示时效和备注摘要；继续保留 `渠道要求`、成本、毛利和既有权限裁剪逻辑，未改 API、数据库和价格口径。发布前 dry-run 仅包含 `.codex-state.md`、`apps/web/src/modules/pricing/PricingPage.tsx`、`apps/web/src/modules/pricing/pricing.test.tsx`；已执行 `npm run sync:47 -- --apply`、47 `docker compose build web`、`docker compose up -d --remove-orphans web`。

验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "备注|时效|查价|亚马逊|欧洲海运|欧洲空运"` 5/5、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/web`、`git diff --check`。47 健康检查通过：`api/web/postgres/redis` 均 Up；容器内 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，本机 Web 200；公网 `/api/health` 正常，公网首页 200；Web 日志显示 Nginx 正常启动并返回 200。剩余：历史价格如原本无 `时效/备注` 数据仍需重新导入或另开回填任务；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 报价查价结果补显时效与备注本地落地

2026-07-08 已按用户确认计划本地修复 `报价查价` 旧报价结果展示：`亚马逊查询`、`欧洲海运超大件查询`、`欧洲空海运铁路快递查询` 共用的业务报价结果表新增独立 `时效`、`备注` 列，推荐卡同步展示时效和备注摘要；继续保留 `渠道要求`、成本、毛利和既有权限裁剪逻辑，未改 API、数据库和价格口径。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "备注|时效|查价|亚马逊|欧洲海运|欧洲空运"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未发布 47；历史价格如原本无 `时效/备注` 数据仍需重新导入或另开回填任务。

## 当前 2026-07-08 客服/运营/仓库/市场/财务/报价增量已发布 47

2026-07-08 已按发布会话梳理当前增量并发布 47，发布范围为 `api+web`，本轮未发现新的 Prisma schema/migration 差异，未运行线上迁移。实现范围：物流轨迹管理新增手动添加轨迹入口、Excel 预览和导入确认；客服生命周期状态确认支持批注、确认离港前置校验与明确反馈；运营专线运单池支持按客户单号/子单号/国内快递号/合票号搜索并保留角色数据范围；仓库待出库改用 `ManagedTable` 列设置/列宽持久化，仓库待排货隐藏应付成本/应付合计，录单草稿箱支持删除并释放仓库包裹；市场已排货表格减少右侧留白并把列设置移到工具区；财务水单未到账点击 `到账` 后即时变为可匹配；报价导入支持振韵小表渠道要求、时效字段解析与查价端渠道要求文案。发布中第一次 47 Docker build 因 nginx 基础镜像源 TLS handshake timeout 中断，未重启服务；第二次重试成功构建 `api/web`，随后 `docker compose up -d --remove-orphans api web` 成功。

验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞；`npm test -w @siyuan/web -- --run src/modules/tracking/tracking.test.tsx -t "手动添加轨迹|轨迹导入|运单号|轨迹日期时间|轨迹信息"` 1/1；`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx -t "物流轨迹管理|手动添加轨迹|权限"` 1/1；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "tracking|import|手动|签收|permission"` 5/5；`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx` 12/12；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit|operations line shipment search|lets business role groups create order entries"` 3/3；`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx -t "专线运单池|搜索|运单号|业务员|权限"` 1/1；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待出库|列设置|列宽|业务数据|待排货|应付成本|应付合计|只读|录单|草稿|提交审核|warehouse package"` 9/9；`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx -t "列设置|列宽|selection"` 5/5；`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx -t "已排货|列设置|market costs"` 1/1；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "water receipt"` 2/2；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "water receipt|水单|到账|录单|草稿箱|提交审核|warehouse package"` 5/5；`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "振韵|渠道要求|备注|价格表|查看线路|时效|查价"` 命中 5 项通过；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "imports pricing transit time|imports 振韵 horizontal price book channel requirements|persists imported price books|feeds price book imports"` 4/4。47 健康检查通过：`api/web/postgres/redis` 均 Up；容器内 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，本机 Web 200；公网 `/api/health` 正常，公网首页 200；API 日志包含 `Nest application successfully started`。

本轮只为发布门禁修正两处测试断言：客服看板 `数据确认` 任务卡定位避免和侧栏同名按钮冲突；订单 e2e 草稿删除审计读取现有 `rows` 字段。剩余：未做真实浏览器截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；报价 API 全量宽关键词测试会命中一个既有欧洲快递旧断言失败，已用本轮相关精确用例覆盖；旧价格表需重新导入才会带新增解析出的时效/渠道要求；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 物流轨迹手动添加轨迹字段本地落地

2026-07-08 已按任务卡 `2026-07-08-物流轨迹管理-手动添加轨迹字段落地` 本地落地，未发布 47。实现范围：`物流轨迹管理` 新增 `手动添加轨迹` 二级入口，仅 `tracking:write` 的管理员/客服可见；Excel 解析按 `运单号 / 轨迹日期时间 / 地点 / 轨迹信息` 字段识别，保留行号、缺字段、日期格式错误、未匹配单号和冲突单号预览；匹配规则扩展为系统单号、客户单号、转单号、子单号。导入确认复用 `/api/shipments/tracking-events/import`，支持同一运单多条轨迹逐条写入 `TrackingEvent`，每票 `latestTracking` 按轨迹时间最新一条更新并清零 `trackingStaleDays`；导入轨迹默认客户可见，包含 `已签收` 也不自动改状态为 `SIGNED`。确认导入写 `tracking.manual_import` 审计，记录文件名、原始行数、成功行数、失败行数、未匹配数量和受影响运单数。验证通过：`npm test -w @siyuan/web -- --run src/modules/tracking/tracking.test.tsx -t "手动添加轨迹|轨迹导入|运单号|轨迹日期时间|轨迹信息"`、`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx -t "物流轨迹管理|手动添加轨迹|权限"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "tracking|import|手动|签收|permission"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器上传截图；未做历史轨迹回填。

## 当前 2026-07-08 客服生命周期状态确认增加批注本地落地

2026-07-08 已按任务卡 `客服生命周期状态确认增加批注` 本地落地，未发布 47。实现范围：`客服管理 -> 待离港 / 已离港 / 已到港 / 已派送 / 已签收` 的生命周期确认操作改为带批注弹窗；批注可留空，非空内容 trim 后随 `statusRemark/remark/comment` 写入 `shipment.operational.update`、`customer_service.status.update` 和签收确认审计；`SIGNED` 状态只支持 `增加批注`，不再向后流转。后端内存仓储与 Prisma 仓储保留 ETA/ETD、转单号、签收权限和状态流转校验。验证通过：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "批注|确认离港|确认到港|确认派送|确认签收|已签收"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "customer service|status|comment|departure|arrival|delivery|signature"`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器点击截图；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 客服确认离港反馈修复本地落地

2026-07-08 已按用户提供任务卡 `客服管理确认离港无反馈修复` 本地落地，未发布 47。实现范围：`客服管理 -> 待离港` 的 `确认已离港` 在前端先校验当前票仍为 `WAITING_DEPARTURE` 且已有 `ETD/ATD` 与 `ETA/ATA`；缺时间字段时在待离港表格上方展示明确错误并保留 toast，不再静默失败。确认成功后继续回写本地运单、清除错误并自动切换到 `已离港`，列表可看到刚确认的运单。后端内存仓储与 Prisma 仓储补齐确认离港边界：只有管理员/客服角色可执行，状态变更只允许 `WAITING_DEPARTURE -> DEPARTED`，重复确认受控拒绝，继续沿用 `shipment.operational.update` 与 `customer_service.status.update` 审计。验证通过：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "确认已离港|待离港|已离港|ETA|ETD"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "departure|customer service|DEPARTED|ETA|ETD"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器点击截图；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 运营专线运单池搜索与权限本地落地

2026-07-08 已按任务卡 `2026-07-08-operations-line-pool-order-search-scope` 本地落地，未发布 47。实现范围：`运营工作台 -> 专线运单池` 共享汇总补齐 `customerOrderNo/subOrderNo` 与包裹 `domesticTrackingNo/combinedOrderNo` 关键字匹配；后端仍先通过 `getShipments(principal)` 做现有角色和数据范围裁剪，再在可见专线运单内匹配，业务员搜索他人运单号/包裹号返回空。内存仓储与 Prisma 仓储均为可见运单生成最小包裹摘要；内存仓储同步修正销售范围角色可见自己录入的运单。前端搜索框文案改为 `搜索客户 / 运单号 / 快递号 / 转单号 / 渠道 / 代理`，货量列展示包裹数、总实重、总体积和国内快递号摘要，不新增应付、利润、代理成本或付款明细字段。验证通过：`npm run build -w @siyuan/shared`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "operations|line shipment|search|operator|scope|customerOrderNo|tracking"`、`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx -t "专线运单池|搜索|运单号|业务员|权限"`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；转单号匹配沿用既有 `transferNo` 字段匹配，本轮测试未造完整出库后转单号状态链；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 仓库待出库列设置与列宽本地落地

2026-07-08 已按任务卡 `2026-07-08-warehouse-dispatch-column-settings-resizable` 本地落地，未发布 47。实现范围：`仓库管理 -> 待出库` 去掉自定义列设置弹窗，改为复用 `ManagedTable` 表头列设置入口 `待出库列设置`；支持显隐、上移/下移排序、恢复默认、本地持久化和表头列宽拖拽持久化。共享 `ManagedTable` 补齐分组子列处理，`业务数据` 下的 `件数/总量/体积/计费重` 可单独隐藏并保留分组表头；`rowSelection` 选择列继续固定第一列；本卡未新增应付、利润、代理成本等仓库无权字段。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待出库|列设置|列宽|业务数据"`、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx -t "列设置|列宽|selection"`、`git diff --check`。补充验证：`npm run typecheck -w @siyuan/web` 当前被既有 `apps/web/src/modules/operations/OperationsPage.tsx(291,29): Property 'packageSummary' does not exist on type 'LineShipmentPoolRow'` 阻塞，非本卡改动。剩余：未做 47 发布；未做真实浏览器拖拽截图验收。

## 当前 2026-07-08 市场已排货表格减少留白本地落地

2026-07-08 已按用户提供任务卡 `市场已排货表格减少留白` 本地落地，未发布 47。实现范围：`市场管理 -> 已排货` 表格收紧默认列宽，`minimumScrollX` 从 2480 降到 2100；该表 `列设置` 从表格内固定空列改为工具区按钮，避免右侧出现只用于列设置的空白列；已排货表格补紧凑单元格 padding 和不换行规则，保留排货时间、客户、运单号、目的地、代理/渠道、市场成本、状态、`修改` 与 `排货日志`。共享 `ManagedTable` 增加可选 `columnSettingsPlacement="toolbar"`，默认仍保持原表头固定列行为，不影响待排货、本周排货等表格。验证通过：`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx -t "已排货|列设置|market costs"`、`git diff --check`。`npm run typecheck -w @siyuan/web` 未通过，阻塞为既有 `apps/web/src/modules/operations/OperationsPage.tsx(291,29): Property 'packageSummary' does not exist on type 'LineShipmentPoolRow'`，不属于本卡允许范围。剩余：未做 47 发布；未做真实浏览器 1366px 截图验收；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 财务水单到账按钮生效本地落地

2026-07-08 已按用户提供任务卡 `2026-07-08-财务管理-水单到账按钮生效` 本地落地，未发布 47。实现范围：`财务管理 -> 水单匹配` 未到账水单的 `到账` 按钮改为直接提交到账操作，行按钮显示 loading 并防重复点击；到账成功后先回写当前行状态，再刷新列表统计，使状态变为 `已到账`、`到账` 按钮消失、`匹配` 按钮可用、顶部已到账数量更新。后端维持水单到账权限校验和未到账匹配拒绝；到账审计 `finance.water_receipt.arrive` 补齐水单编号、付款编号、客户编号、金额、币种、到账前后状态、操作人和操作时间。同步补 Web/API 回归覆盖到账按钮、审计字段和无权限 403。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "water receipt"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "water receipt|水单|到账"`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器点击截图；当前工作树仍包含大量既有未提交改动，本轮只按任务卡触碰水单到账相关文件。

## 当前 2026-07-08 振韵渠道要求按小表解析展示本地落地

2026-07-08 已按任务卡 `2026-07-08-报价查价-振韵渠道要求按小表解析展示` 本地落地，未发布 47。实现范围：振韵横向重量段小表解析时，从表格右侧和小表下方提取 `备注/渠道要求/附加费/磁检/报关/罚款/拒收/超标准` 等要求，只绑定到当前小表生成的渠道行，不继承到其他小表；Web 上传预解析和 API 导入任务解析保持一致。`代理加价规则 -> 查看线路` 线路明细新增 `渠道要求` 列和详情弹窗；查价结果继续复用既有 `渠道要求` 展示。未改报价计算、重量段匹配、代理加价规则、历史已导入数据或权限模型。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "振韵|渠道要求|备注|价格表|查看线路"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "振韵|渠道要求|price book|legacy"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；旧价格表需要重新导入才会带小表渠道要求；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 报价导入时效字段本地落地

2026-07-08 已按任务卡 `2026-07-08-pricing-import-transit-time-field` 本地落地，未发布 47。实现范围：复用价格行既有独立 `transitDays/transitLabel` 字段作为 `时效` 字段；API 与 Web 两套 Excel 解析器均新增 sheet 级时效扫描，识别包含 `时效` 且带天数的文本，支持 `航程时效28-33天`、`时效：28天`、`28至33天`、`28~33天` 等格式并规范化为 `28-33天` 或 `28天`；行级时效优先，行级缺失时继承 sheet 级时效；产品附加/特别说明等备注提取会过滤系统可识别的时效行，避免把 `时效：xx天` 写入备注，真实备注继续保留。补充 API 导入任务测试和前端解析测试，覆盖管理员价格行展示、业务员查价可见时效且不可见成本/毛利。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing|legacy|import|transit|时效"` 20/20、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "备注|时效|查价"` 2/2、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未回填历史已导入价格表；未做 OCR 图片识别。

## 当前 2026-07-08 仓库录单草稿箱提交链路本地落地

2026-07-08 已按任务卡 `2026-07-08-warehouse-order-entry-submit-draft-box` 本地落地，未发布 47。实现范围：复用既有仓库在仓数据录单预填、录单保存草稿和提交审核链路；业务管理二级入口文案收敛为 `草稿箱`；新增专用 `DELETE /api/shipments/:id/order-entry-draft` 删除录单草稿接口，业务员/管理员按现有录单权限和客户范围删除 `DRAFT/REVIEW_REJECTED` 草稿，删除后释放 `draftWarehousePackageIds` 占用，并写入 `shipment.order_entry.draft_delete` 审计；前端草稿箱删除操作改为调用专用草稿删除接口。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "录单|草稿|提交审核|warehouse package"` 4/4、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "录单|草稿箱|提交审核|warehouse package"` 3/3、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order entry|draft|warehouse package|review"` 5/5、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器点击截图；本轮不改变费用计算口径和财务字段可见性。

## 当前 2026-07-08 仓库待排货隐藏应付成本本地落地

2026-07-08 已按任务卡 `2026-07-08-warehouse-pending-routing-hide-payable-cost` 本地落地，未发布 47。实现范围：共享 `createPendingRoutingColumns` 按 `mode: 'warehouse'` 裁剪 `应付成本`、`应付合计` 两列；`仓库管理 -> 待排货` 默认表格和列设置中均不再出现这两列，仓库待排货仍保持只读；客服/市场复用待排货列工厂时不受影响。同步更新仓库断言覆盖应付列不可见、列设置不可恢复，并让客服待排货测试命中任务卡验证命令。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待排货|应付成本|应付合计|列设置|只读"`、`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "待排货"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未跑市场待排货专项测试，市场列逻辑按 `mode !== 'warehouse'` 保持原状。

## 当前 2026-07-08 报价查价渠道要求文案本地落地

2026-07-08 已按任务卡 `2026-07-08-报价查价-亮崽兼容文案业务化为渠道要求` 本地落地，未发布 47。实现范围：`报价查价 -> 查价` 的报价结果卡片、报价表备注入口和详情弹窗统一改为业务文案 `渠道要求 / 渠道要求详情`；导入成功提示和价格表管理解析列从 `亮崽模块 / 亮崽解析` 改为 `渠道分类`；价格表备注状态从 `有备注` 改为 `已填写`，避免同页混用旧备注入口语言。未改 API 字段、数据来源、报价计算、导入逻辑或权限裁剪。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "渠道要求|亮崽兼容|兼容报价|备注|详情"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；代码内部变量和 API 路径仍保留 `legacy` 命名，仅不作为用户界面文案展示。

## 当前 2026-07-08 南非专线/混合加价/客服看板已发布 47

2026-07-08 已按发布会话梳理当前增量并发布 47，发布范围为 `api+migrate+web`。实现范围一：`报价查价 -> 查价 -> 南非专线查询` 改为独立结构化物料规则查价，不复用亚马逊/欧洲价格行；管理员/报价角色可在 `价格表管理` 上传南非图片价格表作为来源凭证，并维护物料分类、关键词、CBM 单价、单询规则、来源图片和备注；业务查询按品名/类别/备注关键词匹配规则，计费方按 `max(0.5, CBM, KG/500)`，自动计算运费、风险费、单证费用和复制报价模板，单询或未匹配时生成待复核提示；API 新增南非图片、物料规则、查价接口，内存仓储和 Prisma 仓储均支持规则 CRUD、图片保存、查价计算和审计；Prisma 新增 `SouthAfricaRateImage`、`SouthAfricaRateRule`、`SouthAfricaLookupPendingReview` 及迁移 `20260708162000_south_africa_rate_rules`。实现范围二：`报价查价 -> 代理加价规则 -> 查看线路` 弹窗补齐按 `当前加价`、`加价来源` 筛选和按加价金额升/降序排序；线路明细每行展示实际生效加价与来源，支持区分 `线路自定义 / 代理默认 / 虚拟默认`；批量设置加价后提示明确范围并刷新弹窗明细和外层聚合状态；API `PriceBookRowsQuery` 增加 `markupAmount/markupSource/markupSort`，分页 total 按过滤后结果返回。实现范围三：`客服管理 -> 客服看板` 从旧同质数字卡改为 `今日待处理 / 运输流转 / 异常与 SLA / 本周结果` 四组轻量任务卡，只展示数量和状态提示，不展示应付、利润、代理成本等财务敏感字段。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "southAfrica|南非|CBM|500|risk|document|consult|keyword"` 3/3、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|mixed|bucket|line custom|agent default|batch"` 3/3、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "南非|品名|物料|图片|价格表|复制报价"` 2/2、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "混合加价|当前加价|加价来源|查看线路|批量设置"` 1/1、`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "客服看板|状态|任务卡|颜色|跳转"` 1/1、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build db-migrate api web` 成功，API 构建阶段生产依赖 prune 后 0 vulnerabilities；`prisma migrate deploy` 成功应用 `20260708162000_south_africa_rate_rules`，当前 67 个迁移全部成功；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，API 日志包含 `Nest application successfully started`；本机和公网 `/api/health` 均返回 `{"ok":true,"service":"siyuan-api"}`，本机和公网首页均 200。剩余：未做真实浏览器点击/截图验收；南非图片上传只留存凭证，不做 OCR/PDF 自动解析，真实图片价格表内容仍需管理员人工维护为结构化规则；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 列设置去重与亚马逊重量段一致性已发布 47

2026-07-08 已按发布会话梳理当前增量并发布 47，发布范围为 `api+web`，未跑数据库迁移。实现范围一：修正“旧列设置 + 新统一列设置”重复出现问题，`仓库管理 -> 今日收货 / 在仓数据` 去掉旧手写 Popover 列设置列，改为只使用 `ManagedTable` 表头最右侧列设置；选择列继续固定第一列并锁定不可隐藏，操作列在在仓数据中锁定不可隐藏；`仓库待出库`、`客服问题件/售后`、`运营专线运单池` 等仍保留自定义列设置逻辑的复杂表格禁用 `ManagedTable` 自动追加列设置，避免双按钮。同步修正客服看板本地漏定义 `isToday` 导致页面渲染失败的问题。实现范围二：亚马逊查价 Shared DTO 补 `weightBand`；API 内存仓储与 Prisma 仓储对亚马逊 `tier/weightBand` 统一归一为 `12KG+ / 50KG+ / 100KG+`，查价和价格行 fallback 都按所选重量段严格过滤，所选重量段无结果时返回空结果，不再静默回退到 `12KG+`；Web 查价请求明确发送当前 `tier` 与 `weightBand`，按所选重量段抬高本次计费重，toast、推荐卡、报价表和复制推荐报价都使用本次结果重量段，切换重量段时清空旧结果并用请求序号避免旧请求覆盖最后一次查询。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "amazon|weightBand|50KG|100KG|12KG|lookup"` 5/5、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亚马逊|重量段|50KG|100KG|12KG|toast|复制报价"` 2/2、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "列设置|在仓数据|待出库|今日收货"` 4/4、`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "问题件|售后|列设置|数据确认"` 1/1、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx` 5/5、`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx -t "专线运单池|列设置"` 无命中用例、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47 -- --apply` 成功；47 `docker compose build api web` 成功，API 构建阶段生产依赖 prune 后 0 vulnerabilities；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，API 日志包含 `Nest application successfully started`；本机和公网 `/api/health` 均返回 `{"ok":true,"service":"siyuan-api"}`，本机和公网首页均 200。剩余：未做真实浏览器点击/截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；财务费用子表等非 `ManagedTable` 的独立列设置仍是原有自定义入口，若要完全统一需单独迁移；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 列设置下移与业务/市场看板已发布 47

2026-07-08 已按发布会话梳理当前 Web 增量并发布 47，发布范围为 `web`，未重建 API、未跑数据库迁移。实现范围一：共享 `ManagedTable` 启用 `columnSettings` 时不再渲染表格上方 `.managed-table-toolbar`，改为自动追加固定最右侧系统列 `__column_settings__`，表头显示 `列设置` 按钮，数据单元格为空白；该系统列不进入列设置弹窗、不参与隐藏/排序/列宽拖拽，`rowSelection` 仍固定第一列；仓库 `今日收货 / 在仓数据 / 待出库`、客服 `问题件 / 售后`、运营 `专线运单池` 的手写列设置触发点同步移到表头最后一列。实现范围二：`市场管理 -> 市场看板` 改为 `待处理 / 流转中 / 今日结果 / 本周风险` 四组轻量任务卡，支持跳转 `待排货`、`已排货`、`本周排货数据`，看板不展示代理成本、利润、应付合计等财务敏感字段。实现范围三：`业务管理 -> 业务看板` 从空占位改为轻量业务工作台，展示 `今日录单 / 录单草稿 / 待审核运单 / 本周录单`、快捷入口和今日提醒，不展示市场排货、代理渠道、应付、利润等无关或敏感字段。同步修正仓库测试中点击市场 `待排货` 的选择器，避免市场看板同名任务卡导致多匹配。验证通过：`git diff --check`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx` 5/5、`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "数据确认|列设置"` 1/1、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "列设置|water receipt|pending review|业务看板|business dashboard"` 3/3、`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx -t "market dashboard|市场看板|本周|颜色|任务卡"` 1/1、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "列设置|在仓数据|待出库"` 4/4、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 显示仅 `.codex-state.md`、`docs/dev-now.md` 与 Web 文件变化；`npm run sync:47 -- --apply` 成功；47 `docker compose build web` 与 `docker compose up -d --remove-orphans web` 成功，`api/postgres/redis` 未重启且保持 Up。健康检查：47 本机 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，本机首页 200；公网 `/api/health` 返回同样健康，公网首页 200。剩余：财务应收/应付/业务成本审核仍使用独立拖拽列设置面板和批量操作工具条，若要完全统一需单独拆“财务审核表格列设置迁移”；未做真实浏览器截图验收；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 报价资源治理/操作日志/录单草稿已发布 47

2026-07-08 已按发布会话梳理当前增量并发布 47，发布范围为 `api+web`，未跑数据库迁移。实现范围一：报价查价导入资源治理与旧数据防回流，价格表列表接口禁止 `includeRows=true` 全量返回，JSON 兼容导入限制 2000 行，导入文件限制 30MB，导入任务补创建/完成/失败审计，代理加价规则导出不再无限 `pageSize=-1`，`pricing:legacy:import` 必须显式 `--source` 和 `--confirm`，发布脚本与 47 文档禁止自动从 `/opt/quote-app` 回流旧报价数据。实现范围二：报价查价四模块请求状态和候选集治理，查询时显示 `查询中`，失败/超时显示 `重试查询`，切换模块时旧请求失效，API 查价按有效价格表、模块、仓库/国家/渠道/重量段裁剪候选，响应推荐最多 100 条并补慢阶段日志。实现范围三：系统操作日志新增 IP 地址列和详情弹窗，HTTP 审计从 `x-forwarded-for` / `x-real-ip` / 连接 IP 提取客户端 IP；导出 Excel 增加 IP。实现范围四：业务管理新增 `录单草稿`，草稿列表按角色/客户归属裁剪，录单可从草稿继续编辑，提交审核后移出草稿箱。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing|lookup|import|legacy|performance|source"` 17/17、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "查询|导入|loading|超时|分页|价格表"` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "business|operator|publicChannel|TPD|YY|美西组合海卡|黄金达海卡|亿阳国际|深圳振韵国际|拓普达|振韵|cleanup"` 4/4、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "业务报价|渠道|TPD|YY|美西组合海卡|黄金达海卡|亚马逊查询|重量段|源文件关键词|只看可报价|亿阳国际|深圳振韵国际|查看线路"` 3/3、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "audit|system|permission denied|order entries|draft"` 3/3、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "audit|操作日志|IP|详情"` 1/1、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "草稿|录单|order-entry"` 3/3、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build api web`、`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：未做真实浏览器点击/截图验收；当前 Excel 解析仍会在单个导入任务内得到解析后的行数组，极端超大/异常公式文件后续可考虑流式解析或更严格行数上限；全局非报价模块的 `pageSize=-1` 导出类入口不在本卡处理范围；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 报价查价四模块查询性能治理本地落地

2026-07-08 已按任务卡 `2026-07-08-pricing-lookup-query-performance-governance` 本地落地，未发布 47。实现范围：报价查价四个查询项目增加前端请求状态治理，查询时显示 `查询中`，网络失败/超时显示明确失败区块和 `重试查询`，切换查询模块时让上一模块未完成请求失效，避免旧请求回写或按钮长期 loading；API 查价侧收敛候选集，当前价格表查价行查询从 50000 行降到 5000 行上限，legacy 查价先按当前有效价格表对应 `LegacyPricingSource` 裁剪来源，再按模块、仓库/国家/渠道/重量段做数据库筛选，避免默认扫描所有亮崽历史源和 80000 行候选；查价响应推荐列表限制为最多 100 条，不返回完整价格表 rows，并补慢阶段耗时日志。验证通过：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing|lookup|amazon|inquiry|europeExpress|southAfrica|performance"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "查询|loading|超时|未匹配|四个模块"`、`git diff --check`。剩余：未做 47 发布；未在真实 47 大数据上测 P95；当前裁剪主要保护常规查价和响应体，极端宽条件仍可能需要进一步加更细索引或强制筛选条件。

## 当前 2026-07-08 系统操作日志 IP 与弹窗详情本地落地

2026-07-08 已按任务卡 `2026-07-08-system-audit-log-ip-detail-modal` 本地落地，未发布 47。实现范围：`系统管理 -> 操作日志` 表格新增 `IP 地址` 列，历史无 IP 显示 `-`；移除右侧常驻 `审计详情` 面板，页面默认只显示统计、筛选和全宽日志表，点击日志行或 `查看详情` 才打开审计详情弹窗；详情弹窗展示结果、时间、操作人、IP、模块、动作、对象、变更前、变更后、原始请求；导出 Excel 增加 `IP 地址`。API HTTP 审计写入 IP，优先 `x-forwarded-for` 首个客户端 IP，其次 `x-real-ip`、请求/连接 IP，并做基础清洗；无新增 Prisma 迁移，IP 复用审计 `after` JSON 并映射为 `AuditLogSummary.ipAddress`。验证通过：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "audit|system|permission denied"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "audit|操作日志|IP|详情"`、`git diff --check`。剩余：未做 47 发布；历史日志不回填 IP；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 业务录单草稿箱本地落地

2026-07-08 已按任务卡 `2026-07-08-business-order-entry-draft-box` 本地落地，未发布 47。实现范围：`业务管理` 二级菜单新增 `录单草稿`；保存草稿后提示 `草稿已保存，可在录单草稿箱继续编辑` 并展示草稿号/运单号；新增草稿列表接口 `GET /api/shipments/order-entry/drafts`，按角色/客户归属裁剪 `DRAFT/REVIEW_REJECTED` 草稿；录单详情和草稿更新接口同步增加草稿可见性校验。录单页支持从草稿加载继续编辑，回显基础字段、已选仓库货物、应收费用、业务成本，业务账号继续不提交应付费用；提交审核后刷新待审和草稿列表，草稿从草稿箱移除并进入待审核运单。测试支撑层同步补草稿列表、草稿详情和草稿更新 mock。验证通过：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order entries|draft"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "草稿|录单|order-entry"`。剩余：未做 47 发布；未新增自动定时清理草稿；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 查价业务渠道中文展示与代理清理接口已发布 47

2026-07-08 已按发布会话梳理当前增量并发布 47，发布范围为 `api+web`，未跑数据库迁移。实现范围：报价查价后端业务侧脱敏路径从“提取英文 public code”改为“提取中文渠道展示名”，`TPD-S4-美西组合海卡` 对业务员显示 `美西组合海卡`，`YY黄金达海卡` 显示 `黄金达海卡`，纯英文渠道显示 `可报价线路`；业务侧不再用代理名兜底，避免内部代理/英文代码绕过展示裁剪；管理员和报价角色仍可看到完整内部渠道名、成本、毛利和加价明细。`报价查价 -> 查价 -> 亚马逊查询` 移除 `源文件关键词` 和 `只看可报价`，重量段固定为 `12KG+ / 50KG+ / 100KG+`。新增受控接口 `POST /api/pricing/cleanup-old-original-agents`，默认 `dryRun=true` 输出 `亿阳国际`、`深圳振韵国际` 在当前有效价格表线路和 legacy 行中的影响数量；`dryRun=false` 仅管理员可执行并写入审计，发布阶段未执行线上真实清理。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "business|operator|publicChannel|TPD|YY|美西组合海卡|黄金达海卡|亿阳国际|深圳振韵国际|拓普达|振韵|cleanup"` 4/4、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "业务报价|渠道|TPD|YY|美西组合海卡|黄金达海卡|亚马逊查询|重量段|源文件关键词|只看可报价|亿阳国际|深圳振韵国际|查看线路"` 3/3、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build api web`、`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：未做真实浏览器截图验收；未执行线上旧代理真实清理，需另行确认 dry-run 结果后再执行；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 亚马逊查询表单简化本地落地

2026-07-08 已按任务卡 `2026-07-08-pricing-amazon-query-form-simplify` 本地落地，未发布 47。实现范围：`报价查价 -> 查价 -> 亚马逊查询` 移除 `源文件关键词` 和 `只看可报价` 两个表单项；重量段下拉固定为 `12KG+ / 50KG+ / 100KG+`，默认仍为 `12KG+`；三项固定下拉关闭虚拟渲染，避免少量选项出现不可见/不可测；其他查价模块、报价公式、价格表导入和 API DTO 不改。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亚马逊查询|重量段|源文件关键词|只看可报价"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未做真实浏览器截图验收。

## 当前 2026-07-08 报价旧原始代理数据清理本地落地

2026-07-08 已按任务卡 `2026-07-08-pricing-clean-old-original-agent-data` 本地落地，未发布 47。实现范围：新增受控接口 `POST /api/pricing/cleanup-old-original-agents`，默认 `dryRun=true` 输出 `亿阳国际`、`深圳振韵国际` 在当前有效价格表线路和 legacy 行中的影响数量；`dryRun=false` 时仅管理员可执行，将 `7.6-拓普达.xlsx` 下 `亿阳国际` 清理为 `拓普达`、`7.3振韵.xls` 下 `深圳振韵国际` 清理为 `振韵`，并写入 `pricing.price_book.original_agent.cleanup` 审计。线路查询展示层同步做兼容清洗，避免清理前查看线路继续露出两个旧值；Web 测试支撑层同步使用价格表文件名代理口径。验证通过：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "亿阳国际|深圳振韵国际|拓普达|振韵|cleanup"`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "亿阳国际|深圳振韵国际|查看线路"`、`git diff --check`。剩余：未做 47 发布；本轮只提供清理接口与本地验证，线上真实数据需发布后先 dry-run 再执行。

## 当前 2026-07-08 代理加价规则混合展示已发布 47

2026-07-08 已按发布会话梳理当前增量并发布 47，发布范围为 `api+migrate+web`。实现范围：代理加价规则外层行支持 `统一加价 / 混合加价 / 仅保留规则` 聚合展示，返回 `markupDisplayMode/defaultMarkupDisplay/markupRange/markupBuckets`；查看线路返回并展示每条线路当前加价与来源 `LINE_CUSTOM/AGENT_DEFAULT/VIRTUAL_DEFAULT`；批量设置按“设置为指定加价”保存，支持当前页、全部筛选结果、已勾选线路范围，保存后刷新外层列表和弹窗线路。同步修正虚拟默认规则删除/停用后不应重新冒出，以及批量匹配按代理名覆盖当前有效价格表线路；新增迁移 `20260708113000_agent_markup_price_book_scope`，为 `AgentMarkupRule.priceBookId` 增加价格表范围字段和索引。同步发布的前端增量还包括：`报价查价 -> 查价` 亮崽兼容模块隐藏代理筛选/结果代理列，`仓库管理 -> 待排货` 隐藏业务成本与业务成本合计且列设置不可恢复。验证通过：`git diff --check`、`npm run prisma:generate -w @siyuan/api`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|batch|mixed|retained|lookup"` 6/6、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "加价|混合|查看线路|批量设置|Liangzai|europe express|代理"` 3/3、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待排货|业务成本|列设置|只读"` 2/2、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build db-migrate api web` 成功；`prisma migrate deploy` 应用 `20260708113000_agent_markup_price_book_scope`，当前 66 个迁移全部成功；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：未做真实浏览器点击巡检；全部筛选结果批量设置仍会分页拉取匹配线路，极大筛选范围下后续可再改为纯后端范围任务；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 报价查价隐藏代理筛选和结果列

2026-07-08 已按用户截图本地处理 `报价查价 -> 查价` 亮崽兼容模块代理字段展示，未发布 47。实现范围：亚马逊查询、欧洲空海运铁路快递查询不再展示 `代理` 筛选；亮崽兼容业务报价结果表和报价详情弹窗不再展示 `代理` 字段；代理加价规则管理、价格表管理、后端查价逻辑和数据库不改。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "Liangzai|europe express|代理"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 仓库待排货隐藏业务成本

2026-07-08 已按任务卡 `2026-07-08-warehouse-pending-routing-hide-business-cost` 本地落地，未发布 47。实现范围：`仓库管理 -> 待排货` 从共享待排货列工厂中按仓库场景裁掉 `业务成本`、`业务成本合计`，确保默认表格和 `列设置` 都不能恢复这两个字段；仓库待排货列设置使用独立 `sunny.warehouse.pendingRouting.columns` 存储键；市场/客服待排货不改；仓库待排货仍保持只读。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待排货|业务成本|列设置|只读"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-08 财务水单到账查询与匹配闭环已发布 47

2026-07-08 已按发布会话梳理当前增量并发布 47，发布范围为 `api` + `web`，未跑数据库迁移。实现范围：`财务管理` 新增 `水单到账查询` 二级入口，并保留 `水单匹配` 作为订单匹配入口；到账页默认查询未到账水单，支持未到账/已到账状态切换；匹配页只允许 `已到账/部分匹配` 且有余额的水单匹配应收，未到账水单前端按钮禁用，后端返回 `水单未到账，不能匹配订单`。`应收审核` 的 `匹配水单编号` 列补充展示 `未匹配/部分匹配/已匹配` 状态与匹配金额。水单匹配后余额为 0 时继续自动归档，并新增 `finance.water_receipt.archive` 审计记录；撤销匹配后恢复余额和非归档状态。同步包含前置水单口径：水单编号由服务端生成 `SDyyyyMMddNNN`，金额纯数字、币种独立展示，付款编号人工维护且只做基础清洗。验证通过：`git diff --check`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "water receipt|receivable"` 8/8、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "water receipt|receivable|水单|应收"` 4/4、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build api web`、`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：未做真实浏览器点击巡检；未改应付、付款申请、银行流水自动导入；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前工作树仍未 git stage/commit。

## 当前 2026-07-08 财务水单匹配字段格式本地落地

2026-07-08 已按任务卡 `2026-07-07-财务管理-水单匹配字段格式修正` 本地落地，未发布 47。实现范围：`财务管理 -> 水单匹配` 主表 `金额/匹配金额/余额` 和顶部 `金额/余额` 汇总改为纯数字展示，不再带 `RMB` 或 `¥`；`币种` 列继续独立显示。`付款编号` 保持人工填写字段，新增/编辑时后端只做 trim、去控制字符/尖括号和 80 字符长度限制，不再生成、不覆盖、不按日期或流水号改写；从客户流水反向补水单的兼容路径也不再把流水 ID 写入付款编号。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "water receipt"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "water receipt|水单"`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未批量修复历史付款编号；未改水单编号规则；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 财务水单编号严格格式本地落地

2026-07-07 已按任务卡 `2026-07-07-财务管理-水单编号严格按表格格式生成` 本地落地，未发布 47。实现范围：后端新增水单编号统一由服务端生成，固定 `SDyyyyMMddNNN` 13 位格式，日期取当前北京时间而非前端提交的到账日期；内存仓储和 Prisma 仓储均按当天已有编号递增，Prisma 端依赖唯一约束并在冲突时重试，超过 `999` 或生成失败返回 `水单编号生成失败，请重试`；前端新增水单不展示编号输入，编辑水单只读展示原编号；测试 Mock 同步按同一口径生成编号。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "water receipt"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "water receipt|水单"`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；未批量修改历史长编号；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 业务录单与待审核详情两列布局本地落地

2026-07-07 已按任务卡 `2026-07-07-business-order-entry-detail-two-column-layout` 本地落地，未发布 47。实现范围：`业务管理 -> 录单` 改为“录入与货物 / 出库与审核”左右两列，核心字段按任务卡顺序展示；新增/回显 `转单号`，`出货单号`、`业务渠道`、`目的地`、`报关`、`货物类型`、`结算方式` 保持必填；货物数据在左侧按件数、实重、体积、计费重展示；选择收货人、业务类型、包裹类型等兼容信息保留在辅助区。`待审核运单 -> 详情` 与全局内部 `运单详情` 弹窗均改为同样两列只读布局；业务员/业务角色继续隐藏代理渠道、业务成本审核日期、应付审核日期、应付费用、代理成本、付款编号和利润，管理员/财务按权限可看。验证通过：`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "录单|pending review|detail"` 4/4、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx src/modules/workspace/workspace.test.tsx src/modules/customerService/customerService.test.tsx -t "录单|pending review|detail|运单详情|数据确认"` 7/7、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order entries|manual shipment edits"` 2/2、`git diff --check`。剩余：未做 47 发布；未跑完整 Web/API 测试；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 报价查看线路超时已发布 47

2026-07-07 已修复并发布 47 `报价查价 -> 代理加价规则 -> 查看线路` 点击后页面无响应/超时的性能根因。实现范围：前端线路弹窗先打开并显示 loading，再异步请求线路，避免接口慢时看起来跳转空白或无响应；API `getPriceBookRows` 的亮崽兼容 fallback 从“先查最多 10000 行再内存分页”改为按数据库 `count + skip/take` 真分页，并先解析有效 `LegacyPricingSource` 的 `sourceId`，避免关系过滤和大数组返回拖垮页面；新增 Prisma 索引迁移 `20260707195000_legacy_pricing_line_lookup_indexes`，补 `LegacyPricingRow(sourceId, agentName, minWeightKg)`、`(sourceId, channelName)`、`(sourceId, destinationCountry, minWeightKg)`，用于代理线路、渠道和目的地分页查询。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run prisma:generate -w @siyuan/api`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing|lookup|large|upload|sync|markup|legacy|deleted"` 10/10、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "imports Yiyang|imports legacy BIFF"` 2/2、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build db-migrate api web` 成功；`prisma migrate deploy` 应用 `20260707195000_legacy_pricing_line_lookup_indexes`，当前 65 个迁移全部成功；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：未用真实浏览器点击验证线上页面；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前代码仍未 git stage/commit。

## 当前 2026-07-07 报价查价按钮与后端动作巡检已发布 47

2026-07-07 已巡检并发布 47 `报价查价` 的主要功能按钮与对应后端动作。实现范围：查价模块切换、查价/清空、推荐报价筛选、报价详情关闭、代理加价规则增删改停启、查看线路、价格表同步体检/导入/备注/删除、线路弹窗关闭/分页/批量加价等按钮统一补 `htmlType="button"`，避免表单或浏览器默认提交导致空白页/误跳转；`查看线路` 改为先打开弹窗并展示 loading，再异步加载线路，避免慢接口时看起来无响应；加价规则批量启停/删除增加 loading 与错误提示。后端修正虚拟默认加价规则的批量删除/停用语义：对只有有效价格表、尚未实体化的默认代理规则，批量删除会创建软删除 tombstone 阻止虚拟默认规则重新出现，批量停用会创建实体禁用规则，避免用户确认删除后列表仍恢复原样。验证通过：`git diff --check`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing|lookup|large|upload|sync|markup|legacy|deleted"` 10/10、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "imports Yiyang|imports legacy BIFF"` 2/2、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build api web` 成功；`docker compose up -d --remove-orphans api web` 成功，未跑迁移。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：未做真实浏览器点击巡检；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前代码仍未 git stage/commit。

## 当前 2026-07-07 查价模块切换空白页修正

2026-07-07 已本地修复 `报价查价 -> 查价` 中点击 `欧洲海运超大件查询` 后页面空白/疑似跳转的问题，未发布 47。实现范围：四个亮崽兼容查询模块切换按钮显式设置 `htmlType="button"`，并在点击时 `preventDefault()` 后再切换模块，避免按钮在表单/浏览器默认行为下触发提交或页面导航。验证通过：`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做浏览器真实点击复验；当前 47 页面不会体现此本地修复，需用户明确要求发布后才会上线。

## 当前 2026-07-07 价格表代理线路弹窗修正已发布 47

2026-07-07 已修复并发布 47 `报价查价 -> 代理加价规则 -> 查看线路` 导入后线路仍显示 0 的问题。根因：线路弹窗调用 `/api/pricing/book-rows`，该接口只查基础 `PriceBookRow`，而部分 Excel 导入主要落入亮崽兼容报价副本，导致价格表列表行数正常但代理线路明细为空。实现范围：API `getPriceBookRows` 在基础价格行查询结果为 0 时，按有效价格表文件名回退查询 `LegacyPricingRow`，并映射为线路展示行；前端线路弹窗关闭统一清理打开状态、loading 和当前行，避免慢请求或异常后看起来关不掉；`scripts/sync-47.sh` 已排除 `apps/api/uploads/`，避免把本地上传文件同步覆盖到 47。验证通过：`git diff --check`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing|lookup|large|upload|legacy"` 10/10、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build api web` 成功；`docker compose up -d --remove-orphans api web` 成功，未跑迁移。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。未通过/未完成：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "imports|加价|线路|price"` 此前超过 2 分钟无用例输出后中断，属于既有 pricing 长链路卡住风险，未作为本轮通过依据；Web 构建仍有既有 `antd-vendor` 大 chunk warning；当前代码仍未 git stage/commit。

## 当前 2026-07-07 价格表导入展示口径与文件名乱码修正

2026-07-07 已修复并发布 47 报价查价价格表导入后的两个展示问题。实现范围：`POST /api/pricing/books/import-jobs` 对上传文件名做 UTF-8/Latin-1 兼容规范化，支持中文文件名在 multipart 链路中一次或二次乱码后恢复，并保留扩展名；`GET /api/pricing/books` 的价格表列表行数口径改为优先使用成功导入任务的 `totalRows/processedRows`，再回退价格行计数和兼容模块计数，避免导入任务成功但列表 `导入行数` 显示 0。验证通过：`npm run typecheck -w @siyuan/api`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "5000-row|pricing|lookup|large|upload"`、`git diff --check`。47 发布：为避免当前脏工作树全量同步带入无关改动，仅同步 `apps/api/src/modules/data.controller.ts` 和 `apps/api/src/modules/prisma.repository.ts` 到 `/opt/siyuan`，执行 `docker compose build api` 与 `docker compose up -d --remove-orphans api`，未跑迁移、未重启 Web；公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 200，API 日志包含 `Nest application successfully started`。剩余：现有线上已乱码的历史文件名如已入库，需另做一次性数据修正或重新导入才会变正常。

## 当前 2026-07-07 报价查价导入 OOM 治理已发布 47

2026-07-07 已按发布会话梳理当前工作树并发布报价模块“批量数据 / 导入 / 全量返回 OOM”专项治理到 47。实现范围：新增 API 端 Excel 解析 `apps/api/src/modules/pricing-excel.ts` 并把 `@e965/xlsx` 纳入 API 依赖；新增 `PriceBookImportJob` Prisma 模型和迁移，用于价格表文件上传导入任务；新增 `POST /api/pricing/books/import-jobs`、`GET /api/pricing/books/import-jobs/:id`、`GET /api/pricing/book-rows`、`GET /api/pricing/books/:id/rows`、`GET /api/pricing/sync-health`；旧 `POST /api/pricing/books/import` 保留兼容但强制轻量返回，不再返回完整 rows。价格表列表默认 `includeRows=false`，线路查看走分页价格行接口，同步体检走后端聚合分页接口，加价规则列表继续 `includeHits=false` 避免全量命中计算。前端 `PricingPage` 导入价格表改为上传文件并轮询任务，成功后只使用价格表摘要；查看线路和同步体检均接入分页数据，不再依赖前端全量 `priceRows`。

验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run prisma:generate -w @siyuan/api`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "pricing|lookup|large|upload|sync|markup|legacy|deleted"` 10/10（覆盖 5000 行 xlsx 上传导入任务、价格表列表不返回 rows、线路分页、同步体检分页、`includeHits=false` 不返回全量线路、查价仍命中默认加价）、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "imports Yiyang|imports legacy BIFF"` 2/2、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build db-migrate api web` 成功；`prisma migrate deploy` 显示 64 个迁移且无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：当前代码仍未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning；超大 xls（例如数万行）、异常公式、重复上传、并发导入仍可能造成 API 解析 CPU/内存压力，后续建议增加任务队列并限制并发、失败行明细分页、上传文件大小/行数策略和任务取消能力。

## 当前 2026-07-07 报价查价卡顿优化已发布 47

2026-07-07 已按发布会话梳理当前工作树并发布到 47。新增待发布运行时代码包含报价查价性能优化：价格表管理首屏不再默认拉取全部报价行，改为 `includeRows=false` 加载价格表摘要，需要价格行时再懒加载；代理加价规则列表默认不计算命中数并支持 `includeHits=false`，减少大价格表下的列表开销；查价后端改为按查询条件缩小价格行加载范围，并为加价规则构建按代理分组索引，避免每条价格行全量扫描规则；价格表删除后继续保留加价规则但不再引用已删除价格行；前端查价结果表接入 `ManagedTable` 并保留现有报价体验。发布范围按当前改动判定为 `api+migrate+web`，已同步、构建、迁移检查并重启 `api/web`。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 6/6（8 skipped）、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|pricing|lookup|deleted price rows|legacy"` 10/10、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build db-migrate api web` 成功；`prisma migrate deploy` 显示 63 个迁移且无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：当前代码仍未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning；本轮未做登录后真实查价页面浏览器耗时采样。

## 当前 2026-07-07 业务待审核运单与录单权限已发布 47

2026-07-07 已按发布会话梳理当前工作树并发布到 47。新增待发布运行时代码包含两块：一是 `业务管理 -> 待审核运单` 列表字段，移除 `转单号/代理渠道`，新增 `货物数据` 分组和按有效汇率折算的 `应收` RMB 合计，API/Shared 同步补齐汇总字段；二是 `业务管理 -> 录单` 对业务账号隐藏并禁止提交应付成本，非管理员/财务角色提交 `payables` 会被 API 拒绝，管理员/财务仍保留原能力。发布范围按当前改动判定为 `api+migrate+web`，已同步、构建、迁移检查并重启 `api/web`；中途 47 SSH 曾因构建负载短暂 reset/refused，服务自动恢复后已补跑 Web 构建和重启。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "pending review"` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "review|business role groups"` 5/5、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "录单|order-entry"` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order entries|business role groups"` 1/1、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build db-migrate api web` 成功；`prisma migrate deploy` 显示 63 个迁移且无待应用；`docker compose up -d --remove-orphans api web` 成功；补跑 `docker compose build web` 和 `docker compose up -d --remove-orphans web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：当前代码仍未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning；Prisma major update notice 仍是提示。

## 当前 2026-07-07 业务录单隐藏应付成本落地

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-business-order-entry-hide-payable-cost.md` 本地落地，未发布 47。实现范围：`业务管理 -> 录单` 中业务账号/业务角色组不再展示 `应付费用` 区块、默认 `出货成本` 行、`出货成本单价`、`付款编号` 等应付敏感字段；前端提交时非管理员/财务角色统一发送 `payables: []`；管理员、财务、财务角色组仍可按现有权限看到应付费用区块。API 内存仓储和 Prisma 仓储均收紧录单应付可见/提交权限，仅 `ADMIN/FINANCE/UG_FINANCE` 可录入应付费用；业务账号通过 `POST /api/shipments/order-entry` 或 `PUT /api/shipments/:id/order-entry-draft` 提交 `payables` 会被拒绝，不能落库。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "录单|order-entry"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order entries|business role groups"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：当前工作树仍包含多项既有未提交改动；未做 47 发布。

## 当前 2026-07-07 业务待审核运单列表字段落地

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-business-pending-review-list-fields.md` 本地落地，未发布 47。实现范围：`业务管理 -> 待审核运单` 列表移除 `转单号`、`代理渠道` 列；将原 `应收/代理计费重` 改为 `货物数据` 分组，展示重量、体积、件数、计费重；新增 `应收` 列，显示有效应收费用折算 RMB 后的合计，USD 按当前有效系统汇率折算，缺少/不支持汇率时返回可读错误字段并在列表显示错误文案；详情页、自审通过、费用录入、财务审核流程未改。Shared `Shipment` 补充列表展示汇总字段，API `GET /api/shipments/review-pending` 在内存仓储和 Prisma 仓储中补齐货物汇总与应收 RMB 合计。验证通过：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "pending review"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "review|business role groups"`、`git diff --check`。剩余：当前工作树仍包含多项既有未提交改动；未做 47 发布。

## 当前 2026-07-07 批量操作表格选择列已发布 47

2026-07-07 已按发布会话梳理当前工作树并将最新前端运行时代码发布到 47。本轮新增待发布运行时代码主要是批量操作表格选择列修正：`ManagedTable` 的 `rowSelection` 选择列固定第一列并锁定，自定义选择列统一识别为不可隐藏/不可移位；仓库今日收货、在仓数据、待理货包裹补齐表头全选；录单仓库数据弹窗和订单费用明细表显式固定选择列。发布范围判定为 `web`，仅重建/重启 47 `web`，未运行线上迁移、未重启 API。验证通过：`git diff --check`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx` 5/5、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "today receipts|warehouse workbench|batch dispatch"` 4/4、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "仓库数据|录单"` 1/1、`npm run build -w @siyuan/web`。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build web` 成功；`docker compose up -d --remove-orphans web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`。剩余：当前代码仍未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning。

## 当前 2026-07-07 47 仓库 7 月真实收货数据导入

2026-07-07 已按用户确认直接操作 47 线上真实库：仅清理仓库包裹相关数据并导入 `/Users/j1ng/Library/Containers/com.tencent.xinWeChat/Data/Documents/xwechat_files/wxid_t9bbwj58u3yk22_45b9/msg/file/2026-07/2026年7月份仓库数据(1).xlsx`，未删除运单、财务、客户、排货、出库历史。操作前已备份 47 `WarehousePackage/WarehouseConsolidation/WarehouseConsolidationItem` 到 `/opt/siyuan/backups/warehouse-import-20260707115145/warehouse_tables_before_import.sql`。执行范围：删除旧 `WarehouseConsolidationItem` 18 条、`WarehouseConsolidation` 8 条、`WarehousePackage` 589 条；导入 Excel 有效测量包裹 892 条为 `RECEIVED`，`scanSource=2026年7月份仓库数据导入`，并按客户资料回填 35 条客户/业务员/站点信息；Sheet `2` 第 230 行 `W1313-合箱后数据` 缺重量/尺寸/时间，未导入。线上复核：`WarehousePackage=892`、组合号 275 个；北京时间日期分布为 2026-07-01 132 条、07-02 200 条、07-03 197 条、07-04 175 条、07-06 188 条；7 月范围今日收货计数 892；仓库合票表已清空；`/api/health` 正常。说明：前端 `今日收货` 默认今日筛选不会显示 7 月 1-6 数据，需要选择 `本月` 或自定义日期查看；`在仓数据` 会展示这批 `RECEIVED` 包裹。

## 当前 2026-07-07 批量操作表格选择列修正

2026-07-07 已按用户截图补齐批量操作表格选择列细节，未发布 47。实现范围：共享 `ManagedTable` 将 `select/selection/checkbox/rowSelection` 等选择列识别为固定第一列并锁定，避免列设置或本地缓存把选择列移动到中间或隐藏；所有使用 `ManagedTable` 的 `rowSelection` 表格统一固定选择列宽度并锁定在左侧，保留 AntD 表头全选能力；仓库 `今日收货`、`在仓数据`、`未完成理货 -> 待理货包裹` 的自定义勾选列新增表头全选框，并保持选择列在第一列；两个未走 `ManagedTable` 但存在批量选择的复杂表格（录单仓库数据弹窗、订单费用明细表）已显式固定选择列为第一列/左侧。验证通过：`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "today receipts|warehouse workbench|batch dispatch"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "仓库数据|录单"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。额外跑完整 `warehouse.test.tsx` 时本次新增断言通过，仍只剩既有报价导入断言失败（`亿阳国际.*¥0.50/kg`），与本次选择列无关。剩余：当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 全局列设置/仓库/客服/录单工作树已发布 47

2026-07-07 已按发布会话梳理当前工作树并发布到 47。发布范围按当前改动判定为 `api+migrate+web`：包含全局业务表格列设置与列宽拖拽、仓库待出库字段/批量出货/代理交接单/已出库归档、客服待排货只读费用查看、在仓数据录单预填、已完成理货历史页内归档，以及既有墨家设备/查价/发布规则文档等未提交改动。发布前补齐测试定位兼容：`warehouse.test.tsx` 与 `customerService.test.tsx` 改用多列设置按钮场景下的稳定断言/点击，并按当前默认列展示放宽数据确认行重复值断言；未改业务逻辑。验证通过：`git diff --check`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞、仓库相关 Web 定向测试 10/10、客服待排货/费用/离港相关 Web 定向测试 11/11、仓库 API 定向 e2e 6/6。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 成功；47 `docker compose build db-migrate api web` 成功；`prisma migrate deploy` 显示 63 个迁移且无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：47 `api/web/postgres/redis` 均 Up，容器内与公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，公网首页 200，API 日志包含 `Nest application successfully started`。剩余：当前代码仍未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning；Prisma major update notice 仍是提示。

## 当前 2026-07-07 全局业务表格列设置与列宽拖拽

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-global-managed-table-column-settings.md` 本地落地，未发布 47。本轮为用户明确授权的一次性全局特例。实现范围：共享 `ManagedTable` 默认提供统一 `列设置`、列显隐、上移/下移排序、列宽拖拽、恢复默认、本地存储持久化和损坏配置容错；操作类列默认锁定不可隐藏，避免关键动作被静默移除；代表页 `客服管理 -> 数据确认` 已改用统一列设置并覆盖列隐藏、排序、列宽存储验证；普通业务表格已尽量复用/迁入 `ManagedTable`，覆盖 pricing、orders、warehouse、routing、customerService、tracking、finance、masterData、settings、reports、problemTickets 等后台模块。未强行改造的复杂表格清单：财务单票费用和录单费用草稿的合计/编辑型表、财务详情抽屉内嵌小表、待付款申请弹窗分组小表、录单仓库包裹/水单选择弹窗等，原因是带合计行、编辑态或嵌套详情流程，后续需单独任务卡适配。验证通过：`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx`、`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "数据确认|列|column"`、`npm run typecheck -w @siyuan/web`、`git diff --check`；额外抽查 `warehouse.test.tsx -t "列设置|待出库"` 和完整 `pricing.test.tsx` 通过。额外尝试完整 `settings.test.tsx` 仍卡在既有主数据长链路 `UPS偏远` 断言，`problemTickets.test.tsx` 仍卡在客服子入口 `问题件` 导航断言，未作为本卡通过依据。剩余：未做 47 发布；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 仓库待出库表格字段落地

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-warehouse-dispatch-table-fields.md` 本地落地，未发布 47。先复核现有 `待出库` 模块后确认：默认表格字段、业务数据分组、唛头列和排序能力大体已在上一张批量出库任务中落地；主要差距是列设置仍按旧 `ManagedTable` 顶层列处理，不能单独控制 `业务数据` 下的 `件数/总量/体积/计费重` 子列，也缺少待出库专用字段清单。已补齐待出库专用列设置：默认顺序严格为运单创建时间、业务员、运单号、代理、代理渠道、客户编号、目的地、渠道、业务数据（件数/总量/体积/计费重）、唛头、品名、报关、敏感；列设置覆盖除选择框外的业务字段并支持单独隐藏/恢复业务数据四个子列；保留主要字段排序、唛头明确标识和现有勾选能力；未改出货、代理交接单、已出库和状态流转逻辑。验证通过：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待出库|唛头|列设置|业务数据"`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 仓库待出库批量出货已出库归档落地

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-warehouse-batch-dispatch-archive.md` 本地落地并复核差距，未发布 47。实现范围：仓库二级目录移除 `收货交接单`，在 `待出库` 后新增最后一个二级目录 `已出库`；`待出库` 默认按指定顺序展示选择、运单创建时间、业务员、运单号、代理、代理渠道、客户编号、目的地、渠道、业务数据（件数/总量/体积/计费重）、唛头、品名、报关、敏感，并保留列设置和主要字段排序；批量勾选后显示 `已选 X 票 / Y 件`，未勾选提示、代理交接单弹窗、打印即完成出库和唛头确认拦截已同步。打印完成后待出库订单移除，并进入 `已出库` 历史。复核补齐：运单列表从 `shipment.dispatch` 审计回填 `handoverNo/outboundBy/batchDispatchSource/outboundAt`，前端 `已出库` 可展示后续状态已推进的出库历史和真实交接单/操作人；同步清理旧 `进入待出库时间` 测试口径，改验收 `运单创建时间`。`已出库` 按 `outboundAt/dispatchedAt` 及本地理货合并出货结果展示仓库阶段历史，避免后续客服/物流状态推进后丢失仓库出库记录；列表展示交接单号、出货单号、客户/目的地、件数、计费重、渠道、代理、出库时间、操作人和状态，不展示财务敏感字段。验证通过：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待出库|已出库|批量出货|代理交接单|收货交接单|唛头|列设置"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "dispatch|label|handover|outbounded"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。额外复核：完整 `warehouse.test.tsx` 当前只剩既有报价导入断言失败（`亿阳国际.*¥0.50/kg`），本卡相关 16 项通过。剩余：未做 47 发布；未新增数据库字段或迁移；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 仓库待出库批量代理交接单落地

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-warehouse-batch-dispatch-handover.md` 本地落地，未发布 47。实现范围：`仓库管理 -> 待出库` 已支持顶部批量勾选、`已选 X 票 / Y 件` 统计和顶部 `出货`；未勾选时提示 `请先勾选待出库订单`；勾选后打开 `代理交接单`，按代理拆分并展示黑白表格、代理/出货时间、运单号、入仓号、渠道、品名、件数、报关退税、备注、目的地、票数/件数与空白 `收件人：`；点击 `打印` 会打开打印窗口并完成本批出货；包含需贴麦头订单时，未确认 `已确认所选需贴麦头订单均已贴好麦头` 不允许打印出货。API 出库审计支持接收批量交接单号和 `batchDispatchSource`，继续复用单票 `shipment.dispatch` 并更新仓库包裹为 `SHIPPED`。验证通过：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "待出库|批量出货|代理交接单|麦头"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "dispatch|label|handover"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；未新增数据库字段或迁移；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 客服待排货只读费用查看落地

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-customer-service-pending-routing.md` 本地落地，未发布 47。实现范围：`客服管理` 二级菜单在 `转单号` 与 `待离港` 之间新增 `待排货`；页面复用共享待排货宽表列工厂，只展示 `WAITING_SORT` 运单；客服待排货操作列只保留 `查看费用` 和 `只读` 标识，不提供审核、修改、排货、出库；`查看费用` 弹窗复用现有运单费用详情接口，仅展示应收费用、业务成本及合计，不渲染应付明细、代理成本和利润字段；`App` 已向客服页传入现有业务成本摘要供列工厂复用。验证通过：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "待排货|费用|transfer|departure"`、`npm run typecheck -w @siyuan/web`。剩余：未做 47 发布；未改 API、Shared、Prisma 或费用口径；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 在仓数据录单预填落地

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-warehouse-instock-order-entry-prefill.md` 本地落地，未发布 47。实现范围：`仓库管理 -> 在仓数据` 顶部框选入口从 `合票` 改为 `批量录单`，行内入口从 `合票` 改为 `录单`；点击单行/批量录单不再打开仓库合票弹窗或创建合票记录，而是跳转 `业务管理 -> 录单` 并通过 `prefillOrderEntryPackageIds/warehousePackageIds` 预选仓库包裹；录单页补齐按 packageIds 加载在仓货物、默认选中并汇总已选货物/件数/实重/方数/计费重；录单包裹查询支持受控 `packageIds`，继续排除已绑定运单、草稿占用、已合票、已出库、已归档包裹并保留业务员客户归属限制；仓库角色无 `orders:write` 时不显示录单入口。补充落地：录单页已选货物列表和仓库数据弹窗在包裹号旁保留 `理` 标记，点击理货包裹号或 `理` 标记展示理货历史详情，包含来源理货任务、来源组合号、理货需求、原始/理货后件重尺、完成时间、标签应用时间、覆盖后包裹号；找不到完整任务时展示包裹自有字段并提示 `理货任务记录待补全`。验证通过：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "录单|批量录单|warehouse package"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "仓库数据|录单"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`git diff --check`。剩余：未做 47 发布；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 已完成理货历史页内归档落地

2026-07-07 已按任务卡 `docs/tasks/2026-07-07-warehouse-tally-history-archive.md` 本地落地，未发布 47。实现范围：`已完成理货历史` 从仓库左侧二级入口迁移到 `已完成理货（近 1 个月）` 卡片标题旁的页内切换；已完成任务仍只展示最近 1 个月；历史视图改为展示最近 1 个月 `TALLIED_ARCHIVED` 原始在仓包裹，包含原始件数、重量、尺寸、来源组合号、归档原因/时间、对应理货任务、覆盖后包裹号；`GET /api/warehouse/in-stock` 新增受控 `status=TALLIED_ARCHIVED` 查询，普通在仓仍排除归档；扫码应用理货标签时补写被归档原包裹的 `tallyTaskId/tallyTaskNo`。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "tally|label|archive"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "tally|label|history|in-stock"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`git diff --check`。剩余：未做 47 发布；当前工作树仍包含多项既有未提交改动。

## 当前 2026-07-07 当前工作树再次复核发布 47

2026-07-07 已再次按发布会话要求梳理当前工作树并发布到 47。当前未提交改动仍为同一批：墨家设备推送失败审计与时间格式兼容、查价/亮崽报价查询范围和索引优化、前端 502/504 友好提示、查价样式压缩、47 增量发布规则、业务视角任务卡体系和状态文档。发布范围按 `api+migrate+web` 执行。验证：`git diff --check`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web` 均通过；`npm run sync:47` dry-run 通过。发布：`npm run sync:47 -- --apply` 成功；47 `docker compose build db-migrate api web` 成功，Web 构建通过但仍有既有 `antd-vendor` 大 chunk warning；`docker compose --profile tools run --rm db-migrate` 成功，63 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Prisma major update notice 仍是提示。

## 当前 2026-07-07 业务视角任务卡体系已落地

2026-07-07 已把后续需求管理调整为“业务视角任务卡 -> 代码会话按卡落地”：新增 `docs/tasks/_template.md` 和 `docs/tasks/_acceptance.md`，正式任务卡统一放入 `docs/tasks/YYYY-MM-DD-模块-业务动作.md`；`AGENTS.md` 写入规划会话、代码会话、发布会话、审查会话分工，代码会话只执行已通过标准的一张任务卡；`docs/dev-thread-rules.md` 补充任务卡准入门槛和多会话协作规则；`docs/sunny-dev-index.md` 改为优先读取 `docs/dev-now.md` 指向的任务卡；`docs/dev-now.md` 已更新为当前任务卡体系落地范围。验证：`git diff --check` 通过。本轮只改文档规则，不改业务代码、不发布 47。

## 当前 2026-07-07 当前工作树重复发布确认

2026-07-07 已再次梳理并发布当前工作树到 47。当前本地未提交改动仍包含墨家设备收货推送失败审计与时间格式兼容、查价/亮崽报价查询范围和索引优化、前端 502/504 友好提示、查价样式压缩、发布规则、任务卡治理文档和状态文档更新；本轮未发现新增 Prisma 迁移，线上 `prisma migrate deploy` 显示 63 个迁移且无待应用。验证通过：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"` 1/1、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "deleted price rows|legacy|pricing|lookup|markup"` 10/10、`npm test -w @siyuan/web -- --run src/modules/shared/ui.test.ts` 3/3、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 走缓存构建通过；`docker compose --profile tools run --rm db-migrate` 无待迁移；`docker compose up -d --remove-orphans api web` 成功；后续新增 docs-only 任务卡治理文档已同步到 47，未重启服务。健康检查：`api/web/postgres/redis` 均 Up，公网首页 200，公网 `/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Prisma major update notice 仍是提示。

## 当前 2026-07-07 墨家设备/查价优化与当前工作树已发布 47

2026-07-07 已梳理并发布当前工作树到 47。发布范围包含：墨家设备收货推送失败审计持久化、毫秒/秒级时间戳与 `2026.07.06/17:15:56` 时间格式兼容、缺快递单号条码兜底；报价查价删除价格表后不再引用已删除价格行、查价/亮崽报价按目的地/仓库/重量缩小查询范围、代理加价规则按代理索引匹配、价格行与亮崽行新增查询索引迁移 `20260707021000_pricing_lookup_indexes`；前端隐藏 Nginx 502/504 HTML 错误并提示 `查价超时，请缩小条件或稍后重试`；查价页面样式压缩与发布范围自动判断规则。验证通过：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "deleted price rows|legacy|pricing|lookup|markup"` 10/10、`npm test -w @siyuan/web -- --run src/modules/shared/ui.test.ts` 3/3、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 通过；`prisma migrate deploy` 成功且无待应用迁移；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，公网首页 `http://47.120.33.111:8899/` 返回 200，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning；Prisma major update notice 仍是提示。

## 当前 2026-07-07 墨家设备收货推送排查

2026-07-07 已只读排查 47 线上仓库 `今日收货` 7 月 6 日数据：`WarehousePackage` 按北京时间 2026-07-06 当天只存在 3 条收货包裹，和页面显示一致；未发现 20 条已入库但被前端过滤的记录。用户提供的设备日志确认历史失败原因：`measuredAt: "1783323455600"` 被旧线上代码写成 `Invalid Date`，以及 `barcode: "J721-"` 被旧线上代码判定为缺少快递单号。AuditLog 当天只有 4 条 `warehouse.package.create`，其中 1 条像重复/异常审计但对应包裹当前不在 `WarehousePackage`；没有持久化的墨家设备失败推送记录。当前 API 容器日志因近期重启只保留较近日志，无法从日志追溯 7 月 6 日全部失败请求。已在本地补齐墨家设备接口失败持久化审计：有效 token 但字段校验/落库失败时记录 `warehouse.request.write.failed`，包含错误原因；同时兼容 13 位毫秒时间戳、10 位秒级时间戳、`2026.07.06/17:15:56` 这类设备/表格时间格式，并保留 `J721-` 这种缺快递单号条码为 `J721-待补充`；未发布 47。验证通过：`npm run typecheck -w @siyuan/api`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"`。

## 当前 2026-07-07 47 重启后状态与最新工作树发布完成

2026-07-07 47 服务器重启后 SSH 已恢复，`docker compose ps` 显示 `api/web/postgres/redis` 均已自动拉起。随后按当前工作树重新发布：先完成 `api` 增量构建重启；发现当前工作树又包含 Prisma schema/migrations 与 Web 运行时代码后，按 `api+migrate+web` 范围重新执行 `npm run sync:47` dry-run、`npm run sync:47 -- --apply`、47 `docker compose build db-migrate api web`、`docker compose --profile tools run --rm db-migrate`、`docker compose up -d --remove-orphans api web`；最后又同步并重建/重启了一次 `web`，确保最新 `styles.css` 已进入线上镜像。迁移 `20260707021000_pricing_lookup_indexes` 已成功应用；API 构建阶段生产依赖 `npm prune --omit=dev` 后 0 vulnerabilities；Web 构建通过但仍有既有 `antd-vendor` 大 chunk warning。健康检查通过：`api/web/postgres/redis` 均 Up；公网 `http://47.120.33.111:8899/` 返回 200，`http://47.120.33.111:8899/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`；容器内 `http://127.0.0.1:18899/api/health` 正常；API 日志包含 `Nest application successfully started`。说明：重启/API 重建期间 Web 日志出现过短暂 502，API 就绪后健康检查正常。

## 当前 2026-07-07 47 增量发布规则已写入

2026-07-07 已把 47 发布范围自动判断写入 `docs/47-cloud-docker-release.md` 和 `AGENTS.md`：发布前按 `git status --short`、`git diff --name-only` 与 Prisma 迁移目录判定 `state/docs-only`、`web`、`api`、`api+migrate`、`web+api` 或 `full`；只构建/重启受影响服务，只有 schema/migrations 变化才运行 `db-migrate`。注意：这是发布会话规则，当前 `sync:47` 脚本本身仍只负责同步文件，不会替代发布会话做 Docker 构建范围判断。

## 当前 2026-07-07 47 发布受 SSH banner 超时阻塞

2026-07-07 本地已梳理当前待发布改动：状态文件滚动归档规则、前端规则优先级收敛、开发效率治理、查价删除价格表后不再引用已删除价格行的内存仓储修正与 API 回归测试。验证通过：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "deleted price rows|legacy|pricing|lookup|markup"` 10/10、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 6/6 通过、8 skipped、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布阻塞：47 服务器 22 端口 TCP 可连通、ping 正常，但 `ssh 47` 持续卡在 banner exchange 并超时；`npm run sync:47` dry-run 因同一 SSH 问题未完成，当前无法执行 `sync:47 -- --apply`、47 Docker 构建、迁移和重启。当前代码未 git stage/commit。


## 当前 2026-07-07 状态文件滚动归档规则

2026-07-07 已按 7 天窗口治理 `.codex-state.md`：当前状态文件只保留最近 7 天内的高信号开发状态和归档索引；超过 7 天的历史记录归档到 `docs/archive/codex-state/`，默认开工不展开旧归档。已归档 2026-06-28 至 2026-06-29 的旧状态到 `docs/archive/codex-state/2026-06-28-to-2026-06-29.md`，并将 2026-06-30 至 2026-07-04 的详细流水归档到 `docs/archive/codex-state/2026-06-30-to-2026-07-04.md`，当前文件只保留近期高信号摘要。后续每次更新状态时，如果发现超过 7 天或低信号的详细流水，先归档再追加新状态，避免 `.codex-state.md` 继续膨胀。

## 归档索引

- `docs/archive/codex-state/2026-06-28-to-2026-06-29.md`：A-F 阶段旧验收、47 受控样本和 2026-06-28 至 2026-06-29 的历史状态。
- `docs/archive/codex-state/2026-06-30-to-2026-07-04.md`：2026-06-30 至 2026-07-04 的客服、仓库、市场、系统管理、UI 治理和发布详细流水。

## 当前 2026-07-07 开发效率治理

2026-07-07 已继续收口 4 个开发效率问题：`docs/dev-now.md` 作为当前任务入口，限制允许改动范围和小/中/大三档验证命令；`AGENTS.md` 和 `docs/sunny-dev-index.md` 已要求开工优先读取 `docs/dev-now.md`；`.codex-state.md` 从 932 行压缩到约 280 行，旧详细流水进入 `docs/archive/codex-state/`；仓库 e2e 的 `approveForRouting` 改为按当前业务自审口径准备待排货数据，不再走旧 finance 终审。验证：`npm run typecheck`、`npm run test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts` 9/9、`npm run test -w @siyuan/api` 58/58 通过；完整 `npm test` 已推进到 Shared/API 通过，仅剩 Web 7 个旧入口断言/旧数据断言/异步超时 gap，已记录在 `docs/current-test-gaps.md`。

## 当前前端规则优先级已收敛

2026-07-07 已明确 sunny 本地 `frontend-design` 是 UI/UX 设计准则最高优先级，Agency Frontend Developer 只作为 React/AntD/状态/性能/可访问性实现辅助；小 UI 改动不调用 custom agents，截图验收优先 Evidence Collector，禁止同一轮完整展开两套 UI 规则。

## 当前 2026-07-07 最新工作树发布确认

2026-07-07 已按发布流程再次梳理并发布当前工作树到 47。当前可发布内容仍是上一轮已整理的亮崽报价兼容、Excel 多 sheet/`.xls` 读取兼容、市场待排货 `审核/修改`、客户收货人 `fbaWarehouseCode`、理货归档、共享待排货列和相关规则/测试更新；并补充发布查价管理入口调整：业务员只看四个查价模块，管理员可在同一报价查价中心进入 `代理加价规则/价格表管理`，`报价表管理/数据体检` 不再显示；`代理加价规则` 在存在导入价格表时按价格表文件分组显示，例如 `7.6-拓普达.xlsx`、`7.3振韵.xls`，点击 `查看线路` 进入该价格表内的代理/渠道明细继续批量加价。本轮未发现新增未发布迁移。验证通过：`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 6/6 通过、8 skipped。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 通过；`prisma migrate deploy` 成功且无待应用迁移；`docker compose up -d --remove-orphans api web` 成功，容器已处于 Running。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`；线上 JS 包确认包含 `价格表/线路`。剩余：当前代码未 git stage/commit；Web 构建仍有 `antd-vendor` 大 chunk warning 和独立 `xlsx` chunk；Prisma/npm major notice 仍是提示；如浏览器仍显示旧页面，需要强刷或清理前端缓存。

## 当前 2026-07-06 最新工作树重复发布确认

2026-07-06 已按发布流程再次同步当前工作树到 47。`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 通过，API 构建使用缓存且生产依赖 prune 后 0 vulnerabilities，Web 构建通过但仍有 `antd-vendor` 大 chunk warning；`prisma migrate deploy` 成功且无待应用迁移；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。本轮未发现新增未发布迁移或新的发布阻塞；当前代码仍未 git stage/commit。

## 当前 2026-07-06 Excel 读取兼容与最新工作树已发布 47

2026-07-06 已再次梳理并发布当前工作树到 47。新增范围：前端 Excel 读取从单页 `read-excel-file` 扩展为动态加载 `@e965/xlsx`，支持读取多 sheet 与原始 `.xls/.xlsx` 工作簿内容，保持原有导出走 `write-excel-file`；价格表导入提示与仓库/共享表格测试断言同步适配；项目规则补充“全局/统一改造”必须先分阶段验证。其余已包含上一轮亮崽报价兼容、市场待排货 `审核/修改`、客户收货人 `fbaWarehouseCode`、理货归档和共享待排货列等改动。

验证通过：`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 12/12、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 16/16、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "staff import|agent|isolates staff menus|defaults new customer ownership"` 2/2、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "legacy|pricing|lookup|markup"` 9/9、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`prisma migrate deploy` 成功且无待应用迁移；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Web 构建仍有 `antd-vendor` 大 chunk warning，同时新增独立 `xlsx` chunk 约 500KB；Prisma/npm major notice 仍是提示；未做浏览器截图巡检。

## 当前开发准则补充：第一性原理 + 对抗式审查

2026-07-06 已采用“复杂问题先第一性原理、交付前对抗式审查”的开发准则。`AGENTS.md` 只保留短硬规则，详细执行方法放在 `docs/dev-thread-rules.md`；该规则不等于默认加载额外 skill，也不等于每轮开启多 Agent。默认只在复杂 bug、跨模块数据流、财务/仓库/权限/发布问题、高风险状态流转、权限裁剪、财务口径和 47 发布前触发；小型文案、局部样式、字段显隐、测试断言修正不强制展开完整审查。

## 当前 Agency custom agents 按需路由规则

2026-07-07 已把 10 个 Agency custom agents 集成到 sunny 开发总则和 Codex 全局总则。默认仍不调用 agents，不增加启动上下文；按任务内容自动最小匹配，默认 1 个主 agent，必要时追加 1 个验证/审查/安全 agent。已配置场景：Frontend Developer、Backend Architect、Rapid Prototyper、Codebase Onboarding Engineer、Code Reviewer、Database Optimizer、API Tester、Evidence Collector、Incident Response Commander、Application Security Engineer。禁止默认全量调用；只有用户明确要求“全局对抗式审查 / 多 Agent 审查 / 并发审查”才扩大范围。

## 当前 2026-07-06 亮崽报价兼容/市场待排货审核修改已发布 47

2026-07-06 已发布本轮更新到 47。实现范围：亮崽报价兼容接入思远 `报价查价`，新增 `LegacyPricingSource/LegacyPricingRow` 与 `/api/pricing/legacy/*` 四类查价、报价源管理/重建/体检接口，新增容器内亮崽数据副本导入脚本；报价查价页新增 `亚马逊查询 / 欧洲海运超大件查询 / 欧洲空海运铁路快递查询 / 南非专线查询` 模块；`市场管理 -> 待排货` 操作改为 `审核 / 修改`，字段完整时审核直接进入 `已排货/待出库`，字段不完整时打开排货修改弹窗；仓库待排货保持只读；新增客户收货人 `fbaWarehouseCode` 迁移并同步录单/基础资料相关展示与测试。发布前补齐测试断言以匹配新的 `审核/修改` 操作和待审核默认隐藏列；亮崽导入脚本改为批量 `createMany`，避免 47 容器导入大文件时 OOM。

验证通过：`npm run prisma:generate -w @siyuan/api`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/shared -- --run src/domain.test.ts` 29/29、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 11/11、`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx` 8/8、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 16/16、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 13/13、`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx` 21/21、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "isolates staff menus|defaults new customer ownership"` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|pricing|lookup|legacy"` 8/8、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "tally"` 1/1、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data|blocks customer and service users|master data|customer master data"` 3/3、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞、导入脚本独立 `tsc --noEmit` 通过。

发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`prisma migrate deploy` 成功并应用 `20260706203000_customer_contact_fba_code`；`docker compose up -d --remove-orphans api web` 成功。47 `/opt/quote-app` 只读挂载导入亮崽数据成功：`data/quotes.json` 429122 行、`inquiry_data/prices.json` 7798 行、`europe-express-data/prices.json` 7390 行、`europe-truck-data/prices.json` 2 行、`south-africa/rates.json` 22 行，`south-africa/prices.json` 与 `south-africa/data.json` 不存在已跳过。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Web 构建仍有 `antd-vendor` 大 chunk warning；Prisma major update notice、npm major notice 仍是提示；未做浏览器截图巡检。

## 当前亮崽报价兼容接入思远报价查价

2026-07-06 本地完成亮崽报价兼容接入思远 `报价查价` 模块，未发布 47。实现范围：新增 `LegacyPricingSource/LegacyPricingRow` 兼容数据表与迁移；新增 `/api/pricing/legacy/*` 四类查价、报价源管理、重建和数据体检接口；新增亮崽数据副本导入脚本 `npm run pricing:legacy:import -w @siyuan/api -- --source /opt/quote-app`，只写入思远数据库，不修改 `/opt/quote-app`；查价链路以亮崽副本成本为成本源，再套用思远现有代理加价规则生成业务报价，管理员可见成本/加价/毛利，非管理员隐藏成本；前端报价查价页加入 `亚马逊查询 / 欧洲海运超大件查询 / 欧洲空海运铁路快递查询 / 南非专线查询` 模块入口、兼容报价结果、卡片摘要和详情弹窗，保留产品附加与特别说明/尺寸要求。验证通过：`npm run build -w @siyuan/shared`、`npm run prisma:generate -w @siyuan/api`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 11/11、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|pricing|lookup|legacy"` 8/8、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、脚本独立 `tsc --noEmit` 语法检查通过。剩余：未接入 47 真机数据导入实测，未发布 47，当前代码未 git stage/commit；Web 构建仍有既有 `antd-vendor` 大 chunk warning。

## 当前市场待排货审核/修改按钮

2026-07-06 本地完成 `市场管理 -> 待排货` 操作列按钮调整，未发布 47。实现范围：待排货表格操作列从单一 `排货/排货日志` 调整为 `审核 / 修改` 两个按钮；`修改` 复用现有市场排货弹窗补齐代理、代理渠道、计费重、单价等字段；`审核` 复用现有 `POST /api/shipments/:id/route`，在当前行已有完整排货字段时直接把运单推进到 `WAITING_DISPATCH`，同步出现在市场 `已排货` 和仓库 `待出库`，字段不完整时自动打开修改弹窗并提示先补齐；仓库待排货仍为只读，不开放审核/修改。验证通过：`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx` 8/8。未通过：`npm run typecheck -w @siyuan/web` 仍被当前工作树既有基础资料/录单共享类型不一致阻塞（`fbaWarehouseCode/customerType/customerShortName/receiverEmail/customerTypeOptions` 等），与本轮待排货按钮调整无关；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "pending routing"` 中管理员仓库待排货用例仍因既有区域定位问题失败，仓库角色只读待排货用例通过。剩余：未发布 47，当前代码未 git stage/commit。

## 当前 2026-07-06 待排货/理货标签/代理资料/历史价格兼容已发布 47

2026-07-06 已发布本轮待排货字段同步、理货标签扫描覆盖与历史归档、代理资料字段/权限补差、历史价格兼容表、仓库/查价/客户范围和提示文案修正到 47。实现范围：市场与仓库待排货共用列工厂，市场可排货/看日志，仓库只读；已完成理货支持标签扫描生成理货后新在仓包裹、原包裹归档隐藏，重复扫描幂等返回既有包裹；代理资料主表收敛严格字段，发票模板上传权限收紧为 `master-data:agents:write`；新增 `LegacyPricingSource/LegacyPricingRow` 兼容历史价格数据；墨家设备时间戳、缺少快递单号条码、业务员客户范围、查价最快推荐、价格表批量删除、专线问题件筛选和多处冗余文案同步修正。验证通过：`npm run prisma:generate -w @siyuan/api`、`npm run build -w @siyuan/shared`、`npm test -w @siyuan/shared -- --run src/domain.test.ts` 29/29、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx` 7/7、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 16/16、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 11/11、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 13/13、`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx` 21/21、`npm test -w @siyuan/web -- --run src/modules/shared/ui-table.test.tsx` 1/1、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "isolates staff menus|defaults new customer ownership"` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "tally"` 1/1、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data|blocks customer and service users|master data|customer master data"` 3/3、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|pricing|lookup|fastest"` 8/8、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`prisma migrate deploy` 成功并应用 `20260706170000_tally_label_apply_archive`、`20260706193000_legacy_pricing_compat`；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Web 构建仍有 `antd-vendor` 大 chunk warning；Prisma major update notice 仍是提示；未做浏览器截图巡检。

## 当前待排货字段同步

2026-07-06 本地完成 `市场管理 -> 待排货` 与 `仓库管理 -> 待排货` 字段同步，未发布 47。实现范围：新增共享待排货列工厂，两个模块统一展示 `日期 / 站点 / 业务员 / 客户编号 / 运单号 / 业务渠道 / 货物数据 / 业务成本 / 业务成本合计 / 选项 / 代理 / 代理渠道 / 应付成本 / 应付合计 / 操作`；市场待排货保留 `排货` 和 `排货日志`，仓库待排货同字段只读显示 `待市场排货/只读`，不再从仓库入口触发排货；仓库页接入现有业务成本摘要仅用于展示；测试桩补齐理货标签扫描接口，保持仓库测试链路可跑。验证通过：`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx` 7/7、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 16/16、`npm run typecheck -w @siyuan/web`。剩余：本轮未做浏览器截图巡检，未发布 47，当前代码未 git stage/commit。

## 当前已完成理货标签扫描覆盖与历史归档

2026-07-06 本地完成 `仓库管理 -> 已完成理货 / 已完成理货历史 / 在仓数据` 标签扫描覆盖与历史归档，未发布 47。实现范围：复用 `WarehousePackage`、`WarehouseTallyTask`、在仓查询、理货标签和审计日志；新增最小归档字段与迁移 `20260706170000_tally_label_apply_archive`；`已完成理货` 默认显示近 1 个月，新增 `已完成理货历史` 查看全部已完成记录；新增 `POST /warehouse/tally-tasks/label-scan`，扫描已完成理货标签后生成理货后新在仓包裹、原包裹置为 `TALLIED_ARCHIVED` 并从默认在仓隐藏，重复扫描返回既有新包裹；新包裹继续在仓流转，并在在仓组合号旁显示 `理` 标记，业务管理录单选择包裹时也显示 `理` 标记；仓库可扫描应用，业务员只读。验证通过：`npm run build -w @siyuan/shared`、`npm run prisma:generate -w @siyuan/api`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "tally"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 16/16。未通过：完整 `USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts` 仍在既有承运商标签用例的财务审核 `approveForRouting` 返回 403，与本次理货标签扫描链路无关。剩余：未发布 47，当前代码未 git stage/commit。

## 当前代理资料严格字段与权限补差

2026-07-06 本地完成 `基础资料库 -> 代理资料` 严格字段与功能补差，未发布 47。实现范围：代理资料主表默认收敛为 `代理简称 / 代理详细公司名 / 仓库地址一 / 仓库地址二 / 仓库地址三 / 仓库联系人 / 发票模板`；`代理编码 / 状态 / 对接类型` 保留为兼容信息，默认不占主表和主筛选，列表设置可打开列，新增/编辑弹窗放入折叠的兼容信息；新增代理不再强制填写代理编码，后端继续按现有默认生成兼容编码；代理发票模板上传接口权限从泛用 `master-data:write` 收紧为 `master-data:agents:write`；API 用例补充管理员上传 `.xlsx`、拒绝 PDF、客服/业务无代理权限上传 403。验证通过：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "isolates staff menus|defaults new customer ownership"` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data|blocks customer and service users"` 1/1。剩余：`npm run typecheck -w @siyuan/api` 和 `npm run build -w @siyuan/api` 仍被当前工作树既有 `apps/api/src/modules/prisma.repository.ts:3209 beforeTask.labelNo` 类型错误阻塞；完整 settings master data 测试仍被既有 `何俊妮` 断言阻塞；当前代码未 git stage/commit。

## 当前 2026-07-06 仓库/查价/客户范围修正已发布 47

2026-07-06 已发布本轮仓库、查价、专线池、客户范围和提示文案修正到 47。实现范围：墨家设备兼容毫秒时间戳和异常/缺失时间兜底，支持 `J721-` 这类缺少快递单号的条码以 `待补充` 入库；手动收货时间按北京时间输入并转换为 ISO，避免本地时区偏移；业务员客户资料范围移除历史兜底的 `operator/业务员`，只看本人用户名/姓名/昵称归属客户；专线运单池 `问题件` 筛选只包含真实问题件/问题工单，风险数仍统计滞留、缺转单等风险；查价最快推荐不再把无时效报价当作最快；价格表管理支持多选批量删除，单选时保留备注修改；非管理员查价页隐藏代理成本价和加价规则指标；计费输入框宽度与后缀挤压做了最小样式修正；同步修正仓库测试中已移除的在仓录单入口断言；财务、运营、订单、基础资料和系统管理页去掉非必要说明文案。验证通过：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/shared -- --run src/domain.test.ts` 27/27、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 11/11、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 17/17、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|pricing|lookup|fastest"` 8/8、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"` 1/1、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "master data|customer master data"` 3/3、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`prisma migrate deploy` 成功且 59 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Web 构建仍有 `antd-vendor` 大 chunk warning；Prisma major update notice 仍是提示；未做浏览器截图巡检。

## 当前墨家 measuredAt 时间戳兼容

2026-07-06 已发布墨家设备 `measuredAt` 毫秒时间戳兼容到 47。原因：厂家真实报文 `measuredAt: "1783323455600"` 导致 Prisma `scanTime: Invalid Date`，接口返回 `result:false` 且不入库。实现范围：墨家入口将数字/数字字符串时间转换为 ISO；不可解析时间回退为系统接收时间；仓库包裹 Prisma 创建处对无效 `scanTime` 兜底为当前时间。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"`；`npm run typecheck -w @siyuan/api`；`npm run build -w @siyuan/api`。47 已同步并重建/重启 api，`/api/health` 正常；按厂家真实报文复测 `SHB056-KK000034086467` 返回 `录入成功`，真库 `WarehousePackage` 已入库，`scanSource=墨家设备`。

## 当前代理加价规则代理聚合与线路弹窗修复

2026-07-06 已发布 `报价查价 -> 代理加价规则` 代理聚合与线路弹窗修复到 47。实现范围：主表继续按代理聚合显示，避免把代理下所有渠道/线路/国家明细铺在主表；`查看线路` 从右侧抽屉恢复为中央大弹窗，展示该代理下所有价格表线路，保留按小表筛选、批量业务员加价、批量统一加价、单条 `自定义加价/修改加价`；前端保留聚合规则 rows 与 `detail=true` 明细规则缓存，保证线路自定义加价仍优先于代理统一加价；行内编辑/停用/删除聚合代理时会解析真实规则 ID，不再把 `agent:xxx` 伪 ID 直接提交；代理筛选合并当前价格表明细中的代理和已加载规则代理，避免加价规则分页时新导入代理不出现在筛选项；项目总则补充 UI 重构不得移除或弱化已有业务操作链路。验证通过：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10、`npm run typecheck -w @siyuan/api`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|pricing|lookup"` 7/7、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`prisma migrate deploy` 成功且 59 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。发布重启瞬间 `/api/health` 曾返回一次 502，API 就绪后复查正常。剩余：未启动浏览器做 1366/1728 视觉截图复核，当前代码未 git stage/commit；Web 构建仍有 `antd-vendor` 大 chunk warning；Prisma major update notice 仍是提示。

## 当前 2026-07-06 代理加价/待付款/财务术语/查价推荐/市场应付已发布 47

2026-07-06 已发布 `报价查价 -> 代理加价规则` 工作台、`财务管理 -> 待付款` UI 优化、财务应付链路术语收敛、查价结果筛选推荐修正、市场排货代理成本进入应付审核到 47。发布前修正了 AgentMarkup 列表响应兼容、加价规则默认字段和测试桩预览响应，保证 Web/API 类型检查与定向测试通过。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 13/13、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing|payable|business data"` 1/1、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "manages payable audits"` 1/1、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`。发布：`npm run sync:47` dry-run 与 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web`、`prisma migrate deploy`、`docker compose up -d --remove-orphans api web` 成功；后续同步最终 Web 修正并重建/重启 web 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Web 构建仍有 `antd-vendor` 大 chunk warning；Prisma major update notice 仍是提示；未做浏览器截图巡检。

## 当前代理加价规则工作台落地

2026-07-06 本地完成并已发布 47 `报价查价 -> 代理加价规则` 工作台落地。实现范围：`AgentMarkupRule` 扩展 `markupType/markupValue/priority/deletedAt`，新增迁移 `20260706090000_agent_markup_workbench` 并保留 `markupPerKg` 兼容；后端规则列表返回 KPI、rows 和 pagination，新增命中预览、导入、导出，删除改软删除，停用只改启用状态；查价排除停用/删除规则，按优先级、范围具体度和更新时间命中，并支持按重量、按票、固定金额和按比例加价；前端代理加价规则页改为顶部 KPI、筛选区、规则宽表、行内查看线路/编辑/停用/启用/删除和右侧规则命中线路抽屉。验证通过：`npm run build -w @siyuan/shared`、`npm run prisma:generate -w @siyuan/api`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "markup|pricing|lookup"` 7/7。剩余：未启动浏览器做 1366/1728 视觉截图复核，当前代码未 git stage/commit。

## 当前待付款页面完整 UI 优化

2026-07-06 本地完成并已发布 47 `财务管理 -> 待付款` 完整页面和弹窗优化，未改 API、未改数据库、未改权限模型。实现范围：待付款筛选区改为紧凑财务筛选条，保留代理、业务员、客户编号、运单号、应付费用、币种、金额、收款方、银行账号、申请付款日期起止、状态、备注等全部字段；批量操作区改为付款工作台命令条，展示已选条数、合并规则、跨组警告、已选金额和列表合计金额；表格金额、凭证、状态和选中行增强扫读；`生成待支付申请` 弹窗按已选付款组摘要、收款银行、手动银行信息、供应商账单截图、备注分区；`记录对账单凭证` 与 `对账单凭证预览` 弹窗补充上传说明、间距和图片预览约束；撤回确认仍复用现有 Popconfirm 和 `财务撤回付款申请` 原因。验证通过：`rg -n "生成待支付申请|记录对账单凭证|对账单凭证预览|finance-pending-filter|finance-modal" apps/web/src/modules/finance apps/web/src/styles.css`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "pending payments"`、`npm run typecheck -w @siyuan/web`。

## 当前财务应付链路术语收敛

2026-07-06 本地完成并已发布 47 财务应付链路前端术语与入口收敛。实现范围：财务菜单、看板快捷入口和应付审核页把 `应付审核` 改为 `市场应付审核`；`代理账单核对` 改为 `代理账单`；`已付款` 入口改为 `待支付/已支付`，页面状态文案同步为 `待支付/已支付`；市场应付审核成功后弹窗引导进入代理账单，待付款提交动作改为 `生成待支付申请`。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 13/13、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "manages payable audits"`、`npm run typecheck -w @siyuan/api`。后端状态枚举、数据库表和付款申请主链路未改，当前仍保留应付审核后生成待付款队列的兼容行为；如要严格阻断“代理账单未核对不能进待付款”，需后续单独加代理账单核对状态门禁。

## 当前查价结果筛选推荐修正

2026-07-06 本地完成 `报价查价 -> 价格表管理` 批量勾选删除：价格表表格从单选改为多选，支持勾选多张价格表后一次确认删除；单张时保留修改备注能力，多选时禁用备注修改；删除后同步移除对应导入报价行并刷新加价规则。本轮未发布 47。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 11/11；`npm run typecheck -w @siyuan/web`。

2026-07-06 本地修复并已发布 47 `报价查价 -> 查价` 结果区筛选推荐：把“最低宜”统一改为“最便宜”；顶部推荐报价从固定展示最便宜改为跟随当前筛选切换，点击“最快”时展示最快推荐，点击“最便宜/有备注/含税/不含税”时展示对应第一条匹配结果；`复制推荐报价` 同步复制当前顶部展示结果。本轮只改前端展示和测试，未改查价接口、价格计算、数据库。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10；`npm run typecheck -w @siyuan/web`。

2026-07-06 本地修复 `报价查价 -> 查价` 最便宜和最快内容重复：后端 `fastestRecommendations` 只纳入有明确 `transitDays` 的报价；如果价格表全是 `时效待确认`，不会再用价格排序结果冒充最快，前端 `最快` 筛选在无明确时效时禁用。本轮未发布 47。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "transit time is unknown"`；`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`。

2026-07-06 已发布 `报价查价 -> 代理加价规则` 代理筛选与价格表管理不一致修复到 47：代理下拉不再只取当前已加载的加价规则页，而是合并当前价格表明细中的代理和已加载规则代理，避免加价规则分页时新导入的 `驰汉/振韵/亿阳` 等代理不出现在筛选项。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10；`npm run typecheck -w @siyuan/web`。

## 当前市场排货代理成本进入应付审核

2026-07-06 本地完成并已发布 47 市场排货填写的 `代理成本` 应付费用进入财务应付审核。实现范围：保留业务员录单应付费用需客服数据确认后再进入财务审核的门槛；市场排货生成、且可由 `shipment.route` 审计匹配的 `代理成本` 不再等客服确认，排货完成后即可出现在 `财务管理 -> 应付审核` 并允许财务审核。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing|payable|business data"`；`npm run typecheck -w @siyuan/api`。

## 当前 2026-07-06 规则/专线池/客服转单/查价修正已发布 47

2026-07-06 已发布项目规则更新、`运营工作台 -> 专线运单池` 优化、客服数据确认后进入待离港与转单号维护池修正、查价计算器严格对图修正到 47。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞、`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx` 21/21、`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx` 10/10、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "operations|line shipment|workspace|customer service|business data|transfer"` 3/3、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web` 均通过。发布：`npm run sync:47` dry-run 和 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`db-migrate` 成功且 58 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；Web 构建仍有 AntD vendor 大 chunk warning；Prisma major update notice 仍是提示。

## 当前项目规则更新

2026-07-06 本地完成第一轮 `frontend-design` 财务模块 UI 优化，本轮未发布 47、未改 API、未改数据库、未改权限模型。实现范围：财务审核页、单票费用、待付款、已付款、水单匹配、代理账单和财务资料库统一接入财务工作卡片/状态条/弹窗样式；弹窗、凭证预览、付款申请、费用编辑、匹配水单、记录凭证等隐藏界面补宽度、分区感、按钮区和表单密度；保持复用现有 `apiClient`、`ManagedTable`、`VoucherImageInput`、列设置、权限与日志链路。验证通过：`rg -n "Modal|Popover|Popconfirm|Upload|凭证|审核|付款|匹配|作废|反审" apps/web/src/modules/finance`、`npm test -w @siyuan/web -- --run src/modules/finance/entryModel.test.ts` 5/5、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 13/13、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/web`。说明：首次完整 finance 测试中代理账单用例触发 5s 单测超时，单独复跑通过，随后完整复跑通过；Web 构建仍有既有 `antd-vendor` 大 chunk warning；本机没有 Playwright 依赖，本轮未做浏览器截图巡检。

2026-07-06 已补充 Sunny 前端准总规则：`frontend-design` 不只覆盖一级页面，也覆盖用户可见、可点击、可提交或会影响业务状态的弹窗、抽屉、详情、上传、预览、确认框、行内操作、字段/列设置、二级/三级入口，以及空态、错误态、加载态、无权限态、禁用态。后续开发坚持最低修改原则，优先复用现有接口、功能、组件、权限、日志和状态流转；只有现有能力无法承载真实字段、无法保证后端权限裁剪、缺少必要状态流转或审计、或复用会造成越权/财务口径错误/追溯断裂时，才新增接口、字段、组件或后端逻辑。已落地模块允许本地 UI 重构，但默认先只落本地，用户检查通过后再按 47 发布流程发布；UI 重构不得顺手改变业务口径、数据库结构、线上数据、权限模型或接口契约。

2026-07-06 已把 `frontend-design` 写入思远物流开发总规则：每次新建前端模块、二级功能、工作台、表单、详情、看板或明显重塑已有页面时，默认先按 `.agents/skills/frontend-design/SKILL.md` 做设计判断；Sunny 后台仍以物流内部运营效率为先，字段清楚、表格可扫读、操作路径短、权限状态明确，小型 bugfix/文案/字段显隐/测试修复不强制重新做完整设计方案。本轮只改规则文档和状态，不改业务代码。

## 当前专线运单池优化落地

2026-07-05 已发布 `运营工作台 -> 专线运单池` 截图工作台优化到 47。实现范围：新增 `/api/operations/line-shipments`，后端基于可见运单返回 KPI、状态聚合、分页 rows，并沿用现有角色裁剪；shared 新增 LineShipmentPool 类型和聚合 helper；前端专线运单池改为顶部 4 KPI、今日完成率、状态分组筛选、查询/重置、批量动作区、固定右侧操作列和横向滚动宽表；缺少完整后端闭环的批量动作只提示待接入，不假装成功。验证通过：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx` 21/21、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "operations|line shipment|workspace"` 2/2、`npm run typecheck -w @siyuan/api`。剩余：未启动浏览器做 1366/1728 视觉截图复核，未 git stage/commit。

## 当前客服数据确认到待离港与转单号工具池修正

2026-07-05 已发布 `客服管理` 数据确认到待离港和转单号维护池修正到 47。实现范围：数据确认保留 5 个字段确认和 `customer_service.business_data.approved` 审计，确认后追加运营状态更新到 `WAITING_DEPARTURE`，提示 `数据确认完成，已进入待离港` 并切到 `待离港`；`转单号` 二级目录改为已完成业务数据确认的客服生命周期维护池，覆盖 `OUTBOUNDED / WAITING_DEPARTURE / DEPARTED / ARRIVED_PORT / DELIVERING / SIGNED`，操作列只保留 `修改转单号`、`上传面单`；后端 Prisma/InMemory 允许无转单号进入 `WAITING_DEPARTURE`，修改转单号仍要求业务数据确认，面单上传允许后续客服生命周期状态补传。验证通过：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx` 10/10；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "customer service|business data|transfer"` 1/1；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`。剩余：未 git stage/commit。

## 当前查价计算器严格对图修正

2026-07-05 已发布 `报价查价 -> 查价` 计算器布局未严格对图问题修正到 47。根因：新计算器表单同时带有旧 `pricing-lookup-form` class，旧 CSS 仍把整个 Form 当两列 grid，导致右侧自动计费重面板覆盖/挤压左侧字段。修正：`pricing-calculator-form` 显式覆盖为 block 布局，清掉旧 grid；计算器主体改为左侧表单 + 右侧 360-400px 固定面板，右侧面板不再压到字段；目的地控件改成带国旗的可搜索 Select，更接近截图。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10、`npm run typecheck -w @siyuan/web`。剩余：未做浏览器截图复核。

## 当前 2026-07-05 查价计算器/应收时间口径已发布 47

2026-07-05 已发布 `报价查价 -> 查价` 报价计算器截图优化、查价工作台截图落地、应收审核制单时间口径修正到 47。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10、`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts` 7/7、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "business cost audits|persists single-shipment finance items"` 2/2、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "business cost audits|persists single-shipment finance items"` 2/2、`npm run test:web:finance` 18/18、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞均通过。发布：`npm run sync:47` dry-run 和 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`db-migrate` 成功且 58 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：本轮未启动浏览器做 1366/1728 视觉截图复核；当前代码未 git stage/commit；Web 构建仍有 AntD vendor 大 chunk warning；Prisma major update notice 仍是提示。

## 当前查价报价计算器截图优化

2026-07-05 已发布 `报价查价 -> 查价` 从工作台结果页改为报价计算器截图效果到 47。实现范围：顶部保留 `重新查询 / 复制推荐报价 / 查询报价`，复制和导出在查询前禁用；新增代理成本价、加价规则、最近查价、今日查询信息条；主卡改为左侧基础信息/计费信息/尺寸补充表单 + 右侧自动计费重和校验状态；报价结果容器查询前展示空状态和表头预览，查询成功后展示推荐报价条和统一报价表，不再显示右侧报价判断面板；前端自动计费重按 `max(实重, CBM x 167, 长宽高件数/6000)` 计算，人工计费重有提示；后端 Prisma/InMemory 查价同步按表单重量、方数、实重、尺寸重新取最大值，避免只信前端传入计费重；共享 `PriceLookupRequest` 补充计费输入字段。验证通过：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts` 7/7。剩余：本轮未启动浏览器做 1366/1728 视觉截图复核。

## 当前应收审核制单时间口径修正

2026-07-05 已发布应收审核“制单日期/制单时间”口径修正到 47。业务员自审核通过进入应收审核的数据，后端应收审核摘要 `createdAt` 改为使用运单 `businessReviewedAt`，页面沿用现有 `制单日期` 列并显示完整北京时间；手工在自审核后新增的应收费用仍使用费用自身创建时间，避免被旧自审时间覆盖。同步修正测试链路中旧财务终审调用，当前流程为业务自审后直接进入 `WAITING_SORT`。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "business cost audits|persists single-shipment finance items"`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "business cost audits|persists single-shipment finance items"`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run build -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run test:web:finance`、`npm run build -w @siyuan/web`。同时修复已有查价页 JSX 闭合与缺失图标导入，避免 Web 类型检查/构建阻塞。

## 当前查价工作台截图落地

2026-07-05 已发布 `报价查价 -> 查价` 工作台截图效果落地到 47。实现范围：查价页顶部改为 `重新查询 / 复制推荐报价 / 查询报价`，查询后展示品名、目的地、邮编、亚马逊代码、计费重、方数摘要；输入区收敛为紧凑横向表单；结果区改为推荐报价卡 + 报价结果表 + 右侧报价判断面板，支持推荐、最低宜、最快、有备注、含税、不含税标签筛选；行操作保留选用报价、详情、复制，选用仅前端选中并提示；非管理员隐藏成本、毛利和加价规则入口，管理员可查看成本/毛利并跳转代理加价规则。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10；`npm run typecheck -w @siyuan/web`。浏览器验证：本地 API/Web + Chrome CDP 检查 1366x768 与 1728x900，报价结果表、查询摘要、右侧报价判断可见，body 无横向外溢，表头无竖排，操作按钮无裁切。剩余：本轮未改代理加价规则/价格表管理，视觉验证用本地临时导入的内存价格行和加价规则触发报价结果，未写入代码。

## 当前 2026-07-05 公司渠道/客服详情/客户资料已发布 47

2026-07-05 14:31 复核发布：本地重新验证 `shared` build、API/Web typecheck、客服/系统/报价 Web 定向测试 33/33、客户资料 API 定向测试 2/2、API/Web build、`npm audit --omit=dev --json` 0 漏洞；`npm run sync:47` dry-run 与 `--apply` 通过；47 `docker compose build db-migrate api web`、`prisma migrate deploy`、`docker compose up -d --remove-orphans api web` 均成功；`/api/health` 正常、首页 `200 OK`、API 日志包含 `Nest application successfully started`。

2026-07-05 已发布公司渠道无承运商时无法新增修复、客服数据确认查看详情、客户资料截图界面落地到 47。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx src/modules/settings/settings.test.tsx` 23/23、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets business users maintain only their own customer master data|lets admins maintain master data"` 2/2、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞均通过。发布：`npm run sync:47` dry-run 和 `npm run sync:47 -- --apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`db-migrate` 成功且 58 个迁移无待应用；首次 `docker compose up` 因旧 API 容器重建残留出现容器名冲突，确认后清理重跑成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`，最近 API 日志无业务错误。剩余：当前代码未 git stage/commit；settings 测试仍有既有 `useForm` warning；Web 构建仍有 AntD vendor 大 chunk warning；Prisma major update notice 仍是提示。

## 当前公司渠道无承运商时无法新增修复

2026-07-05 已发布 `基础资料库 -> 公司渠道` 在 47 承运商资料为空时 `增加` 按钮禁用导致无法新增的问题到 47。根因：前端把 `增加公司渠道` 禁用条件写成 `!canWriteChannels || masterData.carriers.length === 0`，而线上当前公司渠道/承运商为空；后端创建公司渠道也强制必须已有 `carrierId`。实现范围：前端只按写权限禁用新增；无承运商时新建弹窗显示承运商文本输入，提交 `carrierName`；后端 Prisma/InMemory 创建公司渠道时如果没有 `carrierId` 但有 `carrierName`，自动复用或创建承运商再创建公司渠道；shared 类型补充 `ChannelCreateInput.carrierName`。验证通过：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets business users maintain only their own customer master data|lets admins maintain master data"`。

## 当前客服数据确认查看详情

2026-07-05 已发布 `客服管理 -> 数据确认` 增加运单详情查看到 47。实现范围：数据确认宽表操作列新增 `详情`，保留原 `确认` 逻辑；点击详情弹出只读 `运单详情`，展示运单创建时间、出库时间、业务员、客户编号/名称、目的地、品名、件数、实重、计费重、代理计费重、报关、敏感、代理、业务渠道、代理渠道、出货单号、转单号、状态、最新轨迹、备注，缺失显示 `-`；详情关闭不影响当前数据确认弹窗和页面状态。验证通过：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx` 10/10；`npm run typecheck -w @siyuan/web`。

## 当前客户资料界面截图落地

2026-07-05 已发布 `基础资料库 -> 客户资料` 截图效果落地到 47。实现范围：客户资料页顶部 KPI 改为客户资料、启用客户、缺结算信息、收货人；主区域改为左侧客户表格 + 右侧固定客户详情，筛选收敛为客户名称/编码、客户类型、状态、业务员，表格保留详情、编辑、停用/启用、删除；右侧展示基础信息、结算信息、收货人、最近订单和最近变更，默认选中当前筛选第一条。停用继续走 `PUT /api/master-data/customers/:id/enabled`；新增受保护 `DELETE /api/master-data/customers/:id`，存在运单、水单、客户账单、非零客户账户余额或客户账号时拒绝删除，无引用时删除客户、收货人和客户账户并写审计日志。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 13/13；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets business users maintain only their own customer master data|lets admins maintain master data"` 2/2。剩余：settings personal center 测试仍有既有 `useForm` 未连接 warning；当前代码未 git stage/commit。

## 当前代理发票模板与业务上传发票流程

2026-07-05 已发布“市场排货后同时进入仓库待出库和待业务上传发票”的代理发票第一阶段闭环到 47。实现范围：代理资料的发票模板上传从图片改为 `.xls/.xlsx` Excel 上传，后端拒绝图片/PDF/CSV/`.xlsm` 并做基础文件头校验；新增运单业务发票持久化字段和迁移 `20260705162000_add_shipment_business_invoice`；业务管理新增 `待上传发票` 二级页，按已选代理的运单展示代理模板下载、待上传状态、业务发票下载与上传入口；业务上传发票走真实 `/api/shipments/:id/invoice/upload`，写入运单字段并记录 `shipment.business_invoice.upload` 审计；上传完成后不阻断仓库待出库流程，仓库仍按排货后 `WAITING_DISPATCH` 正常处理。验证通过：`npm run build -w @siyuan/shared`；`npm run prisma:generate -w @siyuan/api`；`npm run typecheck -w @siyuan/api`；`npm run typecheck -w @siyuan/web`；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "invoice|routing|dispatch"`。剩余：当前未做 Excel 单元格红字/红底样式必填解析，现阶段只做 Excel 类型和文件头校验；`npm test -w @siyuan/web -- --run src/modules/masterData src/modules/orders` 中既有 `orders.test.tsx` 仍找不到旧的 `运单管理` 按钮，和本次发票上传链路无关；当前代码未 git stage/commit。

## 当前录单业务渠道保存修复

2026-07-05 已发布录单页已选择“业务渠道”但待审详情仍提示“渠道缺失”的修复到 47。根因：前端只提交了渠道名称 `receivingChannel`，Prisma 录单创建只持久化 `channelId`，导致线上订单没有关联渠道；待审详情按 `Shipment.channelName/carrier` 判断，所以显示缺失。实现范围：前端提交时按已选业务渠道补 `channelId`；后端 Prisma 录单创建/草稿更新兼容按 `receivingChannel` 名称反查启用渠道并落 `channelId`；内存仓储同步按渠道名称映射，避免测试环境与真实库不一致。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "shows order-entry fee fields"`；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "order-entry submissions"`；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`。

## 当前市场看板本周代理指标

2026-07-05 已发布 `市场管理 -> 市场看板` 新增 `本周出货代理数量` 和 `本周新代理` 两个卡片到 47。实现范围：复用前端已加载运单数据，不新增接口/数据库；本周出货代理数量按本周 `outboundAt` 且有代理的出货次数统计，同一代理多票按多次；本周新代理按当前数据中代理首次出货时间落在本周的去重代理数统计。验证通过：`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx` 7/7；`npm run typecheck -w @siyuan/web`。

## 当前录单重复运单号提示修复

2026-07-05 已发布录单使用重复运单号时返回 `Internal server error` 的修复到 47。47 日志确认失败原因为 `Shipment.systemOrderNo` 唯一约束冲突，截图中的 `SYOUT000007` 已存在于线上库，状态为 `REVIEW_PENDING`。实现范围：Prisma/InMemory 录单创建和草稿更新前先校验运单号唯一，重复时返回 `400 运单号 xxx 已存在，请更换后再提交`，不再落到 Prisma P2002 500；补充 API 回归覆盖重复运单号录单。验证通过：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "order-entry submissions"`；`npm run typecheck -w @siyuan/api`。

## 当前仓库待出库麦头提示

2026-07-05 已发布市场排货到仓库待出库的麦头提示闭环到 47。实现范围：市场排货弹窗新增 `需要贴麦头` 勾选；运单新增持久化字段 `shippingMarkRequired` 和迁移 `20260705150000_add_shipment_shipping_mark_required`；排货接口保存麦头要求并写入 `shipment.route` 审计；仓库待出库队列和旧待出库订单列表增加 `麦头` 列，需贴麦头显示红色 `需贴麦头`；需贴麦头运单出货确认弹窗必须勾选 `已贴麦头`，后端 `dispatch` 接口同步校验 `shippingMarkConfirmed` 并在 `shipment.dispatch` 审计里记录。验证通过：`npm run build -w @siyuan/shared`；`npm test -w @siyuan/web -- --run src/modules/routing/routing.test.tsx` 7/7；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "sends routed shipments"` 1/1；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "creates mock carrier labels"` 1/1；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing|dispatch"` 1/1；`npm run typecheck -w @siyuan/api`；`npm run typecheck -w @siyuan/web`。剩余：`npm test -w @siyuan/web -- --run src/modules/routing src/modules/warehouse` 中仓库在仓数据相关 2 个既有用例失败（找不到录单/合票按钮），非本次待出库麦头链路；当前代码未 git stage/commit。

## 当前仓库页录单入口移除

2026-07-05 已发布仓库管理内录单入口移除到 47。实现范围：仓库管理不再显示行内 `录单`，顶部不再显示 `合票录单`，合票弹窗只做仓库合票并停留仓库页，不再跳转业务管理录单草稿；业务管理自身录单入口保留。复用现有仓库合票接口和页面状态，未改数据库、未新增接口。验证：`rg` 确认 `WarehousePage.tsx` 和 `App.tsx` 无 `合票录单/确认合票并录单/进入录单草稿/onCreateOrderEntry/openOrderEntryFromWarehouse` 残留；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "in-stock rows|order entry out"` 3/3 通过；`npm run typecheck -w @siyuan/web` 通过。

## 当前欧洲空海运铁路快递查询表单排版修复

2026-07-09 本地完成 `报价查价 -> 查价 -> 欧洲空海运铁路快递查询` 表单排版修复，本轮未发布 47。实现范围：查询条件按 `基础查询条件 / 尺寸信息 / 辅助信息` 分组；欧洲空海铁快模块专用 CSS grid 避免多个表单块叠在同一网格区域；按钮区独立右侧区域并在窄屏换行；提交字段保持目的国家、渠道、计费重量、尺寸、品名、包装等原字段不变。验证通过：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx -t "欧洲空海运铁路快递|查询条件|尺寸|渠道|计费重量|排版|重叠"`；`npm run typecheck -w @siyuan/web`；`git diff --check`。

## 当前客服看板本周新客户

2026-07-05 已发布 `客服管理 -> 客服看板` 新增 `本周新客户` 指标到 47。实现范围：前端复用已加载运单数据派生统计，不新增接口/数据库；按 `entryAt` 回退 `createdAt` 判断本周，`systemOrderNo` 包含 `001` 视为新客户单，并按 `customerCode/customerName/systemOrderNo` 去重计数；新卡片放在本周指标第一位，点击保持在客服看板。验证通过：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx` 10/10；`npm run typecheck -w @siyuan/web`。

## 当前站点资料界面优化

2026-07-05 已发布系统管理 `站点` 页面按截图继续优化到 47。实现范围：站点页顶部 KPI 改为启用站点、停用站点、绑定员工；主卡片标题改为 `站点资料`，增加紧凑工具栏和横向筛选区；表格列收敛为选择、排序、站点名称、绑定员工、状态、操作，并保留编辑与停用/启用动作，停用不再以删除文案呈现。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 13/13；`npm run typecheck -w @siyuan/web`。浏览器验证：本地 API/Web + Chrome headless 检查 1366x768 与 1728x900，站点页 body 无横向外溢，筛选区完整，表头无竖排，操作列按钮可见。剩余：settings personal center 测试仍有既有 `useForm` 未连接 warning；当前代码未 git stage/commit。

## 当前 2026-07-05 代理发票/麦头/看板/系统页更新已发布 47

2026-07-05 已发布代理发票模板与业务上传发票、录单业务渠道保存、重复运单号友好提示、仓库待出库麦头提示、仓库页录单入口移除、市场看板本周代理指标、客服看板本周新客户、站点资料界面优化到 47。验证：`npm run build -w @siyuan/shared`、`npm run prisma:generate -w @siyuan/api`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、API 定向测试 `app.orders.e2e` 2/2、`app.finance.e2e` 1/1、`app.warehouse.e2e` 1/1、Web 定向测试 `routing+customerService` 17/17、`settings` 13/13、`warehouse` 4/4、`finance` 1/1、`reports` 2/2、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞均通过。发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`db-migrate` 成功应用 `20260705150000_add_shipment_shipping_mark_required` 和 `20260705162000_add_shipment_business_invoice`，当前 58 个迁移；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；settings 测试仍有既有 `useForm` 与 AntD `addonBefore` warning；Web 构建仍有 AntD vendor 大 chunk warning；Prisma major update notice 仍是提示。

## 当前员工账号管理界面落地

2026-07-05 已发布系统管理 `用户名 / 员工账号管理` 截图效果落地到 47。实现范围：用户名页顶部 KPI 改为在职账号、停用账号、需改密、资料未完善；主卡片改为紧凑工具栏、停用/删除风险提示、横向筛选区和新员工账号表格；删除与停用同时保留，停用/启用走新增 `PUT /api/system/staff-accounts/:id/enabled`，删除继续走原 DELETE 并二次确认；员工账号列表返回 `lastLoginAt`，Prisma/InMemory 均从 LoginLog 推导最近登录。验证通过：`npm run build -w @siyuan/shared`；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 12/12；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "staff|system|account"` 5/5；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`。浏览器验证：本地 API/Web + Chrome headless 检查 1366x768 与 1728x900，body 无横向外溢，表头无竖排，筛选完整，操作列按钮可见，删除/停用动作独立展示。发布验证：`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞通过；`npm run sync:47` dry-run 和 `--apply` 通过；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；`db-migrate` 成功且 56 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功；`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：settings personal center 测试仍有既有 `useForm` 未连接 warning；Web 构建仍有 AntD vendor 大 chunk warning；Prisma major update notice 仍是提示；当前代码未 git stage/commit。

## 当前待审核运单自审前完整基础字段修改

2026-07-04 已发布业务管理 `待审核运单` 自审前基础字段修改扩展到 47。实现范围：待审核详情 `修改` 弹窗不再只补发货渠道，新增客户单号、品名、目的地、货物属性、件数、实重/计费重、体积、结算方式、报关、敏感等基础字段；保存后仍走既有 `PATCH /api/shipments/:id/operational` 写回运单，业务可直接修正后再自审，不需要重新录入；后端 Prisma/InMemory 同步支持这些字段更新，不新增路由和数据库字段。验证通过：`npm run build -w @siyuan/shared`；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "pending review"` 2/2；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "persists manual shipment edits"` 1/1；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`。

## 当前待审核运单修改补齐渠道

2026-07-04 已发布业务管理 `待审核运单` 自审前直接修改补齐渠道到 47。实现范围：待审核详情里的 `修改` 弹窗新增 `发货渠道` 下拉，复用现有渠道基础资料；保存后通过既有 `PATCH /api/shipments/:id/operational` 写回运单渠道，待审详情可继续直接自审，不需要重新录入；后端 Prisma/InMemory 的人工修改接口补充 `channelId` 更新，不新增路由和数据库字段。验证通过：`npm run build -w @siyuan/shared`；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "pending review"` 2/2；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "persists manual shipment edits"` 1/1；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`。

## 当前操作日志日期筛选美化

2026-07-04 已发布系统管理 `操作日志` 高级筛选的开始/结束时间控件美化到 47。实现范围：把原生 `datetime-local` 改为 AntD `DatePicker + showTime`，保留提交给后端的本地时间字符串格式；同步让筛选区 DatePicker 高度与现有输入框/下拉框一致。排查到系统原生日期控件还分布在仓库、基础资料、客服、财务录单、代理账单、已付款、水单、待付款等页面，本轮只处理截图对应的操作日志筛选，不批量改业务录入日期字段。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 12/12；`npm run typecheck -w @siyuan/web`。

## 当前操作日志筛选区与留白修正

2026-07-04 已发布系统管理 `操作日志` UI 紧凑修正到 47。实现范围：隐藏操作日志页前置空 Col，缩短 KPI 到日志卡片的垂直间距；失败重要操作提示改为更紧凑；筛选条允许换行，`更多筛选` 改为自适应网格，避免开始/结束时间等字段在宽屏或 1366px 下被裁切。验证通过：`npm run typecheck -w @siyuan/web`；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "audit log|role groups"` 2/2。浏览器验证：本地 Vite + Chrome headless 检查 1366x768 与 1728x900，操作日志页 body 无横向外溢，更多筛选无裁切，表头无竖排。

## 当前 2026-07-04 待审核运单修改与操作日志筛选已发布 47

2026-07-04 已发布待审核运单自审前完整基础字段修改、补齐发货渠道、操作日志筛选区紧凑修正和日期筛选美化到 47。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 12/12、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "pending review"` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "persists manual shipment edits"` 1/1、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞均通过。发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities；Web 构建成功但 AntD vendor chunk 因 DatePicker 增至约 1.146MB，重新出现 Vite 大 chunk warning；`db-migrate` 成功且 56 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：当前代码未 git stage/commit；settings personal center 测试仍有既有 `useForm` warning；Prisma major update notice 仍是提示；Vite 大 chunk warning 需后续单独拆 AntD DatePicker/locale 相关 chunk 或接受现状。

## 当前用户组管理界面落地

2026-07-04 已发布系统管理 `用户组` 截图效果落地到 47。实现范围：用户组页顶部 KPI 改为启用用户组、停用用户组、绑定员工数，数据复用现有角色与员工账号接口；主区域改为左侧用户组表格 + 右侧用户组详情面板，支持关键词/站点/状态筛选、行选中、查看权限、编辑、停用；右侧展示基础信息、绑定员工、菜单权限、数据范围和最近变更空状态/审计记录；样式仅新增用户组专用 class，不新增依赖、不改数据库和后端。验证通过：`npm run typecheck -w @siyuan/web`；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 12/12。浏览器验证：本地 Vite + Chrome headless 检查 1366x768 与 1728x900，body 无横向外溢，表头无竖排，按钮无裁切，右侧详情面板与主表正常并列。

## 当前待审核运单业务审核按钮修复

2026-07-04 已发布业务管理 `待审核运单` 审核入口修复到 47。实现范围：业务页待审核运单对业务角色继续显示 `自审通过`，管理员在业务管理页也可显式执行业务审核动作；该动作只写入 `businessReviewedBy/businessReviewedAt` 并保持 `REVIEW_PENDING`，文案仍提示进入财务终审队列，普通管理员不带业务审核标记调用审核接口仍不能替代财务终审。前后端复用现有 `/api/shipments/:id/review/approve`，仅增加可选 `businessReview` 请求体，不新增路由、不新增数据库字段。验证通过：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "pending review"` 2/2；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "business role groups|admins run business review"` 2/2；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`。

## 当前操作日志审计看板落地

2026-07-04 已发布系统管理 `操作日志` 审计看板落地到 47。实现范围：`GET /api/system/audit-logs` 返回真实 dashboard 统计，包含今日操作、失败操作、重要操作、权限/财务变更、昨日同比、14 天趋势和最近失败重要操作；内存仓储和 Prisma 仓储均按北京时间自然日统计，不新增数据库表或迁移。前端操作日志改为顶部 KPI、失败风险提示、紧凑筛选、左侧日志表和右侧审计详情面板，点击行或 `查看详情` 直接展示变更前、变更后、原始请求，不再依赖原始日志弹窗。验证通过：`npm run build -w @siyuan/shared`；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 12/12；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"` 1/1。浏览器验证：本地启动 API/Web 后在 Chrome headless 检查 1366px 与 1728px，操作日志页无竖排文本、body 无横向外溢，右侧详情面板正常显示。说明：计划中的 `-t "audit-logs|system"` 未匹配现有 API 用例名，已改跑承载审计日志断言的 `lets admins maintain master data` 用例。

## 当前 2026-07-04 用户组/审计看板/业务审核已发布 47

2026-07-04 已发布系统管理用户组界面、操作日志审计看板、业务管理待审核运单业务审核按钮到 47。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 12/12、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "pending review"` 2/2、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "business role groups|admins run business review|lets admins maintain master data"` 3/3、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞均通过。发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities，Web 构建无 Vite 大 chunk warning；`db-migrate` 成功且 56 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：settings personal center 测试仍有既有 `useForm` 未连接 warning；Prisma major update notice 仍是提示，未升级 Prisma 7；当前代码未 git stage/commit。

## 当前客户资料列表专属刷新修复

2026-07-04 已发布基础资料库客户资料列表为空但创建提示客户代码已存在的问题到 47。线上只读采证：47 真库 `Customer` 表有 10 条客户，包含 `1344/TILL/jylannie`；当前 Docker API 以 `ADMIN` token 请求 `/api/master-data` 和 `/api/master-data/customers` 均返回 10 条，说明不是管理员权限裁剪导致。实现范围：前端 `ApiClient` 增加 `customers()` 薄封装；`MasterDataPage` 进入 `客户资料` 二级页时直接请求 `/api/master-data/customers` 刷新 `masterData.customers`，避免聚合快照异常/空快照时客户列表继续空白；测试桩补齐 `GET /api/master-data/customers`。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "customer"` 2/2；`npm run typecheck -w @siyuan/web`。

## 当前录单品名保存到资料库修复

2026-07-04 已发布业务录单页“保存新的品名”点击保存无反馈/不落库问题到 47。根因：共享类型和默认资料已支持 `PRODUCT_NAME`，但 API 财务资料库输入校验仍只允许 `FEE_NAME/SETTLEMENT_METHOD/CARGO_TYPE`，导致点击保存后请求被后端拒绝，未写入数据库。实现范围：后端财务资料库创建/更新校验允许 `PRODUCT_NAME`；录单页保存成功后刷新财务资料库；保存失败时显示错误提示；补充 API/Web 回归测试覆盖品名写入。验证通过：`npm run build -w @siyuan/shared`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "finance catalog"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "shows order-entry fee fields"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`。

## 当前 2026-07-04 客户资料刷新/录单品名保存已发布 47

2026-07-04 已发布客户资料专属刷新和录单品名保存到资料库修复到 47。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "finance catalog"` 1/1、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "master data"` 3/3、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "customer"` 2/2、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "shows order-entry fee fields"` 1/1、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞均通过。发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities，Web 构建无 Vite 大 chunk warning；`db-migrate` 成功且 56 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：Prisma major update notice 仍是提示，未升级 Prisma 7；当前代码未 git stage/commit。

## 当前 2026-07-04 客户归属/操作日志/Logo 更新已发布 47

2026-07-04 已发布本轮客户资料业务员自动归属、操作日志表格紧凑排版、Logo 返回运营工作台，以及对应 workspace 测试断言修正到 47。验证：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "master data"` 3/3、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx src/modules/workspace/workspace.test.tsx` 33/33、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web`、`npm audit --omit=dev --json` 0 漏洞均通过。发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 `docker compose build db-migrate api web` 成功，API 生产依赖 prune 后 0 vulnerabilities，Web 构建无 Vite 大 chunk warning；`db-migrate` 成功且 56 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，首页 `200 OK`，API 日志包含 `Nest application successfully started`。剩余：settings personal center 测试仍有既有 `useForm` 未连接 warning；Prisma major update notice 仍是提示，未升级 Prisma 7；当前代码未 git stage/commit。

## 当前客户资料业务员自动归属修正

2026-07-04 本地修正基础资料库客户资料新建时业务员归属口径，本轮未发布 47。实现范围：客户资料弹窗中的 `业务员` 改为只读显示当前登录账号，不再允许管理员或其他角色手填；前端提交仍带当前账号用于提示，后端 Prisma/InMemory 创建客户时统一以当前登录用户名写入 `salesperson`，不再相信请求体里的业务员字段；编辑客户时不再通过弹窗输入覆盖原归属。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "customer"` 2/2；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "master data"` 3/3；`npm run typecheck -w @siyuan/web`；`npm run typecheck -w @siyuan/api`。说明：`客户代码已存在` 仍来自后端真实判重，当前列表筛选、停用状态或数据范围隐藏不代表数据库不存在该客户编码。

## 当前操作日志表格紧凑排版

2026-07-04 本地完成系统管理操作日志表格紧凑排版，本轮未发布 47。实现范围：收窄“时间/操作人/模块/执行结果/审计说明/原始日志”等列宽，操作人列启用省略，表格横向滚动改为按内容宽度计算，减少宽屏下操作人与模块之间的空白，同时保留长动作、对象和审计说明的可读空间。验证通过：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 12/12；`npm run typecheck -w @siyuan/web`。

## 当前 Logo 返回运营工作台

2026-07-04 本地完成员工端侧边栏 Logo/品牌区返回运营工作台。实现范围：品牌区改为可访问按钮，点击后复用现有菜单状态切换到 `workspace`，并回到运营工作台的专线运单池；不新增路由、不改权限、不改后端。验证通过：`npm test -w @siyuan/web -- --run src/modules/workspace/workspace.test.tsx -t "returns to the operations workspace"`；`npm run typecheck -w @siyuan/web`。本轮未发布 47。
