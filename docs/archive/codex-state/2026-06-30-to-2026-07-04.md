# Codex State Archive: 2026-06-30 to 2026-07-04

Archived on 2026-07-07 from `.codex-state.md` to keep the active state file focused on current high-signal work.

## 当前 2026-07-04 API 断言与 AntD 高频 warning 修复

2026-07-04 本地修复完整 API e2e 剩余 12 个既有断言失败，并清理财务高频测试里的 AntD 静态 `message/Modal` warning，本阶段尚未发布 47。API 修复范围：订单录单详情和财务详情中，业务员/业务组可查看本人订单的应付金额和状态，但不能看到代理名、付款编号等敏感字段；非财务/管理员录单提交应付费用时禁止提交代理名和付款编号；财务应收/应付/业务成本摘要补齐 `salesperson`；测试夹具按当前流程更新业务自审、财务终审、排货必填字段和角色权限恢复逻辑。Web 修复范围：为 AntD `App.useApp()` 接入应用级 `AntdApp` 容器，并将财务待审核、录单、应付审核、待付款、凭证上传、代理账单等高频页面的静态 message/modal 改为上下文实例；同步收窄相关测试断言，避免 App context 生成的弹层标题重复导致误判。验证通过：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run build -w @siyuan/api`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api` 53/53、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx src/modules/warehouse/warehouse.test.tsx src/modules/customerService/customerService.test.tsx` 40/40、`npm audit --omit=dev --json` 0 漏洞。剩余：React 19 与 AntD v5 兼容提示仅在尚未改造到 `App.useApp()` 的低频测试路径可能出现；完整 `npm audit --json` 的 dev 链路 `esbuild` low 项未处理。

2026-07-04 已将 API 断言与 AntD 高频 warning 修复发布到 47。执行：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，构建期间 API 生产依赖 `npm prune --omit=dev` 后 `found 0 vulnerabilities`，Web 构建继续拆出 `FinancePage/WarehousePage/CustomerServicePage` 独立 chunk，未出现 Vite 大 chunk warning；`docker compose --profile tools run --rm db-migrate` 成功，56 个迁移且无待应用；`docker compose up -d --remove-orphans api web` 成功重建服务。健康检查：`docker compose ps` 显示 `api/web/postgres/redis` 均 Up，Web 端口 `18899`；`curl http://127.0.0.1:18899/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，`curl -I http://127.0.0.1:18899/` 返回 `200 OK`，API 日志包含 `Nest application successfully started`。本次未执行 `prisma db push`、`migrate reset`、seed 或 demo seed。

## 当前 Audit 风险与构建警告优化验证

2026-07-04 本地完成 Audit 风险与 Vite 大 chunk 优化验证，本轮未发布 47，未执行 `npm run sync:47 -- --apply`。实现范围：`apps/api` 升级 Nest 10 -> Nest 11.1.27，`@nestjs/platform-express` 通过根 `overrides` 固定使用 `multer@2.2.0`，API 直接依赖同步声明 `multer@2.2.0`；`apps/web` 将 `vite` 与 `@vitejs/plugin-react` 从生产依赖移到 devDependencies；`App.tsx` 对 `FinancePage`、`WarehousePage`、`CustomerServicePage` 做页面级 lazy import；保留 `antd-vendor/excel` 分包，并在实际拆分后将 `chunkSizeWarningLimit` 从 950 调整为 1100，避免只靠阈值掩盖首屏业务大模块；批量替换 AntD `destroyOnClose` 为 `destroyOnHidden`。验证通过：`npm run typecheck -w @siyuan/api`、`npm run build -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/web`；构建产物已拆出 `FinancePage`、`WarehousePage`、`CustomerServicePage` 独立 chunk，Vite 大 chunk warning 消失；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "label|upload|review|routing"` 通过 4/4；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "payable audits"` 通过 1/1，覆盖凭证上传所在链路；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx src/modules/warehouse/warehouse.test.tsx src/modules/customerService/customerService.test.tsx` 通过 40/40；`npm audit --omit=dev --json` 为 0 漏洞；完整 `npm audit --json` 仅剩 dev 链路 `esbuild` low 1 项；`npm run sync:47` dry-run 通过。未通过/未执行：本机无 `docker` 命令，`docker compose build db-migrate api web` 未能执行；完整 `USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api` 仍有 12 个既有业务断言失败，集中在仓库/订单/财务角色权限、数据裁剪和测试数据期望，本轮依赖升级相关的定向上传/审核/排货测试已通过。剩余：本轮未安装 47 服务器 buildx 插件；AntD 静态 `message/Modal` 与 React 19 兼容 warning 仍在测试输出中，后续按高频页面逐步改 `App.useApp()`。

2026-07-04 已将上述 Audit/Vite 优化同步并发布到 47。执行：`npm run sync:47 -- --apply` 成功；47 初次 `docker compose build db-migrate api web` 成功但仍提示 buildx 未安装，随后安装系统包 `docker-buildx`，二次构建走 Bake 且无 buildx warning，`api/db-migrate/web` 均 Built；`docker compose --profile tools run --rm db-migrate` 成功，56 个迁移无待应用；`docker compose up -d --remove-orphans api web` 成功重建服务。健康检查：首次重启瞬间 `/api/health` 返回过一次 502，API 就绪后 `curl http://127.0.0.1:18899/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，`curl -I http://127.0.0.1:18899/` 返回 `200 OK`，`docker compose ps` 显示 `api/web/postgres/redis` 均 Up，Web 端口仍为 `18899`。47 构建日志中生产依赖 `npm prune --omit=dev` 后 `found 0 vulnerabilities`；Web 构建继续拆出 `FinancePage/WarehousePage/CustomerServicePage` 独立 chunk，未出现 Vite 大 chunk warning。按只读源码卷在一次性 Node 容器中运行完整 API 测试前先 `npm run build -w @siyuan/shared`，结果与本地一致：41/53 通过，12 个既有断言失败，集中在订单/仓库/财务权限、数据裁剪和测试期望；本轮未为这些业务断言改代码。AntD 静态 `message/Modal` 与 React 19 warning 属前端测试开发链路，47 线上服务不输出该类测试 warning，本轮未展开页面级 `App.useApp()` 改造。

## 当前 2026-07-03 多模块更新已发布 47

2026-07-03 已完成本地更新梳理、风险修正和 47 Docker 发布。发布范围包含：`xlsx` 高危依赖替换为 `read-excel-file/write-excel-file`；基础资料权限细分与业务/市场渠道读取权限；客户资料业务员归属；业务录单应付费用、当前登录业务员归属、业务员自审字段；财务终审权限收敛；市场排货确认后进入已排货；仓库面单/出货后客服数据确认；客服转单号、待离港、已离港、问题件协助确认与售后标注；公共表格滚动、提示、页面 UI 相关既有改动。发布前修正：删除本地和 47 上空迁移目录 `20260703043000_grant_finance_order_review_write`，避免 Prisma `P3015 Could not find the migration file at migration.sql` 阻塞；同步调整仓库/客服测试中与当前市场排货提示、已离港问题件场景不一致的断言/fixture。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web` 均通过；Web targeted tests 通过：`settings + shared/ui` 14/14、`finance + entryModel` 18/18、`routing` 7/7、`pricing` 2/2、`warehouse` 3/3、`customerService` 7/7；API targeted tests `business users maintain only their own customer master data|order-entry|review|routing|payable|persists manual shipment edits` 6/6 通过。`npm ls xlsx read-excel-file write-excel-file --all` 确认无 `xlsx`，存在 `read-excel-file@9.2.0` 与 `write-excel-file@4.1.1`；`npm audit --omit=dev --json` 仍因 Nest/Express/Multer/qs/file-type/esbuild 返回 9 项（0 critical、2 high、6 moderate、1 low）。发布步骤：`npm run sync:47` dry-run 通过；`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，首次 `db-migrate` 已应用前 4 个新迁移后因空目录失败，清理空目录并重建 `db-migrate/api` 后重新执行 `docker compose --profile tools run --rm db-migrate` 成功应用 `20260703043000_limit_final_review_to_finance`；随后 `docker compose up -d --remove-orphans api web` 成功。健康检查：47 `docker compose ps` 显示 `api/web/postgres/redis` 均 Up，Web 端口 `18899`；`curl -I http://127.0.0.1:18899/` 返回 `200 OK`；`curl http://127.0.0.1:18899/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`；未登录 `/api/shipments` 返回 401；API 日志包含 `Nest application successfully started`。本次未执行 `prisma db push`、`migrate reset`、seed 或 demo seed，未做 git stage/commit。

## 当前客服测试审计日志分页默认值修复

2026-07-03 本地完成客服测试桩审计日志分页默认值修复，本轮未发布 47。实现范围：`appTestHarness` 的 `/api/system/audit-logs` mock 默认 `pageSize` 从 `10` 对齐真实 API/内存仓储为 `500`，避免完整跑客服测试时新增审计日志把种子里的 `shipment.operational.update` 跟踪网址日志挤出默认第一页；补强已离港问题件用例，等待已离港表格稳定后再点行内问题件；不改 `CustomerServicePage` 业务逻辑、不改后端接口、不改数据库。验证：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx` 通过，10/10；`npm run typecheck -w @siyuan/web` 通过。

## 当前客服问题件需协助确认与售后标注

2026-07-03 本地完成客服问题件协助标注调整，本轮未发布 47。实现范围：`问题件需协助` 按钮增加二次确认，确认后继续复用现有问题件回复记录；`客服管理 -> 售后` 的售后状态改为颜色标注，`需赔付` 红色、`需协助` 橙色、`已解决` 绿色。验证：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "problem categories"` 通过；`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "after-sale problems"` 通过；`npm run typecheck -w @siyuan/web` 通过。完整 `customerService.test.tsx` 当前仍有 3 个既有跟踪网址断言失败，失败点为找不到 `屏蔽：https://track.example/...`，与本次问题件协助确认/售后标注改动无关。

## 当前客服已离港二次确认已到港

2026-07-03 本地完成客服 `已离港` 流转调整，本轮未发布 47。实现范围：`已离港` 行内 `已到港` 按钮不再直接跳转，点击后弹出二次确认 `确认到达已到港？`，确认后才显式 PATCH `status: ARRIVED_PORT` 并切到 `已到港`；已离港操作列宽同步放大，避免按钮挤压。验证：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "shows departed columns"` 通过；`npm run typecheck -w @siyuan/web` 通过。剩余：本轮未发布 47；测试仍保留既有 AntD `destroyOnClose` 警告。

## 当前市场确认排货后进入已排货

2026-07-03 本地完成市场排货确认交互修复，本轮未发布 47。实现范围：`市场排货` 弹窗点击 `确认排货` 成功后，前端自动切到 `市场管理 -> 已排货`，成功提示改为 `市场排货完成，已进入已排货`；失败时保留排货弹窗和已填写内容，并用 `Modal.error` 弹出 `排货失败` 与后端返回原因。实现上不改后端状态流，仍由 `/api/shipments/:id/route` 把 `WAITING_SORT` 流转为 `WAITING_DISPATCH`；`已排货` 仍按 `WAITING_DISPATCH` 展示。验证：`npm test -w @siyuan/web -- --run src/modules/routing` 通过，7/7；`npm run typecheck -w @siyuan/web` 通过。剩余：未发布 47。

## 当前客服待离港二次确认已离港

2026-07-03 本地完成客服 `待离港` 流转调整，本轮未发布 47。实现范围：`待离港` 行内 `修改` 只保存 ETD/ETA/查询网站等待离港信息，不再提交 `status: DEPARTED`；新增 `确认已离港` 按钮，点击后二次确认 `确认到达已离港？`，确认后才显式 PATCH `status: DEPARTED` 并切到 `已离港`；待离港操作列宽同步放大，避免按钮被挤压。验证：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "confirms departure"` 通过；`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "shows departed columns"` 通过；`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "creates a departed problem"` 通过；`npm run typecheck -w @siyuan/web` 通过。剩余：本轮未发布 47；测试仍保留既有 AntD `destroyOnClose` 警告。

## 当前财务终审通过进入市场待排货

2026-07-03 本地完成待审核运单终审权限收敛，本轮未发布 47。实现范围：业务员自审逻辑保持不变，自审后状态仍为 `REVIEW_PENDING`；最终「审核通过/驳回」后端只允许 `FINANCE/UG_FINANCE` 执行，`ADMIN` 调用终审接口返回 403，不再推进到 `WAITING_SORT`。审核接口网关改为允许 `orders:write` 或 `finance:settle` 进入，再由仓储层按角色判断，避免给财务开放通用运单操作权限；默认角色/种子数据移除财务的 `orders:write`，新增迁移 `20260703043000_limit_final_review_to_finance` 清理 47 真库财务角色的通用运单写权限。前端待审核运单按钮裁剪为：业务员只显示自审通过，财务显示审核通过/驳回，管理员不显示终审通过按钮；财务终审通过后订单进入 `WAITING_SORT`，继续由市场待排货列表读取。验证：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 通过，13/13；`npm test -w @siyuan/web -- --run src/modules/routing` 通过，6/6；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order-entry|review|routing"` 通过，4/4；`npm run typecheck -w @siyuan/web` 通过；`npm run typecheck -w @siyuan/api` 通过。剩余：未发布 47；测试仍保留既有 AntD message/Modal/React 19 兼容提示。

## 当前客服转单号二次确认待离港

2026-07-03 本地完成客服 `转单号` 流转调整，本轮未发布 47。实现范围：客服修改转单号时只保存 `transferNo/latestTracking`，后端不再因为 `OUTBOUNDED` 状态下填写转单号就自动跳到 `WAITING_DEPARTURE`；`客服管理 -> 转单号` 操作列新增 `确认待离港` 按钮，按钮要求已有转单号，点击后弹出二次确认，确认才显式 PATCH `status: WAITING_DEPARTURE` 并切到 `待离港`。验证：`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "confirms outbound business data"` 通过；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "persists manual shipment edits"` 通过；`npm run typecheck -w @siyuan/api` 通过；`npm run typecheck -w @siyuan/web` 通过。剩余：长链路 API 用例 `moves shipments through audit` 当前仍在审核前置处返回 403，属于既有角色权限测试链路问题，本轮未继续扩大修复。

## 当前业务员提交审核进入本人待审核运单

2026-07-03 本地完成业务员录单两步审核链路，本轮未发布 47。实现范围：录单提交继续创建 `REVIEW_PENDING`，后端强制订单 `entryBy/salesperson` 取当前登录用户名；新增运单自审字段 `businessReviewedBy/businessReviewedAt` 和 Prisma 迁移 `20260703033000_add_business_review_fields`。业务员待审核运单按本人 `entryBy/salesperson` 范围展示，点击“自审通过”只写自审标记，状态仍保持 `REVIEW_PENDING`；财务/管理员终审前会校验业务员录入订单必须已自审，终审通过后才流转 `WAITING_SORT`。前端录单提交成功后自动进入“待审核运单”并选中新单，业务员页面只显示“自审通过/查看/修改退回单”，不显示终审通过、驳回或删除。验证：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 通过 12/12；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order-entry|review"` 通过 3/3；`npm run typecheck -w @siyuan/web` 通过；`npm run typecheck -w @siyuan/api` 通过。剩余：未发布 47；测试仍保留既有 AntD message/Modal/React 19 兼容提示。

## 当前仓库确认出货进入客服数据确认

2026-07-03 本地完成仓库 `待出库/面单队列` 点击 `确认出货` 后自动进入 `客服管理 -> 数据确认`。实现范围：出货成功后仍复用现有 `dispatchShipment` 后端接口和 `OUTBOUNDED` 状态，不新增路由、不改数据库；前端将客服模块初始二级目录设置为 `dataConfirm`，并切换主菜单到客服管理，数据确认列表继续按现有规则展示 `OUTBOUNDED` 且未业务数据确认的运单。验证：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "sends routed shipments"` 通过；`npm test -w @siyuan/web -- --run src/modules/customerService/customerService.test.tsx -t "shows customer service dashboard|confirms outbound business data"` 通过；`npm run typecheck -w @siyuan/web` 通过。本轮未发布 47。

## 当前仓库面单显示完整性优化

2026-07-03 本地完成仓库待出库面单预览排版优化，本轮不改后端接口、不改数据库、不改权限、不发布 47。实现范围：`仓库管理 -> 待出库` 的面单队列改用现有 `ManagedTable`，补待出库列设置、稳定横向滚动宽度、固定面单内容列和操作列宽度；内部交货面单卡片增加完整可访问标签信息，预览卡展示面单号、出货单号、国家、件序号和条码，不再被窄列压缩裁切；打印媒体样式避免多张面单被分页截断。验证：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "prints warehouse outbound labels|sends routed shipments"` 通过，2/2。未通过项：完整仓库测试当前 1 个既有断言失败，`manages in-stock rows...` 找不到 `2 条`；`npm run typecheck -w @siyuan/web` 当前失败在财务文件既有类型错误 `FinanceEntryPage.tsx(481)`、`FinancePage.tsx(538)`，与本次仓库面单改动无关。

## 当前基础资料二级权限分配补齐

2026-07-03 本地补齐基础资料库二级功能到现有角色权限分配机制，本轮未发布 47，未删除旧权限。实现范围：保留 `master-data:read/write`、`master-data:agents:*`、`master-data:channels:*` 旧权限作为兼容入口，同时新增客户资料、财务资料、代理渠道、渠道类别、偏远、汇率、资料辅助等细分权限；后端基础资料聚合接口和相关 CRUD 接口改为旧权限或新细分权限均可通过，并按权限裁剪返回内容；前端基础资料二级菜单按对应权限显示，角色权限分配页可勾选新增细分权限；新增 Prisma 迁移 `20260703023000_split_master_data_permissions`，用于 47 真库插入新权限并把旧权限已有角色平滑映射到新细分权限。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 通过，12/12。`npm run typecheck -w @siyuan/api` 当前未通过，失败点为仓库已有未完成改动：`app.orders.e2e.test.ts` 中 `financeToken/serviceToken` 未定义，以及 `prisma.repository.ts` 中 `isSalesScopedRole` 入参类型错误，非本轮基础资料权限改动引入。

## 当前录单应付费用按业务员出货成本处理

2026-07-03 本地完成录单应付费用口径调整，本轮未发布 47。实现范围：录单页对业务员/业务部开放 `应付费用` 表，默认费用名从 `代理成本` 调整为 `出货成本`，业务员可填写费用名称、币种、计费重、单价、总金额和备注；代理字段仍只给管理员/财务显示，不给业务员额外开放代理资料库。录单提交时业务员可提交 `PAYABLE` 行，订单明细返回该应付费用；业务员查看自己订单费用明细时可看到应付金额和审核状态，但不返回代理/付款编号等敏感字段。财务应付审核增加门槛：业务员录入的应付费用与市场排货代理成本一样，在客服 `business_data.approved` 前不进入财务应付审核，直接审核会返回“客服确认数据后才能审核该应付费用”；客服确认后财务可按该金额审核付款。验证：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx src/modules/finance/entryModel.test.ts` 通过，16/16；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order-entry|payable|business users"` 通过；`npm run typecheck -w @siyuan/web` 通过；`npm run typecheck -w @siyuan/api` 通过。剩余：未发布 47；测试仍保留既有 AntD message/Modal/React 19 兼容提示。

## 当前录单业务员按当前登录用户匹配

2026-07-03 本地完成录单业务员归属修复，本轮未发布 47。实现范围：录单页费用表“业务员”固定显示当前登录用户名，不再读取已选仓库包裹历史业务员，也不再因角色不是 `OPERATOR` 显示“系统匹配”；后端订单录单创建/草稿更新统一把订单归属、费用摘要和审计日志里的 `salesperson/entryBy` 写为当前登录用户；客户资料自身的 `salesperson` 仍保留作为客户/包裹可见范围校验，不作为录单后订单业务员覆盖来源。补充回归：业务员录自己的可见客户成功后，返回运单、应收、业务成本和 `shipment.order_entry.submit` 审计日志均归属当前登录账号；录不可见客户仍 403。验证：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 通过，11/11；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "order-entry|business users"` 通过；`npm run typecheck -w @siyuan/web` 通过；`npm run typecheck -w @siyuan/api` 通过。剩余：未做 47 发布；测试仍保留既有 AntD message/Modal/React 19 兼容提示。

## 当前录单费用行操作与全局空按钮自检

2026-07-03 本地修复业务/财务录单费用表的草稿费用行删除动作，本轮未发布 47。实现范围：`FinanceEntryPage` 的应收费用、业务成本、应付费用行内“删除”不再走容易被忽略的二次确认弹窗，改为直接移除当前录单草稿行并提示“费用行已删除”；该动作属于前端草稿行删除，提交保存/审核时才写入后端订单费用。全局静态扫描了 `apps/web/src/modules/**/*.tsx` 中“新增/增加/修改/删除/确认/保存/审核/作废/反核销/查看/处理/批量修改/同步客户/写入审计”等按钮，已发现并处理低频 `ReportsPage`、`ProblemTicketsPage` 中没有后端动作的占位按钮，统一改为禁用态，避免可点击但无业务行为。保留项：价格表上传按钮实际由内嵌 file input 触发导入；水单作废按钮在 Popconfirm 中调用 `voidWaterReceipt`，不是空按钮。验证：`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "shows order-entry fee fields"` 通过；`npm run typecheck -w @siyuan/web` 通过。

## 当前业务渠道/代理渠道权限分离与客户归属

2026-07-03 本地完成业务录单渠道选择与业务员客户资料归属权限修复，本轮未发布 47。实现范围：财务/业务录单页将“代理”与“业务渠道”分离，业务渠道下拉改用公司渠道 `channels`，不再让业务员在业务渠道字段里选择真实代理或代理渠道；业务员/业务部/市场类角色新增只读 `master-data:channels:read`，可读取业务渠道名称，但仍不返回 `agents` 和 `agentChannels`。客户资料权限按后端强制裁剪：业务员类账号新建客户时系统自动把 `salesperson` 写为当前登录用户名，修改客户、收货人、客户账号、启停客户前都校验归属；业务员 A 只能查看/操作自己范围客户，业务员 B 无权查看或操作 A 的客户；管理员仍可查看和维护全部客户。补充修复：内存仓储审计日志写入时改为保存 before/after 快照，避免后续对象修改污染历史审计证据。新增迁移：`20260703013000_grant_business_channel_read`，用于 47 真库给业务/市场类角色补业务渠道读取权限。验证：`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "business users maintain only their own customer master data|lets admins maintain master data"` 通过，2/2；`npm run typecheck -w @siyuan/api` 通过；`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 通过，10/10。

## 当前客户资料业务员权限与归属

2026-07-02 本地已修正客户资料业务员权限与创建归属，本轮未发布 47。实现范围：业务员/业务部相关角色恢复 `master-data:write`，包含 `OPERATOR`、`UG_BUSINESS`、`UG_SZ_WUHAN`、`UG_ZZ_SIHUA`、`UG_WH_JIUYULIAN`、`UG_BUSINESS_MANAGER`、`UG_BUSINESS_SUPERVISOR`；新增线上迁移 `20260703003000_restore_business_master_data_write`，用于 47 真库补齐对应角色权限。客户资料新建时，业务员类账号不再手动填写业务员字段，系统默认使用当前登录用户名作为客户 `salesperson`，只读展示并参与后续客户归属匹配；管理员仍可手动维护业务员并可查看全部客户。客户资料列表和后端客户查询继续按规则裁剪：管理员全量，业务员类账号只看自己归属范围客户。验证：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/shared/ui.test.ts src/modules/settings/settings.test.tsx` 均通过。

## 当前 xlsx 高危依赖替换

2026-07-02 本地已处理 `xlsx@0.18.5` 高危生产依赖，未执行 `npm audit fix --force`，未升级 Nest 大版本，未发布 47。实现范围：`@siyuan/web` 移除直接依赖 `xlsx`，改用 `read-excel-file` / `write-excel-file`；新增共享 Excel helper，改造报价价格表导入、轨迹批量导入、系统员工导入模板/导入、高危审计导出及相关测试；Vite 手动分包从 `xlsx` 改为 `excel`。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx src/modules/settings/settings.test.tsx src/modules/warehouse/warehouse.test.tsx` 通过，3 个文件 38 个用例通过；`npm run build -w @siyuan/web` 通过，仍保留既有 Vite 大 chunk warning；`npm ls xlsx read-excel-file write-excel-file --all` 确认无 `xlsx`，存在 `read-excel-file@9.2.0` 与 `write-excel-file@4.1.1`；`npm audit --omit=dev --json` 从生产 10 项降为 9 项，`xlsx` high 已消除，剩余生产风险为 Nest/Express/Multer/qs/file-type/esbuild 链路，其中 Nest 修复仍需 semver major 升级；完整 `npm audit --json` 仍有 11 项，`form-data` high 属测试/开发链路。

## 当前全局冲突/失败提示修复

2026-07-02 本地修复全局业务提示规则，本轮不改后端接口、不改数据库、不发布 47。实现范围：`renderNoticeBar` 的共享提示层不再过滤相同消息，`createNoticeMessage` 为每次业务提示生成内部事件序号并在展示前剥离，避免用户重复点击同一失败动作时只提醒一次；`buildNoticeBarPresentation` 将 `已存在/重复/冲突/duplicate/conflict` 归类为红色错误态，客户编码已存在等保存冲突不再显示绿色成功提示。`App` 主业务入口与 `SettingsPage` 系统管理入口已接入该共享提示事件生成。验证：`npm test -w @siyuan/web -- --run src/modules/shared/ui.test.ts` 通过，2/2；`npm run typecheck -w @siyuan/web` 未通过，失败为当前仓库既有 XLSX/异步导入类型问题（`bulkImport.loadXlsx`、`xlsx` 类型、`shared/excel` Buffer 分支等），与本次提示改动无关。

## 当前 npm audit 风险分级

2026-07-02 已完成依赖安全审计分级，本轮只审计不修复、不发布 47。`npm audit --json` 结果为 12 项：0 critical、4 high、7 moderate、1 low；`npm audit --omit=dev --json` 结果为 10 项：0 critical、3 high、6 moderate、1 low。生产优先级：`xlsx@0.18.5` 为直接生产依赖且 high，包含 SheetJS prototype pollution 与 ReDoS，`npm audit` 无可用自动修复；Nest 生产链路 `@nestjs/common/core/platform-express@10.4.22` 触发 high/moderate，主要经 `multer/file-type/body-parser/express/qs`，audit 推荐升到 `@nestjs/*@11.1.27` 但属于 semver major，不应直接 `--force`；`form-data@4.0.5` high 来自 `supertest/superagent/@types` 测试链路，非生产运行依赖；`esbuild` low 来自开发工具链，线上容器不运行 dev server。建议顺序：先评估替换或隔离 `xlsx` 导入导出路径，再单独开 Nest 11 升级验证分支；暂不执行 `npm audit fix --force`。

## 当前仓库录单入口收敛

2026-07-02 本地按“仓库不具备录单权限”收敛仓库页入口，本轮不改后端接口、不改数据库、不发布 47。实现范围：`apps/web/src/modules/warehouse/WarehousePage.tsx` 中仓库角色 `WAREHOUSE/UG_WAREHOUSE_RECEIVE/UG_WAREHOUSE_OUTBOUND` 不再显示行内 `录单`，批量按钮从 `合票录单` 收敛为 `合票`，合票完成后只保留仓库合票结果，不跳转录单；管理员等非仓库入口仍保留原 `合票录单` 和从在仓进入录单能力。回归：新增仓库角色测试，确认仓库仍可见 `批量理货/理货/拆票/合票`，但看不到 `录单/合票录单`。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 17/17 通过。

## 当前客户资料弹窗残留与创建失败提示修复

2026-07-02 本地修复 `基础资料库 -> 客户资料` 新建客户弹窗关闭后再次打开仍保留上次输入的问题，并补充客户编码重复时的明确失败提示。本轮最小改动：新建客户前和取消弹窗时重置 `masterCustomerForm`；客户保存接口失败时通过页面通知展示错误；Prisma 仓储创建/更新客户前先校验客户编码重复，返回 `客户代码已存在`，避免唯一键错误表现成“点了没反应”。验证：`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 通过。补充：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"` 仍失败，失败点为既有审计日志断言缺少最近窗口里的 `master_data.agent_channel.update`，与本次客户资料弹窗/客户编码校验无关。

2026-07-02 已发布客户资料弹窗残留/重复编码提示修复与财务嵌入表排版治理到 47。发布前验证：`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx`、`npm run build -w @siyuan/web`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/shared` 均通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-BBqPeDiy.js`、`index-DezN6RPt.css`、`antd-vendor-CjSFUHWv.js`；`docker compose --profile tools run --rm db-migrate` 显示 51 个 migrations 且 `No pending migrations to apply`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899` 返回 `HTTP/1.1 200 OK`，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，未登录 `/api/shipments` 返回 401，API 日志包含 `Nest application successfully started`。发布中仅保留既有 Vite 大 chunk warning、Docker Compose buildx/Bake warning、npm audit、Prisma major update notice 和 AntD 测试警告。

## 当前 47 受控验收矩阵

2026-07-02 已发布并完成 `P-03/P-04` 权限与字段裁剪复验。47 发布流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`prisma migrate deploy` 显示 51 个 migrations 且无待执行迁移，`api/web` 已重启；`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，Web 返回 `HTTP/1.1 200 OK`。本地验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/web`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "returns shipment finance detail only to finance-capable roles"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "payable|sensitive|finance detail|warehouse receive"` 通过。47 真库接口证据：仓库读取今日收货/在仓返回 200 且无应付、付款、银行、利润字段，访问应付、已付款、代理银行接口返回 403；市场读取订单/业务成本返回 200，访问应付、待付款、代理银行接口返回 403；业务员读取订单返回 200，访问应付、已付款接口返回 403；业务员访问 `/api/shipments/cmr32uykp0015s901aykmhzuz/finance-detail` 返回字段只有 `shipmentId/systemOrderNo/receivables/businessCosts/receivableTotal/businessCostTotal`，不再返回 `payables/payableTotal/canViewPayables/profitSections/grossProfit/agentName`。日志证据：47 真库最近 `security.permission.denied` 包含仓库、业务员、市场访问应付、付款、代理银行等财务敏感接口的拒绝日志。结论：`docs/47-controlled-acceptance-matrix.md` 中 `P-03/P-04` 已改为已通过。剩余观察点：市场业务成本列表仍返回权限布尔 `canViewProfit`，但不返回利润金额、付款、银行或应付明细；若后续要求字段名也零出现，可单独清理该布尔字段。

2026-07-02 本地已创建 `docs/47-controlled-acceptance-matrix.md` 作为 47 真库精细验收入口，本轮不改业务代码、不造新样本、不发布 47。矩阵已回填今天 500 条受控收货样本、主链路样本、分票/合票/售后/反核销分支的已有证据，并把后续推进方式固定为“按模块抽样 + 数据证据 + 角色证据 + 日志证据 + 后端字段裁剪证据”。当前下一步：先补权限复验 `P-03/P-04`，重点验证市场、仓库、业务员是否能通过接口拿到付款、银行、利润、应付等财务敏感字段。

## 当前宽表/多列表格统一治理

2026-07-02 本地完成第四批财务嵌入表排版治理，不改后端接口、数据库、权限、金额计算或业务流程。落地原则：主列表/宽表继续使用 `ManagedTable` 承担列设置、分页和横向滚动；财务录单费用明细、订单费用面板、待付款弹窗明细、财务待审核详情 Tab 这类绑定表单、汇总或上下文详情的小表不硬迁，统一加 `finance-embedded-table` 紧凑样式，保证表头不竖排、单元格不被压碎、容器内可横向滚动、汇总行稳定。修改范围：`FinanceEntryPage` 的币种金额、应收/业务成本/应付费用、包裹、水单小表；`OrderFeePanel` 的费用和水单小表；`PendingPaymentPage` 的付款申请弹窗明细表；`FinancePage` 的待审核详情单件、问题、日志、轨迹表；`styles.css` 增加统一嵌入表规则。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 通过，10 个用例通过；`npm run build -w @siyuan/web` 通过。47 状态：已随 2026-07-02 本轮发布同步到 47。

2026-07-02 本地完成第三批财务宽表治理，不改后端接口、数据库、权限和业务流程。迁移范围：应收审核、业务员成本、应付审核、水单管理、待付款主列表、已付款主列表、代理账单列表已接入 `ManagedTable`，复用既有财务列设置、排序、分页、批量选择、汇总和权限裁剪逻辑；应收审核保留原 `Table.Summary` 汇总行，只替换表格底座。未迁移范围：财务录单内嵌费用明细、订单费用面板、待付款弹窗内明细表、财务看板详情 Tab、小型资料库表仍保留原 AntD `Table`，因为它们不是当前暴露的主宽表，且部分绑定金额汇总或表单录入，后续有具体页面问题时再最小迁移。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 通过，10 个用例通过；`npm run build -w @siyuan/web` 通过。本轮未发布 47。

2026-07-02 本地完成第二批宽表治理，不改后端接口、数据库、权限和业务流程。通用层：`ManagedTable` 继续作为系统宽表统一入口，已支持从 `key` 或 `dataIndex` 自动识别列设置项、自动读取字符串表头作为列设置名称、去重列设置项，并默认开启 sticky 横向滚动，避免右侧列不可见、分页被挤到右侧和字段被压成竖排。模块落地：`市场排货/RoutingPage` 的待排货、已排货、本周排货三张宽表已迁入 `ManagedTable`，并补独立列设置；`客服管理/CustomerServicePage` 的问题件、售后、状态池宽表已迁入 `ManagedTable`，继续复用页面原有列设置，不重复造控件。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/routing src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 通过，3 个测试文件 17 个用例通过；`npm run build -w @siyuan/web` 通过。剩余：财务部分宽表仍有领域内自带表格和列设置，需要下一批谨慎迁移；本轮未发布 47。

## 当前 47 生命周期分支矩阵补跑完成

2026-07-02 已继续完成上轮剩余分支，不改业务代码，直接使用 47 真库和现有接口跑真实样本。第二个分票子票：`SY-Q47SPLIT823285-2` / `cmr3f2exo001lnu01t4xe6zxo` 已从 `REVIEW_PENDING` 依次完成管理员审核、`market` 市场排货、客服业务/代理数据确认、仓库出库、客服转单号、待离港、已离港、已到港、派送、业务员签收，最终 `SIGNED`，转单号 `TN-Q47SPLIT823285-2`。费用异常回退：`SY-Q47SPLIT823285-1` / `cmr39tfwv002jpa01oina6rgx` 从 `OUTBOUNDED` 通过 `shipment.reroute_return` 退回 `WAITING_SORT`，再由 `market` 重排回 `WAITING_DISPATCH`，证明费用异常可退回市场重排且不需要管理员代排。反审核/反核销：第二分票的应收 `cmr3f2eys001rnu01425839rc`、业务成本 `cmr3f2eyw001tnu01kvp02603`、应付 `cmr3f67ku0022nu01qkwupe8k` 已先由财务审核为 `CONFIRMED`；付款申请 `FKSQ202607020002` / `cmr3f68ql0040nu01uu6jk4j1` 已确认付款，付款后直接应付反审核返回 400 `该应付已付款，请先在已付款模块反核销`；随后完成已付款反核销，付款申请状态变为 `CANCELED`、`paidAt=null`、`reversedAt=2026-07-02T11:29:21.524Z`，再完成应付/业务成本/应收反审核，三类费用均回到 `PENDING` 且 `locked=false`。售后赔付：第二分票创建售后问题件 `cmr3f9cji004jnu01but989we`，原因 `售后赔付：客户反馈货物破损，拟赔付 50 RMB`，客服回复后关闭，最终 `CLOSED`。

角色证据：审核/系统关键动作使用 `admin`，排货和费用异常重排使用 `market`，出库使用 `warehouse`，轨迹/业务数据/售后问题件使用 `service`，签收使用 `operator`，财务审核、付款确认、反核销、反审核使用 `finance`。日志证据：47 真库最近一小时包含 `shipment.order_entry.submit`、`shipment.review.approve`、`shipment.route`、`customer_service.business_data.approved`、`customer_service.agent_data.approved`、`shipment.dispatch`、`shipment.operational.update`、`shipment.sign`、`shipment.reroute_return`、`finance.receivable.audit/reverse_audit`、`finance.business_cost.audit/reverse_audit`、`finance.payable.audit/reverse_audit`、`finance.payment_application.create`、`finance.paid_payment.confirm/reverse`、`problem.ticket.create/reply/close`、`customer_service.issue.attach/update/close`。当前结论：上轮列出的四个剩余分支已补齐 47 留底；后续若继续大样本，应从“宽表列设置治理”和“更多宽表迁移 ManagedTable”继续，不需要再重复跑这四个基础分支。

## 当前市场角色与分支风险修复

2026-07-02 已按复用优先原则修复 47 市场排货角色与财务列表筛选风险，并继续推进合票分支。开发规则更新：`AGENTS.md` 的 47 发布规则已补充“线上登录启用图片验证码时，不要反复撞 `/api/auth/login`；接口验收优先使用已登录会话、容器内短期 JWT、服务端本地接口或只读数据库查询，临时 JWT 只在 47 容器内读取 `JWT_SECRET`，禁止输出密钥”。市场角色实现：新增迁移 `20260702172000_restore_market_routing_permissions`，复用现有 `Role/Permission/User.roleId/audit_logs`，幂等补齐 `UG_MARKET/市场部` 及 `routing:read/routing:write/orders:read/orders:write/warehouse:read/tracking:read/pricing:lookup/finance:business-cost:*` 等必要权限，未新增第二套 RBAC。47 发布证据：`npm run sync:47 -- --apply` 已同步，47 上 `docker compose build db-migrate api web` 成功，`prisma migrate deploy` 已应用 `20260702172000_restore_market_routing_permissions`，`api/web` 已重启，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`。

47 角色与账号证据：系统管理接口已创建/修正员工账号 `market`，角色为 `UG_MARKET`，显示名 `市场部`，账号启用且 `mustChangePassword=true`；`UG_MARKET` 真库权限已包含 `routing:read/routing:write`。47 排货证据：原业务部录单样本 `cmr31zw5c0008o50172cukw3j` 已由管理员审核后，使用 `market` 账号调用排货接口成功，状态从 `WAITING_SORT` 进入 `WAITING_DISPATCH`，渠道 `DHL HK`，代理 `QA47人民币代理`。日志证据：47 真库存在 `shipment.route`，target `cmr31zw5c0008o50172cukw3j`，actorId 为市场账号 `cmr3arvuf000fnu01qqxanel4`；同时保留 `system.role_permissions.repair`、`system.staff.create/update` 审计。财务风险修复：应收/业务成本列表已调整为“先按查询条件过滤，再做汇率装饰和总计”，避免无关 USD 行缺汇率导致查询当前 RMB 单号也 400；本地 `USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "receivable|business cost|payable"` 通过，47 上 `GET /api/finance/receivable-audits?systemOrderNo=SY-Q47FLOW934695` 与 `GET /api/finance/business-cost-audits?systemOrderNo=SY-Q47FLOW934695` 均返回 200 且各 1 行。

合票分支证据：`9409-MERGE002` / `cmr39s7le002bpa0155i5ybll` 不再停留草稿，已通过现有 `order-entry-draft` 接口补齐应收 `219.9 RMB`、业务成本 `164.98 RMB`、代理应付 `179.9 RMB` 并提交审核；管理员审核通过后，`market` 账号完成合票排货，当前状态 `WAITING_DISPATCH`，关联合票包裹 `9409-Q47FLOW934695-MGA/MGB`，其中 `MGA` 保留异常 `47分支异常：外箱破损`。剩余分支风险：本轮已排除“无独立市场账号/管理员代排货”“财务列表被无关 USD 行阻断”“合票草稿未推进”三项；费用异常回退、反审核/反核销、第二个分票子票完整流转、售后赔付仍需继续按分支矩阵逐项跑 47 留底。

## 当前 47 生命周期分支样本试跑

2026-07-02 已继续从 47 现有 500 条仓库样本推进生命周期分支验证，并按“每一步可能出现的情况要留底”的原则记录真实样本。分支数据证据：分票样本 `9409-Q47FLOW823285-SPLIT` 已拆出 `9409-Q47FLOW823285-SPLIT-1/-2`，源包裹状态 `CONSOLIDATED`，子票状态 `RECEIVED`；合票/异常样本 `9409-Q47FLOW934695-MGA/MGB` 已参与合票，其中 `MGA` 留有异常 `47分支异常：外箱破损`；合票创建运单 `9409-MERGE002` / `cmr39s7le002bpa0155i5ybll` 成功，当前为 `DRAFT` 且未补费用；直接出库主线包裹 `9409-Q47FLOW934695-ORD` 已流转为 `SHIPPED`。主线订单证据：运单 `SY-Q47FLOW934695` / `cmr32uykp0015s901aykmhzuz` 已完成业务录单、审核、市场排货、仓库出库、客服业务数据确认、转单号、待离港、已离港、已到港、派送、问题件处理、业务员签收，当前状态 `SIGNED`，转单号 `TN-Q47FLOW934695`，最新轨迹 `业务员确认客户已签收`。财务证据：该单业务成本 `120 RMB` 已由财务审核为 `CONFIRMED` 并锁定；客户应收 `180 RMB` 已审核并通过水单 `SD20260702001` / `WR-Q47FLOW934695` 到账匹配，水单状态 `ARCHIVED`、余额 `0`、应收 `receiptStatus=RECEIVED`；代理应付 `150 RMB` 已审核为 `CONFIRMED` 并锁定，付款申请 `FKSQ202607020001` / `cmr33mux10005pa01nbntqrb7` 已确认付款为 `PAID`。分票后续证据：子票 `9409-Q47FLOW823285-SPLIT-1` 已由业务员录单为 `SY-Q47SPLIT823285-1` / `cmr39tfwv002jpa01oina6rgx`，第一次携带应付费用被 403 拦截 `当前角色不能录入应付费用`，去掉应付后创建成功；该分票单已由管理员审核、管理员代市场排货、客服业务数据确认、仓库出库，当前状态 `OUTBOUNDED`，转单号 `TN-Q47SPLIT823285-1`。守门证据：业务员直接排货返回 403；分票单未客服确认时仓库出库返回 400 `业务数据确认后才能填写转单号`，确认后出库成功。日志证据：47 真库已有 `shipment.order_entry.submit`、`shipment.review.approve`、`shipment.route`、`shipment.dispatch`、`customer_service.business_data.approved`、`customer_service.status.update`、`customer_service.issue.attach/reply/close`、`shipment.sign`、`customer_service.signature.confirm`、`finance.business_cost.audit`、`finance.receivable.audit`、`finance.water_receipt.create/arrive/match`、`finance.payable.audit`、`finance.payment_application.create`、`finance.paid_payment.confirm`。本轮修复一个真实阻断：应付列表原先先对全量应付行做 USD 汇率装饰再按单号过滤，导致无关 USD 应付缺汇率时，查询当前 RMB 单号也会 400；已改为先按查询条件过滤、再装饰汇率，并补 API 回归。47 发布：已同步代码，构建 `api/web/db-migrate`，线上 `prisma migrate deploy` 显示 50 个 migrations 且无待执行迁移，`api/web` 已重启，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`。剩余风险：47 没有独立市场账号，`UG_MARKET` 也缺少真实排货写权限，本次市场排货仍用管理员代跑留底；财务应收/业务成本列表在存在无关 USD 行且缺 USD 汇率时仍可能因列表总计返回 400，需要后续按筛选后总计或汇率兜底修；合票 `9409-MERGE002` 只生成草稿，仍需补费用与完整流转；费用异常回退、反审核/反核销、第二个分票子票和售后赔付分支仍需继续逐项跑分支矩阵。

## 当前业务部录单权限映射修复

2026-07-02 已修复 47 上 `UG_BUSINESS/业务部` 调用 `POST /api/shipments/order-entry` 返回 403 的阻断点，并发布到 47。根因：47 真库中 `UG_BUSINESS` 仅剩 `pricing:lookup`，缺少录单入口所需 `orders:read/orders:write`；同时部分后端代码仍只按旧 `OPERATOR` 判断业务员范围，可能导致新用户组口径不一致。实现范围：新增迁移 `20260702143000_restore_business_order_entry_permissions`，幂等补回业务组 `workspace:access/orders:read/orders:write`；`PrismaRepository` 与 `InMemoryRepository` 中录单包裹列表、客户校验、派发后业务成本编辑限制改为复用 `operatorCustomerScope`，确保业务部、业务经理、业务主管、站点类业务组仍按业务员归属裁剪数据。47 发布：已同步代码，构建 `api/web/db-migrate`，线上 `prisma migrate deploy` 成功应用该迁移，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`。47 真库权限证据：`UG_BUSINESS` 现有 `workspace:access, orders:read, orders:write, pricing:lookup`。47 真实接口证据：业务账号 `operator` 可见 9409 自己客户包裹、不可见 1344 非本人客户包裹；录 1344 返回 403 `业务员只能录入自己名下客户`；录 9409 返回 201 并生成 `REVIEW_PENDING` 运单 `cmr31zw5c0008o50172cukw3j`。日志证据：47 `AuditLog` 已写 `shipment.order_entry.submit`，target 为 `shipment:cmr31zw5c0008o50172cukw3j`，actor 为 `u-op`；旧 403 仍保留为 `security.permission.denied` 证据。下一步：可继续从 47 现有 500 条仓库样本抽样推进审核、市场排货、仓库出库、客服轨迹和财务闭环。

## 当前系统管理员工账号表格横向滚动修复

2026-07-02 本地修复系统管理 -> 用户名员工账号表在全屏下右侧字段不可见的问题，本轮只改前端布局，不改接口、权限、数据字段或业务流程。实现范围：`apps/web/src/modules/settings/SettingsPage.tsx` 将员工账号表接入现有 `ManagedTable`，保留 1560px 宽表横向滚动，表体限制在当前视口高度内，操作按钮允许换行避免卡片头部顶出页面；`apps/web/src/styles.css` 补 `app-page/module-sub-workspace` 最小宽度收缩规则，避免二级目录内容把主页面撑到视口外，并给表格滚动条补可拖拽样式。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 11/11 通过；本地 Chrome 1366x768 与 1920x960 检查确认页面不再产生整页横向溢出，1366 下员工账号表体 `clientWidth=1082`、`scrollWidth=1560`，可横向滚动看到 `所属用户组/创建时间/操作`。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-BnQPh6s1.js`、`index-_wQet5Gy.css`、`antd-vendor-CjSFUHWv.js`；`docker compose --profile tools run --rm db-migrate` 显示 50 个 migrations 且 `No pending migrations to apply`；`api/web` 已重启。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899` 返回 `HTTP/1.1 200 OK`，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，未登录 `/api/shipments` 返回 401。剩余：测试仍保留既有 AntD `destroyOnClose` 和 useForm 警告；发布中保留既有 Vite 大 chunk、Docker Compose buildx/Bake 和 Prisma major update 提示。

## 当前墨家长快递号接收限制调整

2026-07-02 已将仓库收货的快递单号限制从 15 位放宽到 64 位，并发布到 47。修改范围：`apps/api/src/modules/prisma.repository.ts`、`apps/api/src/modules/in-memory.repository.ts`、`apps/web/src/modules/warehouse/WarehousePage.tsx`；真实仓储、内存仓储和快速入库前端提示保持一致。47 发布：`npm run sync:47 -- --apply` 已同步，47 上 `docker compose build api web` 成功，`prisma migrate deploy` 显示 `No pending migrations to apply`，`api/web` 已重启，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`。47 真库验证：通过墨家设备接口写入 23 位快递单号 `KY4001036478949-LONG-47`，接口返回 `9409-KY4001036478949-LONG-47 录入成功`，数据库 `WarehousePackage` 可查，`createdBy=mojia-device`、`scanSource=墨家设备`。原因：真实快递号、设备条码和验收前缀可能超过 15 位，15 位硬限制会导致外部设备数据无法进入今日收货；64 位保留异常 payload 上限，避免超长字符串污染日志、标签和页面。

## 当前 47 受控收货样本与流程试跑

2026-07-02 已在 47 真库通过现有墨家接收接口创建 500 条今日收货/在仓受控样本，样本前缀 `Q4702T04:`，客户分布复用现有客户资料，未使用本机数据库。数据证据：`WarehousePackage` 新增 500 条，今日收货查询返回 500 行，汇总 `receiptTickets=500`、`totalPackages=500`、`totalWeightKg=7987.4`、`totalCbm=63.74`，在仓查询返回 500 行。仓库模块试跑：异常标记通过，理货创建/完成/标签生成/打印通过，拆票生成 2 个子票，合票生成 `9409-MERGE001`。角色证据：仓库读取今日收货 200 且无敏感字段；业务员读取今日收货当前返回 403；仓库今日收货响应未发现应收、应付、利润、付款、银行账号字段。日志证据：最近一小时 `warehouse.package.create=500`、`warehouse.tally.create=1`、`warehouse.tally.complete=1`、`warehouse.consolidation.create=1`。发现项：业务录单接口使用新用户组 `UG_BUSINESS` 调用 `POST /api/shipments/order-entry` 返回 403，导致后续审核、排货、出库、客服轨迹和财务闭环未能在本轮继续；需要单独修复新用户组权限映射或接口权限判断后，再从现有 500 条样本中抽样继续订单全链路流转。

## 当前开发准则输出纪律补充

2026-07-02 本地已将用户提供的 `DO NOT send optional commentary` 输出纪律写入 `AGENTS.md`，新增 `Output Discipline` 小节，要求减少非必要说明和过程旁白，优先行动，只有歧义/风险/阻塞/需要决策时解释中间过程，最终回复只总结完成内容、验证结果和剩余风险。本轮只改开发准则文档，不改产品代码、不跑业务测试。

## 当前宽表/多列表格统一治理

2026-07-01 本地完成宽表/多列表格展示规则第一批统一治理，本轮不改接口、不改数据库、不改业务流转。实现范围：新增共享 `ManagedTable` 表格底座，统一提供按可见列自动计算横向滚动宽度、分页区域不外溢、可选列设置能力；运单列配置升级为“显示/隐藏 + 排序”，默认保持全列可见以兼容既有页面和测试，用户可主动隐藏不需要的列；运营工作台运单池、订单预览、物流轨迹承运商任务表已迁入共享表格底座；仓库今日收货、在仓数据保留原有列设置弹窗并切换到共享表格底座；全局表格样式补充 `max-width`、分页换行和长字段不竖排兜底。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/orders/orders.test.tsx src/modules/tracking/tracking.test.tsx` 2 个测试文件 4/4 通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 16/16 通过；`npm run build -w @siyuan/web` 通过。剩余：其他已存在 `Table` 的宽表还需按模块逐步替换成 `ManagedTable`，本轮先落共享方案和已暴露问题入口。47 状态：已随 2026-07-01 本轮发布同步到 47。

## 当前客服售后模块落地

2026-07-01 本地完成客服管理 `售后` 二级目录最小闭环落地，本轮不改二级目录、不新增数据库字段、不新增后端路由。实现范围：`apps/web/src/modules/customerService/CustomerServicePage.tsx` 将原空占位售后页接入签收后 `SIGNED` 问题件池，复用现有问题件宽表字段、列设置、横向滚动、问题件需协助回复和问题件关闭接口；售后页独立展示 `售后状态`，按问题件状态和内容派生为 `待处理/处理中/需赔付/已解决`，并确保打开售后页时加载问题件相关审计日志。`apps/web/src/modules/customerService/customerService.test.tsx` 补充签收后创建售后问题、进入售后目录、需协助、已解决的回归。验证：`npm test -w @siyuan/web -- --run src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 2 个测试文件 11/11 通过；`npm run typecheck -w @siyuan/web` 通过。剩余：未做浏览器人工布局检查；AntD `destroyOnClose` 测试警告为既有提示。47 状态：已随 2026-07-01 本轮发布同步到 47。

## 当前全模块表格与二级目录布局自检

2026-07-01 本地完成从仓库页延伸到全模块/二级目录的表格与筛选区布局自检，本轮不发布 47、不改接口、不改数据库、不改权限或业务流转。自动扫描 `apps/web/src/modules` 下 24 个一级/二级模块目录，覆盖仓库、财务、报价、系统、基础资料、订单、客服、轨迹、市场、报表、问题件、客户端等表格/筛选区入口；静态检查未发现公共 `.ant-table-wrapper/.ant-table-container/.ant-table-content` 继续使用 `overflow: hidden` 截断横向滚动。实现范围：继续收敛到 `apps/web/src/styles.css`，让公共 AntD 表格、表头、筛选条、仓库筛选网格、财务/应收/待付款等高频筛选区具备横向滚动兜底，避免宽字段被裁掉且底部不能拖动；`apps/web/src/modules/masterData/MasterDataPage.tsx` 仅补历史汇率按钮可访问名和历史表“币别编码+名称”显示，避免测试误点和过期历史汇率看不到编码；`apps/web/src/modules/settings/settings.test.tsx` 同步精确点击历史汇率按钮。验证：`npm run build -w @siyuan/shared` 通过；`npm run typecheck -w @siyuan/web` 通过；`npm run build -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 11/11 通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx src/modules/finance/finance.test.tsx src/modules/pricing/pricing.test.tsx src/modules/settings/settings.test.tsx` 4 个测试文件 46/46 通过。剩余：未做 47 发布；未做浏览器人工布局检查；Vite 大 chunk、AntD `destroyOnClose`/静态 message 警告仍为既有提示。

2026-07-01 已发布本轮表格/筛选布局、操作日志分页、市场/仓库时间列口径修正到 47。发布前验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx src/modules/finance/finance.test.tsx src/modules/pricing/pricing.test.tsx src/modules/routing`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "returns a clear username or password error"`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web` 均通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-pQ2ED9mf.js`、`index-BSQb0wRz.css`、`antd-vendor-DReXOQUg.js`；`docker compose --profile tools run --rm db-migrate` 显示 49 个 migrations 且 `No pending migrations to apply`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899` 返回 `HTTP/1.1 200 OK`，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，未登录 `/api/shipments` 返回 401，API 日志包含 `Nest application successfully started`。发布中仅保留既有 Vite 大 chunk warning、Docker Compose buildx/Bake warning、npm audit 和 AntD 测试警告。

## 当前市场已排货日期显示修正

2026-07-01 本地修正流转后时间列不随节点变化的问题，本轮只改前端展示，不改接口、不改数据库、不发布 47。根因：市场已排货表复用待排货表的 `createdAt` 日期列，排货动作后后端实际已写入 `routedAt`，但页面仍显示原订单日期。实现范围：`apps/web/src/modules/routing/RoutingPage.tsx` 中市场表按现有字段显示节点进入时间：待排货优先 `reviewedAt`，已排货优先 `routedAt`，已出库优先 `outboundAt`，缺字段时回退 `createdAt`；已排货列标题显示为 `排货时间`。`apps/web/src/modules/warehouse/WarehousePage.tsx` 中仓库 `待出库/面单队列&待仓库出货` 增加 `进入待出库时间`，运单行显示 `routedAt`；理货本地出货行当前无独立时间字段，显示 `-`。补充回归测试锁定市场待排货、市场已排货、仓库待出库时间口径。验证：`npm test -w @siyuan/web -- --run src/modules/routing` 6/6 通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 16/16 通过；`npm run typecheck -w @siyuan/web` 通过。

## 当前操作日志全量保留分页读取

2026-07-01 本地完成系统管理操作日志分页改造，本轮不改数据库、不做数据清理、不发布 47。实现范围：审计日志仍按现有后端日志表/内存仓库全量追加保存，不做 500 条覆盖；`GET /api/system/audit-logs` 支持 `page/pageSize` 并返回 `pagination.page/pageSize/totalItems`，后端默认仍兼容旧口径最多返回 500 条，前端系统管理操作日志页显式按每页 10 条分页请求和展示，筛选/重置会回到第一页。模块/结果这类派生筛选先过滤再分页，避免分页后再过滤导致漏数据。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "loads the existing audit log entry"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "returns a clear username or password error"` 均通过。补充：尝试运行 `USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"` 仍失败，失败点为既有基础资料审计断言缺少 `master_data.agent_channel.update`，不是本次分页逻辑导致。

## 当前全局表格横向滚动修复

2026-07-01 本地修复仓库页截图中“右侧列显示不完整、底部不能拖动”的公共 UI 问题。本轮只改前端 CSS，不改接口、不改字段、不改权限、不改业务状态流转、不发布 47。根因：全局 `.content .ant-table-wrapper { overflow: hidden; }` 会裁掉 AntD 表格自身的横向滚动条；仓库今日/在仓筛选网格在宽屏字段较多时也缺少横向滚动兜底。实现范围：`apps/web/src/styles.css` 将全局表格 wrapper 和 AntD 内部 table content/container 改为允许横向滚动；`AppFilterBar` 增加横向滚动兜底；仓库今日/在仓筛选网格增加最小宽度和横向滚动容器；运单详情财务表格旧 `overflow: hidden` 同步改为横向可滚。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 15/15 通过；`npm run build -w @siyuan/web` 通过。静态检查确认不再存在直接把 `.ant-table-wrapper/.ant-table-container/.ant-table-content` 设为 `overflow: hidden` 的公共截断规则。剩余：未做 47 发布；Vite 大 chunk warning 仍为既有提示。

## 当前全功能 UI/UX 统一升级已发布 47

2026-07-01 已按 Docker 流程发布当前代码到 47。发布步骤：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，随后执行 `docker compose --profile tools run --rm db-migrate`，线上 Prisma `migrate deploy` 连接 47 真库并显示 `49 migrations found`、`No pending migrations to apply`；再执行 `docker compose up -d --remove-orphans api web` 重启服务。健康检查：`docker compose ps` 显示 `api/web/postgres/redis` 均 Up，Web 映射端口 `18899`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`curl http://127.0.0.1:18899/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`。本次未执行 `prisma db push`、`migrate reset`、seed 或 demo seed。发布中仅保留既有 Vite 大 chunk warning、Docker Compose buildx/Bake warning、npm audit 提示。

## 当前墨家设备 POST 重试策略确认

2026-07-01 厂家侧重试策略已确认为：单次请求 5 秒超时，最多重试 3 次。Sunny 已按 `barcode + measuredAt + deviceNo` 做幂等兼容，同一次设备上报重试返回 `已接收`，不重复写入今日收货/在仓数据。本轮未改接口代码、未发布 47，只补充确认该策略仍是后续对接厂家时的固定口径。

## 当前全功能 UI/UX 统一升级 - 第十二阶段收尾检查

2026-07-01 本地完成第十二阶段“全系统一致性检查和最终验收”代码侧收口，本轮仍只做本地前端展示/测试层，不发布 47，不新增接口、不改接口参数、不改返回字段、不改数据库、不改权限或业务流程。补充范围：`apps/web/src/modules/reports/ReportsPage.tsx`、`apps/web/src/modules/problemTickets/ProblemTicketsPage.tsx`、`apps/web/src/modules/routing/RoutingPage.tsx`、`apps/web/src/modules/customerService/CustomerServicePage.tsx`、`apps/web/src/modules/customer/CustomerPortal.tsx` 将最后残留的旧 `page-heading` 接入现有 `AppPageHeader/AppActionGroup`，保留原有按钮、字段、状态和回调；`apps/web/src/modules/workspace/workspace.test.tsx` 对齐当前市场排货真实文案与行为；`apps/web/src/modules/warehouse/warehouse.test.tsx` 对齐当前录单页“已选货物 2 条”的真实摘要口径。验证结果：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/problemTickets/problemTickets.test.tsx src/modules/reports/reports.test.tsx src/modules/routing src/modules/customerService src/modules/workspace/workspace.test.tsx` 5 个测试文件 38 条通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 15/15 通过；`npm run test:web` 12 个测试文件 93/93 通过；`npm run build -w @siyuan/web` 通过。`rg "page-heading|功能后续设计|模拟业务数据|模拟处理|已接入后端|最小闭环" apps/web/src/modules -S` 产品代码无残留，命中仅为测试里的“不应出现”断言。剩余：既有 AntD `destroyOnClose`、静态 message context 与大 chunk warning 仍存在，本阶段未处理；未做浏览器人工截图检查。

## 当前全功能 UI/UX 统一升级 - 第十一阶段

2026-07-01 本地推进第十一阶段“全局状态体验补齐”最小 UI 清理，本轮只做本地前端展示文案，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限、路由或业务处理逻辑。实现范围：`apps/web/src/modules/problemTickets/ProblemTicketsPage.tsx`、`apps/web/src/modules/reports/ReportsPage.tsx` 将低频模块中的开发期文案 `模拟业务数据/模拟处理` 改为面向业务的 `业务数据/处理`；保留原有列表、待办、AI 辅助、快捷动作、导入导出按钮和所有处理函数。全局状态能力继续复用现有 `renderNoticeBar` toast、`AppEmptyState` 和 `PlaceholderPanel`，未新建提示/空状态体系。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/problemTickets/problemTickets.test.tsx src/modules/reports/reports.test.tsx` 2/2 通过；`rg "功能后续设计|模拟业务数据|模拟处理" apps/web/src/modules/problemTickets apps/web/src/modules/reports apps/web/src/modules/shared -S` 无结果。测试输出仍有既有 AntD `destroyOnClose` 弃用警告，本阶段未处理。补充确认：墨家设备厂家侧重试策略已确认为 5 秒超时、最多重试 3 次，系统侧此前已按 `barcode + measuredAt + deviceNo` 做幂等兼容并有本地/47 验证记录。

## 当前全功能 UI/UX 统一升级 - 第十阶段

2026-07-01 本地推进第十阶段“系统管理”最小 UI 改造，本轮只做本地前端展示/测试层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改用户、角色、菜单、权限关系或保存逻辑。实现范围：`apps/web/src/modules/settings/SettingsPage.tsx` 复用公共组件 `AppPage`、`AppPageHeader`、`AppActionGroup`，统一“系统管理”页头和导入配置、导出权限、AI 权限体检按钮组；保留原有用户组、用户名、站点、客户资料、操作日志、角色权限分配、权限安全区、AI 接口安全、系统基础配置和所有处理函数。同步去掉系统管理页中面向用户的 `模拟` 配置文案。自检时完整 settings 测试发现操作日志存在多条 `成功` 时旧断言过窄，已在 `apps/web/src/modules/settings/settings.test.tsx` 改为断言至少一条成功，未改产品逻辑。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 11/11 通过；`rg` 确认系统页已接入公共页面组件且不再残留旧 `page-heading/Title/模拟` 文案。测试输出仍有既有 AntD `destroyOnClose` 弃用和 useForm 连接警告，本阶段未新增或处理。风险说明：本阶段未做浏览器人工布局检查，未全量改造权限矩阵内部布局；后续第十一阶段可继续做全局状态体验补齐。

## 当前全功能 UI/UX 统一升级 - 第九阶段

2026-07-01 本地推进第九阶段“基础资料库”最小 UI 改造，本轮只做本地前端展示层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限、基础资料字段含义或引用关系。实现范围：`apps/web/src/modules/masterData/MasterDataPage.tsx` 复用公共组件 `AppPage`、`AppPageHeader`、`AppActionGroup`，统一“基础资料库”页头和 `AI 资料体检` 按钮组；保留原有客户资料、财务资料、代理资料、代理渠道、公司渠道、渠道类别、偏远、汇率、资料辅助、表格、弹窗、上传和所有处理函数。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "loads real master data"` 通过；`rg` 确认基础资料页已接入公共页面组件且不再残留旧 `page-heading` 顶部页头，弹窗内部 `Title level={5}` 保留。测试输出仍有既有 AntD `destroyOnClose` 弃用警告，本阶段未新增或处理。风险说明：本阶段未做浏览器人工布局检查，未全量改造基础资料内部表格/弹窗；后续第十阶段可继续从系统管理模块页面级 UI 接入推进。

## 当前全功能 UI/UX 统一升级 - 第八阶段

2026-07-01 本地推进第八阶段“财务管理模块”最小 UI 改造，本轮只做本地前端展示层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限、金额计算、汇率计算、账单生成或付款申请业务逻辑。实现范围：`apps/web/src/modules/finance/FinancePage.tsx` 复用公共组件 `AppPage`、`AppPageHeader`、`AppActionGroup`，统一财务/业务/基础资料三种模式的页头展示，并保留财务模式下 `生成 9409 对账单`、`登记 9409 收款并核销` 快捷按钮；保留原有财务看板、录单、应收、业务成本、应付、待付款、已付款、水单、代理账单、资料库子页和所有处理函数。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 10/10 通过；`rg` 确认财务页已接入公共页面组件且不再残留旧 `page-heading/Title` 页头。测试输出仍有既有 AntD 静态 `message/Modal` context 与 React 19 兼容警告，本阶段未新增或处理。风险说明：本阶段未做浏览器人工布局检查，未全量改造财务内部表格/弹窗；后续第九阶段可继续从基础资料库页面级 UI 接入推进。

## 当前全功能 UI/UX 统一升级 - 第七阶段

2026-07-01 本地推进第七阶段“物流轨迹管理模块”最小 UI 改造，本轮只做本地前端展示层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限、轨迹数据来源或同步逻辑。实现范围：`apps/web/src/modules/tracking/TrackingPage.tsx` 复用公共组件 `AppPage`、`AppPageHeader`、`AppActionGroup`，统一“轨迹监控中心”页头和承运商任务按钮；保留原有承运商任务列表、同步轨迹、重试、最新轨迹、运单详情入口和所有处理函数。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/tracking/tracking.test.tsx` 1/1 通过；`rg` 确认轨迹页已接入公共页面组件且不再残留旧 `page-heading/Title` 页头。风险说明：本阶段未做浏览器人工布局检查，未全量改造轨迹卡片/时间线；后续第八阶段可继续从财务管理模块页面级 UI 接入推进。

## 当前全功能 UI/UX 统一升级 - 第六阶段

2026-07-01 本地推进第六阶段“仓库管理模块”最小 UI 改造，本轮只做本地前端展示层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限、标签生成规则、箱序规则、库存计算或仓库状态流转。实现范围：`apps/web/src/modules/warehouse/WarehousePage.tsx` 复用公共组件 `AppPage`、`AppPageHeader`、`AppActionGroup`，统一“仓库管理中心”页头和收货扫描/模拟面单按钮组；保留原有今日收货、在仓、理货、分箱合箱、待排货、待出库、面单队列、交接单、抽屉、弹窗、表格列和所有处理函数。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 15/15 通过；`rg` 确认仓库页已接入公共页面组件且不再残留旧 `page-heading/warehouse-page-heading/Title` 页头。风险说明：本阶段未做浏览器人工布局检查，未全量改造仓库内部表格/抽屉；后续第七阶段可继续从物流轨迹管理模块页面级 UI 接入推进。

## 当前全功能 UI/UX 统一升级 - 第五阶段

2026-07-01 本地推进第五阶段“业务管理模块”最小 UI 改造，本轮只做本地前端展示层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限和订单生命周期/状态流转。实现范围：`apps/web/src/modules/orders/OrdersPage.tsx` 复用公共组件 `AppPage`、`AppPageHeader`、`AppActionGroup`，统一“我的订单中心”页头和导入履约运单、新建出货订单、AI 批量处理按钮组；保留原有订单预览、审核状态池、列表、详情、排货、收款、日志、Modal 和所有处理函数。同步把页面中面向用户的 `mock` 开发文案改为中性业务说明。验证：`npm run typecheck -w @siyuan/web` 通过；`rg` 确认业务页已接入公共页面组件且不再残留 `page-heading/mock/功能后续设计`。风险说明：本阶段未做浏览器人工布局检查，未全量改造业务管理内部表格/弹窗；后续第六阶段可继续从仓库管理模块页面级 UI 接入推进。

## 当前墨家设备 POST 接收重试兼容

2026-06-30 本地确认墨家设备 POST 接收策略：厂家侧 5 秒超时、最多重试 3 次。Sunny 现有接口 `POST /api/integrations/mojia/measurements` 继续复用现有 `WarehousePackage` 今日收货落库，不新建墨家数据模型；通过 `barcode/orderNo + measuredAt + deviceNo/machineNo` 对同一设备上报做幂等识别。补充回归测试：同一 payload 首次返回 `录入成功`，后续 3 次重试均返回 `已接收`，今日收货只保留 1 条记录，避免设备超时重发造成重复入库。验证：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"` 通过；`npm run typecheck -w @siyuan/api` 通过。本轮只改本地测试和状态文件，未发布 47，未改线上数据库。

## 当前全功能 UI/UX 统一升级 - 第四阶段

2026-06-30 本地推进第四阶段“报价查价模块”最小 UI 改造，本轮只做本地前端展示层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限和业务状态流转。实现范围：`apps/web/src/modules/pricing/PricingPage.tsx` 复用公共组件 `AppPage`、`AppPageHeader`、`AppActionGroup`，统一报价查价中心页头和查询报价主按钮；保留原有查价表单、报价结果、费用明细、价格表导入、加价规则维护、详情弹窗、删除/备注等全部入口和处理函数。验证：`npm run typecheck -w @siyuan/web` 通过。风险说明：本阶段未改报价计算逻辑，未做浏览器人工布局检查；后续第五阶段可从业务管理模块页面级 UI 接入继续推进。

## 当前全功能 UI/UX 统一升级 - 第三阶段

2026-06-30 本地推进第三阶段“运营工作台 / 专线运单池”最小 UI 改造，本轮只做本地前端展示层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限和业务状态流转。实现范围：`apps/web/src/modules/operations/OperationsPage.tsx` 复用第二阶段公共组件 `AppPage`、`AppPageHeader`、`AppActionGroup`，统一运营工作台页头和操作按钮组；保留原有导入运单、新建预报、智能录单按钮，保留原有 AI 调用参数、状态池、列设置、批量动作、运单表格和分页。验证：`npm run typecheck -w @siyuan/web` 通过。风险说明：本阶段未做浏览器人工布局检查，未全量替换运营工作台内部卡片/表格；后续第四阶段报价查价模块可继续复用公共组件做页面级 UI 整理。

## 当前全功能 UI/UX 统一升级 - 第二阶段

2026-06-30 本地完成第二阶段“公共 UI 组件沉淀”最小闭环，本轮只做本地前端展示层，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限和业务状态流转。实现范围：复用现有 `apps/web/src/modules/shared/ui.tsx`，新增轻量展示组件 `AppPage`、`AppPageHeader`、`AppFilterBar`、`AppActionGroup`、`AppFormSection`、`AppEmptyState`；低风险接入 `apps/web/src/modules/shared/PlaceholderPanel.tsx`，把旧占位文案改为统一空状态；`apps/web/src/styles.css` 补公共组件样式和窄屏排列规则。未新增依赖，未替换真实业务数据，未删除已有功能入口。验证：`npm run typecheck -w @siyuan/web` 通过。风险说明：`apps/web/src/modules` 当前在 Git 状态中整体为未跟踪目录，普通 `git diff` 不展示 `shared/ui.tsx` 和 `PlaceholderPanel.tsx` 的文件级 diff；本阶段以当前文件内容、`rg` 命中和 typecheck 作为证据。下一步进入第三阶段时应优先挑运营工作台/专线运单池中一处低风险区域接入公共组件，不做全量替换。

## 当前全功能 UI/UX 统一升级 - 第一阶段

2026-06-30 本地完成第一阶段“全局 Layout 和设计规范”最小 UI 改造，本轮只做本地前端样式，不发布 47，不新增接口、不改接口参数、不改响应字段、不改数据库、不改权限和业务状态流转。实现范围：`apps/web/src/styles.css` 统一全局背景层、左侧导航激活态、顶部栏阴影和动作区对齐、全局搜索宽度、内容容器宽度/间距、卡片/表格/按钮/输入控件基础圆角与阴影。保留所有现有菜单、二级入口、按钮、字段、状态、表格和业务组件。验证：`npm run typecheck -w @siyuan/web` 通过。风险说明：当前工作区在本轮开始前已有大量未提交前端和后端改动，`App.tsx`、`appShell/config.tsx`、多处业务文件的 dirty 状态不是本阶段新增；本阶段未做浏览器人工布局检查，后续第二阶段/模块阶段应继续按本地前端 UI-only 范围推进，并避免触碰 47。

## 当前市场排货取消运单审核前置

2026-06-30 本地完成市场排货取消“运单审核通过后才能排货”前置校验，本轮不新增数据库字段、不新增接口、不发布 47。实现范围：`POST /api/shipments/:id/route` 在内存仓储和 Prisma 仓储中不再要求 `reviewedAt/reviewedBy`，只保留现有待排货状态流转、代理、渠道、代理渠道、计费重、单价、其他费用备注等真实排货校验；待排货池里的运单填完市场排货弹窗后可直接确认排货并流转到已排货/待出库。回归测试补充未审核 `WAITING_SORT` 运单可成功排货且不伪造审核字段。验证：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "warehouse receive|routing"`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/web -- --run src/modules/routing`、`npm run build -w @siyuan/api` 均通过。

2026-06-30 已发布市场排货取消二次审核前置到 47。发布流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，API 镜像重新构建，Web 镜像因本轮未改前端复用缓存；`docker compose --profile tools run --rm db-migrate` 显示 49 个 migrations 且 `No pending migrations to apply`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899` 返回 `HTTP/1.1 200 OK`，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，未登录 `/api/shipments` 返回 401，API 日志包含 `Nest application successfully started`。发布中仅保留既有 Docker Compose buildx/Bake warning、npm audit 提示。

## 当前市场代理成本进入财务应付审核门槛

2026-06-30 本地完成“市场排货代理成本需客服确认数据后进入财务应付审核”的后端门槛，本轮不新增数据库字段、不新增状态、不发布 47。实现范围：市场排货生成的 `PAYABLE / 代理成本` 仍在排货时写入现有运单费用；财务应付审核列表、应付审核动作、待付款列表、付款申请创建统一增加客服数据确认门槛，只有该运单存在 `customer_service.business_data.approved` 审计后才展示、审核或申请付款。门槛只识别能和 `shipment.route` 排货日志金额/计费重/单价匹配的市场排货代理成本，避免误伤财务手工应付。验证：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing|warehouse|business-data|payable"`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/web -- --run src/modules/routing`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`、`npm run typecheck -w @siyuan/web` 均通过。额外复跑 `USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts` 仍有 2 个既有权限夹具断言失败：客服账号读已付款返回 403、客服临时授予应付查看后 `canViewPayables` 仍为 false；本轮未改该权限夹具。

2026-06-30 已发布“市场排货代理成本需客服确认数据后进入财务应付审核”和“市场看板压缩/其他费用备注”到 47。本地发布前验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/routing`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing|warehouse|business-data|payable"`、`npm run build -w @siyuan/api`、`npm run build -w @siyuan/web` 均通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-DKKW9xFL.js`、`index--zdo-lnW.css`；`docker compose --profile tools run --rm db-migrate` 显示 49 个 migrations 且 `No pending migrations to apply`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`api/web/postgres/redis` 均 Up，Web 端口 `18899` 返回 `HTTP/1.1 200 OK`，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，未登录 `/api/shipments` 返回 401，API 日志包含 `Nest application successfully started`。发布中仅保留既有 Vite 大 chunk warning、Docker Compose buildx/Bake warning、npm audit 提示。

## 当前市场看板压缩与其他费用备注

2026-06-30 本地完成市场看板压缩和市场排货其他费用备注，本轮不新增数据库字段、不新增接口、不发布 47。实现范围：`市场管理 -> 市场看板` 顶部 KPI 改为紧凑卡，新增 `今日排货` 和 `今日出货` 当天数据；本周代理、本周渠道、本周敏感货物、本周报关货物合并为一张紧凑 `本周排货数据` 卡，代理/渠道仍按本周排货时间统计。`市场排货` 弹窗新增 `其他费用备注`，当 `其他费用 > 0` 时前后端均要求填写，用于说明其他费用包含什么；备注随 `POST /api/shipments/:id/route` 写入现有 `代理成本` 应付费用 `remark`，并进入 `shipment.route` 审计 after 字段，未新增表字段。验证：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/routing`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 均通过。尝试用本地 Chrome/Playwright 做布局截图时登录控件定位超时，未完成浏览器截图；本轮启动的本地 Web/API dev server 已停止。

## 当前市场/仓库排货成本展示与权限裁剪

2026-06-30 本地完成并已发布 47：市场/仓库排货成本展示与权限裁剪，本轮不新增数据库字段、不新增接口。实现范围：`市场管理 -> 已排货` 增加业务成本、业务成本合计、市场成本、市场成本合计，业务成本复用现有 `BusinessCostAuditSummary` 按运单匹配，市场成本展示排货生成的代理成本/`routeCostTotal`；`市场管理 -> 待排货` 去掉计费重、单价、其他费用、总成本等未排货成本列。`仓库管理 -> 待出库` 保留出货单号、国家、件数、计费重、渠道、代理、状态和打单/出货操作，不显示业务成本、市场成本、应付、利润等财务字段；`仓库管理 -> 待排货` 收敛为站点、业务员、渠道、目的地、状态、操作，仓库角色只读且无排货/修改/删除。前端不再向仓库页传入业务成本/应付成本数据；后端 `GET /api/shipments` 对仓库/客服类列表结果裁剪 `routeChargeWeightKg/routeUnitPrice/routeOtherFee/routeCostTotal/routeCurrency` 市场成本字段。本地验证：`npm test -w @siyuan/web -- --run src/modules/routing`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing|warehouse"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 均通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-BGrt_xE5.js`、`index-CXgJ10EE.css`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`docker compose ps` 显示 `api/web/postgres/redis` 均 Up，Web 端口 `18899`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`GET /api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`。

## 当前市场排货成本与操作列修正

2026-06-30 本地完成市场排货弹窗与待排货操作列修正，本轮不新增数据库字段、不发布 47。实现范围：`市场排货` 弹窗新增 `其他费用`，总成本改为 `计费重 × 单价 + 其他费用`，并把其他费用随 `POST /api/shipments/:id/route` 写入应付代理成本、运单返回字段和排货审计；排货弹窗不再默认选择代理，也不再默认填代理渠道，必须由市场人工选择代理、录入/选择代理渠道后才能确认。代理渠道输入改为可模糊选择的手输框，同一代理下同名手输代理渠道在排货应付记录里累计达到 10 次后，会自动沉淀到现有 `AgentChannel` 代理渠道资料，下次主数据刷新后可直接选择。待排货行去掉 `修改` 和 `删除`，保留排货与排货日志；已排货仍可进入必要的人工修改链路。验证：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/routing`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "sends routed"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 均通过。按用户最新要求，本轮不做浏览器人工检查；代理渠道 10 次沉淀已落后端逻辑，本轮未额外塞 10 笔大样本 e2e，后续如需严验可单独补一条轻量资料库用例。

## 当前市场待排货站点关联修正

2026-06-30 本地完成市场管理细节修正，本轮不新增数据库字段、不发布 47。实现范围：`市场管理 -> 市场看板` 移除 `待排货概览` 明细表，看板只保留统计卡和本周分布；待排货明细仍保留在 `待排货` 二级功能。运单列表后端按 `customer.salesperson -> User.username` 匹配业务员账号，并把账号 `site` 作为 `Shipment.site` 返回，市场待排货表的站点列即可自动显示，例如业务员账号归属 `深圳思远` 时显示 `深圳思远`。客户编号、客户等字段继续来自业务员录单/客户资料关联，不在市场模块重复维护。验证：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/routing`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "scopes operator shipment"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 均通过；本地 Chrome 1366px 检查确认市场看板无待排货明细，待排货页站点显示 `深圳思远`。

## 当前市场看板指标补充

2026-06-30 本地完成 `市场管理 -> 市场看板` 指标补充，本轮不新增后端接口、不改数据库、不发布 47。实现范围：看板保留 `待排货`、`已排货`，新增本周统计区：`本周排货代理` 按 `agentName` Top 5 统计，空值显示 `未分配`；`本周排货渠道（空运/海运）` 按 `routeAgentChannelName` 优先、`channelName` 兜底识别，`空运/空派/航班` 归空运，`海运/海卡/海派/船` 归海运，其余归其他渠道；`本周敏感货物` 按 `sensitive === true` 统计并显示 `带电/带磁/敏感`；`本周报关货物` 按 `declarationRequired === true` 统计。所有本周口径均按自然周周一 00:00 起、以 `routedAt` 为排货时间。验证：`npm test -w @siyuan/web -- --run src/modules/routing` 通过，5 个测试通过；`npm run typecheck -w @siyuan/web` 通过；本地启动 Web/API 后用 Chrome 检查 1366px 和 1920px 市场看板，新增统计卡和分布表可读，无重叠、竖排或裁切；待排货宽表仍按既有横向滚动展示。

## 当前客服数据确认模块落地

2026-06-30 已发布客服 `数据确认` 模块到 47。流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `47:/opt/siyuan`；47 上首次 `docker compose build db-migrate api web` 因 `docker.m.daocloud.io` TLS handshake timeout 拉取元数据失败，重试后构建成功，Web 新产物为 `index-Cx_yAhu1.js`、`index-CXgJ10EE.css`、`xlsx-CkFp8p6R.js`、`antd-vendor-sNQNWg1t.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`docker compose ps` 显示 `api/web/postgres/redis` 均 Up，Web 端口 `18899`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`GET /api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`。

2026-06-30 本地完成客服管理现有二级目录下的 `数据确认` 模块落地，本轮不改二级目录、不新增数据库字段、不新增 API 路由、不发布 47。实现范围：`OUTBOUNDED` 已出库运单先进入 `数据确认`，前端按 `customer_service.business_data.approved` 审计拆分状态池；未确认的 `OUTBOUNDED` 显示在 `数据确认`，确认后的 `OUTBOUNDED` 显示在 `转单号`。`数据确认` 表格展示运单创建时间、出库时间、业务员、客户编号、目的地、品名、件数、实重、计费重、报关、敏感、代理、业务渠道、代理渠道、出货单号、转单号和操作；点击 `确认` 弹框只读展示目的国家、出货件数、计费重量、是否报关、是否敏感，并复用 `POST /api/shipments/:id/business-data/approve` 写入审计。后端转单号守卫从“业务数据 + 代理数据双确认”调整为“业务数据确认即可填写转单号”；代理数据确认保留但不阻塞转单号。业务数据确认审计补齐目的国家、件数、计费重量、报关、敏感字段。验证：`npm test -w @siyuan/web -- --run src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 通过，11 个测试通过；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "business data"` 通过；`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web` 通过。剩余：本轮按要求未做浏览器人工检查，未发布 47；测试输出仍有既有 AntD `destroyOnClose` 弃用警告。

## 当前市场管理排货闭环落地

2026-06-30 本地完成 `市场管理` 排货闭环，本轮未发布 47。实现范围：市场二级功能整理为 `市场看板 / 待排货 / 已排货 / 本周排货数据`，去掉占位入口；`WAITING_SORT` 待排货订单可选择基础资料库代理、手输代理渠道、录入计费重和单价，币种默认 RMB，总成本按 `计费重 × 单价` 自动计算，确认后流转 `WAITING_DISPATCH`。后端复用 `POST /api/shipments/:id/route` 写入代理、业务渠道、手输代理渠道文本、市场成本摘要，并生成一条 `PAYABLE` 应付费用 `代理成本`；旧未锁定代理成本会作废，审计记录排货前后代理、渠道、计费重、单价、总成本、操作人和排货时间。仓库确认出库前会校验已完成代理、渠道和市场成本排货，否则拒绝出库。新增 `POST /api/shipments/:id/reroute`，仅允许 `OUTBOUNDED/WAITING_DEPARTURE` 填写原因退回 `WAITING_SORT` 重排，不直接在已出库状态改代理，并记录原代理/渠道/成本/状态和退回原因。前端测试桩和市场/仓库测试同步覆盖排货成本、已排货待出库、本周统计和退回重排。验证：`npm run build -w @siyuan/shared`、`npm test -w @siyuan/web -- --run src/modules/routing`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "sends routed"`、`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "routing"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 均通过。剩余：本轮尚未做浏览器 1366px/宽屏人工检查，未发布 47。

## 当前客服看板可视化落地

2026-06-30 本地完成 `客服管理 -> 客服看板` 可视化展示，本轮不新增后端接口、不新增数据库字段、不发布 47。实现范围：复用当前页面已有 `shipments/problemTickets/auditLogs` 数据，在客服看板展示两组指标：本周指标（本周异常件、本周已离港、本周已到港、本周已派送、本周已签收）和当前状态池（待离港、已离港、已到港、已派送、已签收、未关闭问题件）。本周口径按当前自然周周一 00:00 到下周一 00:00；异常件按问题件创建时间统计，离港/到港/派送/签收按 `customer_service.status.update` 的 `statusTo` 审计统计。卡片使用现有 AntD `Card/Progress` 做进度条可视化，点击卡片跳转到对应二级目录。验证：`npm test -w @siyuan/web -- --run src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 通过，10 个测试通过；`npm run typecheck -w @siyuan/web` 未通过，失败点是既有无关类型错误：`ShipmentRerouteInput/ShipmentRouteInput` 未导出、`Shipment.routeChargeWeightKg/routeUnitPrice/routeCurrency/routedAt/routeReturnedAt` 缺失，以及 `RoutingPage.tsx` 既有参数顺序错误。剩余：未做浏览器人工检查，未发布 47；测试仍有既有 AntD `destroyOnClose` 弃用警告。

## 当前客服问题件模块落地

2026-06-30 本地完成客服管理现有二级目录下的 `问题件` 模块落地，本轮不改二级目录、不新增数据库字段、不新增后端路由、不发布 47。实现范围：`问题件` 默认展示全部真实问题件，顶部增加 `离港前问题件 / 到港问题件 / 派送问题件 / 售后` 四类和票数，点击分类后只显示对应类别；分类优先从 `customer_service.issue.attach.after.originalStatusPool/originalStatus` 派生，缺审计的旧数据回退到当前运单状态。宽表补齐截图字段：运单创建时间、出库时间、问题件类别、问题件停留时间、业务员、客户编号、目的地、出货单号、转单号、品名、件数、实重、计费重、代理、业务渠道、代理渠道、问题件内容、报关、敏感、代理数据、ETD/ATD、ETA/ATA、查件网址、处理人、处理时间、操作；支持列设置/排序和横向滚动，操作列固定展示 `问题件已经解决`、`问题件需协助`。筛选区支持业务员、停留时间最少天数、客户编号、运单号、目的地、代理；停留时间按问题件创建时间计算。处理人/处理时间取最近一条 `customer_service.issue.attach/update/close` 审计；查件网址和代理数据继续复用客服宽表 helper。待离港、已到港、已派送、已签收等状态池创建问题件后会自动跳到 `问题件` 并选中对应类别；`问题件已经解决` 复用 `POST /api/problem-tickets/:id/close`，`问题件需协助` 复用回复接口追加 `问题件需协助`。验证：`npm test -w @siyuan/web -- --run src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 通过；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "problem ticket"` 通过；`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 通过。剩余：本轮按要求未做浏览器人工检查，未发布 47；测试输出仍有既有 AntD `destroyOnClose` 弃用警告。

## 当前客服已派送/提取模块落地

2026-06-30 本地完成客服管理现有二级目录下的 `已派送/提取` 功能，本轮不改二级目录、不新增数据库字段、不新增 API 路由、不发布 47。实现范围：`已派送/提取` 落在 `已到港` 二级目录，展示 `ARRIVED_PORT` 运单专属宽表，字段复用客服状态池口径（运单创建时间、出库时间、业务员、客户编号、目的地、品名、件数、实重、计费重、报关、敏感、代理、业务渠道、代理渠道、出货单号、转单号、代理数据、ETD/ATD、ETA/ATA、查件网址、处理人、处理时间、操作），支持列设置/排序和横向滚动，操作列固定展示 `修改`、`已派送/提取`、`问题件`。`处理人` 取 `customer_service.status.update` 的 `statusTo=ARRIVED_PORT` 操作人，`已到港` 阶段处理时间按需求显示 `-`；流转到 `已派送` 后，`已派送` 列表处理时间读取 `statusTo=DELIVERING` 审计时间。`修改` 弹框复用现有 ETA/ETD/查询网站修改能力，确认后停留当前目录；查件网址继续复用运营更新审计，客服端屏蔽时显示 `屏蔽：网址`。点击 `已派送/提取` 复用 `PATCH /api/shipments/:id/operational` 流转到 `DELIVERING` 并切到 `已派送` 二级目录。`已到港` 问题件常用标签为 `联系不上收货人/收货人地址错误`，支持标签加手写或纯手写并创建真实问题件。验证：`npm test -w @siyuan/web -- --run src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 通过；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "delivery"` 通过；`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 通过。剩余：本轮按要求未做浏览器人工检查，未发布 47；测试输出仍有既有 AntD `destroyOnClose` 弃用警告。

## 当前客服确认签收模块落地

2026-06-30 本地完成客服管理现有二级目录下的确认签收功能，本轮不改二级目录、不新增数据库字段、不新增 API 路由、不发布 47。实现范围：确认签收落在 `已派送` 二级目录，展示 `DELIVERING` 运单专属宽表，字段复用已离港口径（运单创建时间、出库时间、业务员、客户编号、目的地、品名、件数、实重、计费重、报关、敏感、代理、业务渠道、代理渠道、出货单号、转单号、代理数据、ETD/ATD、ETA/ATA、查件网址、处理人、处理时间、操作），支持列设置/排序和横向滚动，操作列固定展示 `正常签收（归档）`、`售后问题`。`处理人` 取 `customer_service.status.update` 的 `statusTo=ARRIVED_PORT` 操作人，`处理时间` 取 `customer_service.signature.confirm` 的签收审计时间；查件网址继续复用 `shipment.operational.update.after.trackingWebsite` 和可见性，屏蔽时客服端显示 `屏蔽：网址`。点击 `正常签收（归档）` 复用 `PATCH /api/shipments/:id/operational` 流转到 `SIGNED` 并切到 `已签收`；后端既有规则继续限制只能订单归属业务员或管理员签收。点击 `售后问题` 复用真实问题件接口，常用标签为 `货物丢失/货物破损`，支持标签加手写或纯手写。验证：`npm test -w @siyuan/web -- --run src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 通过；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "signature"` 通过；`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 通过。剩余：本轮未做浏览器人工检查，未发布 47；`售后` 二级目录仍待后续统一做签收后/问题关闭后的跟进池。

## 当前客服已离港模块落地

2026-06-30 已发布客服 `已离港` 模块到 47。流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `47:/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-Cp86InhZ.js`、`index-CXgJ10EE.css`、`xlsx-CkFp8p6R.js`、`antd-vendor-DLVwB0mc.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`docker compose ps` 显示 `api/web/postgres/redis` 均 Up，Web 端口 `18899`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`GET /api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`。

2026-06-30 本地完成客服管理现有二级目录下的 `已离港` 模块落地，本轮不改二级目录、不发布 47、按用户要求不做浏览器人工检查。实现范围：`已离港` 专属宽表补齐截图字段（运单创建时间、出库时间、业务员、客户编号、目的地、品名、件数、实重、计费重、报关、敏感、代理、业务渠道、代理渠道、出货单号、转单号、代理数据、ETD/ATD、ETA/ATA、查件网址、处理人、处理时间、操作），表格支持列设置/排序和横向滚动，操作列固定展示 `修改`、`已到港`、`问题件`。`处理人/处理时间` 从 `customer_service.status.update` 的 `statusTo=DEPARTED` 审计读取，`代理数据` 从 `customer_service.agent_data.approved` 审计读取。`修改` 弹框支持 ETD/ATD、ETA/ATA、查询网站、查询网站对业务显示、网站是否同步业务、是否推送业务，确认后仍停留在已离港并写入运营更新审计；客服端屏蔽网址显示为 `屏蔽：网址`。`已到港` 直接流转 `DEPARTED -> ARRIVED_PORT` 并切到已到港二级目录。`问题件` 在已离港状态使用常用标签 `目的港运港查验/集装箱被甩在XX码头`，仍支持标签加手写或纯手写并创建真实问题件。后端最小增强：`PATCH /api/shipments/:id/operational` 传入 `trackingWebsite` 或 `trackingWebsiteVisibleToSales` 时，即使转单号未变，也写入 `shipment.operational.update.after` 供列表和弹框读取；未新增 API 路由、未新增数据库迁移。验证：`npm test -w @siyuan/web -- --run src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 通过；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"` 通过；`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 通过。

## 当前客服待离港模块落地

2026-06-30 本地完成客服管理现有二级目录下的待离港相关功能落地，本轮不改二级目录、不发布 47。实现范围：`待离港` 宽表补齐截图字段和列设置/排序入口，操作列提供 `已离港`、`问题件`、`修改转单号`、`上传面单`；确认已离港弹框支持 ETD/ATD、ETA/ATA、查询网站是否对业务显示、网站是否同步业务、是否推送业务勾选，确认后真实更新为 `DEPARTED` 并进入已离港状态池；问题件弹框支持常用标签 `数据不对/起运港查验` 加手写原因并创建真实问题件；修改转单号弹框展示系统匹配的客户编号/运单号/原转单号，支持覆盖转单号和填写分单号；面单上传支持选择文件、拖拽、粘贴截图，前端限制图片/PDF、10MB，后端新增 `POST /api/shipments/:id/labels/upload` 保存到现有 `/api/uploads/labels/...` 访问路径并创建 `ShipmentLabel`。企业微信/AI 推送未真实对接，只保留勾选和提示。权限侧同步修正客服、财务、业务用户组与旧内置角色的后端业务判断，避免菜单可见但接口被旧角色 key 拦截。验证：`npm test -w @siyuan/web -- --run src/modules/customerService src/modules/problemTickets/problemTickets.test.tsx` 通过；`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"` 通过，包含真实面单上传断言；`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 通过。按用户最新要求，本轮未继续做浏览器 1366px/宽屏人工检查。

## 当前 47 发布记录

2026-06-30 已发布当前工作区到 47，包含 `仓库管理 -> 在仓数据` 严格补齐和报价查价 Top3 卡片展示优化。流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `47:/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-C6XiyE8J.js`、`index-IOBLxvZg.css`、`xlsx-CkFp8p6R.js`、`antd-vendor-CyisERKa.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`docker compose ps` 显示 `api/web/postgres/redis` 均 Up，Web 端口 `18899`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`GET /api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`。47 源码采证确认包含 `renderRecommendationMeta`、`.pricing-recommendation-meta` 和 `单价 / 毛利`。

## 当前墨家设备量方接入

2026-06-30 已完成并发布墨家设备 POST JSON 接入到 47。复用现有 `WarehousePackage`、`POST /api/warehouse/packages`、`GET /api/warehouse/today-receipts`、仓库创建审计和今日收货/在仓流转，未新增仓库表、未使用订单 `receiveShipment`。新增设备入口 `POST /api/integrations/mojia/measurements`，支持厂家手册字段 `orderNo/length/width/height/weight/machineNo`，也兼容 Sunny 推荐字段 `barcode/lengthCm/widthCm/heightCm/weightKg/measuredAt/deviceNo`；使用 `MOJIA_DEVICE_TOKEN` 校验 `X-Device-Token` 或 `?token=`，接口内部以 `mojia-device` 仓库身份调用现有仓库创建逻辑。最小验证：本地 `npm run typecheck -w @siyuan/api` 通过；`MOJIA_DEVICE_TOKEN=test-mojia-token USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"` 通过，覆盖错误 token 401、正确 token 写入、今日收货按组合号查回且无财务字段泄露。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 `.env` 已加入 `MOJIA_DEVICE_TOKEN`；47 上 `docker compose build db-migrate api web`、`docker compose --profile tools run --rm db-migrate`、`docker compose up -d --remove-orphans api web` 成功，迁移结果 `No pending migrations to apply`，Web 实际端口 `18899`，`/api/health` 返回 `HTTP 200`。47 真实接口验证：错误 token 返回 `401`；正确 token POST 样本 `9409-KYMJ125556` 返回 `{"result":"true","message":"9409-KYMJ125556 录入成功"}`；47 真库 `WarehousePackage` 查到 `customerCode=9409`、`domesticTrackingNo=KYMJ125556`、`combinedOrderNo=9409-KYMJ125556`、`weightKg=100.4`、`lengthCm=100.1`、`widthCm=100.2`、`heightCm=100.3`、`scanSource=墨家设备`、`remark=设备号：MJ20210327`、`createdBy=mojia-device`。剩余注意：仅靠 `barcode/长宽高/重量/时间/设备号` 已能完成今日收货到在仓的基础流转；若后续要严格表达“总件数/第几件”，再让厂家补 `packageCount/packageIndex/expectedTotalPackageCount`。

2026-06-30 按公网设备接口从本机向 47 补充 10 条不同量方样本：`9409-KYMJ0630T01` 至 `9409-KYMJ0630T10`。请求地址为 `http://47.120.33.111:8899/api/integrations/mojia/measurements`，Header 使用当前固定 `X-Device-Token`，Body 使用 `barcode/lengthCm/widthCm/heightCm/weightKg/measuredAt/deviceNo`。10 次 POST 均返回 `{"result":"true","message":"... 录入成功"}`；47 真库复查 `WarehousePackage` 命中 10 条，均拆出 `customerCode=9409`、`domesticTrackingNo=KYMJ0630Txx`，`scanSource=墨家设备`、`remark=设备号：MJ20210327`、`createdBy=mojia-device`。

2026-06-30 已补墨家设备重试幂等兼容，并发布 47。厂家确认重试策略为 5 秒超时、最多重试 3 次；系统侧在不要求新增 `eventId` 的前提下，按已确认 7 字段中的 `barcode + measuredAt + deviceNo` 做重复判断，同一次扫描重试直接返回 `{"result":"true","message":"... 已接收"}`，不重复写入 `WarehousePackage`。本地验证：`npm run typecheck -w @siyuan/api` 通过；`MOJIA_DEVICE_TOKEN=test-mojia-token USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "Mojia device"` 通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web`、`docker compose --profile tools run --rm db-migrate`、`docker compose up -d --remove-orphans api web` 成功，迁移结果 `No pending migrations to apply`。47 公网验证：同一 Body 连续请求 `9409-KYMJRETRY151324`，第一次返回 `录入成功`，第二次返回 `已接收`，真库 `WarehousePackage` 计数为 1。

## 当前在仓数据严格补齐

2026-06-30 本地完成 `仓库管理 -> 在仓数据` 严格补齐，本轮未发布 47。复用现有 `WarehousePackage`、`GET /api/warehouse/in-stock`、`POST /api/warehouse/packages/:id/split`、`POST /api/warehouse/consolidations`、理货任务、录单入口、仓库权限裁剪和 `AuditLog`，未新增库存表、未新增路由、未引入新前端依赖。最小补差：在仓默认列调整为截图核心字段（勾选、站点、客户编号、组合号、件数、单件实重、尺寸、单件方数、围长、单件5000/6000材积、扫描时间、总实重、总体积、总5000/6000材积、备注、异常、操作）；单件/总量口径分开展示，围长按最长边 + 2 * 另外两边；顶部拆出 `批量理货` 和 `合票录单`，合票使用 `MERGE_ONLY` 进入录单草稿，不直接生成待审核运单；拆票续号按最原始组合号连续编号，继续拆子票生成 `...-3/...-4`，避免 `...-1-1`；拆票后清理源包裹勾选，避免旧选择参与合票。数据证据：API 测试覆盖 `9409-KY-STOCK-075` 75 件拆 50/25 后继续拆 30/20，生成 `-3/-4`，源包裹不再显示在在仓，合票后创建录单草稿且 `draftWarehousePackageIds` 保留所选包裹。角色/字段证据：沿用仓库后端 scope 和无价格字段断言，仓库/业务员接口不返回应收、应付、利润、付款字段。日志证据：测试覆盖 `warehouse.package.split`、`warehouse.tally.start`、`warehouse.consolidation.create`。验证命令：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "in-stock rows"`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web` 均通过。剩余：本轮未做 1366px/宽屏浏览器人工复查，未发布 47；列顺序仍按当前列设置数组渲染，不做拖拽排序。

## 当前报价查价 Top3 卡片展示优化

2026-06-30 本地完成报价查价结果“最便宜 Top3 / 最快 Top3”推荐卡片重点字段优化，本轮未发布 47。只改前端展示和对应测试，未改查价接口、价格计算、排序逻辑或数据库。实现：Top3 卡片保留渠道名加粗和右侧报价/时效 Tag；将 `代理 / 重量段`、`单价 / 毛利` 做成重点行；管理员可见 `代理成本单价` 使用较弱样式；`产品附加` 与 `特别说明/尺寸要求` 保持单行省略，但标签加粗，点击卡片仍打开报价详情查看完整备注。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10 通过；`npm run typecheck -w @siyuan/web` 通过；本地 Web/API 内存环境用 Chrome 检查 1366px 与 1920px，Top3 卡片无异常溢出、无竖排、按钮/Tag 未裁切，备注保持单行省略。剩余：尚未发布 47。
