# Codex State Archive: 2026-06-28 to 2026-06-29

Archived on 2026-07-07 from `.codex-state.md` to keep the active state file within the rolling 7-day window.

## 当前阻塞项

- 本地文档整理无阻塞。
- 47 创建受控 QA 样本已获授权并完成一组 `QA47-9409-Q470466510` 真实样本；历史数据清洗仍需单独授权。
- A-F 第一阶段代码和迁移已同步/发布到 47；`STRICT_FULL_CHAIN_CANDIDATE=0` 仍不能用历史数据总销账。
- 47 上有真实数据库；本机测试、Docker 发布成功、迁移结构检查、只读字段检查都不能替代 47 线上受控业务样本验证。
- 47 `OPERATOR` 缺少 `warehouse:read` 已用现有系统角色权限接口修复；未直接改权限关联表。
- 47 `OPERATOR` 历史利润相关权限已用现有系统角色权限接口收敛；未直接改权限关联表。

## 下一步任务卡

无下一张本地业务任务卡；下一步是发布前复核或 47 受控业务样本复验。

开工时先读：

1. `docs/sunny-dev-index.md`
2. 本文件
3. `docs/sunny-dev-index.md`
4. 相关代码、接口、表、状态、权限、审计 action 和测试

## 最近一次验收结论

2026-06-29 47 `OPERATOR` 利润权限收敛修复。复用现有 `PUT /api/system/roles/:role/permissions`、47 真库 `Role/Permission`、`GET /api/warehouse/in-stock` 和 `AuditLog`，未改业务代码、未新增权限、未直接改 `_PermissionToRole`。根因证据：47 `OPERATOR` 在上次修复后仍保留 `finance:business-cost:view-profit`、`finance:order-fee:profit:receivable-business`，而本地标准 `OPERATOR` 权限不包含利润相关权限。修复动作：用 `admin` 调现有系统角色权限接口，只移除这两个利润权限，保留 `warehouse:read` 和既有业务成本/水单读取权限。数据证据：47 真库 `OPERATOR_PERMS=finance:business-cost:manage,finance:business-cost:read,finance:water-receipt:read,pricing:lookup,warehouse:read`，`HAS_FORBIDDEN_PROFIT=0`；operator 重新登录后调用 `/api/warehouse/in-stock?customerCode=9409` 仍返回 `HTTP 200` 且读取到本轮受控样本 `2` 条。字段裁剪证据：operator 在仓响应未命中 `receivableAmount/payableAmount/profit/grossProfit/payables/bankAccount/paymentApplication/PaymentVoucher/billAmount`。角色证据：权限收敛由 `admin` 执行，读取验证由 `operator` 执行。日志证据：最新 `system.role_permissions.update` target 为 `role:OPERATOR`，before 含两个利润权限，after 已移除且保留 `warehouse:read`。剩余风险：本轮只收敛 `OPERATOR` 已发现的利润权限；其它角色的线上权限矩阵仍需在 E 批受控复验中逐项核对。

2026-06-29 47 `OPERATOR` 在仓读取权限修复。复用现有 `PUT /api/system/roles/:role/permissions`、`warehouse:read`、`GET /api/warehouse/in-stock`、`AuditLog` 和 47 真库角色权限表，未改业务代码、未新增权限、未直接改 `_PermissionToRole`。根因证据：47 真库 `OPERATOR` 原权限只有 `pricing:lookup`、业务成本、水单读取和利润相关权限，`HAS_WAREHOUSE_READ=0`；本地代码和接口均已允许 `OPERATOR + warehouse:read` 读取在仓。修复动作：用 `admin` 调现有系统角色权限接口，只在原权限集合上追加 `warehouse:read`，保留线上既有权限。数据证据：47 真库复查 `HAS_WAREHOUSE_READ=1`；operator 重新登录后调用 `/api/warehouse/in-stock?customerCode=9409` 返回 `HTTP 200` 且读取到本轮受控样本 `2` 条。字段裁剪证据：operator 在仓响应字段为仓库包裹字段，未命中 `receivableAmount/payableAmount/profit/bankAccount/paymentApplication/PaymentVoucher/billAmount`。角色证据：修复由 `admin` 执行，读取由 `operator` 执行，不只用管理员验收。日志证据：`system.role_permissions.update` target 为 `role:OPERATOR`，after 包含 `warehouse:read`；30 分钟窗口内 `warehouse.in_stock.view=12`。后续状态：本条遗留的利润权限风险已在 2026-06-29 `OPERATOR` 利润权限收敛修复中处理完成。

2026-06-29 47 发布与 9409 受控样本验收。已按 `docs/47-cloud-docker-release.md` 先执行 `npm run sync:47` 预演，再执行 `npm run sync:47 -- --apply` 同步到 `/opt/siyuan`，随后在 47 使用 Docker Compose 构建 `db-migrate/api/web`、执行 `docker compose --profile tools run --rm db-migrate`、重启 `api/web` 并健康检查；47 Web `http://127.0.0.1:18899/` 返回 `HTTP 200`，API 日志显示应用启动成功。迁移证据：47 真库已应用 `20260628213000_add_warehouse_tally_label_fields`、`20260629103000_add_agent_bill_voucher_fields`、`20260629113000_add_payment_voucher_transfer_no`、`20260629123000_add_payment_voucher_difference_fields`、`20260629133000_add_payment_voucher_extra_fee_fields`、`20260629143000_add_payment_voucher_kuayue_fields`；结构证据显示 `PaymentVoucher` 已包含账单、转单号、差异、杂费、跨越账单字段，`WarehouseTallyTask` 已包含标签生成/打印/下载字段。受控样本证据：在 47 真实 API 和真实数据库上创建 `QA47-9409-Q470466510` 样本，仓库账号创建 2 条 `WarehousePackage`，查询今日收货和在仓，创建并完成 1 条 `WarehouseTallyTask`，生成/打印/下载 1 个理货标签；财务账号创建 1 条 `WaterReceipt` 并确认到账，复用 9409 现有 Shipment 创建 1 条应付审核、审核生成 1 条待付款，再挂 1 条代理账单 `Q470466510AB`，完成差异处理并归档。数据证据：47 真库查询返回 `WarehousePackage=2`、`WarehouseTallyTask=1(labels=1)`、`WaterReceipt=1(arrived=1)`、`ShipmentFinanceItem PAYABLE=1`、`PayablePaymentApplication=1`、`PaymentVoucher=1(ARCHIVED=1,HANDLED=1)`；字段证据：`PaymentVoucher Q470466510AB` 状态为 `ARCHIVED`、差异为 `HANDLED`、关联 `pendingPaymentId=cmqyg0jh6001mo00164v2e4v2`、上传人为 `finance`，`WaterReceipt SD20260629001` 状态为 `ARRIVED/RMB/940.9`，理货任务 `Q470466510A-KYQ470466510A-TL001` 状态为 `COMPLETED` 且标签号为 `Q470466510A-KYQ470466510A-TL001-LBL`。角色证据：47 登录日志在 2 小时窗口内包含 `warehouse=3`、`finance=4`、`operator=3`、`customer=3`；仓库动作由 `warehouse` 执行，财务动作由 `finance` 执行，`operator` 写仓库包裹返回 403 并写 `security.permission.denied`，`customer` 访问代理账单返回 403。日志证据：47 `AuditLog` 查询到 `warehouse.package.create=2`、`warehouse.tally.create/complete=1`、`warehouse.tally.label.generate/print/download=1`、`finance.water_receipt.create/arrive=1`、`finance.payable.create/audit=1`、`finance.payment_voucher.add/archive=1`、`finance.payment_voucher.difference.handle=1`、`security.permission.denied=1`。限制说明：本轮没有清洗历史数据；`operator` 在 47 读取在仓接口最初返回 403，已在 2026-06-29 后续权限修复中通过追加 `warehouse:read` 解决；E 批仍需单独复验线上利润相关权限是否收敛。

2026-06-29 F6-bill-fee-audit 本地销账；本轮未同步 47。复用现有 `PaymentVoucher`、`GET/POST/PATCH /api/finance/payment-vouchers*`、`PayablePaymentApplication`、`PaymentApplication`、`ShipmentFinanceItem`、财务代理账单页、`finance:payable:read/attachment`、`AuditLog` 和既有财务 API/Web 测试；未新增平行账单模型、平行归档表、新权限或外部会计/AI/PDA 前置。最小补差为复用既有 `status=ARCHIVED/MATCHED` 补同族 `PATCH /api/finance/payment-vouchers/:id/archive`，写入 `finance.payment_voucher.archive/unarchive` 日志，并在原代理账单页补归档/反归档按钮。数据证据：本地 API 样本 `AB-9409-001` 覆盖账单导入、应付费用 id、运单号、转单号、代理渠道、计费重、单价、应付金额、差异处理后归档为 `ARCHIVED`，再反归档为 `MATCHED`；同一测试还覆盖 `AB-9409-EXTRA` 杂费归属和 `KY-9409-001` 跨越账单入口。角色证据：管理员可归档/反归档，`operator` 调用归档接口返回 403；账单 POST/GET/差异处理仍不向业务员开放，不只用管理员账号验收。日志证据：`finance.payment_voucher.add`、`finance.payment_voucher.extra_fee.add`、`finance.payment_voucher.kuayue.add`、`finance.payment_voucher.difference.mark/handle`、`finance.payment_voucher.archive/unarchive` 均由本地 e2e 验证；归档日志包含 before `DIFFERENCE_HANDLED`、after `ARCHIVED` 和归档原因。字段裁剪证据：账单读取仍由后端 `finance:payable:read` 保护，写入/差异/归档由 `finance:payable:attachment` 保护，响应不新增代理银行、付款账号或利润字段；业务员/仓库不能访问完整代理账单入口。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "manages payable audits"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "registers and queries an agent bill"`。47 口径：本轮未同步 47；只读验证显示 47 `PaymentVoucher` 字段仍为 `id,paymentApplicationId,pendingPaymentId,fileName,mimeType,sizeBytes,url,uploadedBy,createdAt,voucherType`，`status=0`，`PaymentVoucher=35`，`9409` 文本命中 `0`，未查到 F 批 `finance.payment_voucher.*` 账单日志输出；47 当前没有 F6 新接口/字段和 9409 账单受控样本，不能用本机测试、结构检查或历史计数替代 47 线上受控业务样本验证。下一步不再进入新的本地业务任务卡；如要继续，需要先做发布前复核，再按授权同步 47 或集中创建 47 受控业务样本。

2026-06-29 F5-kuayue-bill-manual-entry 本地销账；本轮未同步 47。复用现有 `PaymentVoucher`、`POST/GET /api/finance/payment-vouchers`、`PayablePaymentApplication`、`ShipmentFinanceItem`、`finance:payable:read/attachment`、财务代理账单页、`AuditLog` 和既有财务 API/Web 测试；未新增平行跨越账单表、平行账单模型、新路由或新权限。最小补差为在既有 `PaymentVoucher` 上补 `kuayueBillNo/kuayueCustomerCode/kuayueSystemOrderNo/kuayueAmount/kuayueCurrency/kuayueBillDate/kuayueStatus`，登记含跨越账单号时额外写 `finance.payment_voucher.kuayue.add` 日志。数据证据：本地 API 样本 `KY-9409-001` 归属 `跨越速运`、客户 `9409`、订单 `SYPAYABLEAUDIT001`、金额 `88 RMB`、账单日期 `2026-06-29`、状态 `LINKED`、文件 `kuayue-bill.png`，并可从账单查询反查既有应付费用、运单号、转单号和待付款；Web 样本可在原代理账单页录入并展示跨越账单号、客户、订单、金额、账单日期和文件。角色证据：管理员可登记跨越账单，`operator` 调用代理账单 POST/GET 仍返回 403；不只用管理员账号验收。日志证据：`finance.payment_voucher.kuayue.add` 写入 `AuditLog`，after 记录跨越账单号、客户、订单、金额、币种、状态、文件和操作人。字段裁剪证据：账单读取仍由后端 `finance:payable:read` 保护，跨越账单登记复用 `finance:payable:attachment`，响应不新增代理银行、付款账号或利润字段；业务员/仓库不能访问完整代理账单入口。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "manages payable audits"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "registers and queries an agent bill"`。47 口径：本轮未同步 47；只读验证显示 47 `PaymentVoucher` 字段仍为 `id,paymentApplicationId,pendingPaymentId,fileName,mimeType,sizeBytes,url,uploadedBy,createdAt,voucherType`，`kuayueBillNo/kuayueCustomerCode/kuayueSystemOrderNo/kuayueAmount/kuayueCurrency/kuayueBillDate/kuayueStatus` 均为 `0`，`PaymentVoucher=35`，`9409` 文本命中 `0`，未查到 `finance.payment_voucher.kuayue.add` 历史日志；47 当前没有 F5 新字段和 9409 跨越账单受控样本，不能用本机测试、结构检查或历史计数替代 47 线上受控业务样本验证。下一步进入 `docs/slices/F6-bill-fee-audit.md`，开工前必须重新做复用审计。

2026-06-29 F4-extra-fee-attribution 本地销账；本轮未同步 47。复用现有 `PaymentVoucher`、`POST/GET /api/finance/payment-vouchers`、`ShipmentFinanceItem(BUSINESS_COST)`、`POST /api/finance/business-cost-audits`、`finance:payable:read/attachment`、`finance:business-cost:manage`、财务代理账单页、`AuditLog` 和既有财务 API/Web 测试；未新增平行杂费表、平行账单模型、新路由或新权限。最小补差为在既有 `PaymentVoucher` 上补 `extraFeeType/extraFeeAmount/extraFeeCurrency/extraFeeAgentName/extraFeeCustomerCode/extraFeeSystemOrderNo/extraFeeOccurredAt/extraFeeFinanceItemId/extraFeeRemark`，登记含杂费字段时额外写 `finance.payment_voucher.extra_fee.add` 日志。数据证据：本地 API 样本先创建业务成本 `杂费-操作费` 金额 `35 RMB`，再登记 `AB-9409-EXTRA`，归属代理 `宇环`、客户 `9409`、订单 `SYPAYABLEAUDIT001`、发生日期 `2026-06-29`、凭证 `extra-fee-voucher.png`、备注 `代理账单杂费归属`，并关联该业务成本费用 id；Web 样本 `AB-9409-WEB` 可在原代理账单页录入并展示杂费类型、金额、代理、客户、订单、发生日期、费用 id 和备注。角色证据：管理员可录入杂费归属，`operator` 调用代理账单 POST/GET 仍返回 403；不只用管理员账号验收。日志证据：`finance.business_cost.create` 记录杂费进入现有业务成本；`finance.payment_voucher.extra_fee.add` 记录杂费类型、金额、币种、代理、客户、订单、发生日期、关联费用 id、凭证和操作人。字段裁剪证据：账单读取仍由后端 `finance:payable:read` 保护，杂费登记复用 `finance:payable:attachment` 和业务成本 `finance:business-cost:manage`，响应不新增代理银行或付款账号敏感字段；业务员/仓库不能访问完整代理账单杂费入口。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "manages payable audits"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "registers and queries an agent bill"`。47 口径：本轮未同步 47；只读验证显示 47 `PaymentVoucher` 字段仍为 `id,paymentApplicationId,pendingPaymentId,fileName,mimeType,sizeBytes,url,uploadedBy,createdAt,voucherType`，`extraFeeType/extraFeeAmount/extraFeeCurrency/extraFeeAgentName/extraFeeCustomerCode/extraFeeSystemOrderNo/extraFeeOccurredAt/extraFeeFinanceItemId/extraFeeRemark` 均为 `0`，`PaymentVoucher=35`，`ShipmentFinanceItem BUSINESS_COST=83`，`9409 BUSINESS_COST=3`，未查到 `finance.payment_voucher.extra_fee.add` 历史日志；47 当前没有 F4 新字段和 9409 代理账单杂费受控样本，不能用本机测试、结构检查或历史计数替代 47 线上受控业务样本验证。下一步进入 `docs/slices/F5-kuayue-bill-manual-entry.md`，开工前必须重新做复用审计。

2026-06-29 F3-difference-handling 本地销账；本轮未同步 47。复用现有 `PaymentVoucher`、`GET/POST /api/finance/payment-vouchers`、新增同族 `PATCH /api/finance/payment-vouchers/:id/difference`、`finance:payable:read/attachment`、财务代理账单页、`AuditLog` 和既有财务 API/Web 测试；未新增平行差异表、平行账单模型、新路由或新权限。最小补差为在既有 `PaymentVoucher` 上补 `differenceType/differenceAmount/differenceReason/differenceStatus/differenceHandledBy/differenceHandledAt`，并把账单状态扩展为 `DIFFERENCE_PENDING/DIFFERENCE_HANDLED`。数据证据：本地 API 样本 `AB-9409-001` 可标记 `重量差异`、差异金额 `12.5`、原因 `代理账单计费重高于 Sunny 应付`、状态 `PENDING`，处理后变为 `DIFFERENCE_HANDLED`，处理人 `admin`，处理时间存在；Web 样本 `AB-9409-WEB` 可登记差异并在原代理账单列表处理为已处理。角色证据：管理员可标记/处理差异，`operator` 调用差异处理接口返回 403；不只用管理员账号验收。日志证据：`finance.payment_voucher.difference.mark` 和 `finance.payment_voucher.difference.handle` 均写入 `AuditLog`，处理日志包含 before 的 `differenceStatus=PENDING` 和 after 的差异类型、金额、原因、处理状态、处理人和账单状态。字段裁剪证据：账单读取仍由后端 `finance:payable:read` 保护，差异处理由 `finance:payable:attachment` 保护，业务员/仓库无权读取或处理完整代理账单差异；响应不新增代理银行或付款账号敏感字段。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "manages payable audits"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "registers and queries an agent bill"`。47 口径：本轮未同步 47；第一次直接查 `PaymentVoucher.status` 失败，证明 47 真实库仍为旧结构；安全只读复查显示 47 `PaymentVoucher` 字段为 `id,paymentApplicationId,pendingPaymentId,fileName,mimeType,sizeBytes,url,uploadedBy,createdAt,voucherType`，`status=0`、`differenceType=0`、`differenceAmount=0`、`differenceReason=0`、`differenceStatus=0`、`differenceHandledBy=0`、`differenceHandledAt=0`，`PaymentVoucher=35`，`9409` 文本命中 `0`，未查到 `finance.payment_voucher.difference.mark/handle` 历史日志；47 当前没有 F3 新字段和 9409 代理账单差异受控样本，不能用本机测试、结构检查或历史计数替代 47 线上受控业务样本验证。下一步进入 `docs/slices/F4-extra-fee-attribution.md`，开工前必须重新做复用审计。

2026-06-29 F2-agent-bill-payable-match 本地销账；本轮未同步 47。复用现有 `PaymentVoucher`、`GET/POST /api/finance/payment-vouchers`、`PayablePaymentApplication`、`PaymentApplication`、`ShipmentFinanceItem`、`Shipment`、`finance:payable:read/attachment`、`finance.payment_voucher.add`、付款申请/确认付款日志和财务代理账单页；未新增平行代理账单模型、平行应付模型、平行付款模型或新权限。最小补差为在既有 `PaymentVoucher` 上补 `transferNo`，并让账单核对响应从已关联待付款/应付/运单/付款申请派生运单号、代理渠道、计费重、单价、应付金额、付款申请号和已付款记录。数据证据：本地 API 样本 `AB-9409-001` 关联 `SYPAYABLEAUDIT001`、`1Z9409F2001`、应付费用 `sfi-*`、代理渠道、计费重 `20`、单价 `6`、应付金额 `120` 和 RMB 付款申请号；`AB-9409-USD` 在确认付款后可反查付款申请号、`paidPaymentId` 和 `paidAt=2026-06-25`。角色证据：管理员可登记/查询账单核对，`operator` 调用 POST 和 GET 账单接口均返回 403；不只用管理员账号验收。日志证据：`finance.payment_voucher.add` 审计 after 记录账单号、转单号、代理、币种、金额、应付费用 id、运单号、代理渠道、计费重、单价和应付金额；`finance.payment_application.create`、`finance.paid_payment.confirm` 继续证明付款申请和已付款链路。字段裁剪证据：账单查询由后端 `finance:payable:read` 保护，账单上传由 `finance:payable:attachment` 保护，响应只返回核对所需 id/订单/费用字段，不返回代理银行账号或付款方账号；前端隐藏不是事实源。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "manages payable audits"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "registers and queries an agent bill"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `PaymentVoucher` 字段仍为 `id,paymentApplicationId,pendingPaymentId,fileName,mimeType,sizeBytes,url,uploadedBy,createdAt,voucherType`，`PaymentVoucher=35`，`PayablePaymentApplication=39`，`PaymentApplication=14`，`PAYABLE_ITEMS=83`，待付款状态 `APPLIED=4/PAID=6/PENDING=29`，付款申请状态 `PAID=6/WAITING_PAYMENT=8`，`9409` 应付费用 `2` 条，历史仅查到 `finance.paid_payment.confirm=1`；47 当前没有 F2 新字段和 9409 代理账单核对应付/付款受控样本，不能用本机测试、结构检查或历史计数替代 47 线上受控业务样本验证。下一步进入 `docs/slices/F3-difference-handling.md`，开工前必须重新做复用审计。

2026-06-29 F1-agent-bill-import 本地销账；本轮未同步 47。复用现有 `PaymentVoucher`、`POST /api/finance/payment-vouchers`、`finance:payable:attachment/read`、`finance.payment_voucher.add`、待付款/付款申请/应付链路、财务代理账单页入口和既有财务测试；未新增平行代理账单表、平行账单模型或新权限。最小补差为给 `PaymentVoucher` 补 `billNo/agentName/billDate/currency/billAmount/status`，现有 POST 写入并审计这些字段，新增 GET 支持按账单号、代理、币种和状态查询，前端占位页改为人工登记和查询列表。数据证据：本地 API 样本 `AB-9409-001` 关联待付款/付款申请链路，代理 `宇环`、账单日期 `2026-06-29`、`RMB`、金额 `120`、文件 `pending-voucher.png`、状态 `IMPORTED`；Web 样本 `AB-9409-WEB` 可登记并按账单号查询到 `agent-bill.png`。角色证据：管理员/财务可登记和查询，`operator` 调用 POST 返回 403；不只用管理员账号验收。日志证据：`finance.payment_voucher.add` 记录 actor、target、pendingPaymentId、billNo、agentName、currency、billAmount、status、fileName、uploadedBy。字段裁剪证据：GET 由 `finance:payable:read` 保护，POST 由 `finance:payable:attachment` 保护，页面禁用只是辅助，后端 403 是事实源。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "manages payable audits"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "registers and queries an agent bill"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `PaymentVoucher` 字段仍为 `id,paymentApplicationId,pendingPaymentId,fileName,mimeType,sizeBytes,url,uploadedBy,createdAt,voucherType`，总数 `35`，`BILL=29`、`PAYMENT_RECEIPT=6`，`9409` 文本命中 `0`，历史仅查到 `finance.paid_payment.confirm=1`；47 当前没有 F1 新字段和 9409 代理账单受控样本，不能用本机测试、结构检查或历史计数替代 47 线上受控业务样本验证。下一步进入 `docs/slices/F2-agent-bill-payable-match.md`，开工前必须重新做复用审计。

2026-06-29 E5-audit-log-verification 本地销账；本轮未同步 47。复用现有 `AuditLog`、`GET /api/system/audit-logs`、管理员审计查询权限、`recordPermissionDenied`、`workflow.guard_denied`、订单全链路 e2e、仓库 e2e、财务/基础资料审计断言；未新增表、接口、状态、权限或页面。最小补差为在订单全链路 e2e 增加 `expectAuditTrailRow`，把关键动作从只断言 action/target 升级为断言 actor、target、createdAt 可解析、result 和必要 before/after，并覆盖仓库收货、订单录单/审核/排货/出库、客服双审核/转单/状态池/签收、财务应收/水单/应付/付款申请/确认付款、权限拒绝和流程拒绝。数据证据：本地同一客户 `9409`、仓库包裹 `FLOW001-KYFLOW001`、运单 `SYFLOW001`、水单 `FLOW-WATER-001`、付款申请 `payment-app-1` 的关键动作均可按 action 或 target 查询日志；付款申请 target 可查创建和确认付款两条财务日志。角色证据：日志覆盖管理员、业务员 `operator`、仓库 `warehouse`、客服 `service`、财务 `finance`、市场 `R-market` 的成功/拒绝动作；审计查询接口仍只允许管理员访问，非管理员 403。日志证据：关键日志均验证 `actorUsername`、`target`、`createdAt`、`result`、`after`，状态变更类动作验证 `before`；`security.permission.denied` 和 `workflow.guard_denied` 验证失败结果与权限/流程原因。字段裁剪证据：E5 继续沿用 E2/E3 后端字段裁剪和拒绝断言，仓库/业务员/客服/市场/财务不能通过接口拿到敏感财务或越权字段。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "lists in-stock packages"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `AuditLog` 字段为 `id,actorId,action,target,before,after,createdAt`，总数 `394`，`before is not null=50`，`after is not null=394`，核心字段空值 `0`，`9409` 文本命中 `5`；历史 action 查到 `finance.paid_payment.confirm=1`、`finance.receivable.audit=2`、`system.role_permissions.update=4`、`warehouse.consolidation.create=5`、`warehouse.in_stock.view=5`、`warehouse.package.split=1`、`warehouse.tally.start=1`，模块前缀含 `finance=18/pricing=242/qa=30/qa47=50/shipment=21/system=4/warehouse=19`；未查到 E5 受控样本所需的完整基础资料、仓库、订单客服、财务和权限拒绝动作链，不能用本机测试、结构检查或历史计数替代 47 线上受控业务样本验证。下一步进入 `docs/slices/F1-agent-bill-import.md`，开工前必须重新做复用审计。

2026-06-29 E4-object-traceability 本地销账；本轮未同步 47。复用现有 `WarehousePackage`、`Shipment`、`WaterReceipt`、`WaterReceiptMatch`、`ShipmentFinanceItem`、`PayablePaymentApplication`、`PaymentApplication`、`PaymentApplicationItem`、`PaymentVoucher`、`AuditLog`、仓库/订单/财务接口和订单全链路 e2e；未新增表、接口、状态、权限或页面。最小补差为在订单全链路 e2e 的既有 9409 样本末尾补对象互查断言：仓库包裹按组合号和运单号反查，水单按客户和付款编号反查，水单匹配反查运单/应收，待付款反查付款申请/应付/运单/客户，已付款反查付款申请明细/运单/客户，审计 target 反查付款申请创建和确认付款日志。数据证据：本地同一客户 `9409`、组合号 `FLOW001-KYFLOW001`、运单 `SYFLOW001`、水单 `FLOW-WATER-001`、付款申请 `payment-app-1`、付款账号 `FLOW-PAYER-001` 在同一链路可互相反查；仓库包裹出库后 `status=SHIPPED` 且保留 `combinedOrderNo/systemOrderNo`；水单匹配包含 `waterReceiptId/shipmentId/systemOrderNo/customerCode/receivableFinanceItemId`；已付款明细包含 `pendingPaymentId/payableFinanceItemId/shipmentId/systemOrderNo/customerCode`。角色证据：管理员/财务可看完整追溯；业务员/客服仍只能看裁剪后的费用口径，仓库读财务 403，业务员读待付款/已付款 403，市场付款 403，财务出库/签收/改资料 403；没有只用管理员验收。日志证据：`AuditLog` 支持按运单 target 和付款申请 target 查询；同一运单 target 可查录单、审核、排货、出库、客服双审核、状态池、签收；付款申请 target 可查 `finance.payment_application.create` 和 `finance.paid_payment.confirm`，after 带 `systemOrderNos/customerCodes/payableFinanceItemIds/pendingPaymentIds`。字段裁剪证据：追溯验证同时保留 E2/E3 的后端裁剪和拒绝断言，不通过前端隐藏证明权限。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `Customer 9409=c-9409/Daloday`，`Shipment=13`，`ShipmentFinanceItem=8`，`WaterReceipt=1`；结构上 `WarehousePackage` 有 `combinedOrderNo/customerCode/shipmentId/status/systemOrderNo`，`WaterReceiptMatch` 有 `amount/receivableFinanceItemId/shipmentId/waterReceiptId`，`PaymentApplicationItem` 有 `paymentApplicationId/payablePaymentApplicationId/payableFinanceItemId/shipmentId/amount/currency`；历史 action 仅查到 `finance.paid_payment.confirm=1`。47 当前 9409 无仓库包裹、无已绑定仓库包裹、无水单匹配、无付款申请样本，且 `WaterReceiptMatch` 线上不冗余 `systemOrderNo/customerCode`，只能通过关联反查；这些结构和历史数据不能替代 E4 线上受控追溯样本验证。下一步进入 `docs/slices/E5-audit-log-verification.md`，开工前必须重新做复用审计。

2026-06-29 E3-permission-denied 本地销账；本轮未同步 47。复用现有 `RbacGuard`、`recordPermissionDenied`、`AuditLog`、`workflow.guard_denied`、订单全链路 e2e、财务/仓库权限接口和 E1/E2 已落地的多角色样本；未新增业务表、业务接口、权限模型、状态或页面。最小补差为在订单全链路 e2e 的既有拒绝矩阵中补客服跳过双审核填转单号、财务代签收和财务越权改公司渠道资料三类断言，并锁住对应拒绝日志。数据证据：本地同一 9409 运单 `SYFLOW001` 已有仓库包裹、订单、应收、业务成本、真实应付、待付款、付款申请、已付款和转单号样本，可在同一测试链路上触发越权读写。角色证据：业务员读取待付款/已付款均 403；仓库读取单票财务详情 403；市场账号 `R-market` 确认付款 403；客服在业务数据和代理数据双审核前填写转单号返回 400 并写流程拒绝；财务出库 403、财务代签收 403、财务新建公司渠道 403；没有只用管理员验收。日志证据：`security.permission.denied` 记录业务员读待付款/已付款、仓库读财务、市场付款、财务出库、财务签收、财务改资料，含 actor、target、权限点、角色和失败结果；`workflow.guard_denied` 记录客服跳过双审核填转单号，含 actor、target、guard 和缺失节点 `business_data/agent_data`。字段裁剪证据：越权读取不是靠前端隐藏，业务员/仓库/市场/财务相关敏感接口均由后端权限或流程闸口拒绝；E2 字段裁剪断言继续覆盖响应体不含真实应付、代理银行、付款账号和利润。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`。47 口径：本轮未同步 47；只读验证显示 47 真实库现有拒绝相关权限结构为 `finance:order-fee:payable:view=ADMIN,FINANCE`、`finance:payable:paid-confirm=ADMIN,FINANCE`、`finance:payable:paid-read=ADMIN,FINANCE`、`finance:payable:payment=ADMIN,FINANCE`、`orders:write=ADMIN,CUSTOMER,CUSTOMER_SERVICE`、`tracking:write=ADMIN,CUSTOMER_SERVICE`、`warehouse:write=ADMIN,WAREHOUSE`；基础账号有 `admin/finance/operator/service/warehouse`，没有 `R-market`；`customerCode=9409` 关联运单 13、费用 8。47 当前仅有 `system.role_permissions.update=4`，无 `security.permission.denied` 或 `workflow.guard_denied` 历史日志；且 47 权限定义仍是老口径，未查到 `master-data:channels:write`，只有 `master-data:write/system:manage`。这些只读结构不能替代 E3 线上受控越权样本验证，后续需发布/权限调整后再用 47 真实库复验。下一步进入 `docs/slices/E4-object-traceability.md`，开工前必须重新做复用审计。

2026-06-29 E2-sensitive-field-trim 本地销账；本轮未同步 47。复用现有 `maskShipmentListFields`、单票财务详情 payable/profit 裁剪、已付款银行账号掩码、RBAC 权限矩阵、`security.permission.denied`、订单全链路 e2e 和财务 e2e；未新增业务表、业务接口、状态、页面或审计表。最小补差为把业务员纳入订单列表付款字段后端裁剪，从默认 OPERATOR 权限中移除利润权限 `finance:business-cost:view-profit` 和 `finance:order-fee:profit:receivable-business`，并补充业务员/客服/仓库敏感字段响应断言。数据证据：本地同一 9409 运单 `SYFLOW001` 含应收、业务成本、真实应付 `代理运费`、付款申请和已付款账号 `FLOW-PAYER-001`；管理员/财务仍可读取真实应付和付款账号。角色证据：业务员读取单票财务详情只能看到应收和业务成本口径，`payables=[]`、`payableTotal=0`、`profitSections=[]`、`grossProfit` 不返回；客服同样不返回应付、利润、付款账号或代理银行；仓库读取财务详情 403；业务员读取待付款/已付款 403；没有只用管理员验收。日志证据：订单全链路继续断言 `security.permission.denied` 记录业务员访问待付款/已付款、仓库读取财务详情、市场确认付款、财务出库等拒绝事件，含 actor、target、权限点和失败结果；D 批业务动作日志链路保留。字段裁剪证据：业务员订单列表不再返回 `paymentAmountUsd/paymentAmountCny/paymentMethod`，且代理/渠道继续置空；仓库列表不返回付款金额、付款方式、应收/应付/利润；业务员和客服财务详情响应不含 `代理运费`、`FLOW-PAYER-001`、`payerBankAccountNo`、`payeeBankAccount`、`bankAccountNo`、`RECEIVABLE_BUSINESS`。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts`。47 口径：本轮未同步 47；只读验证显示 47 真实库敏感字段结构存在：`Shipment` 有 `agentId/channelId/paymentAmountUsd/paymentAmountCny/paymentMethod/transferNo`，`PaymentApplication` 有付款账号和状态字段，`PayeeBankAccount` 有代理银行字段，`ShipmentFinanceItem` 有费用、代理、单价和付款编号字段；`customerCode=9409` 关联运单 13、费用 8、已付款 0。47 当前 `_PermissionToRole` 仍显示 `finance:business-cost:view-profit` 和 `finance:order-fee:profit:receivable-business` 绑定 `ADMIN,FINANCE,OPERATOR`，且只有 `system.role_permissions.update=4` 历史日志；这说明线上真实库不能用本机测试或结构检查替代 E2 线上业务样本验证，后续需发布/权限调整并用 47 受控角色复验。下一步进入 `docs/slices/E3-permission-denied.md`，开工前必须重新做复用审计。

2026-06-29 E1-role-read-matrix 本地销账；本轮未同步 47。复用现有 `Role`、`Permission`、`User`、RBAC guard、`security.permission.denied`、`GET /api/system/roles`、订单全链路 e2e、财务详情接口、仓库/付款接口和已落地的 A-D 样本；未新增业务表、业务接口、权限模型或页面。最小补差为给 e2e harness 暴露已有 `R-market` 账号登录，并在订单全链路测试中补管理员、财务、业务员、仓库、客服、市场账号读取/拒绝矩阵断言。数据证据：同一 9409 运单 `SYFLOW001` 已有仓库包裹、订单、应收、业务成本、真实应付、待付款、付款申请、已付款样本；管理员和财务读取财务详情能看到真实应付 `代理运费` 与 `payableTotal=60`，财务读取已付款能看到付款账号 `FLOW-PAYER-001`。角色证据：业务员读取同一票只能看到应收和业务成本，`payables=[]`、`payableTotal=0`，且响应不含 `代理运费` 或 `FLOW-PAYER-001`；客服读取财务详情同样不返回应付和付款账号；仓库读取财务详情 403；业务员读取待付款/已付款 403；市场账号 `R-market` 确认付款 403；财务账号执行仓库出库 403；没有只用管理员验收。日志证据：`security.permission.denied` 记录业务员访问待付款/已付款、仓库读取财务详情、市场确认付款、财务出库等拒绝事件，含 actor、target、权限点和失败结果；D7 已覆盖同一票业务日志链路。字段裁剪证据：业务员和客服后端响应均不含真实应付、代理银行、待付款、已付款付款账号；仓库和业务员敏感接口由后端权限拒绝，不靠前端隐藏。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `User=6`、`Role=6`、`Permission=66`、`_PermissionToRole=137`，基础账号 `admin/finance/operator/service/warehouse` 存在，角色权限计数 `ADMIN=64/CUSTOMER=7/CUSTOMER_SERVICE=9/FINANCE=49/OPERATOR=6/WAREHOUSE=2`，关键权限 `finance:order-fee:payable:view`、`finance:payable:paid-read`、`finance:payable:paid-confirm`、`finance:water-receipt:read`、`warehouse:read/write`、`system:manage` 存在；历史 action `system.role_permissions.update=4`。47 当前没有 `R-sales/R-warehouse/R-market/R-service/R-finance/R-admin` 受控验收账号，且历史权限拒绝样本不足，不能替代 E1 线上受控角色样本验证。下一步进入 `docs/slices/E2-sensitive-field-trim.md`，开工前必须重新做复用审计。

2026-06-29 D7-finance-audit 本地销账；本轮未同步 47。复用 D1-D6 已落地的 `WaterReceipt`、`WaterReceiptMatch`、`ShipmentFinanceItem`、`PayablePaymentApplication`、`PaymentApplication`、`PaymentApplicationItem`、`PaymentVoucher`、财务水单/应收/业务成本/应付/待付款/已付款接口、财务页面、`finance:*`/`finance:payable:*`/`finance:water-receipt:*` 权限、`AuditLog`、现有财务 e2e 和订单全链路 e2e；未新增表、接口、路由、状态、权限或页面。最小补差为在订单全链路 e2e 中把 D 批日志断言从“存在 action”升级为校验 `finance.receivable.audit`、`finance.water_receipt.arrive`、`finance.water_receipt.match`、`finance.payable.audit`、`finance.payment_application.create`、`finance.paid_payment.confirm` 的关键 after 字段，证明同一票能串联水单到账、应收匹配/审核、真实应付审核、待付款、付款申请和已付款归档。数据证据：本地订单全链路用 9409 运单完成应收 120、RMB 水单 120 到账并匹配到余额 0；同票创建真实应付 60，审核后生成待付款，生成付款申请并确认已付款，已付款列表返回 `PAID` 和付款账号 `FLOW-PAYER-001`。角色证据：管理员执行财务动作；业务员只能在单票费用详情看到应收和业务成本口径，`payables=[]`、`payableTotal=0`，且访问待付款/已付款返回 403；仓库访问财务详情返回 403；没有只用管理员验收。日志证据：全链路断言覆盖应收审核的运单/客户/金额/状态前后/审核人，水单到账的到账金额和客户余额，水单匹配的匹配人/金额/余额，真实应付审核的金额/币种/待付款状态，应付生成付款申请的付款申请号/关联应付/待付款/运单/客户，以及确认付款的付款金额、付款账号、核销状态和归档状态；业务员和仓库越权访问写入 `security.permission.denied`。字段裁剪证据：业务员响应不包含真实应付 `代理运费` 或付款账号 `FLOW-PAYER-001`；待付款、已付款、财务详情均由后端权限拒绝或裁剪，不靠前端隐藏。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `WaterReceipt=12`、`WaterReceiptMatch=10`、`ShipmentFinanceItem=249`，其中 `RECEIVABLE=83/BUSINESS_COST=83/PAYABLE=83`，`PayablePaymentApplication=39`，状态 `PENDING=29/APPLIED=4/PAID=6`，`PaymentApplication=14`，状态 `WAITING_PAYMENT=8/PAID=6`，`PaymentVoucher=35`；`customerCode=9409` 应收 3、应付 2、已付款 0；历史 action 计数包含 `finance.business_cost.audit=1`、`finance.receivable.audit=2`、`finance.receivable.reverse_audit=6`、`finance.paid_payment.confirm=1`、`finance.paid_payment.water_receipt.add=1`。线上当前历史数据不能替代 D 批受控业务样本验证。下一步进入 `docs/slices/E1-role-read-matrix.md`，开工前必须重新做复用审计。

2026-06-29 D6-paid-payment-proof 本地销账；本轮未同步 47。复用 `PaymentApplication`、`PaymentApplicationItem`、`PaymentVoucher`、`GET /api/finance/paid-payments`、`POST /api/finance/payment-applications/:id/confirm-paid`、`PUT /api/finance/paid-payments/:id`、`POST /api/finance/paid-payments/:id/reverse`、`POST /api/finance/voucher-images` 的 `PAID_PAYMENT_RECEIPT` 上下文、财务已付款页、`finance:payable:paid-*` 权限、`AuditLog` 和现有财务 e2e；未新增表、接口、路由、状态或权限。最小补差为让 `finance.paid_payment.confirm/update/reverse` 和 `finance.paid_payment.water_receipt.add` 的 after 快照显式记录 ``，并补 e2e 断言。数据证据：本地用 9409 运单 `SYPAYABLEAUDIT001` 的 USD 付款申请确认已付款，付款金额 50，付款银行 `思远付款银行`，付款账号 `888800001111`，付款日期 `2026-06-25`；付款凭证 `paid-receipt.png` 和补充凭证 `paid-receipt-extra.png` 均挂在同一付款申请，可从已付款列表反查付款申请、应付、待付款、运单和客户；反核销后状态回到 `WAITING_PAYMENT`。角色证据：管理员执行确认付款、补充凭证和反核销；业务员访问待付款和已付款返回 403；客服只授予已付款只读时银行字段后端掩码，确认付款、导出和上传凭证均 403；没有只用管理员验收。日志证据：`finance.paid_payment.confirm` 记录付款时间、付款人、付款金额、付款对象、付款凭证、关联应付/待付款/运单、`WAITING_PAYMENT -> PAID`、`WRITTEN_OFF` 和归档时间；`finance.paid_payment.water_receipt.add` 记录凭证 id、付款申请号、路径、上传人、金额和归档时间；`finance.paid_payment.reverse` 记录 `PAID -> WAITING_PAYMENT`、反核销人、反核销时间和核销状态回退；`security.permission.denied` 记录业务员/客服越权访问付款链路。字段裁剪证据：业务员无法读取真实应付、待付款、已付款、代理银行和付款凭证；客服只读已付款时 `payeeBankAccount.bankAccountNo` 和 `payerBankAccountNo` 均由后端掩码，不靠前端隐藏。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "payable audits"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `PaymentApplication=14`、状态 `WAITING_PAYMENT=8/PAID=6`，`PaymentVoucher=35`、类型 `BILL=29/PAYMENT_RECEIPT=6`，付款字段 `applicationNo/agentName/currency/totalAmount/status/payeeBankAccountId/paidAt/paidBy/paidRemark/payerBankName/payerBankAccountName/payerBankAccountNo/reversedAt/reversedBy/reverseReason` 和凭证字段 `paymentApplicationId/pendingPaymentId/voucherType/fileName/url/uploadedBy/createdAt` 均存在；`customerCode=9409` 已付款为 0；历史 action 计数 `finance.paid_payment.confirm=1`、`finance.paid_payment.water_receipt.add=1`。线上当前历史数据不能替代 D6 受控业务样本验证。下一步进入 `docs/slices/D7-finance-audit.md`，开工前必须重新做复用审计。

2026-06-29 D5-payment-application 本地销账；本轮未同步 47。复用 `PayablePaymentApplication`、`PaymentApplication`、`PaymentApplicationItem`、`PayeeBankAccount`、`GET /api/finance/pending-payments`、`POST/PUT /api/finance/payment-applications`、`POST /api/finance/payment-applications/:id/cancel`、`POST /api/finance/payment-applications/export`、财务待付款页、`finance:payable:payment/bank/export` 权限、`AuditLog` 和现有财务 e2e；未新增表、接口、路由、状态或权限。最小补差为让 `finance.payment_application.create/update/cancel` 的 after 快照显式记录 `paymentApplicationId/paymentApplicationNo/agentName/bankAccountNo/currency/totalAmount/payableFinanceItemIds/pendingPaymentIds/systemOrderNos/customerCodes/itemCount/appliedBy/appliedAt/statusFrom/statusTo/status/canceledBy/canceledAt/voucherFileNames`，并补 e2e 断言。数据证据：本地用 9409 运单 `SYPAYABLEAUDIT001` 的 RMB 待付款 `ppa-1` 和 USD 待付款 `ppa-2` 生成两张付款申请，RMB 金额 120、USD 金额 50，申请后待付款状态变 `APPLIED`；跨币种银行和跨代理银行提交返回 400；USD 付款申请更新为一次性银行账号 `USD-ONCE-001`；RMB 付款申请撤回后状态为 `CANCELED`，对应待付款回到 `READY`。角色证据：管理员执行付款申请创建、修改和撤回；业务员访问待付款和已付款返回 403；客服确认付款、导出已付款、上传付款凭证返回 403；没有只用管理员验收。日志证据：`finance.payment_application.create` 记录付款申请号、代理、币种、金额、关联待付款/应付、运单、客户、申请人、申请时间和票据文件；`finance.payment_application.update` 记录银行账号 `USD-ONCE-001`、银行、币种、金额和关联应付；`finance.payment_application.cancel` 记录 `WAITING_PAYMENT -> CANCELED`、撤回人、撤回时间和关联待付款；`security.permission.denied` 记录业务员/客服越权访问付款链路。字段裁剪证据：业务员接口无法读取待付款、已付款、代理银行和付款凭证；客服可被临时授予只读已付款时银行账号仍被后端掩码，确认付款/导出/上传仍 403。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "payable audits"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `PaymentApplication=14`、`PaymentApplicationItem=14`、`PayablePaymentApplication=39`，付款申请状态 `WAITING_PAYMENT=8/PAID=6`，待付款状态 `PENDING=29/APPLIED=4/PAID=6`，字段 `applicationNo/agentName/currency/totalAmount/status/payeeBankAccountId/appliedBy/appliedAt/canceledAt/cancelReason` 和明细关联字段均存在；`customerCode=9409` 付款申请为 0；历史 action 计数 `finance.payment_application.create=0`、`update=0`、`cancel=0`、`export=1`。线上当前历史数据不能替代 D5 受控业务样本验证。下一步进入 `docs/slices/D6-paid-payment-proof.md`，开工前必须重新做复用审计。

2026-06-29 D4-payable-review 本地销账；本轮未同步 47。复用 `ShipmentFinanceItem(type=PAYABLE)`、`PayablePaymentApplication`、`GET/POST/PUT /api/finance/payable-audits*`、`POST /api/finance/payable-audits/:id/audit`、`POST /api/finance/payable-audits/:id/reverse-audit`、`GET /api/finance/pending-payments`、财务应付审核页、待付款入口、`finance:payable:*` 权限、`finance:order-fee:payable:view/manage` 字段裁剪和现有财务 e2e；未新增表、接口、路由、状态或权限。最小补差为让 `finance.payable.audit` 和 `finance.payable.reverse_audit` 的 after 快照显式记录 `systemOrderNo/customerCode/realAgentName/agentChannel/chargeWeightKg/unitPrice/amount/currency/routingSource/supplierBillNo/paymentNo/pendingPaymentStatus/statusFrom/statusTo/reviewStatus/reviewedBy/reviewedAt/reversedBy/reversedAt/locked`，并补 e2e 断言。数据证据：本地用 9409 运单 `SYPAYABLEAUDIT001` 创建真实应付 `代理运费`，排货代理 `a-yuhuan`、渠道 `ch-dhl-hk`，计费重 20、单价 6、金额 120、供应商账单 `PAYABLE-001`；审核后状态 `CONFIRMED` 且锁定，并生成 RMB 待付款 `PENDING`；反审核后状态回到 `PENDING` 且待付款 `INVALIDATED`；USD 应付链路也继续覆盖后续付款申请和已付款。角色证据：管理员执行应付审核/反审核；业务员读取应付审核、待付款、已付款均返回 403；客服尝试确认付款、导出已付款、上传付款凭证返回 403；没有只用管理员验收。日志证据：`finance.payable.audit` 记录真实代理、代理渠道、计费重、单价、金额、供应商账单、待付款状态、审核人、审核时间和 `PENDING -> CONFIRMED`；`finance.payable.reverse_audit` 记录 `CONFIRMED -> PENDING`、待付款失效、反审核人、反审核时间和解锁状态；`security.permission.denied` 记录业务员/客服越权访问付款链路。字段裁剪证据：业务员接口无法读取真实应付、待付款、已付款、代理银行和付款凭证；后端权限拒绝不是前端隐藏。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "payable audits"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `PAYABLE=83`，状态 `CONFIRMED=40/PENDING=43`，`customerCode=9409` 应付 2 条，字段 `agentName/chargeWeightKg/unitPrice/paymentNo/reviewedAt/reviewedBy/reconciliationStatus/locked` 均存在；`PayablePaymentApplication=39`，状态 `PENDING=29/APPLIED=4/PAID=6`；历史 action 计数 `finance.payable.audit=0`、`finance.payable.reverse_audit=0`、`finance.payable.create=0`、`finance.payable.update=0`、`security.permission.denied=0`。线上当前历史数据不能替代 D4 受控业务样本验证。下一步进入 `docs/slices/D5-payment-application.md`，开工前必须重新做复用审计。

2026-06-29 D3-business-cost 本地销账；本轮未同步 47。复用 `ShipmentFinanceItem(type=BUSINESS_COST)`、`GET/POST/PUT /api/finance/business-cost-audits*`、`POST /api/finance/business-cost-audits/:id/audit`、`POST /api/finance/business-cost-audits/:id/reverse-audit`、财务业务成本审核页、单票费用详情、`finance:business-cost:*` 权限、`AuditLog` 和现有财务 e2e；未新增表、接口、路由、状态或权限。最小补差为让 `finance.business_cost.audit` 和 `finance.business_cost.reverse_audit` 的 after 快照显式记录 `systemOrderNo/customerCode/salesperson/name/chargeWeightKg/unitPrice/amount/currency/statusFrom/statusTo/reviewStatus/reviewedBy/reviewedAt/reversedBy/reversedAt/locked`，并补 e2e 断言。数据证据：本地用 9409 运单 `SYBIZCOST001` 创建业务成本 `业务员成本运费`，计费重 20、单价 30、金额 600；审核后状态 `CONFIRMED` 且锁定，已审核修改返回 400；反审核后状态回到 `PENDING` 且解锁；批量审核 1 成功 1 失败，批量反审核成功。角色证据：管理员执行业务成本审核/反审核；业务员可通过单票费用详情查看业务成本口径，`payables=[]`、`payableTotal=0`、不返回真实应付和代理敏感信息；业务员不能新增应收，排货后业务员不能修改业务成本。日志证据：`finance.business_cost.audit` 记录业务员、计费重、单价、金额、审核人、审核时间、状态前后和锁定状态；`finance.business_cost.reverse_audit` 记录 `CONFIRMED -> PENDING`、反审核人、反审核时间和解锁状态；排货后业务成本变更沿用 `notification.wecom.business_cost_changed.pending` 待通知日志，不把真实企微推送作为第一阶段前置。字段裁剪证据：业务员费用详情仍只返回应收和业务成本口径，不返回真实应付、待付款、已付款、代理银行或付款凭证；财务业务成本接口由后端 `finance:business-cost:*` 权限控制。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "business cost"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `BUSINESS_COST=83`，状态 `CONFIRMED=51/PENDING=32`，`customerCode=9409` 业务成本 3 条，字段 `chargeWeightKg/unitPrice/reviewedAt/reviewedBy` 均存在；历史 action 计数 `finance.business_cost.audit=1`、`finance.business_cost.reverse_audit=1`。线上当前历史数据不能替代 D3 受控业务样本验证。下一步进入 `docs/slices/D4-payable-review.md`，开工前必须重新做复用审计。

2026-06-29 D2-receivable-match-review 本地销账；本轮未同步 47。复用 `ShipmentFinanceItem`、`ReceivableFee`、`WaterReceipt`、`WaterReceiptMatch`、`CustomerAccount`、`GET/POST /api/finance/receivable-audits*`、`POST /api/finance/receivable-audits/:id/match-receipt`、`POST /api/finance/water-receipts/:id/match-orders`、财务应收审核页、水单匹配页、既有 `finance:water-receipt:*`/财务权限、`AuditLog` 和现有财务 e2e；未新增表、接口、路由、状态或权限。最小补差为让 `finance.receivable.audit` 和 `finance.receivable.reverse_audit` 的 after 快照显式记录 `systemOrderNo/customerCode/amount/currency/paymentNo/matchedReceiptNo/receivedAmount/receiptStatus/waterReceiptMatched/statusFrom/statusTo/reviewStatus/reviewedBy/reviewedAt/reversedBy/reversedAt/locked`，并补 e2e 断言。数据证据：本地用 9409 应收 `SYRECV_AUTO_001` 验证审核后 `CONFIRMED` 且锁定；未到账水单匹配返回 400；已到账 RMB 水单匹配 100 后应收 `receiptStatus=PARTIAL`、水单余额从 300 到 200、客户账户余额同步减少；匹配后反审核失败，撤销匹配后应收 `receivedAmount=0`、`receiptStatus=UNPAID` 且反审核成功；跨币种匹配返回 400。角色证据：管理员/财务可审核、匹配和反审核；业务员只能读取自己客户水单，创建水单、匹配水单、导出水单返回 403；仓库读取水单返回 403；没有用管理员单角色完成验收。日志证据：`finance.receivable.audit` 记录审核前后状态、审核人、审核时间、金额、币种、付款编号和锁定状态；`finance.water_receipt.match` 记录匹配人、匹配时间、匹配金额、水单余额和客户账户余额前后；`finance.water_receipt.unmatch` 可查；`finance.receivable.reverse_audit` 记录 `CONFIRMED -> PENDING`、反审核人、反审核时间和解锁状态；拒绝日志记录仓库/业务员越权访问。字段裁剪证据：应收审核/匹配沿用后端权限和 `finance:water-receipt:*` 闸口；业务员不能写入或导出水单，仓库不能读取水单；本卡未向业务员开放真实应付、待付款、已付款、代理银行或付款凭证。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "receivable"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `ShipmentFinanceItem=249`，应收状态 `CONFIRMED=48/PENDING=35`，收款状态 `PARTIAL=5/RECEIVED=5/UNPAID=73`，`customerCode=9409` 应收 3 条，字段 `reviewedAt/reviewedBy/receivedAmount/receiptStatus` 均存在，`WaterReceiptMatch=10`；历史 action 计数 `finance.receivable.audit=2`、`finance.receivable.reverse_audit=6`。线上当前历史数据不能替代 D2 受控业务样本验证。下一步进入 `docs/slices/D3-business-cost.md`，开工前必须重新做复用审计。

2026-06-29 D1-water-receipt 本地销账；本轮未同步 47。复用 `WaterReceipt`、`WaterReceiptMatch`、`WaterReceiptVoucher`、`CustomerAccount`、`AccountLedger`、`ShipmentFinanceItem`、`GET/POST /api/finance/water-receipts*`、财务水单页面、`finance:water-receipt:*` 权限、`AuditLog` 和现有财务 e2e；未新增表、接口、路由、状态或权限。最小补差为让 `finance.water_receipt.arrive` 审计显式记录 `arrivedAmount/accountBalanceBefore/accountBalanceAfter/customerAccountBalance`，让 `finance.water_receipt.match` 审计显式记录 `matchedBy/matchedAt/matchedAmountDelta/receiptBalanceBefore/receiptBalanceAfter/accountBalanceBefore/accountBalanceAfter/customerAccountBalance`，并补 e2e 断言。数据证据：本地用 9409 水单验证录入后为 `PENDING`，未到账匹配应收返回 400；确认到账后 9409 RMB 客户账户余额增加 300；匹配应收 100 后水单余额从 300 变 200，客户账户余额相应减少；跨币种匹配返回 400。角色证据：财务/管理员可录入、到账、上传凭证和匹配；业务员只能读自己客户水单，创建/匹配/导出返回 403；仓库读取水单返回 403。日志证据：`finance.water_receipt.create/arrive/voucher/match/unmatch` 可查；到账日志记录到账金额和客户账户余额前后；匹配日志记录匹配人、匹配时间、水单余额和客户账户余额前后；拒绝日志记录仓库/业务员越权访问。字段裁剪证据：水单写入、到账、匹配、导出均由后端 `finance:water-receipt:*` 权限控制；业务员没有水单写入/匹配/导出能力，仓库没有读取能力；本卡未开放真实应付、待付款、已付款、代理银行或付款凭证给业务员。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "receivable"`。47 口径：本轮未同步 47；只读验证显示 47 真实库 `WaterReceipt=12`、`WaterReceiptVoucher=10`、`WaterReceiptMatch=10`、`customerCode=9409` 水单 1 条，状态为 `ARCHIVED=5`、`ARRIVED=2`、`PARTIAL_MATCHED=5`，字段 `arrivedAt/currency/accountLedgerId` 均存在；`Permission` 中 `finance:water-receipt:*` 为 10 个，角色权限关联 21 条；`finance.water_receipt.*` 和 `security.permission.denied` 历史 action 计数为 0。线上当前历史数据不能替代 D1 受控水单业务样本验证。下一步进入 `docs/slices/D2-receivable-match-review.md`，开工前必须重新做复用审计。

2026-06-29 C8-order-service-audit 本地销账；本轮未同步 47。复用 `Shipment`、`WarehousePackage`、`ShipmentFinanceItem`、`ProblemTicket`、`ShipmentLabel`、`POST /api/shipments/order-entry`、审核/排货/出库/客服双审核/转单号/状态池/问题件/签收接口、`AuditLog`、既有 RBAC 和现有订单全链路 e2e；未新增表、路由、状态或权限。最小补差为在既有 `/api/system/audit-logs` 查询参数中增加 `target` 过滤，并在订单全链路测试里补同一票问题件挂载和同一运单核心 action 队列断言。数据证据：本地 `SYFLOW001` 从仓库包裹创建和录单提交进入 `REVIEW_PENDING`，经审核到 `WAITING_SORT`、排货到 `WAITING_DISPATCH`、仓库出库到 `OUTBOUNDED`、双审核后补转单号进入 `WAITING_DEPARTURE`，再推进 `DEPARTED/ARRIVED_PORT/DELIVERING/SIGNED`；同一票问题件挂载后订单仍在 `DELIVERING` 且 `hasProblemTicket=true`。角色证据：排货由 `operator` 执行，出库由 `warehouse` 执行，双审核/转单号由 `service` 执行，最终签收由 `operator` 执行；业务员不能填转单号，客服不能代签收，仓库不能读单票费用详情。日志证据：`target=运单ID` 可查到 `shipment.order_entry.submit`、`shipment.review.approve`、`shipment.route`、`shipment.dispatch`、`workflow.guard_denied`、`customer_service.business_data.approved`、`customer_service.agent_data.approved`、`shipment.operational.update`、`customer_service.status.update`、`customer_service.signature.confirm`；问题件日志记录 `shipmentId/originalStatusPool/issueType/handledBy`。字段裁剪证据：业务员订单列表仍不返回追踪网站、面单 URL、真实代理/渠道；业务员费用详情不返回真实应付和付款账号；仓库费用详情 403；后端裁剪不是前端隐藏。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/shared`、`scripts/verify-47-lifecycle.sh`。47 口径：本轮未同步 47；只读脚本显示 47 服务均 Up，最新迁移 `20260628203000_add_customer_contact_receivers`，`FINANCE_ORDER_FEE_PERMISSIONS=5`，C 批关键 action 中 `shipment.order_entry.submit/review.approve/route/dispatch/operational.update/sign/customer_service.*` 均为 0，`STRICT_FULL_CHAIN_CANDIDATE=0`；线上真实库当前历史数据不能替代 C8 受控同一票业务样本验证。下一步进入 `docs/slices/D1-water-receipt.md`，开工前必须重新做复用审计。

2026-06-29 C7-status-pool-signature 本地销账；本轮未同步 47。复用 `Shipment` 状态池、`PATCH /api/shipments/:id/operational`、`ProblemTicket`、问题件接口、业务员签收校验、`orders:write`、`problems:write`、`AuditLog` 和现有订单 e2e；未新增表、接口、路由、状态或权限。最小补差为 `customer_service.status.update` 补 `statusFrom/statusTo/statusAt/dwellHours/changedBy`，问题件三类客服日志补 `originalStatusPool/handledBy`，业务员签收日志补 `signatureConfirmedBy/signatureConfirmedAt/statusFrom/statusTo`。数据证据：本地订单全链路验证 `WAITING_DEPARTURE -> DEPARTED -> ARRIVED_PORT -> DELIVERING -> SIGNED`，确认离港缺 ETA/ETD 返回 400；问题件用例验证挂载后运单保持原状态池且 `hasProblemTicket=true`；签收后运单为 `SIGNED`。角色证据：客服尝试代签收返回 403，最终由归属业务员 `operator` 确认签收；仓库创建问题件返回 403。日志证据：`customer_service.status.update` 记录状态前后、状态时间、停留时长和操作人；`customer_service.issue.attach/update/close` 记录问题 id、运单、原状态池、处理人和处理结果；`customer_service.signature.confirm` 记录签收确认人、确认时间、转单号和状态前后。字段裁剪证据：业务员仍看不到真实代理/渠道，仓库仍无权读取费用详情；问题件是挂载，不把订单移出原状态池。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "creates replies and closes problem tickets"`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `PROBLEM=7`、`SIGNED=6`、`WAITING_DEPARTURE=2`，`9409` 下 `PROBLEM=1`、`SIGNED=1`、`WAITING_DEPARTURE=1`，`ProblemTicket=1`，相关 C7 历史 action 查询为 0，增强状态/问题件/签收日志均为 0；线上当前历史数据不能替代 C7 受控业务样本验证。下一步进入 `docs/slices/C8-order-service-audit.md`，开工前必须重新做复用审计。

2026-06-29 C6-transfer-no-tracking 本地销账；本轮未同步 47。复用 `Shipment`、`ShipmentLabel`、`TrackingEvent.visibleToCustomer`、`PATCH /api/shipments/:id/operational`、`POST/GET /api/shipments/:id/labels`、`WAITING_DEPARTURE`、`orders:write`、`warehouse:read/write`、`AuditLog` 和现有订单/仓库 e2e；未新增表、接口、路由、状态或权限。最小补差为 `ShipmentOperationalUpdateInput` 增加可选追踪网站和显示开关，限制转单号只能由客服/管理员填写，并让 `shipment.operational.update` 和 `shipment.label.create` 日志补转单号、面单 URL、追踪网站、显示开关、填写人和填写时间。数据证据：本地订单全链路验证双审核后业务员填写转单号返回 403，客服填写 `1Z999` 后运单进入 `WAITING_DEPARTURE`；仓库面单用例验证生成面单返回 `labelUrl` 和转单号并可出库沿用。角色证据：转单号由客服账号填写，不只用管理员；业务员不能替代客服填写转单号；客户访问内部面单接口返回 403；仓库面单路径仍由仓库/管理员权限控制。日志证据：`shipment.operational.update` 记录 `transferNoTo/transferNoFilledBy/transferNoFilledAt/trackingWebsite/trackingWebsiteVisibleToSales`；`shipment.label.create` 记录 `labelUrl/transferNo/transferNoFilledBy/transferNoFilledAt/trackingWebsite/trackingWebsiteVisibleToSales`。字段裁剪证据：业务员订单列表不返回 `trackingWebsite/trackingWebsiteVisibleToSales/labelUrl`；面单详情仍由 `warehouse:read` 后端权限保护，客户不能读取内部面单路径。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "creates mock carrier labels"`、`npm run typecheck -w @siyuan/web`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `ShipmentLabel=1`、`ACTIVE_LABELS=1`、`SHIPMENT_TRANSFERNO=66`、`9409_TRANSFERNO=3`，`shipment.operational.update/shipment.label.create` 未查到历史行，增强转单号日志为 0、增强面单日志为 0；线上当前历史数据不能替代 C6 受控业务样本验证。下一步进入 `docs/slices/C7-status-pool-signature.md`，开工前必须重新做复用审计。

2026-06-29 C5-service-data-confirm 本地销账；本轮未同步 47。复用 `Shipment`、`POST /api/shipments/:id/business-data/approve`、`POST /api/shipments/:id/agent-data/approve`、`PATCH /api/shipments/:id/operational`、`OUTBOUNDED -> WAITING_DEPARTURE`、`orders:write`、`AuditLog` 和现有订单/仓库 e2e；未新增表、接口、路由、状态或权限。最小补差为客服双审核接口增加客服/管理员角色边界，并让 `customer_service.business_data.approved` 与 `customer_service.agent_data.approved` 分别记录审核状态、状态前后、审核人、审核时间、差异反馈、代理计费重、代理渠道和客服接收状态。数据证据：本地订单全链路验证出库后未双审核填写转单号返回 400，双审核后客服回填转单号进入 `WAITING_DEPARTURE`。角色证据：业务员账号调用业务数据审核返回 403，客服账号完成业务数据审核和代理数据审核，后续转单号也由客服账号回填，不只用管理员。日志证据：`workflow.guard_denied` 记录 `missing: business_data/agent_data`；业务数据日志记录 `businessDataReviewStatus/statusFrom/statusTo/reviewer/reviewedAt/differenceFeedback/customerServiceReceiveStatus`；代理数据日志记录 `agentDataReviewStatus/agentChargeWeightKg/agentChannelName/reviewer/reviewedAt/differenceFeedback/customerServiceReceiveStatus`。字段裁剪证据：沿用后端可见性和角色闸口，未给业务员、仓库或财务开放客服双审核写入；仓库面单路径回归通过，证明没有用 C5 破坏既有出库前面单流程。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "creates mock carrier labels"`、`npm run typecheck -w @siyuan/web`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `OUTBOUNDED=26`、`WAITING_DEPARTURE=2`、`9409_OUTBOUNDED=1`，`customer_service.business_data.approved/customer_service.agent_data.approved/workflow.guard_denied` 查询无历史行，增强业务审核日志为 0、增强代理审核日志为 0；线上当前历史数据不能替代 C5 受控业务样本验证。下一步进入 `docs/slices/C6-transfer-no-tracking.md`，开工前必须重新做复用审计。

2026-06-28 C4-outbound-bridge 本地销账；本轮未同步 47。复用 `Shipment`、`WarehousePackage`、`POST /api/shipments/:id/dispatch`、仓库管理页待出库/收货交接单、`WAITING_DISPATCH -> OUTBOUNDED`、仓库包裹 `SHIPPED`、`warehouse:write`、`AuditLog` 和现有订单/仓库 e2e；未新增表、接口、路由、状态或权限。最小补差为 `shipment.dispatch` 日志补状态前后、待出库时间、交接单号、代理渠道、出库人、出库时间和客服接收状态 `PENDING_CONFIRMATION`，并补 e2e 断言。数据证据：本地订单全链路验证排货后进入 `WAITING_DISPATCH`，仓库账号出库后订单为 `OUTBOUNDED`、`outboundAt` 有值、最新轨迹停在“仓库已出库，等待客服补齐转单号”，关联仓库包裹变为 `SHIPPED` 并从在仓移除。角色证据：出库动作由 `warehouse` 账号执行，不只用管理员；仓库面单出库用例仍通过，证明没有破坏既有带转单号出库路径。日志证据：`shipment.dispatch` 记录 `statusFrom/statusTo/handoverNo/agentName/channelName/packageCount/chargeableWeightKg/waitingDispatchAt/outboundBy/outboundAt/customerServiceReceiveStatus/warehousePackageStatusTo`。字段裁剪证据：继续复用仓库接口和 `warehouse:write` 后端权限，仓库出库响应不引入应收、应付、利润、水单或付款字段；客服双审核顺序未在 C4 越界改动，留给 C5 专卡处理。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "creates mock carrier labels"`、`npm run typecheck -w @siyuan/web`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `WAITING_DISPATCH=8`、`OUTBOUNDED=26`，`OUTBOUNDED` 且有 `outboundAt` 的记录为 20，`9409 WAITING_DISPATCH=1`、`9409 OUTBOUNDED=1`，`shipment.dispatch=0`、增强出库日志为 0；线上历史状态和字段不能替代 C4 受控业务样本验证。下一步进入 `docs/slices/C5-service-data-confirm.md`，开工前必须重新做复用审计。

2026-06-28 C3-routing 本地销账；本轮未同步 47。复用 `Shipment`、`Channel`、`Agent`、`AgentChannel`、`ShipmentFinanceItem`、`PayableFee`、`POST /api/shipments/:id/route`、前端渠道排货入口、`WAITING_SORT -> WAITING_DISPATCH`、`routing:write`、`AuditLog` 和现有订单 e2e；未新增表、接口、路由、状态或权限。最小补差为排货接口校验现有渠道/代理存在，并让 `shipment.route` 日志显式记录排货状态、状态前后、公司渠道、真实代理、代理渠道、真实应付合计、排货人和排货时间。数据证据：本地 API e2e 验证审核通过后才能从 `WAITING_SORT` 排货到 `WAITING_DISPATCH`；M7 样本验证公司渠道 `M7 Channel 改`、真实代理 `M7 Agent Ltd.`、代理渠道 `M7 代理渠道-改` 和真实应付 `55` 写入排货日志。角色证据：全链路排货动作由 `operator` 账号执行，不只用管理员；仓库后续只能接收待出库，业务员单票费用详情仍看不到真实应付。日志证据：`shipment.route` 记录 `routeStatus/statusFrom/statusTo/companyChannelName/realAgentName/agentChannelName/payableTotal/routedBy/routedAt`。字段裁剪证据：未给 OPERATOR 整体开放应付查看权限，避免业务员角色借 C3 看真实应付；真实应付仅作为后端审计快照写入管理员审计日志，前台角色细分留给后续权限卡复验。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit|lets admins maintain master data"`、`npm run typecheck -w @siyuan/web`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `WAITING_SORT=28`、`WAITING_DISPATCH=8`、`WAITING_DISPATCH` 且有 `channelId/agentId` 的记录为 8，`9409 WAITING_DISPATCH=1`、`9409 WAITING_SORT=3`，`shipment.route=0`、增强排货日志为 0；线上历史字段不能替代 C3 受控业务样本验证。下一步进入 `docs/slices/C4-outbound-bridge.md`，开工前必须重新做复用审计。

2026-06-28 C2-order-review 本地销账；本轮未同步 47。复用 `Shipment` 的 `DRAFT/REVIEW_PENDING/REVIEW_REJECTED/WAITING_SORT` 状态、`reviewedBy/reviewedAt/reviewRejectedReason` 字段、`ShipmentFinanceItem` 费用摘要、`GET /api/shipments/review-pending`、`GET /api/shipments/:id/review-detail`、`POST /api/shipments/:id/review/approve`、`POST /api/shipments/:id/review/reject`、审核台删除/恢复接口、`orders:read/write`、`orders:review:restore`、`AuditLog` 和现有订单 e2e；未新增表、接口、路由、状态或权限。最小补差为驳回时也写入审核人和审核时间，并让 `shipment.review.approve/reject/delete/restore` 日志显式记录审核状态、状态前后、审核人、时间、原因和费用摘要，同时补 e2e 断言。数据证据：本地 API e2e 验证待审核列表/详情能返回审核告警和费用摘要，资料不完整不能审核通过，驳回后运单进入 `REVIEW_REJECTED` 且保留驳回原因；全链路测试验证审核通过前 `dispatch` 和手工切 `WAITING_SORT` 均返回 400，审核通过后进入 `WAITING_SORT`。角色证据：删除恢复测试用管理员、财务、业务员三个账号验证恢复权限边界；全链路测试继续验证业务员可看应收和业务成本但 `payables=[]`，仓库读取财务详情返回 403。日志证据：`shipment.review.reject` 记录 `REJECTED`、`statusFrom/statusTo`、`reviewer/reviewedBy/reviewedAt`、`rejectReason`、费用摘要和审核告警；`shipment.review.approve` 记录 `APPROVED`、审核人/时间和费用摘要；删除/恢复日志记录删除/恢复原因。字段裁剪证据：业务员单票费用详情不返回真实应付、代理运费或付款账号，仓库无权读取费用详情，均由后端接口裁剪/拒绝。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "enforces review-pending|keeps review-deleted|moves shipments through audit"`、`npm run typecheck -w @siyuan/web`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `REVIEW_PENDING=15`、`WAITING_SORT=28`、`REVIEW_REJECTED/WAITING_SORT` 且有 `reviewedBy/reviewedAt` 的记录为 20，`9409 WAITING_SORT=3`，未查到 `shipment.review.approve/reject/delete/restore` 日志输出；这些历史状态和字段不能替代 C2 受控业务样本验证。下一步进入 `docs/slices/C3-routing.md`，开工前必须重新做复用审计。

2026-06-28 A1-customer-master 本地销账并已发布 47。复用 `Customer`、`CustomerContact`、`CustomerAccount`、`User.customerId`、`/api/master-data/customers*`、`MasterDataPage`、`master-data:read/write` 和现有 API/Web 测试；最小补差为 `CustomerContact` 增加公司、地址、国家、州/省、邮编字段并限制每客户最多四组收货人，客户页补结算信息和收货人新增入口，业务员读取客户资料按归属裁剪，客户联系人/客户账号创建和客户停用写入 `audit_logs`。本地数据证据：`9409` 与新建客户均可返回结算信息和收货人字段；本地角色证据：`R-sales` 只能读取 `salesperson=operator` 客户且看不到代理资料；本地日志证据：`master_data.customer.create/update`、`master_data.customer_contact.create`、`master_data.customer_user.create` 可查；后端字段裁剪证据：`GET /api/master-data` 和 `GET /api/master-data/customers` 均对业务员裁剪。验证命令：`npm run prisma:generate -w @siyuan/api`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`。47 发布证据：`npm run sync:47 -- --apply` 通过；47 执行 `docker compose build db-migrate api web`、`docker compose --profile tools run --rm db-migrate`、`docker compose up -d --remove-orphans api web` 通过；只运行 `prisma migrate deploy`，应用 `20260628172000_add_warehouse_package_receipt_evidence`、`20260628184500_add_warehouse_tally_tasks`、`20260628203000_add_customer_contact_receivers`；47 `CustomerContact` 已存在 `company/address/country/state/postalCode`，现有联系人计数为 2，未执行 seed、db push、reset 或历史清洗；Web `http://127.0.0.1:18899/` 返回 `HTTP 200`。47 业务口径：已发布代码和迁移，但未创建受控 QA 同一票样本，仍需线上角色/API 复验。下一步进入 `docs/slices/A2-agent-master.md`，开工前必须重新做复用审计。

2026-06-28 A2-agent-master 本地销账并已发布 47。复用 `Agent`、`AgentChannel`、`PayeeBankAccount`、`AgentBankAccount`、`/api/master-data/agents*`、`/api/finance/payee-bank-accounts`、`/api/finance/agent-bank-accounts`、`MasterDataPage` 代理资料页、`master-data:agents:read/write`、`finance:payable:bank` 和现有 API/Web 测试；最小补差为代理停用写入 `master_data.agent.update`，代理资料页按现有财务银行账号接口只读展示选中代理银行账号。数据证据：代理编码、简称、详细公司名、仓库地址、联系人、发票模板、状态可在代理资料页新增/展示；银行账号通过现有 `PayeeBankAccount` 关联代理显示。角色证据：业务员只保留 `master-data:read`，`/api/master-data` 对其返回空代理资料；财务/管理员可读代理资料，只有具备 `finance:payable:bank` 才能读取银行账号。日志证据：`master_data.agent.create/update` 和 `master_data.agent_channel.create/update` 可由 API 测试查到。字段裁剪证据：代理银行账号仍由后端 `finance:payable:bank` 闸口保护，前端只辅助隐藏。验证命令：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`。47 发布证据：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 通过；47 执行 `docker compose build db-migrate api web`、`docker compose --profile tools run --rm db-migrate`、`docker compose up -d --remove-orphans api web` 通过；`prisma migrate deploy` 输出 `No pending migrations to apply`；Web `http://127.0.0.1:18899/` 返回 `HTTP 200`。47 数据库只读证据：最新迁移仍为 `20260628203000_add_customer_contact_receivers`，`Agent` 表已有 10 条，`PayeeBankAccount` 3 条，`AgentBankAccount` 0 条，`Agent` 字段包含 `code/shortName/integrationType/warehouseAddress1/warehouseAddress2/warehouseAddress3/warehouseContact/invoiceTemplateName/invoiceTemplateUrl/enabled`；未执行 seed、db push、reset 或历史清洗。下一步进入 `docs/slices/A3-channel-master.md`，开工前必须重新做复用审计。

2026-06-28 A3-channel-master 本地销账并已发布 47。复用 `AgentChannel`、`Channel`、`Carrier`、`ChannelCategory`、`/api/master-data/agent-channels*`、`/api/master-data/channels*`、`MasterDataPage` 代理渠道/公司渠道页、`master-data:agents:*`、`master-data:channels:*`、`AuditLog` 和现有 API/Web 测试；未新增表、接口、路由、状态或权限。最小补差为代理渠道启停和公司渠道启停写入既有 `master_data.agent_channel.update` / `master_data.channel.update` 审计 action，并补测试断言 `after.enabled=false`。数据证据：本地 API 测试证明代理渠道可新增/修改/停用；公司渠道字段覆盖业务类型、渠道类别、承运商、除材积、多件重量计算方式、进位规则、大货起始重量、偏远规则和状态，并可被排货使用。角色证据：`R-sales` 只能读自己客户且 `/api/master-data` 对其裁剪代理资料和代理渠道；客服角色在 `/api/master-data` 看不到公司渠道和渠道类别，访问独立渠道/代理接口返回 403。日志证据：`master_data.agent_channel.create/update`、`master_data.channel.create/update` 可查，启停动作有 `after.enabled=false`。字段裁剪证据：后端 `DataController.masterData` 按 `master-data:agents:read` 和 `master-data:channels:read` 裁剪 `agents/agentChannels/channels/channelCategories`，不是只靠前端隐藏。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`。47 发布证据：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 通过；47 执行 `docker compose build db-migrate api web`、`docker compose --profile tools run --rm db-migrate`、`docker compose up -d --remove-orphans api web` 通过；`prisma migrate deploy` 输出 `No pending migrations to apply`；Web `http://127.0.0.1:18899/` 返回 `HTTP 200`。47 数据库只读证据：最新迁移仍为 `20260628203000_add_customer_contact_receivers`，`AgentChannel=0`、`Channel=10`、`Carrier=10`、`ChannelCategory=6`，`AgentChannel` 字段包含 `id/agentId/channelName/enabled`，`Channel` 字段包含 `businessType/category/volumeDivisor/multiPieceWeightRule/singleWeightRoundingRule/settlementWeightRule/settlementWeightRoundingRule/largeCargoThresholdKg/remoteAreaRule/enabled`；当前渠道相关 audit action 计数为 0，说明 47 未创建受控渠道业务样本。47 业务口径：代码和结构已发布，但不能用本机测试或结构检查替代线上业务样本验证，受控角色/API/日志样本后续集中做。下一步进入 `docs/slices/A4-rate-remote-category.md`，开工前必须重新做复用审计。

2026-06-28 A4-rate-remote-category 本地销账并已发布 47。复用 `ChannelCategory`、`Channel.remoteAreaRule`、`Surcharge`、`FuelRate`、`ExchangeRate`、`/api/master-data/channel-categories*`、`/api/master-data/surcharges*`、`/api/master-data/fuel-rates`、`/api/master-data/exchange-rates`、`MasterDataPage` 渠道类别/公司渠道偏远规则入口、`master-data:read/write`、`master-data:channels:*`、`AuditLog` 和现有 API/Web 测试；未新增国家/邮编级偏远表，避免在第一阶段新造平行模型。最小补差为渠道类别新增/修改/停用、偏远附加费新增/停用、燃油费率新增、汇率新增写入既有 `audit_logs`，action 为 `master_data.channel_category.create/update`、`master_data.surcharge.create/update`、`master_data.fuel_rate.create`、`master_data.exchange_rate.create`，并补 e2e 断言。数据证据：本地 API 测试证明渠道类别可新增/修改/停用，停用类别不再用于新建公司渠道；公司渠道可保存 `UPS偏远/无偏远`；附加费可新增/停用；燃油费率按渠道和生效日期新增；汇率按币种、汇率和启用日期新增。角色证据：客服角色在 `/api/master-data` 看不到公司渠道/渠道类别，访问独立渠道类别接口返回 403；业务员不拿代理渠道和公司渠道维护权限；管理员可维护全部资料。日志证据：本地 e2e 可查 A4 相关新增/停用 action，渠道类别和附加费启停动作有 `after.enabled=false`。字段裁剪证据：渠道类别随 `master-data:channels:read` 后端裁剪，附加费、燃油和汇率沿用 `master-data:read/write` 后端权限；前端隐藏不作为事实源。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`。47 发布证据：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 通过；47 执行 `docker compose build db-migrate api web`、`docker compose --profile tools run --rm db-migrate`、`docker compose up -d --remove-orphans api web` 通过；`prisma migrate deploy` 输出 `No pending migrations to apply`；Web `http://127.0.0.1:18899/` 返回 `HTTP 200`。47 数据库只读证据：最新迁移仍为 `20260628203000_add_customer_contact_receivers`，`ChannelCategory=6`、`Surcharge=1`、`FuelRate=1`、`ExchangeRate=1`，A4 表字段包含 `name/enabled/amount/channelId/rate/baseCurrency/quoteCurrency/activeAt`；当前 A4 相关 audit action 计数为 0，说明 47 未创建受控业务样本。47 业务口径：代码和结构已发布，但不能用本机测试或结构检查替代线上业务样本验证，受控角色/API/日志样本后续集中做。下一步进入 `docs/slices/A5-site-user-role.md`，开工前必须重新做复用审计。

2026-06-28 A5-site-user-role 本地销账；按最新目标本轮未同步 47。复用 `Site`、`User`、`Role`、`Permission`、`/api/system/sites*`、`/api/system/staff-accounts*`、`/api/system/roles/:role/permissions`、系统设置页、`system:manage`、现有 RBAC、`AuditLog` 和现有 API/Web 测试；未新增表、接口、路由、状态或权限体系。最小补差为补测试断言，证明站点新增/修改/停用、员工账号新增/站点归属/重置密码、角色权限修改均有日志。数据证据：本地 API 测试验证 `深圳站`、`R-sales/R-warehouse/R-market/R-service/R-finance/R-admin` 存在，并新增 `A5验收站`、`a5staff` 覆盖站点名称、角色、姓名、账号、站点归属和状态。角色证据：只有管理员可访问系统站点、员工账号和角色权限接口；财务修改角色权限返回 403；客服权限被改为只读后无法创建运单。日志证据：本地 e2e 可查 `system.site.create/update/enabled`、`system.staff.create/site.update/password_reset`、`system.role_permissions.update`。字段裁剪证据：系统接口均由后端 `system:manage` 闸口保护，角色权限矩阵响应不包含密码；员工列表返回账号、姓名、角色、站点、状态和初始改密状态，不返回密码哈希。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins edit role permissions|lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 Web `http://127.0.0.1:18899/` 返回 `HTTP 200`，最新迁移仍为 `20260628203000_add_customer_contact_receivers`，`Site=4`、员工用户 `5`、`Role=6`、`Permission=66`，当前 `system.role_permissions.update=4`，未创建 A5 受控业务样本。下一步进入 `docs/slices/A6-master-audit-log.md`，开工前必须重新做复用审计。

2026-06-28 A6-master-audit-log 本地销账；按最新目标本轮未同步 47。复用 `AuditLog`、`AuditLogSummary`、`GET /api/system/audit-logs`、系统设置页“操作日志”、`system:manage`、现有审计 action 推断和现有 API/Web 测试；未新增表、接口、路由、状态或权限。最小补差为补测试断言和前端测试桩，证明审计入口返回动作、操作人、对象、时间、before/after，并支持 action/module/result/operator 筛选。数据证据：本地 API e2e 可查 `master_data.*`、`system.*` 操作日志，`master_data.channel.update` 含 `before.enabled=true` 和 `after.enabled=false`；系统设置页可展示模块、动作、对象、结果和原始 JSON。角色证据：管理员可访问审计日志；客服访问 `/api/system/audit-logs` 返回 403；系统管理菜单仍只对管理员开放。日志证据：A1-A5 的客户、代理、渠道、站点、员工和角色权限动作均可从同一审计入口查询。字段裁剪证据：审计接口由后端 `system:manage` 闸口保护，非管理员无法拿到日志响应；前端隐藏不是事实源。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 Web `http://127.0.0.1:18899/` 返回 `HTTP 200`，真实库 `AuditLog=394`，字段为 `id/actorId/action/target/before/after/createdAt`，当前 action 计数包含 `pricing.markup.create=238`、`qa47.flow.seed=50`、`warehouse.in_stock.view=5`；未创建 A6 受控业务样本，不能用本机测试或结构检查替代线上业务样本验证。下一步进入 `docs/slices/B1-today-receipt.md`，开工前必须重新做复用审计，且不能用订单 `receiveShipment` 代替仓库今日收货。

2026-06-28 B1-today-receipt 本地销账；本轮未同步 47。复用 `WarehousePackage`、`WarehousePackageSummary`、`POST /api/warehouse/packages`、`GET /api/warehouse/today-receipts`、`GET /api/warehouse/in-stock`、仓库管理页“今日收货”、`warehouse:read/write`、`AuditLog` 和现有 API/Web 仓库测试；未新增表、接口、路由、状态或权限。最小补差为补 API 断言，锁住 `warehouse.package.create` 的 after 快照字段，并证明仓库今日收货没有走 `shipment.receive`。数据证据：本地 API e2e 用 `9409-KY-TODAY-001` 验证客户编号、客户单号、快递单号、件数、重量、长宽高、扫描来源、`6000` 材积 `33.33`、派生 `5000` 材积 `40`、今日收货汇总和同票在仓可查；Web 仓库测试覆盖今日收货看板、手动收货表单和异常。角色证据：管理员/仓库可新增收货；业务员只能读自己客户收货且不返回站点，不能新增、修正、拆分或合票；财务无仓库入口。日志证据：`warehouse.package.create` 记录客户编号、组合号、扫描来源、件重尺和 6000/5000 材积；`warehouse.in_stock.view` 可查；B1 样本未产生 `shipment.receive` 日志。字段裁剪证据：仓库响应通过 `expectNoWarehousePriceLeak` 验证不含应收、应付、业务成本、利润、水单、付款和银行账号字段；后端角色闸口拒绝非仓库写入。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "summarizes today receipts"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 Web `http://127.0.0.1:18899/` 返回 `HTTP 200`，真实库 `WarehousePackage=59`，字段包含 ``，当前相关日志仅查到 `warehouse.in_stock.view=5`；未创建 B1 受控业务样本，不能用本机测试或结构检查替代线上业务样本验证。下一步进入 `docs/slices/B2-in-stock.md`，开工前必须重新做复用审计。

2026-06-28 B2-in-stock 本地销账；本轮未同步 47。复用 `WarehousePackage`、`GET /api/warehouse/in-stock`、仓库管理页“在仓数据”、`warehouse:read/write`、既有在仓筛选、既有 `warehouse.in_stock.view` 审计 action 和现有 API/Web 仓库测试；未新增表、接口、路由、状态或权限。最小补差为让真正的在仓接口在 Prisma 和内存实现中写入 `warehouse.in_stock.view`，并补断言锁住 `query.combinedOrderNo` 与 `rowCount`。数据证据：本地 API e2e 用 `9409-KY-STOCK-075` 验证仓库包裹号、组合号、客户编号、件数、重量、尺寸、体积、入仓时间、站点、业务员、异常、出库状态均可在在仓响应中读取，且 `CONSOLIDATED/SHIPPED` 不进入在仓列表。角色证据：管理员/仓库可看全部在仓；业务员只看自己客户货物且不返回站点；业务员不能修改件重尺、异常、拆票或合票。日志证据：`warehouse.in_stock.view` 由在仓接口直接记录；`warehouse.package.update` 记录件数、重量、尺寸、备注和异常前后值；`warehouse.package.exception.update` 沿用既有异常日志。字段裁剪证据：在仓响应通过 `expectNoWarehousePriceLeak` 验证不含应收、应付、业务成本、利润、水单、付款和银行账号字段；页面“在仓数据”也通过仓库前端测试确认无财务敏感文案。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "lists in-stock packages"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 Web `http://127.0.0.1:18899/` 返回 `HTTP 200`，真实库当前 `WarehousePackage` 状态为 `CONSOLIDATED=9`、`SHIPPED=50`，没有 `RECEIVED` 在仓样本，`customerCode=9409` 未查到在仓/仓库包裹样本，当前 `warehouse.in_stock.view=5`；不能用 47 当前库替代 B2 受控业务样本验证。下一步进入 `docs/slices/B3-sorting.md`，开工前必须重新做复用审计。

2026-06-28 B3-sorting 本地销账；本轮未同步 47。复用 `WarehouseTallyTask`、`WarehouseTallyTaskSummary`、`GET/POST/PATCH /api/warehouse/tally-tasks`、`POST /api/warehouse/tally-tasks/:id/complete`、仓库管理页“未完成理货/已完成理货”、`warehouse:read/write`、`AuditLog` 和现有 API/Web 仓库测试；未新增表、接口、路由、状态或权限。最小补差为补 API 断言，让 B3 理货动作由仓库账号执行，并锁住 `warehouse.tally.create/update/complete` 的 actor、before 和 after 字段。数据证据：本地 API e2e 用 `9409-KY-STOCK-075` 验证理货任务号、仓库包裹号、理货需求、原始件数/重量/尺寸、创建人、创建时间、完成时间、完成人、理货后件数/重量/尺寸和标签状态；未完成列表按 `PENDING` 查询，已完成列表按 `COMPLETED` 查询。角色证据：仓库账号可发起、修改和完成理货；业务员只能读取自己客户理货任务，完成理货返回 403；响应不含财务敏感字段。日志证据：`warehouse.tally.create` 记录来源包裹、原始件重尺和理货需求；`warehouse.tally.update` 记录需求/备注前后值；`warehouse.tally.complete` 记录 `PENDING -> COMPLETED`、理货后件重尺和完成人。字段裁剪证据：理货任务响应通过 `expectNoWarehousePriceLeak` 验证不含应收、应付、业务成本、利润、水单、付款和银行账号字段；仓库页面“未完成理货/已完成理货”由 Web 测试覆盖。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "lists in-stock packages"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 Web `http://127.0.0.1:18899/` 返回 `HTTP 200`，真实库 `WarehouseTallyTask` 字段覆盖任务号、状态、来源包裹、原始件重尺、理货需求、创建/完成信息和标签状态；当前无理货任务样本，`customerCode=9409` 未查到理货任务，仅有旧 `warehouse.tally.start=1` 日志；不能用 47 当前库替代 B3 受控业务样本验证。下一步进入 `docs/slices/B4-sorting-label.md`，开工前必须重新做复用审计。

2026-06-28 B4-sorting-label 本地销账；本轮未同步 47。复用 `WarehouseTallyTask`、`WarehouseTallyTaskSummary`、既有理货接口族、仓库管理页“已完成理货”、`warehouse:read/write`、`AuditLog` 和现有 API/Web 仓库测试；未新增平行标签表、平行仓库模型或新权限体系。最小补差为在既有 `WarehouseTallyTask` 增加标签号、二维码内容、生成/打印/下载时间和操作人字段，补 `POST /api/warehouse/tally-tasks/:id/label`、`/label/print`、`/label/download`，并在已完成理货表格补标签号、二维码内容、状态和操作。数据证据：本地 API e2e 用 `9409-KY-STOCK-075-TL001-LBL` 验证标签号、二维码内容、客户编号 `9409`、日期、件数 `75`、关联仓库包裹和打印/下载状态；仓库页测试可从已完成理货生成、打印、下载同一标签。角色证据：仓库账号可生成、打印和下载理货后标签；业务员调用生成标签接口返回 403；仓库响应仍不含财务敏感字段。日志证据：`warehouse.tally.label.generate`、`warehouse.tally.label.print`、`warehouse.tally.label.download` 写入同一 `AuditLog`，target 为标签号，after 包含二维码内容、关联包裹和操作人。字段裁剪证据：标签接口沿用 `warehouse:write` 和 ADMIN/WAREHOUSE 角色闸口，响应通过 `expectNoWarehousePriceLeak` 验证不含应收、应付、业务成本、利润、水单、付款和银行账号字段。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "lists in-stock packages"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 真实库 `WarehouseTallyTask` 当前仍只有旧 `labelStatus`，没有 `labelNo/labelQrContent/labelGeneratedAt/labelPrintedAt/labelDownloadedAt` 字段；`WarehouseTallyTask=0`，`customerCode=9409` 理货任务为 0，`warehouse.tally.label.%` 日志为 0；不能用本机测试或结构检查替代 47 受控业务样本验证。下一步进入 `docs/slices/B5-split-consolidation.md`，开工前必须重新做复用审计。

2026-06-28 B5-split-consolidation 本地销账；本轮未同步 47。复用 `WarehousePackage`、`WarehouseConsolidation`、`WarehouseConsolidationItem`、`POST /api/warehouse/packages/:id/split`、`POST /api/warehouse/consolidations`、`GET /api/warehouse/consolidations/:id/items`、仓库管理页“在仓数据”的拆票/合票操作、`warehouse:read/write`、`AuditLog` 和现有 API/Web 仓库测试；未新增表、接口、路由、状态或权限。最小补差为修正拆票子票参与合票时的合计重量口径，拆票审计补源包裹、子包裹、拆分前后件重体，合票审计补客户编号、来源包裹列表和合计件重尺，并补 e2e 断言。数据证据：本地 API e2e 用 `9409-KY-STOCK-075` 拆出 `9409-KY-STOCK-075-1` 和 `9409-KY-STOCK-075-2`，件数 `50/25`、重量 `50.67/25.33`、源包裹号可回溯；合票生成 `9409-OUT...`，`totalPackages=75`、`totalActualWeightKg=76`、`totalVolumetricWeightKg=2525`、`totalChargeableWeightKg=2525`，合票明细可反查两个子票和源包裹。角色证据：仓库/管理员可拆票、合票和查看合票明细；业务员拆票/合票返回 403；业务员只通过在仓读取自己客户拆票结果且不返回站点。日志证据：`warehouse.package.split` 记录源包裹、子包裹号列表、拆分前后件重体和操作人；`warehouse.consolidation.create` 记录合票批次、来源包裹列表、客户编号、合计件重尺和理货需求；`warehouse.tally.start` 继续记录合票进入后续理货/出货输入。字段裁剪证据：拆票、合票和明细响应均通过 `expectNoWarehousePriceLeak` 验证不含应收、应付、业务成本、利润、水单、付款和银行账号字段；写入接口沿用 `warehouse:write` 后端权限。验证命令：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "lists in-stock packages"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `WarehousePackage.sourcePackageId is not null` 为 2 条、其中 `customerCode=9409` 为 0 条，`WarehouseConsolidation=6`、`WarehouseConsolidationItem=14`，历史日志 `warehouse.package.split=1`、`warehouse.consolidation.create=5`；不能用这些历史数据替代 B5 受控业务样本验证。下一步进入 `docs/slices/B6-outbound-handover-archive.md`，开工前必须重新做复用审计。

2026-06-28 B6-outbound-handover-archive 本地销账；本轮未同步 47。复用 `Shipment`、`WarehousePackage`、`ShipmentLabel`、`POST /api/shipments/:id/dispatch`、仓库管理页“待出库/收货交接单”、`WAITING_DISPATCH -> OUTBOUNDED`、仓库包裹 `SHIPPED`、`warehouse:write`、`AuditLog` 和现有 API/Web 仓库测试；未新增表、接口、路由、状态或权限。最小补差为现有出库审计 `shipment.dispatch` 补出库单号、交接单号、代理/渠道、件数、重量、出库人和归档状态，收货交接单页面和导出 HTML 补派生交接单号，测试桩改为真实 `OUTBOUNDED` 出库状态。数据证据：本地 API e2e 验证仓库角色出库后运单为 `OUTBOUNDED`、`outboundAt` 有值、转单号沿用已生成面单；订单全链路 e2e 验证关联仓库包裹变 `SHIPPED` 且从在仓列表移除；Web 仓库测试验证待出库行可打单、可出货，收货交接单显示 `HD-1399-OUT001`。角色证据：出库动作由 `warehouse` 账号执行，不只用管理员；客户读取员工面单接口返回 403；仓库读取运单列表不返回应收、应付或利润字段。日志证据：`shipment.dispatch` 记录 `outboundOrderNo`、`handoverNo`、`agentName`、`channelName`、`packageCount`、`chargeableWeightKg`、`outboundBy`、`archiveStatus`、`outboundAt` 和仓库包裹状态变化。字段裁剪证据：仓库运单列表和仓库相关响应继续验证不含 `receivableAmount/payableAmount/profit` 等价格敏感字段；后端 `warehouse:write` 控制出库，不靠前端隐藏。验证命令：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts -t "creates mock carrier labels"`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `Shipment OUTBOUNDED=26`、`WarehousePackage SHIPPED=50`、`shipment.dispatch=0`、`9409 SHIPPED=0`；真实库存在历史出库/归档状态，但没有 9409 受控样本和出库日志，不能用本机测试或结构检查替代 47 线上受控业务样本验证。下一步进入 `docs/slices/B7-warehouse-audit.md`，开工前必须重新做复用审计。

2026-06-28 B7-warehouse-audit 本地销账；本轮未同步 47。复用 B1-B6 已落地的 `WarehousePackage`、`WarehouseTallyTask`、`WarehouseConsolidation`、`Shipment`、仓库管理页、仓库接口、订单出库接口、`warehouse:read/write`、`orders:read`、`AuditLog` 和现有 API/Web 测试；未新增业务代码、表、接口、状态、权限或页面。最小补差：无，现有测试已逐项覆盖 B 批字段、角色、日志和字段裁剪，新增汇总测试会依赖测试顺序，已跳过。数据证据：仓库 API 全文件验证今日收货、在仓、件重尺修改、异常、理货、理货后标签、拆票、合票、待出库和出库审计；订单全链路测试验证关联仓库包裹出库后变 `SHIPPED` 并从在仓移除；仓库 Web 全文件验证前端今日收货、在仓、理货、标签、合票、待出库、交接单和出货操作。角色证据：仓库/管理员执行仓库动作；业务员只能读自己客户货物且写入返回 403；客户面单细节返回 403；仓库/业务员响应不含财务敏感字段。日志证据：本地测试覆盖 `warehouse.package.create/update/exception.update/split`、`warehouse.in_stock.view`、`warehouse.tally.create/update/complete/start`、`warehouse.tally.label.generate/print/download`、`warehouse.consolidation.create`、`shipment.dispatch` 和 `security.permission.denied`。字段裁剪证据：`expectNoWarehousePriceLeak` 覆盖仓库今日收货、在仓、理货、拆合票和出库相关响应，后端权限闸口不是只靠前端隐藏。验证命令：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `WarehousePackage=59`、`WarehouseTallyTask=0`、`WarehouseConsolidation=6`、`Shipment OUTBOUNDED=26`，仓库日志包含 `warehouse.consolidation.create=5`、`warehouse.consolidation.create_shipment=1`、`warehouse.in_stock.view=5`、`warehouse.package.remark.update=6`、`warehouse.package.split=1`、`warehouse.tally.start=1`，`customerCode=9409` 仓库包裹为 0；不能用这些历史数据替代 B 批 47 受控业务样本验证。下一步进入 `docs/slices/C1-order-from-warehouse.md`，开工前必须重新做复用审计。

2026-06-28 C1-order-from-warehouse 本地销账；本轮未同步 47。复用 `Shipment`、`WarehousePackage`、`ShipmentFinanceItem`、`POST /api/shipments/order-entry`、`GET /api/shipments/order-entry/packages`、`GET /api/shipments/:id/order-entry`、仓库管理页“在仓数据”录单按钮、财务录单页、`DRAFT/REVIEW_PENDING`、`orders:write`、`AuditLog` 和现有 API/Web 测试；未新增表、接口、路由、状态或权限。最小补差为现有 `shipment.order_entry.submit/draft` 审计 after 补来源仓库包裹、组合号、客户编号、件重体、目的地、收货人、业务员、业务渠道、货物摘要、录单人和录单时间，并补 e2e 断言。数据证据：本地 API e2e 用 `ORDER-ENTRY-SUBMIT-KYENTSUB001` 验证仓库包裹绑定到 `SYORDERENTRY0001`，订单进入 `REVIEW_PENDING`，订单件数、计费重和体积从仓库包裹汇总，录单详情可反查包裹尺寸、重量、体积和组合号；Web 仓库测试验证在仓行点击“录单”可带入 9409 和包裹件数。角色证据：管理员可提交录单并看到应付；业务员读取录单详情能看到应收和业务成本，但 `payables=[]` 且 `canViewPayables=false`；业务员提交应付返回 403。日志证据：`shipment.order_entry.submit` 记录 `warehousePackageIds`、`combinedOrderNos`、`customerCode`、件重体、目的地、业务员、业务渠道、货物摘要、录单人和费用数量。字段裁剪证据：录单详情按 `canViewOrderEntryPayables` 后端裁剪应付，业务员响应不返回真实应付明细；录单阶段填写转单号返回 400。验证命令：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "creates order-entry drafts"`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "moves shipments through audit"`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "opens order entry"`。47 口径：本轮未同步 47；仅做只读验证，47 真实库当前 `Shipment=100`、`REVIEW_PENDING=15`、`draftWarehousePackageIds not null=0`、`WarehousePackage bound=50`、`9409 Shipment=13`，未查到 `shipment.order_entry.submit/draft` 日志输出；不能用 47 历史数据替代 C1 受控业务样本验证。下一步进入 `docs/slices/C2-order-review.md`，开工前必须重新做复用审计。

## 2026-06-29 归档补充

## 当前今日收货字段严格补齐

2026-06-29 本地完成今日收货字段严格补齐，本轮未同步 47。复用现有 `WarehousePackage`、`GET /api/warehouse/today-receipts`、`POST /api/warehouse/packages`、`PUT /api/warehouse/packages/:id/remark`、异常更新接口、`warehouse:read/write` 和 `AuditLog`，未新增表、路由或重复仓库模型。最小补差：今日收货默认列调整为截图核心字段（勾选、站点、客户编号、组合号、件数、单件实重、尺寸、单件方数、5000/6000 材积、扫描时间、备注、异常）；备注改为仓库可编辑并走真实 remark API；手动添加抽屉支持可编辑扫描时间和组合号，组合号默认按客户编号和快递单号生成但可手动修正；今日收货查看日志改为 `warehouse.today_receipts.view`；低频字段保留在列设置；仓库新用户组/仓库账号通过 `warehouse:read/write` 查看全量仓库数据，业务员通过客户归属范围只看自己客户且后端不返回站点。数据证据：前端测试覆盖今日收货看板、默认 `单件方数`、备注编辑、手动添加扫描时间/组合号和异常；API 仓库 e2e 覆盖今日收货查看日志、备注 before/after 日志、在仓、理货、拆票、合票和出库链路。角色证据：今日收货和在仓接口用后端 scope 裁剪业务员数据，仓库角色/仓库用户组可读写仓库链路；仓库和业务员响应继续通过无价格字段断言。日志证据：`warehouse.today_receipts.view`、`warehouse.package.remark.update`、`warehouse.package.create` 等均由测试验证。验证命令：`USE_PRISMA_REPOSITORY=false npm test -w @siyuan/api -- --run src/modules/app.warehouse.e2e.test.ts`、`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx -t "shows today receipts"`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web` 均通过。剩余：本轮未发布 47，未做浏览器 1366px 实测。

## 当前全系统后端审计兜底

2026-06-29 已发布全系统重要操作后端审计兜底和操作日志通俗化到 47。实现：新增全局 `AuditInterceptor`，所有通过后端的 `POST/PUT/PATCH/DELETE` 以及路径含 `import/export` 的请求都会写入 `AuditLog`，按路径归类为认证登录、系统设置、基础资料、仓库管理、财务结算、报价查价、我的订单等模块；已有业务细审计继续保留。登录成功现在同时写 `LoginLog` 和 `AuditLog(auth.login.success)`；登录失败、验证码失败写 `AuditLog(auth.login.failed)`，不记录密码，请求失败兜底日志只写泛化失败信息，避免泄露内部错误。操作日志页面将常见接口目标翻译为业务功能，例如财务资料库、运单列表、报价查价等，原接口路径仍保留作技术追溯。验证：本地 `npm run typecheck -w @siyuan/api` 通过；`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "failed login|8 character"` 2/2 通过；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "audit log"` 通过；`npm run typecheck -w @siyuan/web` 通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-CpOW6PJk.js`、`index-BU4YfLMB.css`、`xlsx-CkFp8p6R.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：Web `HTTP/1.1 200 OK`，`/api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。47 源码采证确认包含 `AuditInterceptor`、`recordHttpAudit`、`auth.login.failed` 和 `getAuditTargetDisplay`。

## 当前全局查询区与 SQL 注入审查

2026-06-29 本地修正仓库“今日收货/在仓数据”查询区畸形显示，本轮未发布 47。复用现有 `renderFilterField/renderFilterActions`，只把仓库查询按钮放回栅格列，并让 `warehouse-today-filter-grid` 使用统一筛选块样式；未改查询字段、接口、权限或数据范围。SQL 注入风险初筛：全仓库搜索 `$queryRaw/$executeRaw/queryRawUnsafe/executeRawUnsafe/Prisma.raw/Prisma.sql/sql\`/whereRaw/knex.raw/sequelize.query/createQueryBuilder`，业务代码未命中；仅文档中出现 47 只读 psql 说明。当前 API 查询主要走 Prisma `findMany` 对象条件和 `contains/orderBy` 参数化路径，未发现用户输入拼接 SQL 的 sink。验证：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`、`npm run typecheck -w @siyuan/web` 均通过。

## 当前操作日志通俗化

2026-06-29 操作日志“操作对象/审计说明”通俗化已随全系统后端审计兜底发布 47。实现：前端展示层将常见接口目标翻译为业务功能，例如 `GET /api/finance/catalog?category=SETTLEMENT_METHOD` 显示为 `查看财务资料库：结算方式`，`GET /api/shipments` 显示为 `查看运单列表`；原接口路径仍以次要文本保留，便于技术追溯和排查。导出 Excel 的对象列也使用业务功能名，并额外保留接口列；原始日志弹窗仍展示完整原始数据。验证记录见“当前全系统后端审计兜底”。

## 当前报价加价规则主表聚合

2026-06-29 已发布代理加价规则主表展示口径调整到 47。规则：代理加价规则主表只展示代理级规则，即 `代理 / 全部渠道 / 全部线路 / 全部国家` 入口；按渠道、线路、国家生成的明细加价规则不再全量铺在主表里，仍在“查看线路”弹窗里按价格行全量查看和维护，并继续参与查价。新增迁移 `20260629235900_backfill_agent_level_markup_rules`：清理线上可能残留的 `a代理/b代理` demo 规则，并为已有价格表行中的 `深圳振韵国际`、`驰汉` 补齐代理级入口，避免旧数据只有线路级规则导致主表无入口。本地验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 10/10 通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 15/15 通过；`npm run typecheck -w @siyuan/web` 通过；`DATABASE_URL='postgresql://user:pass@localhost:5432/siyuan' npx prisma validate --schema apps/api/prisma/schema.prisma` 通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-BBgSGVBZ.js`、`xlsx-CkFp8p6R.js`；`docker compose --profile tools run --rm db-migrate` 成功应用该迁移，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：Web `HTTP/1.1 200 OK`，`/api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。47 数据复查：代理级规则只剩 `深圳振韵国际=1`、`驰汉=1`，线路级明细规则保留 `780` 条供“查看线路”和查价使用。

## 当前报价亿阳查价口径

2026-06-29 本地修复亿阳真实表导入 `Internal server error`，本轮未发布 47。根因：`12KG+ / 51KG+` 被通用 `+` 重量段规则先匹配，导致导入行只有 `minWeightKg`、没有 `maxWeightKg`，后端校验未挡住 `undefined`，写 Prisma 必填字段时变成 500。修复：Excel 解析先识别 `12KG+ -> 12-50.999`、`51KG+ -> 51-99.999`、`100KG+ -> 100-99999`，再走通用加号规则；Prisma/InMemory 导入校验补 `Number.isFinite`，以后不完整行会返回明确 400，不再落到数据库 500。真实文件 `/Users/j1ng/Public/报价/6.30号亿阳(1).xls` 复测解析 `4835` 行，`invalid=0`，最大成本价 `14.1`，CBM 的 1940/2020 未混入 kg 报价。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx`、`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 均通过。

2026-06-29 本地完成亿阳报价导入/查价差异口径，本轮未发布 47。规则：亿阳仓库代码汇总表只按“下单渠道/仓库代码/明确 KG 重量档”导入，不导入 `CBM/方` 价格列，不抓取或展示同工作簿里的 `产品附加`、`特别说明`；亿阳可用亚马逊仓库代码加计费重直接查价，不再强制填写目的地和邮编。其他无亚马逊代码查价仍要求前端填写目的地和邮编，后端无目的地且无仓库代码时继续拒绝。实现复用现有 `PriceBookRow.warehouseCode`、报价导入、`/api/pricing/lookup` 和既有仓库代码匹配逻辑，未新增表、接口或权限。验证：`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web` 均通过；真实文件 `/Users/j1ng/Public/报价/6.30号亿阳(1).xls` 已确认包含 `YY美西快线海卡渠道汇总` 等亿阳汇总页。

## 当前系统管理用户名批量导入

2026-06-29 本地完成系统管理“用户名/员工账号”批量导入，本轮未发布 47。复用现有 `xlsx` 前端依赖和 `POST /api/system/staff-accounts` 单个新增接口，未新增后端接口、表或权限；每一行导入都会走既有账号校验、密码强度、角色校验、审计日志和首次登录改密逻辑。页面新增“模板下载”和“批量导入”按钮，模板字段为：账户、密码、业务员、中文名、性别、所属站点、状态、所属用户组；导入白名单仅允许 `.xlsx`，文件选择 `accept=".xlsx"`，代码里也二次校验扩展名，`.xls/.csv` 直接拒绝。导入成功/失败按行汇总提示，并刷新员工账号列表。验证：`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx`、`npm run build -w @siyuan/web` 均通过；构建仍有既有大 chunk 提示。

## 当前报价亿阳规则清理与全选

2026-06-29 本地完成代理加价规则里 `亿阳国际` 清理和全选删除能力，本轮未发布 47。实现：移除 API/InMemory/Web 测试桩/seed 中默认 `亿阳国际` 加价规则，新增数据迁移 `20260629235500_remove_yiyang_markup_rules`，上线执行迁移时会删除现有 `AgentMarkupRule.agentName = '亿阳国际'` 的加价规则，不删除价格表和报价行；代理加价规则表从单选 radio 改为 checkbox 多选，表头支持全选，删除按钮按选中条数批量调用现有单条 DELETE 接口，继续复用原权限和审计路径；修改、查看线路、停用仍限制单选。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 9/9 通过；`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx` 15/15 通过；`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts` 7/7 通过；`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 通过；`DATABASE_URL='postgresql://user:pass@localhost:5432/siyuan' npx prisma validate --schema apps/api/prisma/schema.prisma` 通过。首次 `npx prisma validate` 未带 `DATABASE_URL` 返回环境变量缺失，补临时 URL 后通过。

## 当前报价查价结果备注补充

2026-06-30 已补齐 47 现有 `驰汉导入(5).xlsx` 查价备注历史数据。根因：功能代码已经能从 `产品附加`、`特别说明` 工作表抓取并在查价详情展示，但 47 上这张价格表是在字段上线前导入，84 行 `PriceBookRow.productSurchargeRemark/specialRemark` 均为空，所以页面显示“暂无产品附加说明/暂无特别说明”。处理：用本地真实文件 `/Users/j1ng/Public/报价/驰汉导入(5).xlsx` 经现有 `parsePriceWorkbook` 解析出备注，只更新 47 真库该价格表、代理为 `驰汉` 的缺失字段，不重导、不删除价格表、不改报价行价格。47 复查：`UPDATE 84`，`驰汉导入(5).xlsx` 当前 `rows=84, product_notes=84, special_notes=84`，样本内容包含 `欧洲空运包税加价产品一览表` 和 `A.尺寸及偏远说明`。

2026-06-29 本地完成报价查价结果展示价格表内 `产品附加`、`特别说明/尺寸要求`。口径：导入价格表时从同一工作簿的 `产品附加`、`特别说明`/尺寸相关工作表抓取表格文本，作为查价提示随命中的价格行保存和返回；这两项只展示给业务员自行判断快递派附加/尺寸要求，不参与系统报价金额计算。实现：新增 `PriceBookRow.productSurchargeRemark`、`PriceBookRow.specialRemark` 持久化字段和迁移；Excel 解析统一裁剪真实有值范围后提取说明表文本；API 导入、InMemory/Prisma 查价返回和前端 mock 均透传；Top3 卡片增加两行简要备注，报价详情弹窗显示完整 `产品附加` 与 `特别说明/尺寸要求`。实测 `驰汉导入(5).xlsx` 解析 84 行，代理为 `驰汉`，已抓到 `欧洲空运包税加价产品一览表` 和 `A.尺寸及偏远说明`。验证：`npm run build -w @siyuan/shared`、`npm run prisma:generate -w @siyuan/api`、`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx`、`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts`、`npm run typecheck -w @siyuan/web`、`npm run typecheck -w @siyuan/api` 均通过。本轮未发布 47。

## 当前报价规则保留口径

2026-06-29 本地优化报价查价页布局，本轮未发布 47。仅调整 `apps/web/src/styles.css`，未改查价字段、报价接口、价格表导入、加价规则或数据库。查价表单从单列大块竖排改为基础信息/计费信息双列工作台，移动端仍回落单列；卡片内距、表单项间距和自动计费重提示压缩，减少首屏被表单占满的问题。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 8/8 通过；`npm run typecheck -w @siyuan/web` 通过。

2026-06-29 已发布报价规则保留口径、加价规则删除/停用按钮、报价导入 413 修复到 47。发布范围：删除价格表只删除成本价格表与报价行，不删除代理加价规则；代理加价规则新增独立“停用”按钮，原“删除”按钮改为物理删除规则；Web nginx `client_max_body_size` 提升到 `25m`，与 API body limit 对齐。发布流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-BQhP9CMK.js`、`xlsx-CkFp8p6R.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：Web `HTTP/1.1 200 OK`；等待 API 启动后 `/api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`；47 容器内 `nginx -T` 确认 `client_max_body_size 25m;`。重启后一瞬间 API 曾返回 502，等待约 10 秒后复查已恢复 200。

2026-06-29 本地修正“删除价格表是否同步删除加价规则”的业务口径，本轮未发布 47。最新确认：价格表是代理成本数据，加价规则是代理报价策略，规则需要长期落地保留；删除亿阳等代理价格表后，亿阳加价规则仍应存在，后续重新导入亿阳价格表会继续沿用该规则；规则只在“代理加价规则”里通过“删除”物理删除，或通过“停用”停止使用。实现：撤回价格表删除时同步清加价规则的联动，`deletePriceBook` 只软删价格表并移除其报价行；前端删除价格表后可重新拉规则列表但不清规则；测试锁定“删价格表后规则仍在”。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 8/8 通过；`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts` 7/7 通过；`npm run typecheck -w @siyuan/web` 通过；`npm run typecheck -w @siyuan/api` 通过。

## 当前报价加价规则按钮修正

2026-06-29 修复报价价格表导入 `413 Request Entity Too Large` 的本地配置根因。本轮未发布 47。根因：上传的 `.xls` 文件本身约 237KB，但前端会先解析为价格行 JSON 再提交 `POST /api/pricing/books/import`；API Express body limit 已是 `25mb`，线上截图里的 413 由 Web nginx 默认 `client_max_body_size` 返回，请求未到 API。修复：`deploy/nginx.conf` 在 server 层增加 `client_max_body_size 25m;`，与 API body limit 对齐；未改报价解析、导入接口、数据库或权限。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 8/8 通过；`npm run typecheck -w @siyuan/web` 通过。47 生效需要按 Docker 流程重建/重启 web 容器。

2026-06-29 本地修复代理加价规则“删除/停用”语义。本轮未发布 47。根因：页面“删除”按钮调用 `DELETE /api/pricing/markup-rules/:id`，但后端 `deleteAgentMarkupRule` 实际复用 `enabled=false`，所以删除变成停用。修复：后端 Prisma/InMemory 仓储的 DELETE 改为物理删除并写 `pricing.markup_rule.delete` 审计；前端增加独立“停用”按钮，走现有 `PUT enabled=false`，原“删除”按钮走 DELETE 并从列表移除；测试桩同步 DELETE 语义。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 8/8 通过；`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts` 7/7 通过；`npm run typecheck -w @siyuan/web` 通过；`npm run typecheck -w @siyuan/api` 通过。

## 当前报价加价规则修正

2026-06-29 修复并发布“驰汉导入后加价规则只有 4 条详细渠道、点击行会排到第一位”的问题到 47。根因：上一轮历史数据按“代理 + 渠道”补了 4 条 `驰汉` 加价规则，且前端为了让新规则可见曾把选中规则挪到表格第一行；业务预期应是价格表导入后只补一条代理级默认规则，明细渠道/线路在“查看线路”里维护。本轮修复：`buildMissingImportedMarkupRules` 改为只按代理补缺失规则，默认显示为 `代理 / 全部渠道 / 全部线路 / 全部国家 / +0.5/kg`；自动同步创建规则时不再传渠道；代理加价规则表 `dataSource` 恢复为原始 `markupRules`，点击行只选中不重排。线上数据已清理：删除 4 条 `markup-chihan-*` 渠道级误补规则，插入 1 条 `markup-chihan-default`，复查为 `驰汉 / 全部渠道 / 全部线路 / 全部国家 / +0.5/kg / enabled=true`。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 8/8 通过；`npm run typecheck -w @siyuan/web` 通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-DPPg0-lQ.js`、`xlsx-CkFp8p6R.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：Web `HTTP/1.1 200 OK`；等待 API 启动后 `/api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`。重启后一瞬间 API 曾返回 502，等待约 10 秒后复查已恢复 200。

## 当前全局提示与报价导入修复

2026-06-29 修复并发布“驰汉”导入后不显示在代理加价规则里的根因到 47。47 线上核查：`驰汉导入(5).xlsx` 已导入 84 行，但因该横向表没有标准“代理”表头，导入时代理名落成 `未知代理`；且该文件是在“上传后同步加价规则”发布前导入，因此没有自动生成加价规则。本轮修复：`parsePriceWorkbook` 支持接收源文件名，横向价格表若 sheet 名/工作簿标题识别不到代理，则从文件名推断代理名，`驰汉导入(5).xlsx` 会识别为 `驰汉`；上传调用已传入 `file.name`。47 历史数据已最小修正：该价格表 84 行 `PriceBookRow.agentName` 从 `未知代理` 改为 `驰汉`，并补 4 条渠道级加价规则，分别为 `UPS欧洲海运双清-1:300减0.5 不包税/包税`、`UPS英国海运双清-1:300减0.5 不包税/包税`，默认 `+0.5/kg`。本地验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 8/8 通过；`npm run typecheck -w @siyuan/web` 通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-BLeV0qv6.js`、`xlsx-CkFp8p6R.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：Web `HTTP/1.1 200 OK`；等待 API 启动后 `/api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；数据复查确认 `AgentMarkupRule` 已有 4 条 `驰汉` 规则。重启后一瞬间 API 曾返回 502，等待约 10 秒后复查已恢复 200。

2026-06-29 修复并发布代理加价规则同步后仍看起来“不生效”的展示问题到 47。线上核查发现 47 数据库已有 `241` 条加价规则，其中 `238` 条为 `亿阳国际 / YY铂金达海卡 / 美国`，并非未创建；截图停留在第 25 页，只显示最后一条旧的 `亿阳国际 / 全部渠道`，导致误以为同步失败。修复：代理加价规则表分页改为受控；价格表导入并同步/匹配到规则后，自动选中相关规则并把加价规则分页重置到第 1 页；表格把当前选中规则排到第一行，避免新规则藏在后面的分页里。本地验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 7/7 通过；`npm run typecheck -w @siyuan/web` 通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-C362mZ8A.js`、`xlsx-CkFp8p6R.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：Web `HTTP/1.1 200 OK`；等待 API 启动后 `/api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；API 日志包含报价路由和 `Nest application successfully started`。重启后一瞬间 API 曾返回 502，等待约 10 秒后复查已恢复 200。

2026-06-29 已发布“价格表上传后同步代理加价规则”到 47。发布流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `47:/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-CkVftMZt.js`、`xlsx-CkFp8p6R.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`docker compose ps` 显示 `siyuan-api-1`、`siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；等待 API 启动后 `GET /api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；API 日志包含报价加价规则路由和 `Nest application successfully started`。重启后一瞬间 API 曾返回 502，等待约 10 秒后复查已恢复 200。

2026-06-29 修复报价价格表上传后“代理加价规则”不同步的问题。本轮复用现有 `POST /api/pricing/markup-rules`，未新增表、接口或权限；导入价格表成功后，前端会按导入行补齐缺失的“代理 + 渠道”基准加价规则，默认沿用同代理已有基准加价，否则为 `+0.5/kg`，已有同代理同渠道规则不重复创建。这样上传新价格表后，代理加价规则页会立即出现新代理/渠道入口，可继续点“查看线路”做批量或单线路加价。若价格表导入成功但规则同步失败，提示会明确为“价格表已导入，加价规则同步失败”，不再误报价格表导入失败。验证：`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 7/7 通过；`npm run typecheck -w @siyuan/web` 通过。本轮未发布 47。

2026-06-29 已再次发布报价导入最终修复到 47，包含 `驰汉导入(5).xlsx` 横向 KG 表块停止条件、重复 `301kg+` 分组、最后一档上限修复，以及全局提示 toast 化和价格表解析裁剪空白范围加速。发布流程：本地 `npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `47:/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，Web 新产物为 `index-B1qxWkSX.js`、`xlsx-CkFp8p6R.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`docker compose ps` 显示 `siyuan-api-1`、`siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；等待 API 启动后 `GET /api/health` 返回 `HTTP/1.1 200 OK` 和 `{"ok":true,"service":"siyuan-api"}`；API 日志包含报价路由和 `Nest application successfully started`。重启后一瞬间 API 曾返回 502，等待约 10 秒后复查已恢复 200。

2026-06-29 定位并修复 `驰汉导入(5).xlsx` 导入失败原因。本地复现：旧解析器会在 `非英海运` 的第一个 KG 表块后继续向下读取，把后续 CBM/托盘价区域套用上方 KG 表头，产生异常高成本价；同时同一行存在“不包税/包税”两组 `301kg+` 时，前一组最后一档被算成 `minWeightKg=301/maxWeightKg=301`，触发后端 `maxWeightKg <= minWeightKg` 校验，报“第 N 行报价数据不完整”。修复：横向 KG 表解析只读取当前连续表块，遇到下一段说明/报价总费用/托盘价等立即停止；重复重量段按包税/不包税分组处理，最后一档上限为 `99999`；渠道名清理为 `UPS欧洲海运双清-1:300减0.5 不包税/包税`。复测 `驰汉导入(5).xlsx`：解析 84 条 KG 成本价，`invalid=0`，不再导入 CBM/托盘价/整车价。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 7/7 通过。本轮修复尚未发布 47。

2026-06-29 已发布全局提示 toast 化、报价横向重量段导入和 Excel 大空白范围解析加速到 47。发布流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `47:/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，Web 构建产物包含新的 `xlsx-CkFp8p6R.js` 和 `index-HV3Ezgdb.js`；`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，未执行 `db push/reset/seed`；`docker compose up -d --remove-orphans api web` 已重启。健康检查：`docker compose ps` 显示 `siyuan-api-1` 和 `siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`；API 日志包含报价相关路由和 `Nest application successfully started`。重启后一瞬间 `/api/health` 曾返回 502，等待 API 完成启动后复查已恢复 200。

2026-06-29 修复报价价格表导入慢的问题。原因是部分 Excel 虽然肉眼只有几十行，但工作表 `!ref` 被标成超大空白范围，例如 `英国海运 A1:U1048576`、`特别说明 A1:XFD51`，原解析器用 `sheet_to_json` 直接扫完整范围导致浏览器端解析明显变慢。修复：报价导入解析统一先按真实有值单元格裁剪范围，再转换为二维行数据；同一份 `驰汉导入(5).xlsx` 本地解析仍为 211 行，耗时约 `30ms`。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 7/7 通过。本轮未发布 47。

2026-06-29 修复全局操作提示形态，并扩展报价价格表导入解析。本轮将共享 `renderNoticeBar` 从页面内常驻 Alert 改为 Ant Design toast，成功提示 2 秒、错误/警告 3.5 秒后自动消失；错误不再用绿色成功样式，且包含“必须”的校验报错会识别为 error。主菜单切换时清空全局 `notice`，避免上一模块的报错在新模块残留。报价价格表导入器新增横向 KG 重量段表解析，可识别类似 `目的地/国家/重量区间 + 15KG+/50kg+/100kg+`、`渠道 + 5KG+/11KG+/21KG+` 的同行价表；只导入明确为 KG 的成本价，CBM/整柜等不同计价单位暂不强行映射为每 kg。验证：`npm run typecheck -w @siyuan/web` 通过；`npm test -w @siyuan/web -- --run src/modules/pricing/pricing.test.tsx` 7/7 通过。本轮未发布 47。

## 当前报价查价模块盘点

2026-06-29 已盘点报价查价模块现状，并补一处最小权限闭环。本轮确认现有能力：业务员/客服/财务等有 `pricing:lookup` 的内部角色可使用 `/api/pricing/lookup` 查价；管理员可管理代理价格表、价格表备注、代理加价规则、线路自定义加价；查价会按目的地、计费重、亚马逊仓库/区域映射匹配价格表，返回最便宜 Top3 和最快 Top3；非管理员返回会裁剪代理成本、加价规则、毛利等内部字段。修复：仓储层 `getPriceBooks/importPriceBook/updatePriceBookRemark/deletePriceBook` 从员工报价访问收紧为管理员限定，避免绕过控制器调用时业务员能管理价格表。补充 API e2e：业务员 `GET/POST/PUT/DELETE /api/pricing/books*` 管理路径均 403，管理员仍可导入、备注、删除。验证：`npm test -w @siyuan/api -- --run src/modules/app.pricing.e2e.test.ts -t "persists imported price books"` 通过。`npm run typecheck -w @siyuan/api` 未通过，失败为既有 `FinanceDashboardItem/FinanceDashboardResponse` 未从 `@siyuan/shared` 导出，和本轮报价改动无关。本轮未发布 47。

## 最近一次增量修复

2026-06-29 完成财务看板 v1 本地落地，本轮未发布 47。复用现有财务表、仓储查询、RBAC 权限和财务子页入口，新增最小聚合接口 `GET /api/finance/dashboard`，返回 `kpis/todos/exceptions/quickActions`，前端“财务看板”从占位页改为“待办 + 异常 + 快捷入口”工作台；点击看板项切换到现有应收审核、业务成本审核、应付审核、待付款、已付款、水单匹配、代理账单核对页面。看板响应不返回利润、银行账号、付款方账号、水单凭证明细等敏感字段；仓库角色访问看板返回 403。验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts -t "finance dashboard"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "finance dashboard"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx` 均通过。补充：完整 `npm test -w @siyuan/api -- --run src/modules/app.finance.e2e.test.ts` 当前仍有 4 个既有权限预期失败，集中在 operator/service 对 `order-entry`、`finance-detail`、`paid-payments` 的 403/200 预期差异；本次新增的 `finance dashboard` API 用例单独通过，未在本轮顺手修改旧权限测试语义。

2026-06-29 已发布员工主菜单权限裁剪和“入口权限与数据范围权限分离”开发规则到 47。同步命令：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启。健康检查：`docker compose ps` 显示 `siyuan-api-1`、`siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`。本次未执行 `db push/reset/seed`，未创建或覆盖线上业务样本。

2026-06-29 固化“入口权限与数据范围权限分离”开发规则。已写入 `AGENTS.md`：菜单权限只决定是否能进入模块，不能代表全量数据授权；所有数据范围和字段可见性必须以后端查询裁剪和序列化裁剪为准。业务员、仓库、财务等内部角色默认只能看到职责范围内的数据，例如业务员只能看自己的单及自己单关联的业务成本，仓库不能看到财务敏感字段、利润、应付或付款信息。后续新增或开放菜单时，必须同步补接口对象归属过滤、字段裁剪和测试断言。本次仅更新开发规则和状态文件，未改业务代码。

2026-06-29 修复员工主菜单权限裁剪。本次复用现有 `getVisibleStaffMenuKeysByPermissions` 和角色权限数据，未新增权限点、接口、表或迁移。根因是“客服管理”菜单用 `tracking/problems` 权限判断，业务员和仓库因需要轨迹/问题件能力被误显示客服入口；“财务管理”菜单用业务成本权限判断，业务员因业务成本权限被误显示财务入口。修复：`customerService` 主菜单增加客服角色边界，仅 `CUSTOMER_SERVICE/UG_CUSTOMER_SERVICE` 且具备客服相关权限时显示；`finance` 主菜单增加财务角色边界，仅 `FINANCE/UG_FINANCE/UG_FINANCE_CASHIER` 且具备财务相关权限时显示；运营、业务、市场、仓库、轨迹、报价、基础资料仍按既有权限显示。验证：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "isolates staff menus"` 通过；`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 11/11 通过；`npm run typecheck -w @siyuan/web` 通过。本轮未发布 47。

2026-06-29 移除系统管理“客户端角色权限”入口。本系统按用户确认仅内部使用，不开放客户门户，因此该静态客户权限表不需要存在；本次只删除前端二级菜单、静态 `clientRoleRows` 和对应表格，不改 `Role/Permission/User.roleId` 内部权限体系，不改客户资料主档，不新增迁移。验证：`rg "客户端角色权限|clientPermissions|clientRoleRows|分配客户端角色权限" apps/web/src/modules/settings/SettingsPage.tsx` 无结果；`npm run typecheck -w @siyuan/web` 通过。补充验证：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 运行 11 个用例，10 个通过，1 个既有权限隔离用例失败，失败点为业务员仍可见“客服管理”，与本次删除客户端入口无直接关系；后续应单独修员工菜单权限裁剪。

2026-06-29 已发布旧内置员工角色向新用户组体系迁移到 47，并完成线上真实库迁移。复用现有 `Role`、`Permission`、`User.roleId` 和角色权限矩阵，未新建第二套 RBAC。迁移 `20260629223000_migrate_builtin_staff_to_role_groups` 已通过 Docker `db-migrate` 执行线上 `prisma migrate deploy`：`ADMIN` 显示为“管理员组”并保留最大权限；旧 `CUSTOMER_SERVICE/OPERATOR/WAREHOUSE/FINANCE` 账号分别迁到 `UG_CUSTOMER_SERVICE/UG_BUSINESS/UG_WAREHOUSE_RECEIVE/UG_FINANCE`；迁移写入 `system.staff.role_group_migration` 审计记录。前端“用户名/所属用户组”和“角色权限分配”只展示新用户组和管理员组，不再把旧内置角色混入新建/修改员工入口。后端禁止新建/修改员工到旧内置业务角色，并把业务范围裁剪从 `role === OPERATOR` 改为识别业务部/市场部/站点类业务用户组；代理渠道写入仓储层补 `master-data:agents:write` 权限保护。本地验证命令：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins edit role permissions"`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "loads and saves the real role permission matrix|maintains role groups"` 均通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 已应用 `20260629223000_migrate_builtin_staff_to_role_groups`，`docker compose up -d --remove-orphans api web` 已重启；Web `18899` 返回 `HTTP/1.1 200 OK`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。数据库证据：`_prisma_migrations` 中该迁移 `applied=true`；`Role` 中 `ADMIN=管理员组`、`UG_BUSINESS=业务部`、`UG_CUSTOMER_SERVICE=客服`、`UG_WAREHOUSE_RECEIVE=仓库收货`、`UG_FINANCE=财务` 均启用；用户计数为管理员组 1、业务部 2、客服 1、仓库收货 1、财务 1；`AuditLog` 中 `system.staff.role_group_migration=1`。本次未执行 `db push/reset/seed`，未创建或覆盖线上业务样本。

2026-06-29 修复首次登录改密被 `workspace:access` 权限拦截的问题并发布到 47。原因：`POST /api/auth/change-password` 原来使用 `@RequirePermission('workspace:access')`，如果新账号所属用户组没有工作台权限，登录后虽然需要强制改密，但改密接口会返回 403 `没有访问权限`。修复：复用现有 `RbacGuard`，新增最小 `RequireAuth` 元数据，只验证 Bearer token，不检查业务权限；`/api/auth/change-password` 改为 `@RequireAuth()`，仍校验当前密码、密码强度并写 `auth.password.change` 审计日志。验证命令：`npm run typecheck -w @siyuan/api`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets authenticated first-login users change password"` 均通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启；Web `18899` 返回 `HTTP/1.1 200 OK`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。本次未执行 `db push/reset/seed`，未改线上业务数据。

2026-06-29 优化系统管理“角色权限分配”界面并发布到 47。复用现有 `RolePermissionMatrix`、`draftPermissions` 和 `/api/system/roles/:role/permissions` 保存接口，未改权限模型、接口或数据库。界面从“所有角色横向表格 + 全量 checkbox 墙”改为左侧角色列表、右侧当前角色权限分组；每个角色显示权限数量，每个权限分组显示已选/总数，保存按钮固定在当前角色头部。验证命令：`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "loads and saves the real role permission matrix"` 均通过；本地 Vite + Chrome 检查 1366px 和 1920px 视口，角色权限编辑器渲染正常且无横向溢出。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启；Web `18899` 返回 `HTTP/1.1 200 OK`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。本次未执行 `db push/reset/seed`，未改线上业务数据。

2026-06-29 修复首次登录强制改密“点击保存没响应/未改密提前进入”的问题并发布到 47。原因：强制改密弹窗的提交按钮已是 submit，但提交失败时没有在弹窗内展示 API 错误，用户会感觉没反应；同时登录后即使 `mustChangePassword=true`，通用 `session` effect 仍会提前加载工作台数据。修复：强制改密弹窗增加提交 loading、错误 Alert 和防重复提交；登录用户要求改密时不再提前 `refreshWorkspace`，保存新密码成功后才关闭弹窗、同步本地 session 的 `mustChangePassword=false` 并加载工作台；本地已有 session 若仍标记 `mustChangePassword`，刷新页面也会继续弹出强制改密。验证命令：`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "blocks first-login"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 均通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启；Web `18899` 返回 `HTTP/1.1 200 OK`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。本次未执行 `db push/reset/seed`，未改线上业务数据。

2026-06-29 修改密码强度最小长度规则并发布到 47。规则从“长度必须大于 8 位”改为“长度大于或等于 8 位”，也就是 8 位可通过、7 位仍拒绝；字符类型规则不变，仍需大写字母、小写字母、数字、特殊字符中的至少 3 类。复用现有 `getPasswordStrengthError` 和 `getPasswordStrengthErrorForUi`，同步更新个人中心/首次登录改密文案，未新增密码策略表或配置项。验证命令：`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "accepts 8 character staff passwords"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "opens personal center"` 均通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启；Web `18899` 返回 `HTTP/1.1 200 OK`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。本次未执行 `db push/reset/seed`，未改线上业务数据。

2026-06-29 完成员工账号查询/修改/删除能力并发布到 47。复用现有 `User`、`Role/User.roleId`、`Site`、密码强度校验和 `audit_logs`，未新增员工表、权限表或迁移。接口补齐：`GET /api/system/staff-accounts` 支持 `keyword/site/role/status` 查询，`PUT /api/system/staff-accounts/:id` 支持修改账户、业务员、中文名、手机号、性别、站点、状态、所属用户组和可选重置密码，`DELETE /api/system/staff-accounts/:id` 采用 `enabled=false` 软删除/离职，不物理删除历史数据；后端禁止停用当前登录账号或修改自己的用户组，并防止手写 `role=CUSTOMER` 从员工账号列表查出客户账号。前端系统管理“用户名”页补查询区、新增/修改/删除按钮，员工表支持点击行选中；删除提示明确为停用不删历史。审计 action：`system.staff.update`、`system.staff.delete`，保留既有 `system.staff.create/password_reset/site.update`。本地验证：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins edit role permissions"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 均通过；补充 `role=CUSTOMER` 防护后重跑 `npm run typecheck -w @siyuan/api` 和同一 API e2e 通过。47 发布：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步；补充防护后再次 `npm run sync:47 -- --apply` 并重建发布；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启；Web `18899` 返回 `HTTP/1.1 200 OK`，`/api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含新路由 `GET/POST/PUT/DELETE /api/system/staff-accounts` 和 `Nest application successfully started`。本次未执行 `db push/reset/seed`，未创建或覆盖 47 业务样本。

2026-06-29 完成系统管理“用户名/新建用户”字段补齐，本轮未发布 47。复用现有 `User`、`Role/User.roleId`、`Site`、员工 profile 字段和 `system.staff.create` 审计日志，未新增用户表、用户组表或权限模型。新建用户弹窗按样例补齐：业务员、中文名、账户、密码、性别、所属站点、状态、所属用户组；密码可填自定义初始密码，不填继续使用既有 `账户@123` 规则，前后端均复用现有密码强度校验，列表不回显明文密码，只显示“已设置”。员工列表列名调整为：序列、业务员、中文名、账户、密码、性别、所属站点、改密要求、状态、所属用户组、创建时间、操作；状态显示为在职/离职，所属用户组继续来自现有角色/用户组。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 均通过。本轮未执行 47 发布、未改线上数据库。

2026-06-29 已发布系统管理“用户组”权限角色组到 47，并完成线上数据库适配。复用现有 `Role`、`Permission`、`User.roleId`、角色权限矩阵和 `audit_logs`，未新建第二套 RBAC。同步命令：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 已应用迁移 `20260629194500_add_role_group_metadata`，`docker compose up -d --remove-orphans api web` 已重启服务。健康检查：`docker compose ps` 显示 `siyuan-api-1` 和 `siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`。数据库证据：47 真库 `_prisma_migrations` 中 `20260629194500_add_role_group_metadata` 的 `applied=true`；`Role` 表包含 `label/description/site/sortOrder/enabled/systemBuiltin` 六个元数据字段；`UG_%` 默认用户组为 12 条，包含仓库收货、仓库出货、客服、财务、出入账财务、市场部、业务部、深圳思远武汉、漳州思华、武汉九域联、业务经理、业务主管。本次未执行 `db push/reset/seed`，未创建或覆盖线上业务样本。

2026-06-29 已发布客户资料收货人信息与客户来源改造到 47，并完成线上数据库适配。同步命令：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`。47 上执行 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 已应用迁移 `20260629183000_add_customer_source`，`docker compose up -d --remove-orphans api web` 已重启服务。健康检查：`docker compose ps` 显示 `siyuan-api-1` 和 `siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`；API 日志包含 `Nest application successfully started`。数据库证据：47 真库 `Customer` 表包含 `id/code/name/customerSource`，其中 `customerSource` 为 nullable text；`_prisma_migrations` 中 `20260629183000_add_customer_source` 的 `applied=true`。本次未执行 `db push/reset/seed`，未创建或覆盖线上业务样本。

2026-06-29 完成客户资料收货人信息与客户来源改造，本轮未发布 47。复用现有 `CustomerContact` 作为收货人资料库，未新建收货人表；`Customer` 新增 nullable 字段 `customerSource`，迁移文件为 `20260629183000_add_customer_source`。客户资料页支持客户来源新增/编辑/列表/筛选，客户收货人最多 4 组，支持新增、修改、停用，停用复用 `enabled=false` 不物理删除。录单页复用 App 已加载的 `customers/contacts`，输入客户编号后可选择启用收货人并自动带出姓名、公司、电话、地址、国家、州/省、邮编；勾选“保存到客户资料”后，录单成功再回写收货人，且同名同电话同地址不重复保存，已有 4 组时禁用保存。验证命令：`npm run prisma:generate -w @siyuan/api`、`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "loads real master data"`、`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "shows order-entry fee fields"` 均通过。47 发布时需按 Docker 流程执行线上 `prisma migrate deploy`，让 47 真实库补 `Customer.customerSource` 字段；本轮未执行线上发布、未改线上真实数据。

2026-06-29 已发布基础资料库“汇率”历史区间逻辑到 47，并完成线上数据库适配。发布流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 已应用迁移 `20260629165000_add_exchange_rate_end_at`，`docker compose up -d --remove-orphans api web` 已重启。服务证据：`docker compose ps` 显示 `siyuan-api-1` 和 `siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`。数据库证据：47 真库 `ExchangeRate` 字段包含 `id/baseCurrency/quoteCurrency/rate/activeAt/enabled/endAt`，其中 `endAt` 为 nullable timestamp；`_prisma_migrations` 中 `20260629165000_add_exchange_rate_end_at` 的 `applied=true`。本次未执行 `db push/reset/seed`。

2026-06-29 按用户样例完成基础资料库“汇率”的历史区间逻辑。本次复用现有 `ExchangeRate`、`ExchangeRateSummary` 和 `/api/master-data/exchange-rates`，只补最小字段 `endAt` 与迁移 `20260629165000_add_exchange_rate_end_at`，未新建平行汇率表或接口。页面改为样例口径：上方显示“当前汇率”，下方“历史汇率:新增”包含币别、开始日期、汇率、结束日期和新增/保存按钮，“历史汇率:列表”包含序号、币别、开始日期、结束日期、汇率、修改、删除；新增历史汇率后端也要求结束日期，费用计算只取当前日期落在开始/结束区间内的启用汇率。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 均通过。本轮未发布 47；47 发布时需按 Docker 发布流程执行 `prisma migrate deploy`，让线上真实库补 `ExchangeRate.endAt`。

2026-06-29 已发布基础资料库“汇率”页到 47 并完成线上数据库适配验证。发布流程：`npm run sync:47` dry-run 通过，`npm run sync:47 -- --apply` 已同步到 `/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启，`docker compose ps` 显示 `siyuan-api-1` 和 `siyuan-web-1` 均 Up，Web 端口为 `0.0.0.0:18899->80`。健康检查：`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`，`GET /api/health` 返回 `{"ok":true,"service":"siyuan-api"}`，API 日志包含 `Nest application successfully started`，并确认已映射 `/api/master-data/exchange-rates` 的 `GET/POST/PUT/DELETE` 路由。数据库适配：线上 `ExchangeRate` 表已有 `id/baseCurrency/quoteCurrency/rate/activeAt/enabled` 六个字段，无需新增迁移；线上只执行 `prisma migrate deploy`，未执行 `db push/reset/seed`。47 真库验证：使用管理员账号和线上验证码登录后，创建 `QA47/RMB` 汇率 `6.66`，修改为 `6.77`，再通过 DELETE 停用，返回 `enabled=false`；`AuditLog` 中对应 `master_data.exchange_rate.create=1`、`master_data.exchange_rate.update=2`。因前两次脚本汇总阶段失败但接口已完成，线上留下 3 条 `QA47/RMB` 验证记录，均为停用状态；本轮未物理删除这些记录，以保留审计链。

2026-06-29 完成基础资料库“汇率”页。本次复用现有 `ExchangeRate` 表、`ExchangeRateSummary`、`/api/master-data/exchange-rates` 和 `master-data:write` 权限，未新增表、迁移或平行接口；只在既有汇率路径下补 `PUT /api/master-data/exchange-rates/:id` 修改和 `DELETE /api/master-data/exchange-rates/:id` 停用。基础资料库“汇率”二级功能已从占位页改为可用维护页，支持增加、修改、删除/停用、增加历史汇率；当前汇率按同一币种对最新启用记录展示，历史汇率列表保留所有记录。日志证据：API e2e 覆盖 `master_data.exchange_rate.create` 和 `master_data.exchange_rate.update`，停用也记录在同一 action。验证命令：`npm run build -w @siyuan/shared`、`npm run typecheck -w @siyuan/shared`、`npm run typecheck -w @siyuan/api`、`npm run typecheck -w @siyuan/web`、`npm test -w @siyuan/api -- --run src/modules/app.orders.e2e.test.ts -t "lets admins maintain master data"`、`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 均通过。本轮未发布 47，未改线上真实数据库。

2026-06-29 偏远资料入口从占位页改为可用上传页，并已发布 47。本次复用公司渠道 `remoteAreaRule` 和渠道类别生成偏远清单，支持在“偏远”页按 `渠道类别 / 偏远逻辑 / 文件上传` 查看；上传入口支持 `.xls/.xlsx/.csv` 和图片，支持粘贴图片/表格文件，文件资料暂存浏览器本地 `localStorage`，不新增表、接口或迁移，不影响现有报价/偏远费计算。权限口径：只有具备公司渠道写权限的角色显示上传/删除动作，业务无写权限不显示维护动作。验证命令：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 通过，`npm run typecheck -w @siyuan/web` 通过。47 发布证据：`npm run sync:47` dry-run 成功，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启，`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`，API 日志包含 `Nest application successfully started`；47 源码确认包含 `remoteAreaFiles`、`siyuan.master.remoteAreaFiles`、`文件上传` 和 `仅支持 xls、xlsx、csv 或图片`。本次未执行 `db push/reset/seed`。若要 47 长期保存偏远文件，下一步需先确认 xls 字段格式，再补最小后端持久化。

2026-06-29 删除基础资料库“代理资料”下方重复的代理银行账号只读展示区，并已发布 47。银行信息维护入口保留在新增/编辑代理弹窗的“收款方银行信息”，底层仍复用现有 `AgentBankAccount` 和 `/api/finance/agent-bank-accounts`，未新增表、接口或迁移，未改已有数据。验证命令：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 通过，`npm run typecheck -w @siyuan/web` 通过。47 发布证据：`npm run sync:47` dry-run 成功，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启，`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`，API 日志包含 `Nest application successfully started`；47 源码确认仅保留 `上传点` 和 `收款方银行信息`，没有 `代理银行账号/agentBankColumns/selectedAgentBanks` 展示区残留。本次未执行 `db push/reset/seed`。

2026-06-29 代理资料新增收款银行信息和上传点图片入口。本次复用现有 `AgentBankAccount`、`/api/finance/agent-bank-accounts`、`agentBankAccounts()`、`saveAgentBankAccount()`、`Agent.invoiceTemplateName/invoiceTemplateUrl`，未新增表、接口或迁移，未改动已有代理/银行账号数据。基础资料库“代理资料”的新建/编辑代理弹窗新增“收款方银行信息”：收款方、银行账号、开户银行；填完整后保存到既有代理银行账号资料，可被代理银行账号列表继续读取。“上传点”保留现有发票模板 URL 字段作为存储位置，支持图片文件选择和粘贴图片，暂不接对象存储、不生成新文件表。验证命令：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 通过，`npm run typecheck -w @siyuan/web` 通过。本轮尚未发布 47。

2026-06-29 修正并发布财务资料库迁移形态到 47。根据用户澄清，“财务资料库”不是拆成费用名称/结算方式/货物类型/品名多个二级入口，而是完整迁移为基础资料库下的单个二级功能“财务资料”。本次只改前端入口，未改 `FinanceCatalogItem` 数据、未改 `/api/finance/catalog`、未改数据库表或迁移；`FinanceCatalogPage` 仍完整展示费用名称、结算方式、货物类型、品名。验证命令：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 通过；`npm run typecheck -w @siyuan/web` 通过。47 发布证据：`npm run sync:47` dry-run 成功，`npm run sync:47 -- --apply` 已同步；47 上 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启，`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；47 源码确认 `activeMasterSection` 默认 `financeCatalog`，二级菜单 `label: '财务资料'`，页面 `title="财务资料"`。本次未执行 `db push/reset/seed`，未改线上资料库数据。

2026-06-29 已发布财务资料库入口迁移到 47。同步命令 `npm run sync:47` dry-run 成功，`npm run sync:47 -- --apply` 已同步到 `47:/opt/siyuan`；47 上执行 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启服务。发布证据：`docker compose ps` 显示 `siyuan-api-1`、`siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；API 日志包含 `Nest application successfully started`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；47 源码 `apps/web/src/modules/masterData/MasterDataPage.tsx` 已包含 `productNames: 'PRODUCT_NAME'`、`label: '品名'` 和 `title="资料字典"`。迁移证据：线上只执行 `prisma migrate deploy`，未执行 `db push/reset/seed`。剩余风险：本次只做发布和服务健康检查，未在 47 浏览器里按角色实测基础资料库入口。

2026-06-29 财务资料库入口迁移到基础资料库。复用现有 `FinanceCatalogPage`、`useFinanceCatalog`、`FinanceCatalogItem` 和 `/api/finance/catalog`，未新增表、接口或数据库迁移；财务管理侧边栏移除“财务资料库”，基础资料库补齐财务字典入口：费用名称、结算方式、货物类型、品名。财务录单/审核继续读取同一套资料库数据。验证命令：`npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx` 通过；`npm test -w @siyuan/web -- --run src/modules/finance/finance.test.tsx -t "shows receivables"` 通过；`npm run typecheck -w @siyuan/web` 通过。全量 `npm run test:web` 已执行，结果为 10 个测试文件通过、73 个测试通过、1 个既有用例失败：`src/modules/finance/finance.test.tsx` 的 `registers and queries an agent bill manually` 在第 262 行因多个同名“处理差异”按钮导致测试选择器不唯一；同一问题也使 finance 全文件测试失败，和本次资料库入口迁移无直接关系。本轮未发布 47。

2026-06-29 已发布待排货成本/应付列修复到 47。同步命令 `npm run sync:47 -- --apply` 成功；47 上执行 `docker compose build db-migrate api web` 成功，`docker compose --profile tools run --rm db-migrate` 返回 `No pending migrations to apply`，`docker compose up -d --remove-orphans api web` 已重启服务。发布证据：`docker compose ps` 显示 `siyuan-api-1`、`siyuan-web-1` 均 Up，Web 映射 `0.0.0.0:18899->80`；`curl -I http://127.0.0.1:18899/` 返回 `HTTP/1.1 200 OK`；API 日志包含 `Nest application successfully started`；47 源码中 `apps/web/src/modules/warehouse/WarehousePage.tsx` 已包含 `业务成本合计`。迁移证据：线上只执行 `prisma migrate deploy`，未执行 `db push/reset/seed`。剩余风险：本次只做结构发布和服务健康检查，未在 47 真库创建/操作新的待排货受控样本。

2026-06-29 待排货页补齐截图要求的成本/应付列，并按角色裁剪。复用现有 `Shipment WAITING_SORT -> WAITING_DISPATCH` 状态、`POST /api/shipments/:id/route` 排货接口、App 已加载的 `businessCostAudits` 和 `payableAudits` 数据，未新增表、接口、状态或权限。数据证据：`WAITING_SORT` 运单在仓库管理“待排货”显示，点击“排货”后进入“待出库”。角色证据：管理员可见业务成本、业务成本合计、代理、代理渠道、应付成本、应付合计；仓库角色不可见这些成本/应付字段。字段裁剪证据：前端只对 `ADMIN/FINANCE` 渲染成本列，且数据源仍来自已有财务权限加载结果，不给仓库页新开平行数据源。验证命令：`npm test -w @siyuan/web -- --run src/modules/warehouse/warehouse.test.tsx`、`npm run typecheck -w @siyuan/web`。

## 当前批次

F - 代理账单批

## 当前任务卡

A-F 第一阶段 39 张细卡本地已落地；当前无未完成本地任务卡。

## 已完成任务卡

- `docs/diagrams/`：已输出 Sunny 总业务流转图、跨岗位时序图、业务闭环追溯链路图，并补充 A-F 六张详细分图；PNG/SVG/Mermaid 源文件和详细分图生成脚本均保留。
- `A1-customer-master.md`：本地已完成复用审计和最小补差；待 47 受控样本复验。
- `A2-agent-master.md`：本地已完成复用审计和最小补差，并已发布 47；待 47 受控角色/API 复验。
- `A3-channel-master.md`：本地已完成复用审计和最小补差，并已发布 47；待 47 受控代理渠道/公司渠道样本复验。
- `A4-rate-remote-category.md`：本地已完成复用审计和最小补差，并已发布 47；待 47 受控渠道类别、偏远附加费和汇率样本复验。
- `A5-site-user-role.md`：本地已完成复用审计和最小补差；本轮按最新要求未同步 47，47 仅做现有结构只读验证。
- `A6-master-audit-log.md`：本地已完成复用审计和最小补差；本轮按最新要求未同步 47，47 仅做现有 AuditLog 结构和日志计数只读验证。
- `B1-today-receipt.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 WarehousePackage 结构、数量和日志计数只读验证。
- `B2-in-stock.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 WarehousePackage 状态和日志计数只读验证。
- `B3-sorting.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 WarehouseTallyTask 结构和日志计数只读验证。
- `B4-sorting-label.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 WarehouseTallyTask 结构、数量和标签日志计数只读验证。
- `B5-split-consolidation.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有拆票/合票表、数量和日志计数只读验证。
- `B6-outbound-handover-archive.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有出库状态、包裹归档状态和日志计数只读验证。
- `B7-warehouse-audit.md`：本地已完成 B 批总销账复用审计；未新增业务代码，本轮未同步 47，47 仅做仓库表和日志计数只读验证。
- `C1-order-from-warehouse.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 Shipment、WarehousePackage 绑定和 order-entry 日志计数只读验证。
- `C2-order-review.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有审核状态、审核字段和审核日志只读验证。
- `C3-routing.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有排货状态、排货字段和排货日志只读验证。
- `C4-outbound-bridge.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有待出库/已出库状态、出库字段和出库日志只读验证。
- `C5-service-data-confirm.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有客服双审核状态和日志计数只读验证。
- `C6-transfer-no-tracking.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有转单号、面单和日志计数只读验证。
- `C7-status-pool-signature.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有状态池、问题件和签收日志计数只读验证。
- `C8-order-service-audit.md`：本地已完成 C 批总销账复用审计和最小补差；本轮未同步 47，47 仅做生命周期脚本只读验证。
- `D1-water-receipt.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有水单表、字段、权限和日志计数只读验证。
- `D2-receivable-match-review.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有应收、水单匹配表、字段、状态和日志计数只读验证。
- `D3-business-cost.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有业务成本表、字段、状态和日志计数只读验证。
- `D4-payable-review.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有应付表、待付款表、字段、状态和日志计数只读验证。
- `D5-payment-application.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有付款申请表、明细表、字段、状态和日志计数只读验证。
- `D6-paid-payment-proof.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有已付款、付款凭证字段和日志计数只读验证。
- `D7-finance-audit.md`：本地已完成 D 批总销账复用审计和最小补差；本轮未同步 47，47 仅做现有财务表、状态和日志计数只读验证。
- `E1-role-read-matrix.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有用户、角色、权限和权限日志只读验证。
- `E2-sensitive-field-trim.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有权限、敏感字段结构和 9409 历史数据只读验证。
- `E3-permission-denied.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有权限、拒绝日志和 9409 历史数据只读验证。
- `E4-object-traceability.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有客户、仓库、订单、财务追溯字段和 9409 历史数据只读验证。
- `E5-audit-log-verification.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 AuditLog 结构、历史 action 和 9409 文本命中只读验证。
- `F1-agent-bill-import.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 PaymentVoucher 结构、数量和日志计数只读验证。
- `F2-agent-bill-payable-match.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 PaymentVoucher、PayablePaymentApplication、PaymentApplication、ShipmentFinanceItem 结构、数量和日志计数只读验证。
- `F3-difference-handling.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 PaymentVoucher 结构、数量和差异日志计数只读验证。
- `F4-extra-fee-attribution.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 PaymentVoucher、ShipmentFinanceItem 业务成本结构、数量和日志计数只读验证。
- `F5-kuayue-bill-manual-entry.md`：本地已完成复用审计和最小补差；本轮未同步 47，47 仅做现有 PaymentVoucher 结构、数量和跨越账单日志计数只读验证。
- `F6-bill-fee-audit.md`：本地已完成 F 批总销账复用审计和最小补差；本轮未同步 47，47 仅做现有 PaymentVoucher 结构、数量和 F 批账单日志计数只读验证。

## 未完成任务卡

无；A1-F6 细卡本地队列已完成。

## 当前样本对象状态

| 对象 | 样本值 | 当前状态 |
| --- | --- | --- |
| 客户编号 | `9409` | 第一阶段固定主识别键。 |
| 客户名称 | `Daloday` | A1 已验证客户主档、结算信息和收货人字段。 |
| 业务员 | `operator` / `R-sales` | A1 已验证业务员只能读取自己客户资料。 |
| 代理 | `宇环` / `深圳宇环` | A2 已验证代理主档字段和银行账号只读挂载。 |
| 代理渠道 | `AGCH-UPS-EXP` / `M7 代理渠道` | A3 已验证复用 `AgentChannel`、代理渠道接口、页面和启停日志；47 真实库当前 `AgentChannel=0`，待受控样本复验。 |
| 公司渠道 | `COCH-US-UPS-EXP` / `M7 Channel` | A3 已验证复用 `Channel` 字段、公司渠道接口、页面和启停日志；47 真实库当前 `Channel=10`。 |
| 渠道类别 | `UPS` / `美西卡车` | A4 已验证复用 `ChannelCategory`、渠道类别接口、页面和启停日志；47 真实库当前 `ChannelCategory=6`。 |
| 偏远/附加费 | `UPS偏远` / `偏远附加费` | A4 已验证复用 `Channel.remoteAreaRule` 和 `Surcharge` 表达第一阶段偏远规则入口；未新增国家/邮编级偏远表。 |
| 汇率 | `USD/RMB` / `EUR/RMB` | A4 已验证复用 `ExchangeRate` 历史生效日期口径和新增日志；47 真实库当前 `ExchangeRate=1`。 |
| 站点 | `深圳站` | 用于仓库和站点字段裁剪验收。 |
| 用户组/权限 | `ADMIN/CUSTOMER_SERVICE/OPERATOR/WAREHOUSE/FINANCE/CUSTOMER` | A5 已验证复用现有 RBAC、角色权限矩阵和 `system.role_permissions.update` 日志。 |
| 用户名 | `R-sales/R-warehouse/R-market/R-service/R-finance/R-admin` | A5 已验证六类验收账号存在；本地测试新增 `a5staff` 验证员工账号、站点归属和重置密码日志。 |
| 操作日志 | `AuditLog` / `操作日志` | A6 已验证复用现有审计表、系统审计接口和系统设置页；本地测试覆盖动作、操作人、对象、时间、before/after 和非管理员 403。 |
| 组合号 | `9409-KY-STOCK-075` | B1-B7 已完成本地仓库批总销账；后续 C1 从仓库货物进入录单/订单入口。 |
| 合票批次 | `9409-OUT001` | B5 已验证合票进入录单样本；B6 已验证交接单号派生和待出库页面出货动作；C1 已验证仓库货物可进入录单。 |
| 运单号 | `SY-9409-001` 或系统自动号 | C2 已验证待审核详情、费用摘要、审核/驳回/删除/恢复日志和未审核不能排货。 |
| 转单号 | `1Z9409001` | C6 已验证双审核完成后只能客服/管理员回填；日志记录填写人、填写时间、追踪网站和显示开关，业务员响应不返回追踪网站或面单 URL。 |
| 状态池/签收 | `WAITING_DEPARTURE -> DEPARTED -> ARRIVED_PORT -> DELIVERING -> SIGNED` | C7 已验证状态池推进日志记录状态时间和停留时长；问题件为挂载不移出原状态池；最终签收只能业务员确认。 |
| C 批订单闭环 | `SYFLOW001` / `1Z999` | C8 已验证同一票从仓库包裹录单到审核、排货、出库、客服双审核、转单号、状态池、问题件挂载、业务员签收；`AuditLog` 已支持按 `target` 过滤运单核心动作。 |
| 水单编号 | `WR-9409-001` / `SD20260625001` | D1-D2 已验证水单录入、确认到账、凭证上传、未到账不可匹配、同币种应收匹配、应收审核/反审核和客户账户余额变化；47 真实库当前 `WaterReceiptMatch=10`、`9409` 应收 3 条。 |
| 应收审核 | `SYRECV_AUTO_001` / `sfi-4` | D2 已验证应收审核后锁定，未到账水单不能匹配，匹配后不能反审核，撤销匹配后可反审核，日志显式记录状态前后、审核人、审核时间、付款编号、水单匹配状态。 |
| 业务成本 | `SYBIZCOST001` | D3 已验证业务成本费用 id、业务员、费用名称、计费重、单价、金额、审核人、审核时间和审核/反审核状态；业务员可看业务成本口径但看不到真实应付。 |
| 真实应付审核 | `SYPAYABLEAUDIT001` / `PAYABLE-001` | D4 已验证真实应付复用排货代理和渠道字段，计费重 20、单价 6、金额 120；审核后生成待付款，反审核后待付款失效；业务员不能访问应付、待付款和已付款。 |
| 付款申请 | `PA-9409-001` / `FKSQ202606280001` | D5 已验证待付款按代理、银行账号、币种分组生成付款申请；日志记录付款申请号、代理、银行账号、币种、金额、关联应付、申请人、申请时间和状态；47 真实库当前无 9409 付款申请样本。 |
| 已付款凭证 | `paid-receipt.png` / `paid-receipt-extra.png` | D6 已验证付款凭证挂在付款申请记录，已付款可反查付款申请、应付、运单和水单；日志记录付款时间、付款人、付款金额、付款对象、凭证、核销状态和归档时间；47 真实库当前无 9409 已付款样本。 |
| D 批财务闭环 | `SYFLOW001` / `SYPAYABLEAUDIT001` | D7 已验证水单到账、应收匹配、应收审核、业务成本口径、真实应付审核、待付款、付款申请、已付款和凭证日志可串联；47 真实库当前 `9409` 有应收/应付历史但无已付款受控样本。 |
| 角色读取矩阵 | `admin/finance/operator/warehouse/service/R-market` | E1 已验证同一票在管理员、财务、业务员、仓库、客服、市场账号下的读取/拒绝边界；47 真实库当前只有基础账号，未创建 `R-*` 受控角色样本。 |
| 敏感字段裁剪 | `代理运费` / `FLOW-PAYER-001` / `RECEIVABLE_BUSINESS` | E2 已验证业务员/客服响应不含真实应付、付款账号、代理银行和利润；仓库/业务员订单列表不返回付款金额字段；47 真实库当前 OPERATOR 仍有利润权限，需后续发布/权限调整后再做线上受控复验。 |
| 越权拒绝矩阵 | `operator/warehouse/service/finance/R-market` | E3 已验证业务员读待付款/已付款、仓库读财务、市场确认付款、客服跳过双审核、财务出库/签收、财务改基础资料均被后端拒绝并写日志；47 真实库当前无 `security.permission.denied`/`workflow.guard_denied` 历史样本。 |
| 对象追溯链 | `9409` / `SYFLOW001` / `FLOW-WATER-001` / `FLOW-PAYER-001` | E4 已验证客户、组合号、仓库包裹、运单、水单匹配、待付款、付款申请、已付款和审计 target 可互相反查；47 真实库当前 9409 有客户/订单/费用/水单历史，但无仓库包裹、水单匹配或付款申请受控样本。 |
| 关键动作日志 | `AuditLog` / `SYFLOW001` / `payment-app-1` | E5 已验证基础资料、仓库、订单客服、财务和权限日志查询均包含 actor、target、createdAt、result 和必要 before/after；47 真实库当前 `AuditLog=394`、核心字段非空为 0，但缺少 E5 线上受控全链路样本。 |
| 代理账单导入 | `AB-9409-001` / `AB-9409-WEB` | F1 已验证复用 `PaymentVoucher` 作为第一阶段代理账单人工导入事实源，记录账单号、代理、账单日期、币种、金额、明细文件/图片、导入人、导入时间和 `IMPORTED` 状态；47 真实库当前仍为旧 `PaymentVoucher` 结构，无 F1 新字段和无 9409 代理账单受控样本。 |
| 代理账单核对应付/付款 | `AB-9409-001` / `AB-9409-USD` | F2 已验证复用 `PaymentVoucher`、`PayablePaymentApplication`、`PaymentApplication`、`ShipmentFinanceItem` 和 `Shipment` 链路；账单可反查运单号、转单号、代理渠道、计费重、单价、金额、币种、应付费用 id、付款申请号和已付款记录；47 真实库当前仍为旧 `PaymentVoucher` 结构，无 F2 新字段和无 9409 代理账单核对受控样本。 |
| 代理账单差异 | `AB-9409-001` / `重量差异` | F3 已验证复用 `PaymentVoucher` 记录差异类型、差异金额、差异原因、处理状态、处理人和处理时间；差异标记/处理写入 `finance.payment_voucher.difference.mark/handle` 日志；47 真实库当前仍为旧 `PaymentVoucher` 结构，无 F3 差异字段和无 9409 差异受控样本。 |
| 杂费归属 | `AB-9409-EXTRA` / `杂费-操作费` | F4 已验证复用 `PaymentVoucher` 记录杂费类型、金额、币种、代理、客户、订单、发生日期、凭证和备注，并关联现有 `ShipmentFinanceItem(BUSINESS_COST)` 费用 id；47 真实库当前仍为旧 `PaymentVoucher` 结构，无 F4 杂费字段和无 9409 代理账单杂费受控样本。 |
| 跨越账单入口 | `KY-9409-001` / `跨越速运` | F5 已验证复用 `PaymentVoucher` 记录跨越账单号、客户、订单、金额、币种、账单日期、状态和文件，并继续关联既有待付款/应付链路；47 真实库当前仍为旧 `PaymentVoucher` 结构，无 F5 跨越字段和无 9409 跨越账单受控样本。 |
| F 批账单总销账 | `AB-9409-001` / `KY-9409-001` | F6 已验证账单导入、应付/付款关联、差异标记/处理、杂费归属、跨越账单入口、归档/反归档和账单日志均复用 `PaymentVoucher` 与 `AuditLog`；47 真实库当前仍为旧 `PaymentVoucher` 结构，无 F6 归档状态和无 9409 账单受控样本。 |
