# 代码瘦身治理第八十八阶段

- 状态：`completed`
- 会话标题：`Sunny｜代码瘦身治理｜88`
- 续接自：`docs/dev-now/codebase-slimming-phase-87.md`
- 上下文状态：`green`
- 输入来源：用户明确要求落地仓库首屏减载，成功路径不再请求全量包裹
- 会话 slug：`codebase-slimming-phase-88`
- 分支：`codex/codebase-slimming-phase-1`
- worktree：`/Users/j1ng/Tools/sunny/.worktrees/codebase-slimming-phase-1`
- 认领时间：`2026-07-27 Asia/Shanghai`

## 输入摘要

- 目标：仓库首屏只使用今日收货、在仓、理货归档和理货任务等现有窄查询；站点和合票工作集从这些结果派生；旧 `/warehouse/packages` 仅保留为窄查询失败时的兼容回退。
- 固定样本：仓库角色同时拥有今日收货和在仓查看权限，两个窄查询成功时全量包裹客户端调用次数必须为0；两个窄查询同时失败时共享回退只能调用全量包裹一次。
- 硬边界：API路径、HTTP方法、参数、状态码、错误文案、返回字段、RBAC、数据范围、字段裁剪、数据库、写入、状态流转、审计、页面入口、按钮、筛选、表格字段和提交载荷不变。

## 修改

- `apps/web/src/modules/warehouse/WarehousePage.tsx`
- `apps/web/src/modules/warehouse/WarehousePage.loading.test.tsx`
- `docs/dev-now/codebase-slimming-phase-88.md`
- `.codex-state.md`

## 当前进度

- 删除仓库工作区成功路径中的全量 `warehousePackages()` 首屏 effect；今日收货、在仓和近期理货归档成功结果按包裹ID合并为页面工作快照。
- 全量查询客户端和服务端 `GET /warehouse/packages` 均保留；仅当任一窄查询失败时延迟调用，并以 Promise 缓存保证并发失败只发起一次兼容请求；全量接口自身失败时继续使用原本地 fixture 回退。
- 47现行工作区按页签加载和15秒缓存能力完整保留；合票页现在加载现有在仓查询，站点选项、可合票包裹、选中包裹、详情和客户到仓进度继续由合并后的窄查询工作快照承接。
- 本地生产源码增加55行、删除32行，净增加23行；新增93行定向测试。本阶段以消除重复网络/服务端查询、浏览器映射和内存占用为收益，不计作代码行减量。

## 验证

- Web安全runner定向组件测试2/2通过：窄查询成功时全量包裹调用0次；两个窄查询同时失败时兼容全量调用1次。
- `git diff --check`通过；新增测试文件ESLint通过。`WarehousePage.tsx`仍有变更前已存在的 `Window is not defined` 单个ESLint基线问题；Web全量typecheck仍被财务响应夹具和仓库测试夹具共6个既有错误阻断，目标文件无新增错误。
- 既有仓库整页测试曾在120秒无结果后按安全runner停止，不记为通过；后续以直接组件固定样本替代，最终2/2通过且无测试残留进程。
- 47发布前只读样本：全量包裹1539行、1,632,780 bytes原始JSON、单次604.9ms；在仓查询1512行、1,599,040 bytes、单次466.6ms。成功首屏现在由代码与组件测试证明不会再叠加全量包裹请求，因此消除一份近乎重复的数据读取与传输；单次耗时不等同P95，不外推比例。
- 候选基于47当前203,209 bytes源码在全局发布锁内生成，保留线上独有的页签按需加载、15秒缓存、仓租、重复理货和矩阵表格能力；候选净增加18行，最终SHA-256为 `6cce4a4db2ac4204edb5ce1df5e16cb6f454c82058d619192c786593f920cebe`。
- 通过 checksum 条件白名单发布，只构建/重启Web，无API构建和数据库迁移；47 production build通过，线上源码只有1处 `warehousePackages()` 且位于延迟回退函数，旧首屏触发标记为0，合票页在仓查询标记存在。
- Web容器、公网首页和公网API health均为200，发布窗口实际 `ERROR|FATAL|Unhandled` 日志为0。
- 严格47漂移审计通过：`55 changed + 46 remote-only`，生产树备份/临时遗留物数量和字节数均为0。

## 交接

- 阻塞：无。
- 发布状态：`已发布47`；Web新镜像已运行，无迁移。
- 未验证项：未使用真实浏览器网络面板复核；成功路径的0次全量请求由直接组件测试和线上源码指纹证明。合并快照不再包含历史已出库包裹，跨历史包裹的客户到仓进度口径需在后续聚合接口阶段单独验证。
- 准确下一步：当前47在仓查询本身仍返回1512行、约1.60MB原始JSON、单次466.6ms；下一阶段优先把在仓首屏改为服务端分页/窄字段查询，并为站点选项和汇总提供独立聚合，继续保持现有API兼容入口和业务口径。
