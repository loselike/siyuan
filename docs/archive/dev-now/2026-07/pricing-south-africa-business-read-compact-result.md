# 南非查价：业务员全量规则与紧凑结果

## 本轮范围

- 业务员及既有业务用户组可只读查看完整南非物料规则表；新增、修改、启停、删除仍仅限维护权限。
- 南非查价结果压缩为总价、命中规则、计费口径和备注的一张摘要卡；报价文案改为按需展开，复制操作保留在卡片右上角。

## 实施与验证

- `rbac.ts` 为 OPERATOR 默认权限增加 `pricing:south-africa:rules-read`。
- Prisma migration 为现有业务用户组补齐同一只读权限，并留下审计记录。
- 已通过 API 南非权限回归、RBAC 单测、前后端 TypeScript 类型检查和 `git diff --check`。前端南非查价用例已覆盖业务员全量规则、无维护操作、按需展开报价文案。
- 已于 2026-07-16 发布到 47：`db-migrate` 成功应用只读权限迁移，API/Web 均已重启并通过 HTTP 200 健康检查；47 的 `operator` 账号属于 `UG_BUSINESS`，已验证在授权用户组内。
