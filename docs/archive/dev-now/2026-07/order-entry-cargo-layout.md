# 录单货物数据排版优化

## 输入

- 用户授权特例执行：录单页面货物数据区域排版优化。

## 允许修改

- `apps/web/src/modules/finance/entry/FinanceEntryPage.tsx`
- `apps/web/src/styles.css`
- 录单相关前端测试。

## 不做

- 不改自动匹配、公司渠道重算、计费重、草稿或提交审核逻辑；不发布 47。

## 完成情况

- 货物数据改为独立工具条与四等分字段网格，重算按钮不再挤占输入框宽度。
- 重量/体积单位固定在输入框右侧；1180px 以下两列、680px 以下单列。
- 自动匹配、重算、草稿保存和提交审核沿用原表单字段及回调。
