# 财务审核选择入口与筛选区紧凑化

## 本轮范围

- 应收审核、业务成本审核、市场应付审核移除顶部“全选本页/取消全选”。
- 保留 `ManagedTable` 表头选择列的当前页全选，并保留已选数量、禁用行和批量确认。
- 筛选区改为常用条件 + 可展开的更多筛选，保留全部既有筛选字段。
- 批量操作部分失败时展示接口返回的失败原因。

## 边界

- 不改审核、反审核、删除/作废、权限、接口或财务口径。
- 不发布 47，不提交 Git。

## 验证

- 通过：财务应收选择、筛选折叠、批量确认和市场应付定向测试。
- 通过：`npm run typecheck -w @siyuan/web`、`git diff --check`。
