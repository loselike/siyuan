# 代理资料账期（2026-07-17）

- 状态：`completed`
- 输入来源：`用户当前会话明确请求`
- 分支：`codex/agent-settlement-cycle`

## 目标

- 在代理资料新增可选的代理账期，支持周结、月结、单票结算。
- 写入数据库、API 返回与审计记录，旧代理保持未设置。

## 完成内容

- 代理资料新增“代理账期”下拉：周结、月结、单票结算；创建、编辑和列表展示均已接入。
- 新建/编辑代理弹窗改为“代理简称 + 代理账期”同排，代理全称独占下一行，消除顶部无意义空白。
- 仓库地址和收款银行账户改为按需维护：默认各展示 1 条，点击 `+` 才新增，最多 3 条；删除仓库地址直接从表单移除，删除已有收款账户会在保存时停用，保留历史付款关联。
- Agent 数据模型、共享 DTO、内存仓储和 Prisma 仓储均已持久化该字段；非法值会被后端拒绝。
- 新增 Prisma migration：`20260717113000_agent_settlement_cycle`；已有代理默认保持未设置。

## 验证

- `npm run typecheck` 通过。
- `npm run build` 通过。
- 本轮补充通过：`npm run typecheck -w @siyuan/web`、`npm run build -w @siyuan/web`、`git diff --check`。
- API 定向 E2E：创建、修改和非法账期拒绝通过。
- 前端定向测试在 Vitest 启动后异常长跑，已按安全规则停止；生产构建通过。
