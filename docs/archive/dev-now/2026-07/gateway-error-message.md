# 网关错误提示修复

状态：已发布 47。

2026-07-17：将全局 API 与通知条对 502/504、Nginx HTML 错误的提示统一改为“服务暂不可用，请稍后重试”。报价页面自己的超时逻辑保持“查价超时”，避免业务、财务等非报价模块出现错误文案。新增 `apiClient` 回归测试；Web 定向测试与类型检查通过。47 仅修改两处运行时文案并重建/重启 `web`，未运行迁移、未重启 API；容器与公网首页、API 健康检查均返回 200，发布产物包含新提示。
