# 客服转单号批量录入与权限

- 新增转单号查看、单票/批量填写、字段可见和数据范围权限，客服默认具备完整授权，其他用户组可在系统权限分配中配置。
- 新增受控转单号列表与批量填写接口：仅 `OUTBOUNDED`、无转单号、业务/代理数据均审核通过的订单进入；填写后原子流转到 `WAITING_DEPARTURE`。
- 前端转单号页改为固定选择列、受控字段表格和单票/批量录入弹窗；批量失败逐票返回原因，推送勾选只记录 `PENDING` 审计快照。
- 已通过 Shared/API/Web 类型检查与 `git diff --check`；尚未运行定向 E2E/UI 测试。
