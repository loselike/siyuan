# 47 快速发布规则

- 状态：`complete`
- 输入来源：当前会话明确请求
- 会话 slug：`release-speed-rules`

## 完成内容

- 在 `AGENTS.md` 增加 47 快速发布路径，将低风险局部 Web/API 与迁移、权限、财务、状态机等高风险发布分流。
- 低风险发布不再扫描全部脏工作树、不启动审查 Agent、不跑浏览器或整页慢测试、不重复本地和远端 production build。
- 最小闭环固定为目标文件差异、一次类型/定向验证、精确同步、仅构建受影响服务、容器/公网健康和错误日志。
- 目标文件夹带其他会话改动时继续以 47 为基线生成白名单补丁，避免用速度换取误发布风险。

## 验证

- `git diff --check` 通过。
- 本轮只修改发布规则和状态文档，不发布 47，不运行应用构建或测试。
