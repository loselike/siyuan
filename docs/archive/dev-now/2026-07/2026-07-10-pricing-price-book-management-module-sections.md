# 价格表管理按查价模块分区

- 状态：`complete`
- 任务卡：`2026-07-10-报价查价-价格表管理按查价模块分区`
- worktree：`/Users/j1ng/Tools/sunny`

## 范围

- 价格表管理固定七模块分区、同模块列表/明细过滤、当前模块导入锁定。
- 价格表代理、状态、关键词最小筛选和当前模块体检上下文。

## 不做

- 不改解析、报价计算、业务员查价来源裁剪或 47 发布。

## 验证

- 通过：任务卡指定的报价 Web/API 定向测试、Shared 构建、Web/API 类型检查与 `git diff --check`。

## 完成内容

- 价格表管理固定为七个查价模块分区，进入时默认亚马逊分区；分区切换只请求对应 `targetModule` 的价格表。
- 当前分区支持代理简称和价格表名称/备注筛选；导入模块由当前分区自动带入并锁定。
- API 的价格表列表、线路明细查询均新增并强制使用 `targetModule` 过滤；跨模块价格表不返回。
- 体检请求使用当前管理分区的模块上下文；切换有加载态并在八秒后给出超时提示。

## 风险

- 价格表当前仅有删除能力，尚不存在独立的停用状态字段；本卡未新增 Prisma 状态迁移，因此管理筛选暂限当前有效价格表。
- 未发布 47。
