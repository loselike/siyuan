# 驰汉价格表规则同步失败修复

- 状态：已本地修复并验证，待用户授权发布 47。
- 根因：驰汉欧洲表将不包税、包税两组 KG 档放在同一表头。旧解析在不包税 `301KG+` 后错误取到包税 `5KG+` 作为下一档，生成 `minWeightKg=301`、`maxWeightKg=4.999`，规则同步的完整性校验因此拒绝第 24 条解析行。
- 修复：只有下一档重量确实更大时才封顶开放档；欧洲空海运铁路快递规则版本提升至 v8，以便线上自动重新同步历史原文件。
- 验证：报价 API 驰汉定向测试通过；API 类型检查通过；从 47 下载的原始 `7.8-驰汉.xlsx` 复跑解析 456 条，完整性校验不合格行从 1 条降为 0 条，所有 `301KG+` 均为 `301-99999KG`。
