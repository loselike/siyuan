# 应收审核是水单分配的唯一落账入口

财务只在“应收审核”按应收费用审核并落账水单分配，“水单匹配”仅负责业务分配、财务代匹配、待审核维护和关系查询。每条费用允许分期收款并多次审核，每次审核和反审核只影响本次分配；反审核只冲回正式账务，并将该分配恢复为待审核、继续占用同额水单容量，审核和反审核过程保留在审计日志。这样牺牲整单批次操作的便捷性，换取费用、收款人与水单之间可独立追溯且可直接纠错的资金事实。
