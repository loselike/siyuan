# 角色权限分配整体排版优化

- 状态：`complete`
- 范围：系统管理 / 角色权限分配；仅前端布局和测试，未改 RBAC、接口或权限 code。

## 已完成

- 权限配置区从两列表格改为响应式双列功能点清单；窄屏自动降为单列，功能点左侧、允许勾选右侧。
- 移除该清单的表格列设置入口，不再产生表头或空白列；全选、清空、保存继续保留在目录标题右侧。
- 四栏工作台在宽屏使用角色、模块、主配置、概览的固定/自适应布局；1540px 以下将概览压缩到横向摘要，避免 1366px 横向溢出。
- 角色概览统计改为紧凑卡片组；所有 10 个一级模块复用同一功能点清单。
- 测试覆盖所有一级模块切换、无“权限项”列/权限 code/列设置按钮，以及原保存流程。

## 验证

- `npm test -w @siyuan/web -- --run src/modules/settings/settings.test.tsx -t "角色权限|权限模块|功能权限|列设置|全选当前目录|清空当前目录|保存权限"`：1 passed。
- `npm run typecheck -w @siyuan/web`：通过。
- `git diff --check`：通过。

## 边界

- 未发布 47；当前工作树包含其他会话改动，未 stage 或 commit。
