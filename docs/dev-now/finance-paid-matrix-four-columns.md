# 财务已付款矩阵四列均分

## 用户验收目标

- 页面：财务管理 -> 已付款 -> 矩阵视图。
- 除固定选择列外，“付款与代理 / 运单与费用 / 凭证与银行 / 操作”四个业务分组等宽分配表格空间。
- 缩小原“运单与费用”和“操作”区域的无效留白；操作按钮、字段、权限、筛选、详情、列设置和财务状态流转保持不变。

## 固定样本

- 已支付付款申请 1 条，操作区同时显示“反核销”和“补充”。
- 四个矩阵表头对应的列宽均为 280px 基准，在固定布局下按可用宽度同比例扩展。

## 改动范围

- `apps/web/src/modules/finance/paidPayment/PaidPaymentPage.tsx`
- 定向测试文件（若新增）

## 禁止范围

- 不改 API、Shared、Repository、数据库、金额口径、权限或付款状态流转。
- 不改精密台账模式，不改全局矩阵样式。

## 验证结果

- 定向组件测试 1/1 通过：四个矩阵业务列的基准宽度均为 280px，且“反核销 / 补充”操作保留。
- Web TypeScript 类型检查通过。
- `git diff --check` 通过。

## 发布状态

- `local_complete_not_published`
- 用户于 2026-08-06 明确要求停止本会话发布；47 目标文件 checksum 仍为发布前的 `0e1fca2432e42631ef4f16391fb4140a6d15c6e7ecd8577582177b934c5fb969`，本任务候选未写入 47。
- 其他会话的发布不属于本任务，本会话不再占用或操作 47 发布队列。
