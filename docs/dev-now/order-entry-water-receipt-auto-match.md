# 录单水单后置匹配与自动关联

- 状态：`completed`
- 输入来源：`当前会话任务卡`
- 会话 slug：`order-entry-water-receipt-auto-match`
- 分支：`当前共享工作区`
- worktree：`/Users/j1ng/Tools/sunny`
- 完成时间：`2026-07-12 Asia/Shanghai`

## 完成内容

- 录单水单保持可空，未选择时显示未匹配且不影响草稿、提交审核。
- 水单到账、已到账水单调整、撤销匹配后自动扫描未匹配应收；仅在客户、币种、余额均符合且候选唯一时自动关联。
- 多候选或余额不足不强行匹配，向应收费用保存可读提示；手动匹配写为手动来源，自动匹配不会覆盖既有匹配。
- 自动匹配同步水单余额、客户余额、应收状态和审计日志；水单匹配页展示自动/手动来源。

## 验证

- API：后置水单到账自动匹配定向 E2E 通过。
- Web：录单费用行定向测试通过。
- Shared build、API/Web typecheck、`git diff --check` 通过。

## 交接

- 未发布 47。
- 全量财务 E2E 仍有 4 个已有串行依赖失败；其中一条客户余额断言会因本任务新增自动匹配而改变，需在合并时将旧“未自动扣减”断言更新为新口径。
