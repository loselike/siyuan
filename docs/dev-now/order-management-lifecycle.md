# 运单管理全生命周期视图

状态：本地完成，未发布 47。

- Prisma 运单列表与详情的数据范围统一为：业务员本人录入（`entryBy`）或客户归属该业务员的全部未删除运单。
- 运单管理筛选由审核维度改为：全部、审核处理、仓内待出、运输中、问题件、已完成。
- 表格固定补充当前生命周期节点与最新轨迹；审核状态仍作为独立字段保留。
- 业务员数据范围与敏感字段裁剪保持原有后端控制；无需数据库迁移。
- 验证：API/Web 类型检查通过；API 运单范围定向 E2E 通过。Web Vitest 定向执行在当前工作区启动后未返回用例结果，未将其计为通过。
