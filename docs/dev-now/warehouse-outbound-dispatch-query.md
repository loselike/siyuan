# 仓库出货待出库专用查询

- 状态：已完成本地实现；未发布 47，未提交 Git。
- 范围：仓库出货组不再调用要求 `business:shipment:list` 的全量运单接口；新增只返回待出库及已出库订单的仓库专用接口，并按待出库/已出库权限分别裁剪状态范围。
- 验证：仓库出库定向 API E2E、API/Web 类型检查、API 构建和 `git diff --check` 已通过。
- 注意：未给仓库组追加业务运单列表权限，仍不开放业务管理全量运单数据。

## 在仓数据录单跳转

- 状态：已完成本地实现；未发布 47，未提交 Git。
- 范围：单票录单和批量录单改为通过统一路由跳转至 `/app/business/finance-entry`，不再仅修改菜单状态后被旧仓库路由回滚；仓库录单入口同时要求“进入录单页面、新建录单、选择在仓货物”三项权限。
- 验证：仓库单票/批量录单定向 Web 测试、Web 类型检查和 `git diff --check` 已通过。

## 完成态通知统一

- 状态：已完成本地实现；未发布 47，未提交 Git。
- 范围：公共通知条将“已新建/已保存/已完成/成功”等完成态优先展示为成功；带后续说明（如“首次登录必须修改密码”）不再误报红色。完成中含部分失败展示黄色关注提示，真实阻断或失败仍为红色。
- 验证：完成态定向测试、Web 类型检查及目标文件 `git diff --check` 已通过；完整 `ui.test.ts` 的既有日期控件异步清理会在 Vitest teardown 报 `window is not defined`，与本次纯文本分类断言无关。

## 录单客户名称资料库映射

- 状态：已完成本地实现；未发布 47，未提交 Git。
- 范围：录单页客户名称改为只读，由客户编号精确匹配客户资料自动带出；客户编号被清空或未匹配时同步清空名称，不再保留前一客户的旧名称；仓库货物回填也不再使用“仓库客户”兜底名称。
- 验证：Web 类型检查和目标文件 `git diff --check` 已通过。录单集成测试命令在 Vitest 启动后无输出且超时中止，未获得该定向测试结果。

## 录单代理字段权限裁剪

- 状态：已完成本地实现；未发布 47，未提交 Git。
- 范围：录单页“代理渠道”及费用表中的代理选择改为依据 `master-data:agents:read` 权限显示；业务员/业务组无此权限时不加载、不展示也不提交代理字段。API 同步裁剪非财务角色录单详情中业务成本的代理名称，避免仅隐藏前端仍可读取敏感数据。
- 验证：Web/API 类型检查、业务组录单定向 API E2E 和目标文件 `git diff --check` 已通过。
