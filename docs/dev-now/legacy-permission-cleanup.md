# 系统管理旧权限清理

- 状态：本地完成，未发布 47。
- 已完成：角色权限页删除“其他功能权限”回退区；报价查价作为独立一级模块；旧权限从运行时定义、默认角色、Guard 兼容、前端菜单/按钮判断和后端关键接口判断中移除；角色保存只接受新权限白名单。
- 数据：新增 `20260714170000_remove_legacy_permission_keys` migration，清理旧权限关联与定义并写入系统审计；审计历史文本不回写。
- 验证：API/Web typecheck、权限 API 定向测试、系统角色权限 UI 定向测试、`git diff --check` 通过。
- 风险：当前工作树已有其他会话的旧测试夹具和注释性历史矩阵，运行时不使用；待统一测试夹具重构时可再清除其文本残留。
- 补充：角色权限明细已移除“权限项”列；权限 code 继续仅作为表格 row key、勾选状态与保存请求内部数据，不再向配置人员渲染。
