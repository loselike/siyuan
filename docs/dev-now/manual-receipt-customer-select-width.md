# 手动收货客户选择框宽度

- 问题：手动添加收货抽屉中的客户编号 `AutoComplete` 没有明确宽度，选择客户后按内容宽度收缩，已选编号视觉上被裁掉。
- 实现：客户选择框增加全宽和最小宽度约束；回归断言同时校验选择后的客户编号与客户名称回填。
- 验证：`npm run typecheck -w @siyuan/web`、`git diff --check` 通过。仓库抽屉端到端定向用例在本机 Vitest worker 启动后未返回结果，已终止测试进程；未发布 47。
