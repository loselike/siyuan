# 2026-07-15 价格表规则自动同步

- 已实现按查价模块维护的解析规则版本；只需提升受影响模块的版本号。
- API 启动后串行扫描保留原始文件的旧价格表，按单表、分批、带间隔重建；不再要求管理员重新上传。
- 重建在事务中替换当前价格行与关联副本，成功后才更新规则版本；失败保留旧报价并标记失败。
- 无法安全关联历史原文件或历史副本的价格表标记为 `UNAVAILABLE`，不做猜测性刷新。
- 价格表管理新增当前查价模块的规则同步进度条：展示代码规则版本、已同步价格表数、等待/运行、失败或原文件不可用数量；存在待处理任务时每 2 秒刷新一次，完成后停止轮询。
- 发布后 API 启动会依据各查价模块的 `PRICING_PARSER_RULE_VERSIONS` 仅为版本落后的价格表创建持久化同步任务；后续修改某模块解析/匹配规则时只需提升该模块版本，不需要重新上传原表。
- 已获用户确认并发布到 47：只同步本功能涉及的 Shared/API/Web、Prisma schema 与迁移文件，未同步工作树中的其他改动。
- 47 已执行 `prisma migrate deploy`（迁移 `20260715193000_price_book_rule_refresh` 已应用），API/Web 均重建并重启；容器和公网健康检查均为 200。
- 首次启动已串行完成 6 个历史价格表规则刷新任务，所有任务 `SUCCESS`；当前 8 个价格表均为 `CURRENT`。启动期间发现并修复 `PriceBookImportJob.kind` 为非空字段却查询 `null` 的校验异常，修复后 API 保持稳定。
