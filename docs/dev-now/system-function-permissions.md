# 系统管理功能权限细分

- 状态：本地实现完成，未发布、未提交。
- 范围：系统管理 8 个真实二级目录的功能权限定义、权限分配页系统管理工作区、导航入口裁剪，以及用户组、账号、站点、审计、角色权限、链路追溯接口的细权限拦截。
- 审计：列表已保留 IP 地址列；详情仅以弹窗展示，变更前后和原始请求按细权限隐藏。
- 验证：系统管理定向 Web 测试命令无匹配用例而跳过；Web/API 类型检查均被其他会话的 AgentMarkup shared DTO 不一致阻断，新增系统权限相关文件未出现类型错误；`git diff --check` 通过。
- 注意：历史自定义角色需由超级管理员在系统管理权限矩阵中重新勾选新权限，旧 `system:manage` 不再放行系统接口。
