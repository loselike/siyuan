# 区分内部轨迹与外部物流轨迹

- 状态：`completed`
- 输入来源：`无（当前会话明确请求）`
- 会话 slug：`separate-internal-logistics-tracking`
- 分支：`当前共享工作区`
- worktree：`/Users/j1ng/Tools/sunny`
- 认领时间：`2026-07-12 Asia/Shanghai`

## 输入摘要

- 目标：运单详情、仓库可进入的通用运单详情和待审核详情将内部生命周期与外部物流轨迹分成独立标签。
- 不做：不发布到 47；不改既有财务口径。

## 当前进度

- `TrackingEvent` 增加外部物流来源、承运商、地点、转单号、原始内容和分类；历史内部节点迁移为 `INTERNAL`，详情查询只读取 `LOGISTICS`。
- 录单、审核、面单等内部流程不再写物流轨迹；内部状态事件进入内部轨迹。
- 后台运单详情和待审核详情新增“内部轨迹 / 物流轨迹”标签；仓库角色不加载费用详情，内部轨迹屏蔽财务敏感内容。

## 验证

- 通过：Shared/API/Web typecheck、`git diff --check`。
- 已知基线失败：仓库/订单全量定向 E2E 有 3 项与当前共享工作区既有主数据/权限状态有关；物流导入 UI 用例在现有 FileReader 预览阶段超时。

## 交接

- 阻塞：无
- 剩余风险：上线前需在迁移后的真实数据上抽样核对历史内部轨迹分类正则；未发布 47。
