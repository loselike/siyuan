# 拓普达加拿大区域价格解析

- 状态：`completed`
- 输入来源：`当前会话明确请求`
- 会话 slug：`pricing-topuda-canada-regional-parser`
- 分支：`共享工作区，未切换分支`
- worktree：`/Users/j1ng/Tools/sunny`
- 完成时间：`2026-07-15 Asia/Shanghai`

## 输入摘要

- 目标：拓普达加拿大价格表中的渠道层级、国家分区和仓库分区分别解析；不能将仓库分区混入渠道或目的地。
- 口径：例如 `TPD-加拿大直航经济线-TPD-加拿大直航海卡经济`，目的地为 `加拿大东部/加拿大西部`，仓库编码仅用于仓库匹配。

## 修改与验证

- 导入作业将所选代理简称传入解析器；选择“拓普达”后，优先且仅使用拓普达国家分区/仓库分区阶梯表规则，不依赖文件名或查价模块。
- 新增国家分区/仓库分区阶梯表解析，保留原表下单渠道为内部业务线路，并生成对外可读渠道层级。
- 该类区域分表命中后，不再重复解析同一工作簿的“仓库渠道汇总表”。
- 通过：加拿大 YYZ 回归、拓普达渠道层级回归、API 类型检查、API 构建、`git diff --check`。
- 使用原始 `/Users/j1ng/Downloads/7.6-拓普达.xlsx` 本地复跑：以任意文件名、`amazon` 目标模块和所选代理“拓普达”导入，仍仅输出三个加拿大区域分表共 48 条，目的地区域均正确；经济线的 21KG+ 区间为 `21-99.999KG`，100KG+ 为无上限。

## 交接

- 未发布 47；需要发布后触发价格表规则重建，已导入的拓普达价格表才会更新为新解析结果。
