# 运营工作台渲染循环修复

- 目标：修复线上 `/app/workspace/shipment-pool` 的 React #185（最大更新深度）页面加载失败。
- 根因：受控表格分页回传相同页码时，仍创建新的运单池查询状态；请求完成后的重渲染会再次触发表格回传，形成循环。
- 修改：运营页查询状态和通用 `ManagedTable` 分页状态均改为同值不更新；相同分页事件不再回调外层查询。
- 验证：Web TypeScript 检查、生产构建通过；待发布到 47 后用专线运单池复验。
