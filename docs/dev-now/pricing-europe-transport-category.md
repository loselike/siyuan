# 2026-07-15 欧洲空海铁归类，取消快递筛选

- 输入：`2026-07-15-报价查价-取消快递分类并归并为空海铁`。
- 已完成：欧洲空海运铁路快递查询的渠道筛选固定为“全部渠道、空运、海运、铁路”，前端不再提供“快递”。后端统一按工作表、价格组/展示渠道、原始真实线路归类 `AIR / SEA / RAIL / UNCLASSIFIED`；不读取备注、渠道要求、赔付或时效文本。
- 已完成：仅含“快递”且无空海铁证据的线路不会进入欧洲业务报价；旧客户端直传“快递”筛选会明确拒绝。导入任务会输出空运/海运/铁路/待归类统计，并将待归类行写入管理员导入体检提示和审计。
- 验证：API 定向覆盖海运/空运/铁路历史快递线路、全部渠道排除待归类、旧快递筛选拒绝和业务员成本裁剪；Web 定向覆盖默认全部渠道、无必填错误和无快递选项。Shared/API/Web typecheck、`git diff --check` 通过。
- 已知：完整 API pricing 文件仍有 11 个共享工作树中的既有失败（默认加价、原有时效期望、美国邮编/仓库夹具和上一轮欧洲展示期望）；与本任务直接相关的 5 个既有欧洲用例及新增用例通过。
- 未发布 47。

## 2026-07-15 驰汉欧洲价格组与时效回归

- 已完成：欧洲水平价格表现在以“Sheet 表名 - 价格组标题”生成渠道展示。`7.8-驰汉.xlsx` 的英国海运行解析为 `英国海运 - UPS英国海运双清`，促销后缀 `-1:300减0.5` 与行级 `包税/不包税` 不再污染渠道标题；完整原始线路仍保存在 `realChannelName`，不改变费率和加价匹配。
- 已完成：保留价格组中本身有业务含义的“包税”，不会误删如“中欧海运快船包税”。同名 Sheet/价格组也按统一口径显示两层名称，例如 `欧洲海运电池快递专线 - 欧洲海运电池快递专线`；系统下单渠道、备注、包装要求不进入渠道展示。
- 已完成：驰汉英国海运时效从原始时效列稳定保留为 `正常开船后40-45天左右`（包含 `40-45天`），不再回退为“时效待确认”。
- 验证：以驰汉结构生成的回归样表覆盖渠道、促销清理、时效和电池专线展示；定向 API 3 项、API/Web typecheck、API build、真实 `/Users/j1ng/Downloads/7.8-驰汉.xlsx` 解析（84 行）和 `git diff --check` 通过。
- 未发布 47；已导入的历史价格行会在后续读路径按新展示规则清理，完整解析字段需要重新导入对应价格表才能回写。
