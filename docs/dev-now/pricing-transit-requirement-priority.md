# 报价查价：渠道要求中的自然日时效优先

- 状态：已完成并发布 47，未提交。
- 输入来源：当前会话明确请求。
- 范围：当航班说明含 `航程1-3天`、渠道要求含 `10个自然日内提取` 时，时效列只显示后者；渠道要求保留完整原文。
- 修改：时效从 `transitLabel`、`specialRemark`、`productSurchargeRemark` 全部提取候选，优先选择自然日提取承诺。
- 验证：美国原表实测 `美国十日提空派带电专线` 解析为 `10个自然日内提取`；API 定向 3 项测试、类型检查和 `git diff --check` 通过。
- 发布：仅同步 `apps/api/src/modules/pricing-excel.ts`，重建并重启 47 API，未运行 Prisma migration、未同步无关改动。
- 线上验证：ZIP `51500`、100KG 下 `美国十日提空派带电专线` 的时效为 `10个自然日内提取`，渠道要求原文仍保留；API、首页与公网健康检查正常。
