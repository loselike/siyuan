# 全局列表列设置入口移入表头右侧

- 状态：已完成（本地，未发布 47、未提交 Git）。
- `ManagedTable` 默认将列设置齿轮作为固定右侧表头列，不再渲染仅包含齿轮的顶部工具栏；保留 `toolbar` 参数兼容旧调用。
- 仓库与市场待排货/已排货显式调用已改用表头位置，其余 `ManagedTable` 调用自动继承默认值。
- 验证：共享表格、市场待排货与客服数据确认定向测试通过；Web typecheck 与 `git diff --check` 通过。
- 已知：仓库和客服完整测试仍有既有任务（字段口径、问题件/转单号命名）导致的失败，与本次列设置调整无关。
