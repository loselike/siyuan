# 运营工作台功能权限细分

- 状态：已完成（本地，未发布 47、未提交 Git）。
- 新增专线运单池、AI 优先队列、产品地图、导入质检四组功能权限，并将默认运营、客服、仓库、财务、客户角色按任务卡收紧。
- 运营专用列表、内部日志、状态修改、轨迹、问题件与导入接口已使用独立权限；未授权请求由 RBAC 拒绝并留下拒绝审计。运营 AI 场景也改为单独校验。
- 运营页面按权限隐藏二级入口和动作，后端裁掉未授权角色的应收、备注、代理信息；角色权限页改为角色 / 运营模块树 / 功能权限表 / 权限概览布局，未勾选即无权限。
- 验证：Web/API typecheck、运营 Web 定向测试、角色权限 Web 定向测试、运营 API 权限定向测试和 `git diff --check` 通过。
- 已知：完整 `settings.test.tsx` 仍有 3 个既有失败（代理全称旧断言、审计样例 IP、代理银行信息），与本任务无关。
