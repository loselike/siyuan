# 思远物流项目 Codex 开发规则

## Project

- 项目名称：思远物流 / Siyuan Logistics
- 项目路径：`/Users/j1ng/Tools/sunny`
- 项目类型：跨境物流 TMS/OMS 系统
- 代码结构：monorepo，主要包含 `apps/web`、`apps/api`、`packages/shared`

## Default Context

- 默认只读取本文件、`.codex-state.md`、`git status --short` 和相关代码。
- 不主动加载额外流程说明或通用指南；除非用户明确要求，或系统规则强制要求。
- 所有判断以当前仓库文件、命令输出、类型和测试为准，不依赖旧聊天历史。

## Mandatory Start Protocol

每次开发任务开始时先做这些事，不要依赖旧聊天历史：

1. 读取 `.codex-state.md`
2. 执行 `git status --short`
3. 用 `rg`、`rg --files`、`ls` 和 focused read 查看相关代码
4. 根据当前文件状态继续最近的未完成任务

如果 `.codex-state.md` 和实际代码冲突，以实际代码和命令输出为准，并更新 `.codex-state.md`。

## Execution Rules

- 不重复确认旧目标。
- 不反复规划。
- 普通实现细节自行决策，选择与现有代码风格一致的保守方案。
- 每轮必须推进实际闭环：读代码、改文件、跑测试、启动服务、接口验证、浏览器验证或更新状态。
- 上下文压缩或恢复后，只允许简短确认一次当前状态，然后继续执行。
- 每完成一个阶段，更新 `.codex-state.md`。
- 不要为了节省时间跳过验证；优先运行最小相关测试，再按风险扩大验证范围。

## Output Discipline

- DO NOT send optional commentary.
- 不要输出非必要说明、过程旁白或显而易见的下一步描述。
- 优先采取具体行动：查看文件、修改代码、运行命令、验证结果。
- 只有在任务有歧义、存在风险、遇到阻塞、需要用户决策时，才解释中间过程。
- 最终回复只总结：改了什么、验证了什么、还有什么风险或后续动作。

## Ponytail Development Discipline

本项目默认采用 Ponytail 风格作为常驻开发纪律：少写代码、少加依赖、少造抽象，优先交付最小正确闭环。用户不需要每次手动输入 `/ponytail`；只要当前任务位于 `/Users/j1ng/Tools/sunny`，就按本节执行。该纪律用于控制复杂度，不能覆盖安全、权限、财务一致性、数据完整性、测试验证和用户明确需求。

后续开发默认遵守：

- 先复用现有模块、类型、API、样式和测试，再考虑新增文件或新抽象。
- 优先使用 TypeScript/Node/浏览器平台能力和仓库已有依赖；新增依赖前必须说明为什么现有能力不足。
- 每次改动尽量靠近需求涉及的模块；禁止顺手重构无关代码、批量格式化无关文件或改动无关业务流程。
- 新抽象只有在减少真实重复、降低共享复杂度或符合现有模块边界时才允许添加。
- 能删掉旧分支、旧 fallback、重复 UI、重复映射或无用 helper 时，优先删除；但删除前必须确认没有测试、路由、页面或线上兼容依赖。
- 对临时简化方案，用 `ponytail:` 注释写明边界和后续触发条件，避免“以后再说”变成永久债务。

处理既有代码时遵守：

- 普通功能开发不触发旧代码审计：例如新增字段、修 bug、补页面、接接口，只按本次需求做最小相关改动。
- 只有用户明确要求重构、瘦身、整理旧代码、降低复杂度，或当前文件已经大到继续堆叠会明显增加风险时，才先审计再改动。
- 审计时先列出重复、过度封装、无用依赖、大文件堆积和可合并逻辑，不一次性全项目重构。
- 旧代码瘦身按小批次推进，每批必须有清晰验收点和最小验证命令。
- 业务大文件拆分只沿现有领域边界推进，例如 finance、warehouse、orders、routing；禁止为了“看起来更架构化”新增无业务价值的层。
- 如果 Ponytail 简化建议与本文件的模块交付、安全、权限、47 发布规则冲突，以本文件的业务和安全规则优先。

## Long Requirement Protocol

用户贴长 PRD、截图拆解、阶段计划或详细字段清单时，不要把整份文档当成一次性实现范围。先压缩成一张短执行卡片：

```md
目标：
本次阶段：
涉及模块：
必须做：
不要做：
验收标准：
最小验证：
```

处理规则：

- 如果文档包含多阶段计划，默认只执行第一阶段或用户明确指定的阶段。
- 只读取与本次阶段相关的代码；不要因为文档同时提到 API、数据库、权限、UI、发布就全部展开。
- 每次实现必须控制在一个可验证闭环，优先复用现有接口、类型、测试和页面结构。
- 大功能优先落地“展示/读取”或“单类 CRUD”，再做批量操作、权限矩阵、列设置、通知、导入导出等增强。
- 同一轮不要同时做多阶段功能、47 发布、巨型模块重构、数据库大迁移；除非用户明确要求。
- 如果用户说“按这份文档全部落地”，仍先切成阶段并完成最小可验收阶段，不一次性实现整份文档。

## 47 Deployment Rules

- 47 云服务器按纯 Docker Compose 发布处理；宿主机不要求安装或调用 `node`、`npm`、`npx`。
- 同步/发布到 47 时，按 `docs/47-cloud-docker-release.md` 执行：构建镜像、运行 `db-migrate`、重启 `api`/`web`、做健康检查。
- 线上数据库只允许执行 `prisma migrate deploy`；禁止 `prisma db push`、`prisma migrate reset`、`prisma:seed`、`demo:seed`。
- 线上密钥和 `.env` 只保存在服务器，不随代码同步、不写入日志、不提交 Git。
- 47 线上登录接口可能启用图片验证码；做接口验收、角色验收或数据验证时，不要反复用普通 `/api/auth/login` 碰验证码。优先使用已登录浏览器会话、容器内短期 JWT、服务端本地接口或只读数据库查询完成验证；生成临时 JWT 时只在 47 容器内读取 `JWT_SECRET`，禁止输出密钥或写入日志。

## Ask User Only When

只有这些情况需要暂停询问用户：

- 破坏性或不可逆操作，例如删除数据、重置数据库、覆盖大量文件。
- 涉及密钥、付款、线上发布、外部通知。
- 需求会明显改变业务流程、数据库结构或权限模型。
- 安全、合规或授权边界不清。
- 多个方案会造成明显不同的产品方向或迁移成本。

## Commands

常用命令：

```bash
npm run dev
npm run dev:api
npm run build
npm test
npm run test:web
npm run test:api
npm run lint
npm run typecheck
npm run sync:47
```

验证优先级：

1. 修改共享领域逻辑时，优先跑 `npm run test -w @siyuan/shared`
2. 修改 API 时，优先跑 API 相关测试、类型检查或局部请求验证
3. 修改前端时，优先跑 Web 测试，并启动页面做浏览器验证
4. 跨模块改动后，再跑根目录 `npm test`、`npm run typecheck` 或 `npm run build`

快速验证入口：

- Web 分域集成测试：`npm run test:web`
- API e2e 测试：`npm run test:api`
- 47 同步预演：`npm run sync:47`
- 47 真实同步：`npm run sync:47 -- --apply`

## Security Requirements

所有开发默认考虑：

- 鉴权和 RBAC 权限控制
- 输入校验和输出转义
- 防止越权访问、SQL 注入、XSS、SSRF、危险文件上传
- 错误处理不泄露敏感信息
- 审计日志覆盖关键业务动作
- 敏感信息不进入日志、前端包、Git 历史
- 财务、客户、订单、用户相关接口必须校验租户、角色和对象归属
- 菜单权限只决定是否能进入模块；数据范围和字段可见性必须以后端查询裁剪和序列化裁剪为准。开启仓库、财务或其他模块入口，不等于开放全量数据。
- 业务员、仓库、财务等内部角色默认只能看到自己职责范围内的数据；例如业务员只能看自己的单及自己单关联的业务成本，仓库不能看到财务敏感字段、利润、应付或付款信息。

## Product Priorities

当前系统开发以效率和业务闭环为主：

- 财务资料库必须支持真实增删改查，不做静态假页面。
- RMB 统一、币种自动识别、结算方式和费用计算属于跨前后端与数据库的核心能力。
- 订单、客户、财务、仓储、权限相关改动要保持 API、前端状态、数据库字段一致。
- 业务动作尽量可追溯，涉及财务和权限的变更要考虑审计。

## Module Delivery Checklist

新增或大改业务模块时，默认按这张清单交付，避免录单/待审核这类问题重复出现：

- 先确定模块边界：前端放对应 `apps/web/src/modules/<domain>`，API 放 `apps/api/src/modules`，共享类型放 `packages/shared`；禁止重新把页面逻辑堆回 `App.tsx`。
- 后端新功能必须优先落在领域子模块里，例如 `apps/api/src/modules/finance/*`、`shipments/*`、`warehouse/*`；禁止继续把新业务路由堆进 `data.controller.ts`，禁止把新业务数据库逻辑堆进 `PrismaRepository` 大仓储。Controller 只做路由/权限/输入输出，Service 做业务规则，Repository 做持久化。
- API 测试默认按领域拆分，例如 `app.finance.e2e.test.ts`、`app.warehouse.e2e.test.ts`、`app.orders.e2e.test.ts`、`app.pricing.e2e.test.ts`；公共启动、登录和数据构造放 `apps/api/src/modules/test-support`。新模块测试不要继续把完整生产逻辑复制到 `InMemoryRepository`，优先使用模块 fixture 或 Prisma 测试库；每迁出一个领域，就删除对应内存实现。
- 需求字段必须做“前端表单/展示、共享类型、Prisma 字段或持久化位置、API 输入输出、测试数据”五点对应；暂不落地的字段必须写明原因和后续阶段，例如水单余额匹配。
- 需求里的字段、流程逻辑、状态流转、权限裁剪、校验规则和日志要求默认都必须严格落地；只有现阶段确实依赖外部联动或后续基础能力的小功能，才允许标记为延期，并要写清楚“不落地原因、后续依赖、临时表现”。
- 后端必须成为业务事实来源：新增字段不能只停留在前端状态或测试桩，必须确认写入数据库或明确映射到现有持久化字段；跨模块数据要能被后续流程读取，例如录单提交后待审核、费用、仓库包裹、日志能互通互联。
- 开发前必须把 PRD 字段拆成验收矩阵，逐项标记“已落地 / 已映射 / 延期”；不能只按截图做相似 UI，费用、状态、只读/隐藏、默认值、自动计算和跨模块读取都必须能在代码或测试里找到对应实现。
- 每个新模块交付前必须输出一份简短“需求落地核对”：已落地字段、已落地流程、后端互通点、延期项和原因；发现缺项先补实现，再验收或发布。
- 新状态或流程节点必须同时补共享状态、后端状态流转、前端筛选/展示、日志记录和回归测试；不能长期复用旧状态绕过业务语义。
- 涉及审核、反审核、撤销、反核销的模块，必须检查下游流转状态：已收款、已申请付款、已付款、已归档等状态不能直接回退，必须先走对应撤销链路并写审计日志。
- 导出必须按当前完整筛选条件或勾选范围输出，不能只导当前分页或固定前 100 条；前端要触发真实可下载文件，后端仍要按权限裁剪字段。
- 上传、粘贴、凭证、水单等入口不能只做视觉按钮；如果暂未接对象存储，必须明确保存文件元数据和 URL/路径，并避免页面文案暗示已保存二进制文件。
- 列表到详情类页面禁止默认选中第一条数据，除非需求明确要求；进入列表先只显示列表，点击行/主键后再加载详情，摘要和详情必须使用同一个 selected id。
- 前端测试桩必须 mock 新增的真实 API 路径，不允许只依赖本地备用数组或旧接口让测试“看起来能过”。
- 聚合提交接口要在后端事务里完成主表、明细、费用、仓库包裹、审计日志等同步写入；草稿和正式提交的副作用必须分开。
- 涉及角色权限时，写入和读取都要以后端裁剪为准：无权限角色既不能提交敏感字段，也不能在详情接口拿到敏感字段；前端隐藏只是辅助，不能替代后端权限。
- 涉及菜单权限时，必须把“入口权限”和“数据范围权限”分开实现和验证；新增或开放菜单后，要补对应接口的对象归属过滤、字段裁剪和测试断言，不能用前端菜单隐藏或菜单开放来代表数据授权。
- 完成前至少跑对应模块最小测试、相关 API/共享测试、typecheck/build；前端改动必须覆盖 1366px 和宽屏布局检查，必要时补回归测试锁定交互。

## Frontend Layout Guardrails

- 不允许把 4 列以上的数据表格塞进窄卡片或三列卡片网格；必须使用全宽区块、页签、折叠区或明确的横向滚动。
- 后台表格必须定义关键列宽和 `scroll.x`，操作列不能被裁切，中文列名和字段值不能被挤成竖排。
- 表格操作按钮使用稳定的横向排布；如果空间不足，优先让表格横向滚动，而不是压缩字段内容。
- 财务、仓库、订单等高频工作页以“可扫读、可比较、少滚动”为优先，不为了卡片对称牺牲表格可读性。
- 功能标题、二级目录或卡片标题已经说明用途时，不再额外放大块说明 Alert；说明块只用于风险提示、错误、空状态或必须强调的业务规则。
- KPI/统计卡与主操作区之间保持紧凑，通常为 12-16px；禁止出现没有内容承载的大面积空白。隐藏未激活功能的空容器、空列和占位布局。
- 财务类页面必须优先保障录入和审核效率：字段、筛选、表格、按钮按实际操作流上下排布，不把多个独立表格硬塞进并列窄卡片。
- 页面文案避免出现开发阶段词汇，例如 `mock`、`M3`、`已接入后端`、`最小闭环`；面向用户只描述当前可操作的业务功能。
- 筛选区、资料库、费用表等高频组件必须使用“字段清楚、按钮靠近动作、行高稳定、分页明确”的排版，不允许说明文字挤占核心操作空间。
- 前端修改后至少检查 1366px 与宽屏两类视口，确认字段、按钮、表头没有覆盖、重影、竖排或裁切。

## Final Response Format

最终回复保持简短，只汇报：

1. 完成了什么
2. 修改了哪些文件
3. 验证结果
4. 剩余风险或下一步
