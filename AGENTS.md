# 思远物流项目 Codex 开发规则

`AGENTS.md` 只放每次会话必须加载的硬规则和索引。详细工程规则按任务需要读取 `docs/dev-thread-rules.md`；A-F 第一阶段开发入口读取 `docs/sunny-dev-index.md`。

## Project

- 项目名称：思远物流 / Siyuan Logistics
- 项目路径：`/Users/j1ng/Tools/sunny`
- 项目类型：跨境物流 TMS/OMS 系统
- 代码结构：monorepo，主要包含 `apps/web`、`apps/api`、`packages/shared`

## Default Context

- 默认只读取本文件、`.codex-state.md`、`git status --short` 和相关代码。
- `docs/dev-now.md` 只读说明多会话状态规则，不再承载唯一“当前任务”，代码会话不得自动修改它。
- 代码会话以用户当前明确请求或用户指定的 `docs/tasks/*.md` 为真实输入，并只读写自己认领的 `docs/dev-now/<session-slug>.md`；不得仅凭其他会话状态文件猜测任务。
- 不主动加载额外流程说明或通用指南；除非用户明确要求、当前任务需要，或系统规则强制要求。
- 所有判断以当前仓库文件、命令输出、类型和测试为准，不依赖旧聊天历史。
- 默认不调用 custom agents；命中任务场景时按 `Agent Auto Routing` 选择最小必要 agent。

## Context Efficiency

- 长视频、会议转录和口语需求只解析一次，整理为当前任务唯一的一页规范；后续分析、实施、审查和新会话只引用该规范，不重复复制原始口语。若原始内容与规范冲突，以用户最新明确确认为准。
- 首次代码检查形成事实基线；后续只检查需求变化、代码变化和未验证项，不重复扫描未变化的调用链，也不反复生成整套方案。发现代码、线上结果或用户反馈与基线冲突时，必须重新核查受影响范围。
- 默认由主线程完成任务，不调用多个代理重复扫描同一范围。只有权限、财务、状态机、迁移、敏感数据或生产风险确有独立审查价值时，才按 `Agent Auto Routing` 最多追加一个专项审查代理；用户明确要求多 Agent 审查时除外。
- 输出长度以完整表达必要事实为准，不设置机械字数限制；避免复述需求、搜索过程、旧结论和已确认方案，优先汇报新增事实、差距、决定和验证结果。
- `.codex-state.md` 只保留当前 47 指纹、活跃风险、当前工作和少量高信号事实，默认不得超过 16 KB；完整记录移入 `docs/archive/codex-state/`。`docs/dev-now/` 只保留模板和进行中、待交接或阻塞会话；完成项移入 `docs/archive/dev-now/`，本地完成但尚未发布的项移入 `docs/backlog/dev-now/`。
- 修改状态治理、归档规则或结束主推进任务前运行 `npm run context:check`；需要整理当前会话状态时先运行 `npm run context:archive` 查看 dry-run，只有主推进会话或用户明确授权的治理会话才可加 `-- --apply` 执行归档。
- 本节只减少重复上下文和重复劳动，不得跳过 `Evidence-First Output Gate`、`Effect Acceptance Gate`、权限、安全、迁移及 47 发布门禁。

## Mandatory Start Protocol

每次开发任务开始时先做这些事，不要依赖旧聊天历史：

1. 读取 `.codex-state.md`
2. 执行 `git status --short`
3. 用 `rg`、`rg --files`、`ls` 和 focused read 查看相关代码
4. 根据当前文件状态继续最近的未完成任务

如果 `.codex-state.md` 和实际代码冲突，以实际代码和命令输出为准。需要更新状态时，只改与本轮任务直接相关的状态内容。

发生 `stream disconnected`、工具中断、上下文恢复，或用户说“继续执行 / 继续完成”时，恢复后的第一步必须重新读取当前工作目录的 `AGENTS.md`、`git status --short` 和本任务状态文件；不得沿用中断前缓存的发布、浏览器或验收规则。当前文件中的最新规则优先于旧会话记忆和旧状态文件。

## Evidence-First Output Gate

- 所有关于“当前系统已经实现什么、为什么如此、能否点击、权限范围、数据是否保存、状态如何流转、功能是否完成”的输出，必须先取得当前代码层面的直接证据；不得根据旧聊天、页面名称、按钮文案、截图外观、经验或记忆补全结论。
- 回答现状或完成度前，至少沿真实调用链核对相关入口和关键结果：前端事件或调用方 -> API/权限 -> Repository/持久化或查询 -> 返回类型/页面渲染；只核对其中一层时，只能汇报该层已证实的事实，不得扩展成完整业务结论。
- 最终结论必须能指向具体文件和行号，或指向本轮实际执行的定向测试、接口结果、数据库只读结果、线上代码指纹；没有证据的内容必须明确写“未证实”，不得使用“已完成、就是、目前一定、不能、可以”等确定性措辞。
- 用户提供的线上截图或实际操作结果与本地代码判断冲突时，必须立即撤回原判断，重新读取当前工作树、相关测试和线上已发布代码/API 证据；冲突未解决前不得继续下确定性结论。不得以本地组件结构否定用户已经提供的运行事实。
- “部分满足”不能表述为“已完成”。必须逐项对照用户验收条件，特别检查单条与批量、多来源与单来源、首次与二次处理、成功与异常分支；仅有单一样本证据时，结论范围必须限定到该样本。
- 引用代码不是证据闭环本身。代码变更任务仍须遵守 `Effect Acceptance Gate`：用最相关的定向测试或实际接口/持久化结果证明效果后，才能汇报完成。

## Execution Rules

- 不重复确认旧目标，不反复规划。
- 普通实现细节自行决策，选择与现有代码风格一致的保守方案。
- 每轮必须推进实际闭环：读代码、改文件、完成本地最小验证；涉及运行时代码时继续精确发布到 47，并通过线上 API、容器状态、日志、构建产物或代码指纹完成发布后验证，再更新状态并汇报。
- 运行时代码任务在最终回复前必须再次核对本文件的 `Non-Browser Acceptance And 47 Validation` 与 `47 Deployment Rules`。若本地安全门已通过且没有真实阻断，禁止以“尚未发布 47”“未取得页面截图”或“等待用户检查”为结束语；必须先实际尝试发布，只有命令失败、边界不清或命中高风险确认项时才能报告未发布，并给出具体阻断证据。
- 后续需求优先走业务视角任务卡，但不强制。用户在当前会话明确要求实施，且目标、范围、边界和最小验证足够清楚时，代码会话可直接执行非任务卡需求。
- 非任务卡需求若存在关键业务决策、财务/权限/状态流转风险或多个明显不同方向，必须先补充确认；不能因为允许非任务卡就自行猜需求。
- 每个代码会话只维护自己的 `docs/dev-now/<session-slug>.md`，不得删除、覆盖或重写其他会话的状态文件；根目录 `docs/dev-now.md` 是只读索引。完成后只更新自己的终态；归档由主推进会话或用户明确授权的治理会话统一执行。
- `.codex-state.md` 不作为并发任务锁；代码会话完成后先把结果写入自己的状态文件，只由当前主推进会话或用户明确授权的会话追加高信号完成记录。
- 多个代码会话并发时必须使用独立 worktree，并为每个 worktree 使用独立 `codex/<session-slug>` 分支；仅切分支但共享同一工作目录不能形成文件隔离。确实无法创建 worktree 时必须按文件所有权完全分区；同一文件或同一业务调用链只能保留一个写会话，其他会话改为只读或排队。
- 并行开发不等于并行发布：47 是全局串行资源。同一时间只允许一个发布协调会话执行源码同步、白名单上传、Docker 构建、迁移、重启、健康检查和发布状态写入；其他已验证会话进入发布队列，不得绕过队列直接运行 `sync:47 --apply`、`deploy:47`、`scp` 或远端 Docker 命令。
- `codex/agent-master-data` 这类大集成分支只作为基线；后续新功能优先从基线切小分支，单分支只处理一个模块或一个 bug。
- 不要为了节省时间跳过验证；优先运行最小相关测试，再按风险扩大验证范围。
- 先复用现有模块、类型、API、样式和测试，再考虑新增文件或新抽象。
- 禁止顺手重构无关代码、批量格式化无关文件或改动无关业务流程。
- 遇到“所有、全局、全部模块、统一改造”类需求时，必须先拆成阶段：先做通用能力，再接入一个代表页面并完成验证，通过后再逐模块推广；禁止一轮横跨全系统落地。

## Behavior-Preserving Refactor Gate

- 本轮深度重构默认是“只改结构，不改业务逻辑”。GitHub 优秀项目只用于借鉴模块边界、依赖方向、测试方法和 UI 组织，不得覆盖 Sunny 已运行的业务语义。
- 重构基线以 47 当前实际运行代码、API 行为、数据库持久化结果、权限裁剪和审计证据为准；旧文档或旧测试与当前运行事实冲突时，先记录冲突并修复保护网，不得借重构恢复未经确认的旧行为。
- 必须保持以下可观察契约不变：路由与 HTTP 方法、请求与响应字段、默认值和状态码；角色权限、数据范围、对象归属和敏感字段裁剪；业务状态、前置条件、状态流转、幂等和并发结果；金额、币种、汇率、精度和舍入口径；事务写入、关联关系、审计事件、附件和异常文案语义；前端入口、字段、操作链路和提交后结果。
- 每个切片先写或修复 characterization/contract/E2E 保护，再切换调用边界；同一固定样本在重构前后必须得到等价的响应、持久化副作用、权限结果和审计结果。只有类型检查或构建通过，不能证明逻辑未改变。
- 禁止为了让重构测试通过而擅自改预期、放宽权限、改变状态或金额口径。测试确实落后时，必须用 47 当前代码、线上只读证据或已有正式需求证明当前语义，再更新测试。
- 发现现有逻辑可能错误、数据不一致或确需改变契约时，立即停止把该项作为重构处理；单独建立 bug/需求任务，列出当前行为、目标行为、数据影响和回滚方案，取得用户确认后再实施。
- 行为等价无法证明、调用链未覆盖、生产基线冲突或变更同时触及 schema、权限模型、财务口径、核心状态机时，不得合并或发布该重构切片。

## Test Process Safety

- Codex 会话禁止直接运行裸 `npm test`、`npm test -w ...`、`npx vitest`、`npm exec vitest` 或 `vitest`。所有 Vitest/Jest/Playwright/前端测试必须通过安全 runner 启动，避免会话结束后遗留 `node (vitest)` 高 CPU 进程。
- 默认使用 `npm run test:safe -- <真实测试命令>` 包裹测试命令；Shared/API/Web 分别优先使用 `npm run test:shared:safe -- ...`、`npm run test:api:safe -- ...`、`npm run test:web:safe -- ...`，需要完整回归时使用 `npm run test:all:safe`。
- Web/Vitest 测试必须限制 worker 与超时：保留或追加 `--poolOptions.threads.maxThreads=1 --testTimeout=30000 --hookTimeout=30000`；除非用户明确要求压力/并发验证，不得开启多 worker 长跑。
- 若历史命令、文档或脚本示例仍写 `npm test -w @siyuan/web -- ...`，Codex 执行时必须改写为安全 runner 等价命令，不得照抄裸跑。
- 若测试超过 30 分钟仍未完成，默认视为异常长跑；超过 1 小时必须停止对应进程组并在最终回复说明。清理时优先按 `CODEX_THREAD_ID`、`PGID` 和运行时长确认归属，避免误杀用户正在保留的进程。
- 低风险 47 快速发布不得启动已知会卡住的整页 Web 集成测试。优先执行一次最小定向测试或对应 workspace 类型检查；定向测试 30 秒没有产生有效结果时停止且不重试，改以类型检查和生产 Docker 构建作为发布门。
- 同一代码状态不得重复执行等价的类型检查、生产构建或健康检查。低风险发布若 47 Docker production build 必然执行，本地默认只做类型检查或最小定向测试，不再额外跑一次本地 production build。
- 修改 Agent 配置、测试入口、发布脚本或本文件验证规则后必须执行 `npm run governance:check`，并对相关 shell 脚本执行 `bash -n`。

## Effect Acceptance Gate

- “代码已修改、类型检查通过、构建成功”只证明代码可运行，不等于功能完成。功能完成必须同时满足：用户目标已转成可观察验收场景、实际效果经过验证、验证结果与用户需求一致。
- 开工前先用一句话确定用户真正要看到或完成的结果，并选 1 个最能代表真实使用的固定样本；完成后必须按该样本走完整路径，不能只验证内部函数、接口返回或 CSS 类名存在。
- 开发闭环顺序固定为：代码修改 -> 本地最小效果与安全验证 -> 必要审查 -> 精确发布 47 -> 47 线上 API/代码/容器验证 -> 汇报结果。运行时代码本地验证通过后默认继续发布，不再等待浏览器验收或重复询问常规发布授权。
- 验证层级按需求选择最接近效果且成本最低的一层：
  - 纯逻辑/计算：定向单元测试，验证输入与最终输出。
  - API/权限/状态流转：先用定向接口或 E2E 验证角色、前置状态、动作和持久化结果；发布后再通过 47 服务端接口、容器内探针或只读数据库查询验证线上实际状态。
  - UI 字段/按钮/交互：使用组件或页面定向测试、类型检查和生产构建验证代码行为；发布后检查线上源码 checksum、静态产物标记、接口返回与错误日志。
  - 排版、响应式、遮挡、密度、颜色等纯视觉效果：Codex 不操作浏览器、不做截图验收；发布到 47 后明确列出页面路径、变更区域和人工检查点，由用户进行最终视觉验收。
- 本地测试不符合用户描述时继续修改并复测，不得带着已知失败发布。47 发布后验证失败时先定位并修复；若影响服务可用性、数据正确性或权限安全，立即停止扩大发布并按发布规范回滚或恢复。
- 效率与结果的默认平衡是“一条效果证据 + 一条安全证据”：先跑一个最小、确定性、直接覆盖用户场景的效果测试，再按风险补类型检查、构建、权限或数据验证；避免全量测试、重复构建和与当前需求无关的多层验收。
- 不确定改动应跑哪些验证时，执行 `npm run validation:select -- <changed-path...>`；它只选择最接近业务效果的一条测试与一条安全门，不自动执行，也不替代权限、迁移和 47 发布门禁。
- 已知会卡住的整页测试不作为默认效果门。30 秒没有有效结果就停止，改用更小的组件测试、固定数据接口验证、生产构建或线上代码/接口探针；只有无法获得等价效果证据时才扩大测试范围。
- 最终回复必须区分并如实汇报：实现了什么、本地验证看到了什么、47 发布后验证通过了什么，以及需要用户人工检查的 UI 视觉点。没有线上效果或安全证据时不得只凭 typecheck/build 宣称发布完成。

## Non-Browser Acceptance And 47 Validation

- 验收主链路采用 47 真实环境的原因：验证码和浏览器点击耗时高、稳定性差；本地数据与 47 生产数据不保证一致，本地页面通过不能证明线上业务数据、权限或状态正确。
- 后续验收默认不操作浏览器、不做验证码登录、Session 注入或自动截图。UI 设计、排版、响应式和视觉细节由用户在 47 上人工验证；Codex 只需在发布结果中提供准确入口和检查清单。
- 未取得真实页面截图不是阻断项，也不是“尚未发布 47”的理由；UI 人工验收发生在代码发布和服务端/代码验证之后。
- 本地阶段只执行能阻止明显错误上线的最小安全门：`git diff --check`，以及一个最相关的定向测试、类型检查或必要审查；47 必然执行生产构建时，不在本地重复 production build。不得为了视觉验收启动本地登录旁路。
- 最小安全门通过后立即按任务白名单精确发布 47，不等待额外浏览器检查、人工确认或全量测试。发布前只再核对候选文件、迁移指纹和受影响服务，避免把其他脏文件带入。
- 47 发布后，公开能力使用公网/容器内 health 与目标 API 验证；需要鉴权的接口优先使用 47 容器内短期 JWT、服务端本地接口或只读数据库查询，不反复请求图片验证码，也不依赖已登录浏览器。
- 权限、字段裁剪和数据范围验收必须使用与目标岗位匹配的角色或服务端身份，验证允许路径与至少一个拒绝路径；菜单可见性不能替代后端 RBAC 和序列化裁剪证据。
- 线上写接口验收只使用明确、安全、可回滚的固定样本，并核对最终持久化状态和审计日志；会污染真实财务、订单、仓库或客户数据时，改用只读证据或在发布前向用户确认专用样本。
- 47 验证发现问题时，直接进入“定位根因 -> 修改代码 -> 最小相关本地验证 -> 重新精确发布 -> 重新线上验证”循环，直到线上证据通过；不得退回验证码登录和浏览器点击链路，也不得把已知失败留给用户发现。
- 禁止输出或落盘 JWT 密钥、完整 token、密码和生产敏感数据；临时 JWT 只在 47 容器内生成和使用。`DISABLE_LOGIN_CAPTCHA`、开发会话路由、自动 Session 注入及相关 `VITE_*` 开关不得进入 47。

## Context Rotation Gate

- 上下文轮换采用宽松门槛，不因第一次压缩或普通长对话频繁换会话：观察到 0–1 次压缩且仍在同一功能时继续；观察到第 2 次压缩时刷新当前 `docs/dev-now/<session-slug>.md` 的交接检查点但继续；观察到第 3 次压缩时，在当前原子修改、测试或发布动作结束后切换新会话。
- 不可靠地估算 token 数；以下任一事实可提前进入切换：连续两次非工具等待明显超过约 60 秒、开始遗漏或反复确认已经确定的事实、出现前后矛盾或重复劳动、黄色状态下准备进入明显不同模块、用户明确说“切换新会话”。测试、构建、部署和网络工具自身耗时不计入模型变慢。
- 用户说“切换新会话”即视为明确授权执行会话交接、创建新会话、连续命名，并在新会话成功接管后归档旧会话；不视为发布、提交、外部通知或扩大代码修改范围的授权。
- 会话标题使用 `Sunny｜<业务主题>｜NN`。同一业务主题继续时编号递增，例如 `01 -> 02`；进入无关业务主题时重新从 `01` 命名。首次轮换时，如旧标题不符合格式，先将旧会话规范为 `01`，新会话命名为 `02`。
- 切换顺序固定为：结束当前原子动作；刷新当前状态文件交接区；把旧状态标为 `handed_off`；创建相同工作目录的新会话；新会话读取 `AGENTS.md`、`.codex-state.md`、`git status --short`、指定交接文件和相关代码；新会话复述目标/完成项/效果证据/风险/下一步并创建自己的唯一状态文件；确认接管成功后才归档旧会话。
- 若新会话创建或接管失败，旧会话恢复为 `in_progress` 且不得归档。不得在测试、构建、迁移、发布、文件写入或其他外部动作尚未得到结果时切换。
- 交接只保留当前任务的事实，不复制完整聊天、长日志或大段代码；必须包含用户验收目标、关键决定、允许/禁止范围、改动文件、效果证据、安全证据、未验证项、发布状态、稳定截图/附件路径和准确下一步。交接正文目标控制在 2–4 KB。
- 详细命名、交接、接管确认和失败恢复流程见 `docs/session-context-rotation.md`；交接结构使用 `docs/dev-now/_handoff-template.md`。

## Reasoning And Review Gates

- 复杂 bug、跨模块数据流、财务/仓库/权限/发布问题，必须先做第一性原理检查：基于当前代码、数据流、权限和业务事实找根因，不只补表层现象。
- 高风险改动、状态流转、权限裁剪、财务口径和 47 发布前，必须做对抗式审查：主动寻找越权、脏数据、重复提交、异常时间、兼容和发布失败风险。
- 小型文案、局部样式、字段显隐、测试断言修正不强制展开完整审查；需要详细执行方法时再读取 `docs/dev-thread-rules.md`。
- “发布到 47”本身不自动把低风险局部 Web 改动升级为高风险任务；只有变更内容实际命中权限、财务口径、状态机、迁移、敏感数据或不可逆操作时才进入专项审查。

## Grill With Docs Auto Routing

- 实施前若存在尚未解决、会实质改变业务流程、领域边界、数据模型、权限模型、财务口径、共享契约或架构的决策，自动调用已安装的 `grill-with-docs`；用户、代码与文档术语冲突，或“全局/全部/统一改造”存在多个明显不同产品方向时同样触发。
- 不因任务复杂就机械触发。小型文案、局部样式、字段显隐、列宽、测试断言、预期行为明确的局部 bug、只读诊断/状态汇报，以及没有实质决策的清晰任务卡不调用。
- 触发后先从代码、文档、API 和数据模型核实可发现事实，再向用户说明触发原因；每次只确认一个决策并给出推荐答案。用户明确确认共同理解前，不实施运行时代码，也不把业务决策下放给子代理；`Ask User Only When` 与高风险门禁继续适用。
- 已确认的规范术语写入根目录 `CONTEXT-MAP.md` 与 `docs/contexts/<context>/CONTEXT.md`；只有难以撤销、反直觉且存在真实取舍的决定才写入 `docs/adr/`；达到实施条件的结果再形成 `docs/tasks/YYYY-MM-DD-*.md`。不得把实现细节写进领域术语表，也不得把 `docs/dev-now/` 当作规划文档。
- 用户明确要求只读、不要文档或不要改代码时，访谈保持只读，所有写入延后到用户明确授权。

## Agent Auto Routing

- 日常主线程默认使用项目 `.codex/config.toml` 中的 `gpt-5.6-terra` Medium，负责理解目标、检查真实状态、风险定级、任务拆分、结果整合和验收；明确、常规、可用一次 focused validation 闭环的任务由主线程直接完成。子代理不能替代主线程承担这些职责。
- 每个任务先做一次路由判断，默认结果是 `NO_DELEGATION`。范围或入口不明确时只做一次 focused scan，形成事实基线后直接实施；不得让多个代理重复理解同一需求、扫描同一调用链或分别生成整套方案。
- 路由依据是实际变更面、风险、不确定性、验证成本和并行价值，不按“改 CSS”“扫代码”等任务名称或关键词机械分配。
- 只使用项目 `.codex/agents/` 中的 GPT-5.6 固定角色：`sunny_mapper`（Luna Medium，只读扫描）、`sunny_frontend`（Terra Medium，前端实施）、`sunny_backend`（Terra High，边界明确的后端实施）、`sunny_verifier`（Terra Medium，独立验证）、`sunny_reviewer`（Sol High，独立代码审查）、`sunny_risk_reviewer`（Sol XHigh，高风险审查）。
- 默认不委派；确有独立收益时只委派 1 个边界明确的主子任务，独立验证、专项审查或互不依赖的只读搜索才可再追加 1 个辅助子任务，并发工作流上限为 2。写任务默认串行；两个写代理只有在不同 worktree、文件所有权完全分离且主线程明确授权时才可并行。
- Codex 不会因 `AGENTS.md` 自动切换当前主线程模型。出现下列任一条件时，Terra/Luna 必须在修改运行时代码前停止，输出 `MODEL_ESCALATION_REQUIRED`，说明触发条件并请用户从模型菜单切换到 Sol High 后继续：
  - Prisma schema/migrations；Auth/JWT/RBAC、租户或对象归属、数据范围、敏感字段裁剪；金额、币种、汇率、付款、核销、利润口径；核心业务状态机；生产数据写入或不可逆操作；
  - 47 发布异常、远端基线冲突或回滚；存在两个会实质改变实现的业务方向；预计同时跨前端、API、Repository、Shared 四层；
  - 首次实现已经超出允许范围，或同一个 focused validation 在一次修正后再次失败。
- 普通 47 发布、明确的局部前后端改动和第一次可定位的测试失败不自动升级；只按真实风险触发。Sol High 完成高风险实现后，仅在权限、财务、状态机、迁移、敏感数据或生产风险确有独立价值时追加 1 个 `sunny_risk_reviewer`，不得机械同时调用多个审查代理。
- 委派写任务时必须明确：一页任务规范、固定验收样本、允许修改文件、禁止范围、验收标准、安全验证命令和升级触发器。代理发现实际范围越界时必须停止扩改并返回 `ESCALATE_TO_SOL`：发现事实、已执行操作、未处理内容、升级原因、可能受影响的文件或状态。
- 低成本模型只允许围绕同一失败证据修正一次；再次失败即升级，不继续堆轮次。升级交接只保留一页任务规范、改动文件、失败证据和准确下一步，不复制原始口语、完整聊天或重复方案。
- 禁止因为代理已配置就全量调用；禁止代理扩大用户未要求的范围；只有用户明确要求“全局对抗式审查 / 多 Agent 审查 / 并发审查”时才允许扩大并发审查。

## Frontend Design Rule

- 每次新建前端模块、二级功能、工作台、表单、详情、看板或明显重塑已有页面时，默认先按 `.agents/skills/frontend-design/SKILL.md` 做设计判断，再落代码。
- sunny 本地 `frontend-design` 是 UI/UX 设计准则最高优先级；`sunny_frontend` 只负责 React/AntD/状态/性能/可访问性实现，不替代 sunny 后台工作台规范。
- 禁止同一轮重复展开两套 UI 规则；`sunny_frontend` 只读取和应用当前任务必要部分。
- 若两者冲突，以 sunny 现有设计系统、AntD 用法、业务字段完整性、权限裁剪和可扫读效率为准。
- `frontend-design` 覆盖所有用户可见、可点击、可提交或会影响业务状态的界面，不只覆盖一级页面；新增/编辑/详情弹窗，审核/反审/作废/删除/付款/确认弹窗，抽屉详情，表格行操作，批量操作面板，上传/预览，字段或列设置，二级/三级入口，空态、错误态、加载态、无权限态和禁用态都在范围内。
- Sunny 是跨境物流内部运营系统，使用 `frontend-design` 时必须服务业务效率：字段清楚、表格可扫读、操作路径短、权限状态明确；不得把后台页面做成营销页、装饰页或只追求视觉冲击。
- 设计落地前要先明确该页面的单一业务任务、目标岗位、核心字段、主要操作和一处可解释的视觉/交互记忆点；视觉特色不能覆盖财务、仓库、订单、权限和审计的一致性。
- 小型纯确认框可按比例处理，但必须检查动作文案、业务后果、权限裁剪和提交后状态；不能只完成主页面，把弹窗、抽屉和隐藏入口留成默认组件堆叠。
- 现有已落地模块允许做 UI 重构；完成本地代码验证后按白名单精确发布到 47，不等待浏览器验收。发布后由用户人工检查视觉效果，用户反馈再作为下一轮 UI 调整输入。
- UI 重构默认不得改业务口径、数据库结构、线上数据、权限模型或接口契约；若发现必须补接口、字段或后端逻辑，必须单独列为后端改动。
- UI 重构不得移除、替换或弱化已有业务操作链路；涉及弹窗、抽屉、行操作、批量操作、筛选、优先级、状态流转时，必须先回溯旧逻辑并保留原功能，除非用户明确要求改变业务逻辑。
- 新增或改造后台业务表格默认使用 `ManagedTable`；涉及批量操作、批量审核、批量出库、批量删除、批量付款等选择列表时，默认使用 AntD/`ManagedTable` 的 `rowSelection`，选择列必须固定在第一列并带表头全选框。不要手写插在中间的 checkbox 列；确因复杂表格无法使用 `rowSelection` 时，自定义选择列 key 必须使用 `select`/`selection`/`checkbox`/`rowSelection` 这类标准命名，并同样固定第一列、支持全选、不可被列设置隐藏或移到中间。
- 若只是小型 bugfix、文案修正、字段补显隐或测试修复，可复用现有设计，不强制重新做完整设计方案。

### Impeccable Auto Routing

- Impeccable 作为 `frontend-design` 的辅助设计检查器，不是第二套产品规范。每次命中新建或明显重塑前端界面、设计/重设计、审查、打磨、排版、字体、颜色、布局、响应式、可访问性、动效或前端性能优化时，先读取并遵守 Sunny 的 `frontend-design`，再自动读取 `.agents/skills/impeccable/SKILL.md`，仅加载最贴合当前意图的一份 Impeccable playbook；不得同时展开多份命令手册或以外部审美覆盖本地业务规则。
- Sunny 后台页面固定按 Impeccable 的 `Operate` 模式理解：业务任务完成效率、字段完整、可扫读、权限状态、标准交互和信息密度优先于视觉表达。用户明确要求 `critique`、`audit`、`polish`、`typeset`、`layout`、`adapt`、`harden`、`optimize` 等能力时，按同名 playbook 执行；未明确命令但意图唯一时自动路由，存在两个同等合理方向时只确认一次。
- 自动调用不覆盖后端-only、纯业务逻辑、API、RBAC、数据范围、金额口径、状态流转、Prisma、日志或测试断言任务；小型文案、单字段显隐、已明确的局部列宽/按钮尺寸修正，也不为此机械加载 Impeccable，除非用户明确要求进行 UI 质量检查。
- Impeccable 只提出和验证 UI 候选问题，不得自行删除业务字段、改变功能入口、合并不同语义字段、替换系统字体栈、扩大操作尺寸或修改接口与业务逻辑。Sunny 已确认的高密度办公界面、中文系统字体、表格 `12px` 正文/表头层级、矩阵 `11px` 字段标签、紧凑桌面操作按钮和矩阵行蓝色识别线均属允许约束，不能仅因通用检测器报警而改写。
- 项目设计 Hook 对直接编辑的 `.tsx`、`.jsx`、`.ts`、`.js`、`.css` 等 UI 文件执行机械检测；发现项必须结合当前页面、Sunny 设计令牌和用户验收目标复核后再修复，禁止形成无边界的自动美化循环。Hook 不替代 `frontend-design`、定向测试、类型检查、生产构建或 47 发布验收。

## Minimum Change Rule

- 开发前先检查现有页面、弹窗、抽屉、详情面板、API、DTO、查询参数、数据库字段、权限判断、字段裁剪、操作日志和测试样本是否可复用。
- 默认优先复用现有接口、功能、组件、权限、日志和状态流转；能补齐现有组件或接口参数时，不新增重复接口、重复组件或重复后端查询。
- 只有现有能力无法承载真实需求字段、无法保证敏感字段后端裁剪、缺少必要状态流转、缺少关键审计日志，或复用会造成越权、财务口径错误、重复提交、追溯断裂时，才新增接口、字段、组件或后端逻辑。

## Output Discipline

- DO NOT send optional commentary.
- 不要输出非必要说明、过程旁白或显而易见的下一步描述。
- 优先采取具体行动：查看文件、修改代码、运行命令、验证结果。
- 只有在任务有歧义、存在风险、遇到阻塞、需要用户决策时，才解释中间过程。
- 最终回复只总结：改了什么、验证了什么、还有什么风险或后续动作。

## 47 Deployment Rules

- 47 云服务器按纯 Docker Compose 发布处理；宿主机不要求安装或调用 `node`、`npm`、`npx`。
- 所有 47 写入与发布必须持有同一把远端全链路发布锁，锁从读取远端基线之前开始，直到源码同步/条件上传、构建、必要迁移、重启、健康检查和状态更新全部结束后释放。锁被占用时自动排队，禁止另一个会话先同步源码或单独重启服务。
- 标准干净候选只能从发布协调 worktree 执行：任务开始先运行 `npm run release:47:baseline`；该命令只有在 worktree 干净、当前提交的三类运行时 manifest 与 47 实际树完全一致时才生成绑定 worktree/分支/祖先 commit 的 receipt。完成合并/验证后把记录值传给 `deploy:47 -- --expected-release-id ...`；远端 baseline 已变化或 receipt 不匹配时必须重新合并，禁止临时读取新值后覆盖旧分支。默认要求 HEAD 与同名 origin 分支一致；仅当用户明确不使用 GitHub 时，允许 `--source-bundle` 在同一发布锁内生成并保存只读 Git bundle，release receipt/state 必须绑定 bundle 路径与 SHA-256，线上 audit 必须验证 bundle 含当前 commit。origin 与已验证 bundle 两种耐久来源证据至少具备一种，禁止无证据绕过。
- 只有发布协调 worktree 在 baseline 匹配、持有全链路锁且发布后远端 manifest 与候选一致时，标准同步才可用 `rsync --delete` 形成精确镜像；功能 worktree 不得直接镜像 47。白名单删除仍须单独声明目标、核对 checksum 并保留回退备份。
- 本规则视为用户已对目标明确、边界清楚且本地验证通过的常规运行时代码授予 47 发布授权；开发任务默认连续完成“本地验证 -> 精确发布 -> 线上验证 -> 汇报”，不再逐次询问是否发布。破坏性迁移、真实付款、批量生产数据写入或其他不可逆操作仍须单独确认。
- 同步/发布到 47 时，按 `docs/47-cloud-docker-release.md` 自动判定发布范围：只构建/重启受影响服务；只有 Prisma schema/migrations 变化才运行 `db-migrate`；再做对应健康检查。
- 47 发布默认不做浏览器、截图或多层重复验收，由用户上线后人工查看界面；低风险局部 Web 改动完成最小相关测试或类型检查、生产构建、精确同步、容器状态、公网健康和错误日志检查即可，不额外启动审查 Agent。只有权限、财务口径、状态机、迁移、敏感数据或发布异常等高风险变更才追加专项审查或接口验收。
- 线上数据库只允许执行 `prisma migrate deploy`；禁止 `prisma db push`、`prisma migrate reset`、`prisma:seed`、`demo:seed`。
- 线上密钥和 `.env` 只保存在服务器，不随代码同步、不写入日志、不提交 Git。
- `DISABLE_LOGIN_CAPTCHA`、开发会话路由、自动 Session 注入和相关 `VITE_*` 开关不得传入 47；生产环境必须保持验证码与正常 JWT/RBAC 链路，发布检查发现鉴权旁路配置时必须阻断。
- 47 线上登录接口可能启用图片验证码；接口验收、角色验收或数据验证时，不要使用普通 `/api/auth/login` 碰验证码，也不依赖浏览器会话。优先使用容器内短期 JWT、服务端本地接口或只读数据库查询完成验证；生成临时 JWT 时只在 47 容器内读取 `JWT_SECRET`，禁止输出密钥或写入日志。

### 47 Fast Release Path

- 常规开发任务默认包含发布到 47；只发布当前已完成、边界明确且验证通过的白名单改动，不重复询问，也不得扩大到其他脏文件、未完成任务或高风险迁移。
- 低风险快速发布适用于：文案、局部样式、纯展示字段、表格列宽/排序/布局、只读弹窗和不改变权限或业务状态的前端交互。目标耗时为 2–5 分钟。
- 快速发布只认当前任务明确修改的文件或当前会话认领的状态文件，不把 `git status` 中其他会话、历史未提交或已发布但仍脏的文件纳入候选，也不为低风险任务扫描全部 `docs/dev-now`。
- 若用户说“检查本地更新发布到 47”，优先从当前任务、最近完成记录和远端 checksum 确定发布白名单；只有无法识别归属时才做一次全局差异摘要，禁止默认逐文件深审整个脏工作树。
- 低风险 Web 快速发布固定执行：
  1. 进入全局发布队列并取得 47 发布锁；锁内核对目标文件及其相对 47 的内容差异，记录远端基线 checksum；
  2. 执行 `git diff --check`，再选择 Web 类型检查或一个能在 30 秒内完成的最小定向测试；不重复执行 47 将完成的 production build；
  3. 精确同步目标文件；目标文件夹带其他未发布内容时，以锁内读取的 47 当前文件为基线生成白名单补丁，并以原 checksum 作为条件更新；checksum 已变化时立即停止，不得覆盖；
  4. 只执行 `docker compose build web` 和 `docker compose up -d --no-deps web`；
  5. 检查线上源码 checksum/静态产物标记、`docker compose ps`、公网首页/API health 和最近错误日志；
  6. 线上验证失败时立即修改、重新执行最小相关验证、重新发布并复验；通过后一次性汇报，需要视觉确认的 UI 点交由用户人工检查。
- 低风险 API 快速发布沿用相同流程，但只做一个相关接口/类型验证并仅构建、重启 API；未改 Prisma 时不得构建或运行 `db-migrate`。
- 快速发布禁止：浏览器/截图验收、整模块或全量测试、多个审查 Agent、重复内外网多轮验收、等待观察期、顺带同步文档/测试/其他运行时文件、无差别执行全仓 `sync:47 --apply`。
- 白名单发布根据 targets 强制推导唯一 scope；Prisma 发布必须同时列出 schema 与至少一个 reviewed migration，且线上 pending 集合必须与 approved 集合完全一致。`docker-compose.yml` 等基础设施变更不得走通用标准/白名单入口。
- 源码替换后的构建、迁移、重启或健康检查失败会写入 recovery-required 标记并阻断后续发布；只能在主推进会话完成恢复并核对标记 checksum 后，用 `release:47:resolve` 显式清除。
- 快速发布同样禁止并发发布、锁外 `scp`、锁外 `rsync --apply`、在另一个发布尚未完成时重建或重启相同/不同服务；Web、API、迁移共享同一全局队列，不按服务拆锁。
- `packages/shared/**`、根依赖、Dockerfile 或 Compose 变化按实际影响构建必要服务，不走“只重建 Web”的捷径。
- 高风险发布仍按专项路径执行，但审查范围必须限制在本次候选文件和真实线上数据；不得因工作树很脏而审查所有无关历史改动。发现阻断项时可以发布相互独立的安全子集，并明确列出暂缓项。
- 发布记录和 `.codex-state.md` 在健康检查后一次性更新；不得为写状态重复构建、重启或重新验收。

## Security Requirements

所有开发默认考虑：

- 鉴权和 RBAC 权限控制。
- 输入校验和输出转义。
- 防止越权访问、SQL 注入、XSS、SSRF、危险文件上传。
- 错误处理不泄露敏感信息。
- 审计日志覆盖关键业务动作。
- 敏感信息不进入日志、前端包、Git 历史。
- 财务、客户、订单、用户相关接口必须校验租户、角色和对象归属。
- 菜单权限只决定是否能进入模块；数据范围和字段可见性必须以后端查询裁剪和序列化裁剪为准。开启仓库、财务或其他模块入口，不等于开放全量数据。
- 业务员、仓库、财务等内部角色默认只能看到自己职责范围内的数据；例如业务员只能看自己的单及自己单关联的业务成本，仓库不能看到财务敏感字段、利润、应付或付款信息。

## Ask User Only When

只有这些情况需要暂停询问用户：

- 破坏性或不可逆操作，例如删除数据、重置数据库、覆盖大量文件。
- 涉及读取、更换或暴露密钥、真实付款、批量生产数据写入、外部通知；常规代码发布已由本规则授权，不再单独询问。
- 需求会明显改变业务流程、数据库结构或权限模型。
- 安全、合规或授权边界不清。
- 多个方案会造成明显不同的产品方向或迁移成本。

## Documentation Index

- `docs/dev-thread-rules.md`：详细工程规则、多会话分工、Ponytail 细则、长需求拆卡、模块交付、前端布局和验证矩阵。
- `docs/sunny-dev-index.md`：A-F 第一阶段开发入口、固定样本、任务卡队列、开工检查门和完工销账格式。
- `docs/tasks/_template.md`：业务视角任务卡模板；规划会话按此输出正式任务卡。
- `docs/tasks/_acceptance.md`：正式任务卡进入代码会话前的通过标准，以及非任务卡直接执行的准入边界。
- `docs/dev-now.md`：多会话状态规则的只读索引；代码会话不得自动修改。
- `docs/dev-now/`：每个代码会话独立的任务状态文件和模板。
- `docs/archive/dev-now/`：已完成或已被后续阶段取代的会话状态归档，不属于默认上下文。
- `docs/backlog/dev-now/`：本地完成但未发布或仍待处理的历史会话状态，不属于默认上下文。
- `docs/archive/codex-state/`：`.codex-state.md` 的完整历史归档，不属于默认上下文。
- `docs/session-context-rotation.md`：长会话压缩后的宽松轮换门槛、连续命名、交接、接管确认和旧会话归档规则。
- `.codex-state.md`：当前 47 指纹、活跃风险、当前工作、少量高信号事实和归档索引，不作为并发任务锁；只由当前主推进会话或用户明确授权的会话追加或归档。
- `docs/47-cloud-docker-release.md`：47 Docker 发布流程。

## Final Response Format

最终回复保持简短，只汇报：

1. 完成了什么
2. 修改了哪些文件
3. 验证结果
4. 剩余风险或下一步
